# Moli 生产上线流程

> 生产路径与检查项。敏感 `application-pro.yml` / `conf/moli-*.env` 不提交 Git。  
> env 模板：`deploy/linux/moli-*.env.example` · 工程 Runbook：`docs/ops/v1-release-runbook.md`

**部署根目录**：`/opt/moli-project-distribute`  
**本批服务**：user-center · knowledge · gateway（v1 最小三件套）

---

## 0. 上线前（T-1 天）

| # | 项 | 负责人 | ✓ |
|---|----|--------|---|
| 0.1 | 目标分支已合并，本地/CI 构建通过 | | |
| 0.2 | 完成 `docs/ops/production-checklist.md` | | |
| 0.3 | MySQL 备份（含 `moli` 库） | | |
| 0.4 | 确认 Nacos `prod` 命名空间与各服务实例 | | |
| 0.5 | 确认各服务 `conf/moli-*.env` 密码非占位符（「请替换」） | | |
| 0.6 | 约定发布窗口 + 回滚联系人 | | |

---

## 1. 本地准备（Windows 开发机）

```powershell
cd D:\work\moli_project\moli-project-distribute

# 检查并编辑（不提交 Git）
#   deploy\user-center\conf\moli-user-center.env
#   deploy\gateway\conf\moli-gateway.env
#   deploy\knowledge\conf\moli-knowledge.env
#   deploy\*\application-pro.yml
# 模板：deploy\linux\moli-*.env.example
```

打包三服务：

```powershell
mvn -pl moli-user-center/moli-user-center-server,moli-gateway,moli-knowledge/moli-knowledge-server -am package -DskipTests
```

产物：

| 服务 | JAR |
|------|-----|
| user-center | `moli-user-center\moli-user-center-server\target\moli-user-center-server-*.jar` |
| gateway | `moli-gateway\target\moli-gateway-*.jar` |
| knowledge | `moli-knowledge\moli-knowledge-server\target\moli-knowledge-server-*.jar` |

---

## 2. 上传到服务器

将以下内容同步到 **`/opt/moli-project-distribute`**（git pull / rsync / scp 均可）：

```
/opt/moli-project-distribute/
├── moli-user-center/
│   ├── moli-user-center-server-*.jar      ← 新 JAR
│   ├── application-pro.yml                ← 来自 deploy/user-center/ 模板
│   └── conf/moli-user-center.env
├── moli-gateway/
│   ├── moli-gateway-*.jar
│   ├── application-pro.yml
│   └── conf/moli-gateway.env
├── moli-knowledge/
│   ├── moli-knowledge-server-*.jar
│   ├── application-pro.yml
│   └── conf/moli-knowledge.env
├── moli-knowledge/kb/                     ← wiki 源（有文档变更时必须更新）
└── deploy/linux/moli-service.sh
```

**本地 `deploy/` → EC2**（只上传配置与 JAR，勿把含密钥的目录提交 Git）：

| 本地 | EC2 |
|------|-----|
| `deploy/user-center/application-pro.yml` | `moli-user-center/application-pro.yml` |
| `deploy/user-center/conf/moli-user-center.env` | `moli-user-center/conf/moli-user-center.env` |
| `deploy/gateway/*` | `moli-gateway/` |
| `deploy/knowledge/*` | `moli-knowledge/` |
| JAR（target 产物） | 各 `moli-*/` 根目录 |

```bash
cd /opt/moli-project-distribute
chmod +x deploy/linux/moli-service.sh
chmod 600 moli-*/conf/*.env

./deploy/linux/moli-service.sh user-center start
./deploy/linux/moli-service.sh knowledge start
./deploy/linux/moli-service.sh gateway start
```

---

## 3. 数据库（有增量 SQL 时）

新环境全量：

```bash
# 见 scripts/init-db.ps1 或 scripts/README.md
mysql ... < scripts/moli.sql
# 再按 docs/ops/sql-migration-order.md 顺序执行增量
```

已有库：只跑**未执行过**的 `docs/sql/*.sql`，顺序见 `docs/ops/sql-migration-order.md`。

---

## 4. Wiki → MySQL 同步（有 wiki 变更时必做）

```bash
cd /opt/moli-project-distribute/moli-knowledge/kb

python tools/lint.py --strict --wiki-dir wiki
bash tools/ci/run_sync.sh dry-run-all
bash tools/ci/run_sync.sh sync-all
```

---

## 5. 停服 → 换包 → 启服

**顺序**：先停 gateway，再停业务，换 JAR，再按依赖启动。

```bash
cd /opt/moli-project-distribute
SH=./deploy/linux/moli-service.sh

# --- 停止（逆序）---
$SH gateway stop
$SH knowledge stop
$SH user-center stop

# --- 替换 JAR（上传后已就位则跳过）---

# --- 启动（正序）---
$SH user-center start
$SH knowledge start
$SH gateway start

# --- 状态 ---
$SH user-center status
$SH knowledge status
$SH gateway status
```

systemd（若已安装）：

```bash
sudo systemctl restart moli-user-center
sudo systemctl restart moli-knowledge
sudo systemctl restart moli-gateway
```

---

## 6. 上线冒烟（P0 最小集）

网关基址：`http://<host>:21000`

| # | 命令 | 期望 |
|---|------|------|
| S1 | `GET /UserCenter/login`（POST 登录拿 token） | 200，有 token |
| S2 | `GET /OrderServer/seckill/ping` | 200（order 若同机部署） |
| S3 | `GET /KnowledgeServer/kb/index?spaceId=900000000000000001` + Authorization | 200 |
| S4 | `GET /BiServer/demo/test` | 200（bi 若部署） |
| S5 | 前端登录 + 知识库浏览 | 页面正常 |

完整清单：`docs/test/release-smoke-checklist.md` · 网关专项：`docs/test/gateway-smoke.md`

---

## 7. 发布后

| # | 项 | ✓ |
|---|----|---|
| 7.1 | 各服务 `logs/*.log` 15 分钟内无 ERROR 刷屏 | |
| 7.2 | Nacos 三服务实例健康 | |
| 7.3 | 知会测试/业务「v1 范围」 | |
| 7.4 | 记录版本号、发布时间、执行人 | |

---

## 8. 回滚

1. **应用回滚**：恢复上一版 JAR，按 §5 逆/正序重启  
2. **DB**：优先只回滚应用；动库需用发布前备份  
3. **Wiki**：`git checkout` 上一 tag 的 `kb/` → 重新 `sync-all`  

详情：`docs/ops/rollback-guide.md`

---

## 9. 常用命令速查

```bash
cd /opt/moli-project-distribute
SH=./deploy/linux/moli-service.sh

$SH user-center logs 200
$SH gateway restart
$SH knowledge status

cd moli-user-center
source <(sed 's/\r$//' conf/moli-user-center.env)
$JAVA_HOME/bin/java $JAVA_OPTS -jar moli-user-center-server-*.jar \
  --spring.profiles.active=pro \
  --spring.config.additional-location=file:./application-pro.yml
```

---

## 10. 配置对照

### 10.1 目录与文件

| 服务 | APP_HOME | env 文件 | 业务配置 |
|------|----------|----------|----------|
| user-center | `.../moli-user-center` | `conf/moli-user-center.env` | `application-pro.yml` |
| gateway | `.../moli-gateway` | `conf/moli-gateway.env` | `application-pro.yml` |
| knowledge | `.../moli-knowledge` | `conf/moli-knowledge.env` | `application-pro.yml` |

**两层配置（勿混淆）**：

| 层 | 位置 | 作用 |
|----|------|------|
| **bootstrap**（在 JAR 内 `bootstrap.yml`，pro 段） | 读 env 里 `NACOS_*` | Nacos **服务注册发现** |
| **application-pro.yml**（JAR 旁外挂） | 端口、路由、MySQL、Redis 等 | **业务与网关路由** |

启动命令由脚本注入：`--spring.config.additional-location=file:./application-pro.yml`  
**改 Nacos 地址/namespace → 改 `conf/moli-*.env`**，不必重打 JAR。

### 10.2 env 常用键

| 键 | 说明 |
|----|------|
| `MOLI_DEPLOY_ROOT` / `MOLI_ROOT` / `APP_HOME` | 部署根；**脚本会强制** `APP_HOME=/opt/moli-project-distribute/moli-<服务>`（env 里旧 `/opt/moli/*` 会被忽略） |
| `SPRING_PROFILES_ACTIVE=pro` | 必须 |
| `NACOS_SERVER_ADDR` | Nacos HTTP 地址，如 `http://127.0.0.1:8848` |
| `NACOS_NAMESPACE` | 生产命名空间 ID 或名（如 `prod`），**各服务必须一致** |
| `NACOS_USERNAME` / `NACOS_PASSWORD` | Nacos 登录（bootstrap pro 用） |
| `SPRING_DATASOURCE_*` | user-center / knowledge MySQL |
| `SSO_SHARED_SECRET` | user-center 必改 |
| `KB_WIKI_ROOT` | knowledge wiki 源，默认 `.../moli-knowledge/kb` |
| `SERVER_PORT` | gateway 默认 `21000` |

---

## 附录 A：EC2 常见问题

### `$'\r': command not found` / `bash\r: No such file or directory`

**原因**：Windows 编辑或上传的文件为 CRLF。`.env` 与 **`deploy/linux/*.sh`** 都会中招；后者会导致 shebang 变成 `bash\r`，脚本根本跑不起来。

**一次性修复（EC2 上执行）**：

```bash
cd /opt/moli-project-distribute
sed -i 's/\r$//' deploy/linux/*.sh
sed -i 's/\r$//' moli-*/conf/moli-*.env
chmod +x deploy/linux/moli-service.sh
```

或：

```bash
sudo yum install -y dos2unix
dos2unix deploy/linux/*.sh moli-*/conf/moli-*.env
```

`moli-service.sh` 加载 `.env` 时会自动 strip `\r`，但 **脚本自身必须是 LF**（需 `sed`/`dos2unix` 或 git pull 后 `--renormalize` 的版本）。

### `java not found`

脚本找不到的是 **`java` 命令在 PATH 里**，不是说你没装 JDK。常见原因：JDK 在 `/usr/lib/jvm/...` 但非交互 shell 的 PATH 里没有。

```bash
which java
ls /usr/lib/jvm/
java -version
```

在 `conf/moli-*.env` 里指向**已有** JDK（与线上一致）：

```bash
JAVA_HOME=/usr/lib/jvm/你的jdk目录名
```

新版脚本也会自动扫描 `/usr/lib/jvm/*`。

---

## 附录：首次上线 checklist

- [ ] `/opt/moli-project-distribute` 整仓就位  
- [ ] `moli-user-center/`、`moli-gateway/`、`moli-knowledge/` 配置与 JAR 就位  
- [ ] `deploy/linux/moli-service.sh` 可执行  
- [ ] MySQL `moli` 库 + 增量 SQL 已执行  
- [ ] Nacos / Redis 可达  
- [ ] Wiki sync 完成（若有 kb 变更）  
- [ ] §6 冒烟通过  
