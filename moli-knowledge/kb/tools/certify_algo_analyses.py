# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for 技術要素（アルゴ） (10 questions)."""

ANALYSES: dict[int, dict[str, str]] = {}

def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}

_a(1, "200 元数组线性搜索，不存在概率 0.25，求平均比较次数。", "125", """**考点：** 线性搜索期望比较次数。

**公式（HTML 反馈）：**
- 不存在：需比较 **200** 次（概率 0.25）
- 存在：平均 **(1+200)/2 = 100.5** 次（概率 0.75）

**期望：** 200×0.25 + 100.5×0.75 = 50 + 75.375 = **125.375 ≈ 125**

**易错点：** 存在时平均是 (n+1)/2，不是 n/2；概率加权勿漏。""")

_a(2, "哪棵二叉树满足 BST 条件？", "选项 a（见 HTML 插图）", """**考点：** 二叉搜索树性质。

**规则：** 左子树 **< 根 < 右子树**（递归成立）。

逐棵检验四棵候选树（HTML 内嵌图）：
- 违反「左小右大」的 **立即排除**
- **选项 a** 为唯一满足 BST 的树 ✓

**易错点：** 仅根左右满足不够，**所有** 左/右子树须递归满足。""")

_a(3, "二叉树三种遍历，哪条描述正确？", "中間順 → VXUYWZ", """**考点：** 先序/中序/后序遍历。

树结构（反馈）：根 **U**，左子 **V-X**，右子 **W-Y,Z**

| 遍历 | 顺序 | 结果 |
|------|------|------|
| 先序 | 根-左-右 | **UVXWYZ** |
| **中序** | 左-根-右 | **VXUYWZ** ✓ |
| 后序 | 左-右-根 | XVYZWU |

**易错点：** 选项 c 才是中序+正确串；a 把先序结果标成中序。""")

_a(4, "按 16→14→18→20→10→11→13→17→15 插入空 BST，正确树形？", "选项 c（见 HTML 插图）", """**考点：** BST 逐条插入。

**手工要点（顺序插入）：**
1. 16 为根
2. 14 左，18 右；20 在 18 右
3. 10 在左链；11 在 10 右；13 在 11 右、14 左
4. 17 在 18 左；15 在 14 右（或依 17 位置微调）

对照四棵候选图 → **选项 c** 与逐步插入一致。

**易错点：** 与模擬問題5 Q6 插入序列 **不同**（本卷含 10,11,13）。""")

_a(5, "8×8 数组 P→Q 流程图【a】应填何种赋值？", "Q(7-j, i)←p(i, j)", """**考点：** 二维数组 **旋转** 坐标映射。

反馈验证 P(0,1)→Q(6,0)：
- Q(7−1, 0) = Q(6,0) ✓

一般式：**Q(7−j, i) ← p(i, j)**（90° 旋转变换之一，见 HTML 图1～3）

**易错点：** 下标从 0 起；7−j 与 7−i 勿混。""")

_a(6, "哈希法存 123456，数组中位置？", "0", """**考点：** 除法/折叠哈希（见 HTML 题图条件）。

**计算（反馈）：**
10 − mod(1+2+3+4+5+6, 11)  
= 10 − mod(21, 11)  
= 10 − 10 = **0**

**易错点：** 先对各位数字求和再 mod；最终用 **10 − 余数**（题设公式）。""")

_a(7, "递归定义 F(n), G(n)，求 F(5)。", "27", """**考点：** 递推/相互递归手算。

| n | F(n) | G(n) |
|---|------|------|
| 0 | 1 | 0 |
| 1 | 1 | 3×1−1=2 |
| 2 | 2×2+2=6 | 3×1−2=1 |
| 3 | 2×1+3=5 | 3×6−3=15 |
| 4 | 2×15+4=34 | 3×5−4=11 |
| 5 | **2×11+5=27** | … |

**F(5) = 27**

**易错点：** F、G 交替依赖，须 **从 0 顺序** 算到 5。""")

_a(8, "相邻元素比较交换、反复排序的算法？", "バブルソート", """**考点：** 排序算法特征。

**冒泡排序：** 未排序区 **相邻** 比较，逆序则 **交换**，每轮将极值「冒」到端点。

**易错点：** 选择=每轮选最值；插入=插入已排序区；快排=分区。""")

_a(9, "按基准分成「≥基准」与「<基准」两组反复排序？", "クイックソート", """**考点：** 快速排序。

**快排：** 选 pivot，分成 **≥** 与 **<** 两组 **递归** 排序。

**易错点：** 与 Q8 冒泡对比记忆：快排=分区，冒泡=相邻交换。""")

_a(10, "86 品目冒泡降序，比较次数？", "3,655", """**考点：** 冒泡排序比较次数。

**公式：** n 个元素最多比较 **n(n−1)/2** 次。

86×85/2 = 43×85 = **3,655**

**推导：** 第 k 轮比较 (n−k) 次，总和 85+84+…+1。

**易错点：** 7310=2×3655（误乘 2）；172 无此意义。""")
