# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for 模擬問題5 (50 questions)."""

ANALYSES: dict[int, dict[str, str]] = {}

def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}

_a(1, "十六进制【　】× 0.2₁₆ = A.BF₁₆，求【　】。", "55.F8", """**考点：** 十六进制小数乘除。

**推导：**
- A.BF₁₆ = 10 + 11/16 + 15/256 ≈ **10.746**
- 0.2₁₆ = 2/16 = **0.125**
- 被乘数 = 10.746 ÷ 0.125 ≈ **85.97**
- 55.F8₁₆ = 85 + 15/16 + 8/256 = **85.96875** ✓

**易错点：** 题面「6進数」为笔误，实际按 **16 进制幂次** 计算；小数按 16 负幂展开。""")

_a(2, "三寄存器浮点值，哪条关于桁落ち/情報落ち描述正确？", "レジスタ2+3→桁落ち；1+2→情報落ち", """**考点：** 浮点误差（见 `67_02.png`）。

| 寄存器 | 值 |
|--------|-----|
| R1 | -0.81726×10⁵ |
| R2 | -0.81727×10⁻⁵ |
| R3 | +0.81728×10⁻⁵ |

- **R2+R3**：同量级相近数相加/相消 → **桁落ち** ✓
- **R1+R2**：极大+极小 → 小数被吞没 → **情報落ち** ✓

**易错点：** R2−R3 才是相近相减；本题问的是 **加算**。""")

_a(3, "image 五字母排列，两端均为母音的个数？", "36", """**考点：** 排列组合。

字母 i,m,a,g,e；母音 a,i,e（3 个），辅音 m,g（2 个）。

1. 两端各 1 母音：P(3,2) = **6** 种
2. 中间 3 位排列剩余 3 字母：3! = **6**
3. 合计 6×6 = **36**

**易错点：** 120=5! 未限制两端；72 只算了一端。""")

_a(4, "状态机从 s 起按给定信号序列输入，最终状态？", "S3", """**考点：** 有限状态机逐步迁移（见 `67_01.png`）。

从初始 **s**，按 r→s→p→q→r→p→s→q→S 依次查表；空栏表示 **保持当前状态**。

逐步追踪至最后一输入 S 后 → **S3**。

**易错点：** 勿漏「不变」格；每步用 **当前状态+输入** 查下一状态。""")

_a(5, "排序算法说明表：C 行对应哪种排序？", "バブルソート", """**考点：** 四种排序特征对照（见 `67_05.png`）。

**冒泡排序：** 相邻元素比较交换，大（小）值逐步「冒泡」到端点。

表中 **C 行** 描述与此一致 → **バブルソート**。

**易错点：** 快排=分区递归；选择=每轮选最值；插入=已排序区插入。""")

_a(6, "按给定顺序插入空 BST，正确树形？", "选项 d（67_06_04.png）", """**考点：** 二叉搜索树插入规则。

顺序：16→14→18→20→17→15→13→11→10  
规则：小左大右，逐棵从根向下比较插入。

对照四棵候选树（`67_06_01`～`04.png`）→ **d** 与手工建树一致。

**易错点：** 20 在 18 右子树；10 在 11 左子树，勿与插入顺序混淆。""")

_a(7, "8×8 数组 P→Q 流程图，Q 表示 P 的何种变换？", "右旋 90°", """**考点：** 二维数组坐标变换（见 `67_07.png`）。

对照图 1 四种变换：
- 右旋 90°：Q[c,d] 与 P 的对应关系为 **列变行、行镜像** 等（依流程图式子）

流程图执行结果 = **P 图形右旋 90°** → 选项 **d**。

**易错点：** 左/右旋与上下/左右翻转公式不同；务必对照流程图赋值式。""")

_a(8, "寻址方式表：A 行是哪种寻址？", "間接アドレス指定", """**考点：** 四种寻址（见 `67_08.png`）。

**间接寻址：** 指令给出 **地址的地址**，先读主存得有效地址，再访问操作数。

**易错点：** 直接=地址即操作数地址；立即=操作数在指令中；相对=PC+偏移。""")

_a(9, "三 CPU 每秒指令数从多到少排序。", "CPU-2, CPU-1, CPU-3", """**考点：** IPS ≈ 时钟频率 / CPI（见 `67_09.png`）。

| CPU | f/CPI | 约 GIPS |
|-----|-------|---------|
| CPU-2 | 3.6/1.4 | **2.57**（最高） |
| CPU-1 | 3.3/1.3 | 2.54 |
| CPU-3 | 3.9/1.6 | 2.44 |

顺序：**CPU-2 > CPU-1 > CPU-3**

**易错点：** 频率最高≠最快，要看 **f÷CPI**。""")

_a(10, "四系统缓存，有效访问时间最短？", "SC", """**考点：** Te = Tc×p + Tm×(1−p)（见 `67_10.png`）。

| 系统 | 计算 | Te (ns) |
|------|------|---------|
| SA | 8×0.8+61×0.2 | 18.6 |
| SB | 10×0.8+57×0.2 | 19.4 |
| **SC** | 12×0.9+73×0.1 | **18.1** ✓ |
| SD | 14×0.9+75×0.1 | 20.1 |

**易错点：** 命中率 p 用小数；未命中路径含 Tm。""")

_a(11, "RAID 描述正确的一项。", "ミラーリング…RAID1", """**考点：** RAID 术语。

**正确：** **ミラーリング** = 同一数据 **重复写入多盘** 提高可靠性，规定于 **RAID1**。

**易错点：** ストライピング=分散写入提速（RAID0 等）；勿与镜像混淆。""")

_a(12, "三系统 MTTR 从短到长排序。", "S2, S3, S1", """**考点：** 稼动率 = MTBF/(MTBF+MTTR) → MTTR = MTBF×(1−A)/A（见 `67_12.png`）。

| 系统 | MTBF | 稼动率 | MTTR |
|------|------|--------|------|
| S1 | 264h | 0.96 | **11h** |
| S2 | 291h | 0.97 | **9h**（最短） |
| S3 | 490h | 0.98 | **10h** |

顺序：**S2 < S3 < S1**

**易错点：** MTBF 长不等于 MTTR 短；本题问 **修复时间**。""")

_a(13, "LRU 换页时，被替换的页是？", "最久未被引用的页", """**考点：** LRU 页面置换。

**LRU：** 替换 **最后引用以来经过去时间最长** 的页。

**易错点：** FIFO=最早进入主存；LFU=使用频率最低；≠「页入时间最长」。""")

_a(14, "就绪→执行 的迁移①对应何种事件？", "CPU使用権の割当て", """**考点：** 任务状态迁移（见 `67_14.png`）。

**実行可能 → 実行中**：调度程序 **分配 CPU** → **CPUの使用権の割当て**

**易错点：** 时间片到=就绪←执行；I/O 请求=等待；I/O 完成=就绪。""")

_a(15, "编译前将扩展语法转为可编译命令的工具？", "プリプロセッサ", """**考点：** 语言处理程序。

**预处理器：** #include、宏展开等 **编译前** 文本/语法变换。

**易错点：** 交叉编译=目标机不同；编译器编译器=生成编译器；生成器=部分工具链用语。""")

_a(16, "校验表：D 行对应哪种 check？", "フォーマットチェック", """**考点：** 输入校验（见 `67_16.png`）。

四类：数值/平衡/格式/上下限。**D 行** → **フォーマットチェック**（位数、符号位置等格式）。

**易错点：** リミット=范围；バランス=借贷平衡。""")

_a(17, "边收边播以缩短等待的媒体方式？", "ストリーミング", """**考点：** 流媒体。

**Streaming：** 缓冲一定数据即开始播放，**边下载边播放**。

**易错点：** スラッシング=虚存抖动；ディスパッチング=调度。""")

_a(18, "教职员-科目 UML 模型（1:N，科目至少1人，可有闲职员）", "选项 b（67_18_イ.png）", """**考点：** UML 类图 cardinality。

(1) 1 职员 → 多科目  
(2) 1 科目 → **≥1** 职员（必须有人教）  
(3) 可有 **不任课** 职员  

**多对多 + 可选参与** → **イ** 图。

**易错点：** 勿选强制每职员必任课；科目侧不能 0..1。""")

_a(19, "Roll-forward 的正确说明。", "检查点恢复后用日志 after image 前滚", """**考点：** DB 恢复。

**Roll-forward：** 先恢复到 **检查点**，再用日志 **更新后信息（after image）** **向前** 重做已提交更新。

**易错点：** Rollback=用 before image 撤销；不是回到事务开始前。""")

_a(20, "三事务锁类型填空 a,b（见测试场景）。", "a:共有　b:専有", """**考点：** 共享/排他锁与并发（见 `67_20.png`）。

由测试结果反推：
- T1 持 R1 等，T2 同时跑 **不等待** → 读锁可共存
- T3 与 T1/T2 组合 **会等待** → 需 **排他** 冲突

→ **a:共有ロック b:専有ロック**

**易错点：** 同时运行上限 2 个；排他与共享/排他冲突会阻塞。""")

_a(21, "DB 术语表：C 行是？", "データマート", """**考点：** 数据仓库术语（见 `67_21.png`）。

**数据集市（Data Mart）：** 面向 **特定部门/主题** 的 **小规模** 分析用数据集合。

**易错点：** 挖掘=发现模式；字典=元数据；清洗=质量修正。""")

_a(22, "438Mbps 线路、219MB/件、利用率 0.8，1 小时最多传几件？", "720", """**考点：** 传输时间与吞吐。

**每件时间** = 219×10⁶×8 bit ÷ (438×10⁶×0.8)  
= 1752/350.4 ≈ **5 秒/件**

**1 小时** = 3600/5 = **720 件**

**易错点：** 字节×8 转 bit；利用率 0.8 乘在带宽上。""")

_a(23, "邮件中处理图像/音视频等的扩展规范？", "MIME", """**考点：** 电子邮件扩展。

**MIME：** Multipurpose Internet Mail Extensions，非 ASCII 附件与多部分正文。

**易错点：** DNS=域名；NAPT=地址转换；IPoE=接入方式。""")

_a(24, "OSI 网络层说明。", "端到端路由与中继", """**考点：** OSI 各层职责。

**网络层：** **端到端** 路径选择、**中继**、逻辑寻址（IP 等）。

| 层 | 职责 |
|----|------|
| 物理 | 比特传输 |
| 数据链路 | 相邻节点帧 |
| 运输 | 端到端可靠/透明 |
| **网络** | **路由** ✓ |""")

_a(25, "验证数字签名时使用哪把钥？", "送信者の公開鍵", """**考点：** 数字签名。

签名 = 发送者用 **私钥** 加密摘要；验证 = 接收者用 **发送者公钥** 解密比对。

**易错点：** 加密保密才用接收者公钥；签名验证用 **发送者公钥**。""")

_a(26, "DDoS 攻击说明。", "多僵尸机向目标服务器洪泛包", """**考点：** 攻击类型。

**DDoS：** 控制 **多台** 计算机作 **踏台**，**同时** 向目标 **大量发包** 使服务瘫痪。

**易错点：** a=中间人/篡改；b=水坑；c=零日。""")

_a(27, "加密勒索赎金才恢复使用的恶意软件？", "ランサムウェア", """**考点：** 恶意软件分类。

**Ransomware：** 加密/锁定系统后 **勒索** 支付赎金。

**易错点：** 间谍=窃密；广告=弹广告。""")

_a(28, "WAF 说明。", "监控 Web 通信并阻断攻击模式", """**考点：** Web 应用防火墙。

**WAF：** 监控 Web 服务器/应用 **外部访问**，日志+规则 **检测并阻断** SQLi/XSS 等攻击。

**易错点：** a=IPsec；c=VPN；d=Wi-Fi 安全。""")

_a(29, "IETF RFC 2440、混合加密的邮件加密规范？", "OpenPGP", """**考点：** 邮件加密标准。

**OpenPGP：** 公钥+对称 **混合加密**，RFC 2440 等定义。

**易错点：** DKIM=域名签名验证邮件；RADIUS=认证协议。""")

_a(30, "DFD 说明。", "进程/数据存储/外部/数据流", """**考点：** 结构化分析 DFD。

**DFD：** 用 **进程、数据存储、外部实体、数据流** 表示业务 **数据流动**。

**易错点：** b=ER 图；c=流程图(JIS)；a=设计模式。""")

_a(31, "UML 图说明表：A 行是？", "シーケンス図", """**考点：** UML 图类型（见 `67_31.png`）。

官方反馈：**A=シーケンス図** B=状态图 C=活动图 D=用例图。

**易错点：** 序列图强调 **对象间消息时序**；活动图是流程/并行。""")

_a(32, "泛化（generalization）说明。", "子类共性提取为超类", """**考点：** OOP 泛化 vs 继承。

**泛化：** 从 **多个子类共同性质** 抽象出 **超类**（自底向上建模）。

**易错点：** b 描述的是 **继承**（超类→子类）；d 是 **封装**。""")

_a(33, "测吞吐/响应时间验证规格是否达标？", "性能テスト", """**考点：** 测试类型。

**性能测试：** 测 **吞吐量、响应时间** 等是否满足 **性能需求**。

**易错点：** 负载=高负载下表现；回归=变更后再测；功能=正确性。""")

_a(34, "科目代码 11≤x≤38 的边界值测试数据？", "10,11,38,39", """**考点：** 边界值分析。

有效范围 [11,38]，边界值取 **刚好无效/刚好有效** 各一侧：

- 下界外 **10**、下界 **11**
- 上界 **38**、上界外 **39**

→ **10,11,38,39**

**易错点：** 12/37 是健壮性扩展，标准四边界为 min−1,min,max,max+1。""")

_a(35, "箭线图关键路径？", "A → F → I → K", """**考点：** CPM 关键路径（见 `67_35.png`）。

对各路径 **累加工期**，**最长** 且无浮动时间为关键路径。

计算得 **A→F→I→K** 为最长路径。

**易错点：** 关键路径决定最短完成工期；并行分支取大不取和时需看清汇合点。""")

_a(36, "甘特图示例？", "横棒对比各作业预定与实绩", """**考点：** 项目日程 **甘特图**。

**甘特图：** 时间轴上 **横条** 表示各作业 **计划 vs 实际** 工期。

**易错点：** a=气泡图；b=折线；d=雷达图。""")

_a(37, "TCO 说明。", "导入+运维+教育等全生命周期总费用", """**考点：** Total Cost of Ownership。

**TCO** = 系统 **导入费** + **运行维护、教育** 等 **全生命周期** 总成本。

**易错点：** d 仅导入 CAPEX；c 仅运营 OPEX 一部分。""")

_a(38, "按流程输入→审批→自动流转的系统？", "ワークフローシステム", """**考点：** 业务系统类型。

**工作流：** 定义流程， **回览/承认** 后 **自动** 进入下一工序。

**易错点：** SFA=营业支援；CIM=生产信息；追溯=追踪履历。""")

_a(39, "Web 缩短文书 0.1h/人、客户 10 人/日，其他业务最多几小时？", "1.0", """**考点：** 时间分析表（见 `67_39.png`）。

**约束：** 总业务 **8h** 不变；**每人** 对面销售 **0.55h** 不变。

| 项目 | 计算 |
|------|------|
| 对面 10 人 | 10×0.55 = **5.5h** |
| 文书 10 人 | 10×(0.25−0.1) = **1.5h** |
| その他 | 8−5.5−1.5 = **1.0h** |

**易错点：** 文书原 2.0h/8 人=0.25h/人；减 0.1 后 0.15h/人。""")

_a(40, "PPM 矩阵：W 对应？", "花形", """**考点：** PPM 四象限（见 `67_40.png`）。

**花形（Star）：** **高市场增长率 + 高相对份额**，需投资维持地位。

对照矩阵 **W** 位置 → **花形**。

**易错点：** 金牛=高份额低增长；问题儿=低份额高增长；败犬=双低。""")

_a(41, "分析强弱项与机会威胁定策？", "SWOT 分析", """**考点：** 战略分析框架。

**SWOT：** **S/W**（内）+ **O/T**（外）→ 匹配策略。

**易错点：** 3C=客户/competitor/company；五力=行业结构；价值链=活动分解。""")

_a(42, "企业内知识共享提升解决问题能力？", "ナレッジマネジメント", """**考点：** KM vs ERP/CRM/SCM。

**知识管理：** 散在 **个人/部门知识** **企业共享** 提升 **问题解决与业绩**。

**易错点：** b=ERP 资源计划；c=CRM 客户；d=SCM 供应链。""")

_a(43, "工程系统术语表：X 行是？", "CAE", """**考点：** CAD/CAE/CAM/MRP（见 `67_43.png`）。

**CAE（Computer Aided Engineering）：** **仿真分析**（强度、流体等）。

表中 **X** 行 → **CAE**。

**易错点：** CAD=设计；CAM=制造；MRP=物料计划。""")

_a(44, "个人闲置资产经互联网共享借贷的经济？", "シェアリングエコノミー", """**考点：** 新经济用语。

**Sharing Economy：** 个人 **闲置资产** 经 **互联网平台** 共享/租赁。

**易错点：** 众包=外包任务；长尾=小众商品聚合。""")

_a(45, "CGM 说明。", "用户投稿生成内容的 Web 等", """**考点：** Consumer Generated Media。

**CGM：** **消费者投稿/提供信息** 经整理 **生成** 网站内容（博客、点评等）。

**易错点：** b=FinTech；c=智慧城市；d=大众媒体。""")

_a(46, "ABC 分析：累计 70% 销售额的 A 群商品几种？", "6", """**考点：** ABC（帕累托）分析（见 `67_46.png`）。

1. 按销售额 **降序** 排列商品 1～20  
2. 算 **累计构成比**  
3. 累计达 **≥70%** 为止归入 **A 群**

对照表 → **6 种** 商品。

**易错点：** 须按 **金额** 排序；不是按商品编号。""")

_a(47, "两产品线性规划，最大日利润？", "145,000", """**考点：** 线性规划（见 `67_47.png`）。

设 P=x，Q=y：
- max **1500x+1000y**
- x+y≤120，2x+y≤170，x,y≥0

交点 **(50,70)**：1500×50+1000×70 = **145,000 円**

**易错点：** (120,0)=180000 但违反 B 约束；验算 2×50+70=170。""")

_a(48, "两散布图相关系数 r1,r2 与回归斜率不等式。", "−1<r2<0<r1<1", """**考点：** 相关系数与散布形态（见 `67_48.png`）。

- 散布 1（p-q）：**正相关** → **0<r1<1**，斜率 k1>0  
- 散布 2（s-t）：**负相关** → **−1<r2<0**，斜率 k2<0  

→ **−1 < r2 < 0 < r1 < 1**（选项 d）

**易错点：** 正相关 r>0；负相关 r<0；勿与 k 的大小混选。""")

_a(49, "损益表求营业利益与经常利益。", "15,000 / 15,500 百万円", """**考点：** 日本损益计算（见 `67_49.png`）。

| 项目 | 金额 |
|------|------|
| 売上高 | 70,000 |
| 売上原価 | 50,000 |
| 売上総利益 | 20,000 |
| 販管費 | 5,000 |
| **営業利益** | **15,000** |
| +営業外収益 −営業外費用 | +1,000−500 |
| **経常利益** | **15,500** |

**易错点：** 经常利益含 **营业外** 收支；营业利润不含。""")

_a(50, "产业财产权表：C 行是？", "意匠権", """**考点：** 四权对照（见 `67_50.png`）。

**意匠权：** 保护 **物品形状、图案、色彩** 等 **外观设计**。

**C 行** → **意匠権**（选项 a）。

**易错点：** 特許=发明；商標=标识；实用新案=小改进。""")
