# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for 開発技術 (20 questions)."""

ANALYSES: dict[int, dict[str, str]] = {}


def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}

_a(1, "调用被测模块、代替上位模块的测试用模块？", "ドライバ", """**考点：** 集成测试中的测试桩/驱动。

**推导：** **ドライバ（Driver）** = 调用被测模块，代替 **上位** 调用者。

**易错点：** スタブ=代替 **下位** 被调用模块。""")

_a(2, "着眼内部结构、走通模块内全部路径的测试？", "ホワイトボックステスト", """**考点：** 白盒 vs 黑盒。

**推导：** 内部结构 + 全路径覆盖 = **ホワイトボックステスト**。

**易错点：** ブラックボックス=不看内部；トップ/ボトム=集成策略。""")

_a(3, "UML 行为图中表示系统功能与外部交互的图？", "ユースケース図", """**考点：** UML 行为图分类。

**推导：** 功能 + 利用者/外部系统 = **ユースケース図**。

**易错点：** シーケンス=时序消息；ステート=状态迁移。""")

_a(4, "売上月有效范围 1~12 的限界值分析测试数据组合？", "0, 1, 12, 13", """**考点：** 限界値分析（边界值分析）。

**推导：** 有效 [1,12] → 边界及邻值：**0, 1, 12, 13**。

**易错点：** 含无效端 0 与 13；不是 0,1,13,14 或仅有效内点。""")

_a(5, "从最下层模块开始逐层向上结合的测试？", "ボトムアップテスト", """**考点：** 集成测试策略。

**推导：** 最下位→逐层向上结合 = **ボトムアップテスト**。

**易错点：** トップダウン=从最上位开始。""")

_a(6, "从最上层模块开始逐层向下结合的测试？", "トップダウンテスト", """**考点：** 集成测试策略。

**推导：** 最上位→逐层向下结合 = **トップダウンテスト**。

**易错点：** 与 Q5 方向相反。""")

_a(7, "UML 行为图中表示对象状态与迁移的图？", "ステートチャート図", """**考点：** UML 行为图。

**推导：** 状态 + 事件/条件 → 迁移 = **ステートチャート図**。

**易错点：** アクティビティ=业务流程/控制流。""")

_a(8, "修改后验证其他正常部分是否出现新问题的测试？", "リグレッションテスト", """**考点：** 回归测试。

**推导：** 变更后确认无 **副作用** = **リグレッションテスト**。

**易错点：** 負荷=峰值数据量；性能=吞吐/响应时间。""")

_a(9, "不考虑内部结构、检查输入输出对应关系的测试？", "ブラックボックステスト", """**考点：** 黑盒测试。

**推导：** 不看内部 + I/O 对应 = **ブラックボックステスト**。

**易错点：** 与 Q2 白盒相对。""")

_a(10, "将子类共性汇总为超类的 OOP 术语？", "汎化", """**考点：** OOP 基本概念。

**推导：** 子类共性→超类 = **汎化（Generalization）**。

**易错点：** 特化=继承后局部再定义；カプセル化=隐藏实现。""")

_a(11, "UML 行为图中按时序描述对象间消息交互的图？", "シーケンス図", """**考点：** UML 行为图。

**推导：** 对象间消息 + 时系列 = **シーケンス図**。

**易错点：** ユースケース=功能与 Actor；不是状态图。""")

_a(12, "被测模块调用、代替下位模块的测试用模块？", "スタブ", """**考点：** 测试桩 Stub。

**推导：** 被测模块 **调用** 它 → 代替 **下位** = **スタブ**。

**易错点：** Q1 ドライバ=被测模块被 **上位** 调用。""")

_a(13, "将 Bus/Truck 一般化为 Automobile 的 OOP 术语？", "汎化", """**考点：** 泛化与一般化。

**推导：** 多个具体类→共同超类 = **汎化**。

**易错点：** 与 Q10 同概念，题面用具体例子。""")

_a(14, "峰值数据量下验证系统能否正常运行的测试？", "負荷テスト", """**考点：** 非功能测试分类。

**推导：** ピーク時データ量 = **負荷テスト（Load/Stress）**。

**易错点：** 性能テスト=测吞吐/响应是否达标。""")

_a(15, "数据与方法一体化并隐藏实现细节的机制？", "カプセル化", """**考点：** 封装 Encapsulation。

**推导：** 数据+方法一体 + 信息隐藏 = **カプセル化**。

**易错点：** 汎化=类层次；インスタンス化=生成对象。""")

_a(16, "按类定义分配内存、生成具体对象的术语？", "インスタンス化", """**考点：** 实例化。

**推导：** 类→内存→具体对象 = **インスタンス化**。

**易错点：** 不是泛化或封装。""")

_a(17, "DFD 图中 C1、C2（圆）表示什么？", "処理名（プロセス名）", """**考点：** DFD 符号。

**推导：** 见 HTML 插图——**圆（○）= 処理/プロセス**；C1、C2 为圆 → **処理名**。

**对照：** 矩形=外部；双线=数据存储；箭头=数据流。

**易错点：** D1/D2 才是数据存储（平行线）。""")

_a(18, "测量吞吐/响应时间验证是否满足规格的非功能测试？", "性能テスト", """**考点：** 性能测试。

**推导：** スループット・応答時間 vs 要件 = **性能テスト**。

**易错点：** Q14 負荷=极端负载下能否运行。""")

_a(19, "UML 行为图中表示业务/处理执行流程的图？", "アクティビティ図", """**考点：** UML 行为图。

**推导：** 業務・処理の手順/制御流 = **アクティビティ図**。

**易错点：** シーケンス=对象消息；ステート=单对象状态。""")

_a(20, "DFD 图中 s1、s2（双线）表示什么？", "データストア名", """**考点：** DFD 数据存储符号。

**推导：** 见 HTML 插图——**平行横线（≡）= データストア**；s1、s2 → **データストア名**。

**易错点：** q1/q2 矩形=外部；r1/r2 圆=处理；p* 箭头=数据流。""")

