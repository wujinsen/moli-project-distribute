# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for 模擬問題6 (50 questions)."""

ANALYSES: dict[int, dict[str, str]] = {}


def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}


_a(1, "十进制式 2⁵+2⁴+2²+2⁻¹+2⁻³+2⁻⁵ 的结果用十六进制表示？", "34.A8", """**考点：** 十进制幂次 → 十六进制（整数+小数）。

**推导：**
- 整数：32+16+4 = **52₁₀** = **34₁₆**
- 小数：0.5+0.125+0.03125 = **0.65625₁₀**
  - 0.65625×16 = 10.5 → **A**
  - 0.5×16 = **8**
- 合计 **34.A8₁₆**

**易错点：** 负幂按 1/2ⁿ 展开；小数逐位乘 16 取整。""")

_a(2, "三变量浮点值 x,y,z，哪条关于桁落ち/情報落ち描述正确？", "x+z→桁落ち；x+y→情報落ち", """**考点：** 浮点误差（见 `_q6_imgs/q2_0.png`）。

| 变量 | 值 |
|------|-----|
| x | −0.33125×10⁵ |
| y | −0.33126×10⁻⁵ |
| z | +0.33127×10⁵ |

- **x+z**：同量级、符号相反 → 相消 → **桁落ち** ✓
- **x+y**：极大+极小 → 小数被吞没 → **情報落ち** ✓

**易错点：** x−z 才是相近相减；本题问 **x+z 与 x+y**。""")

_a(3, "1～100 均匀随机整数，既非 3 倍数也非 13 倍数的概率？", "31/50（エ）", """**考点：** 容斥原理。

- 3 的倍数：⌊100/3⌋ = **33**
- 13 的倍数：⌊100/13⌋ = **7**
- 39 的倍数：⌊100/39⌋ = **2**
- 并集：33+7−2 = **38**
- 满足条件：100−38 = **62**
- 概率 = 62/100 = **31/50** → 选项 **エ**

**易错点：** 须减去 3 与 13 公倍数 39 的重复计数。""")

_a(4, "三 PCM 方式按采样间隔从短到长排序。", "方式 X，方式 Y，方式 Z", """**考点：** 采样频率 = 比特率(bit/s) ÷ 量化位数（见 `_q6_imgs/q4_0.png`）。

| 方式 | 计算 | 采样频率 |
|------|------|----------|
| X | 16000×8÷8 | **16000 Hz**（最高） |
| Y | 30000×8÷16 | 15000 Hz |
| Z | 36000×8÷24 | 12000 Hz（最低） |

间隔短 = 频率高 → **X > Y > Z**

**易错点：** 字节/秒须 ×8 转 bit/s；间隔与频率成反比。""")

_a(5, "双栈 + 函数 f/g，可能的数据输出顺序？", "E → D → C → B → A", """**考点：** 栈 LIFO 与双栈转移（见 `_q6_imgs/q5_0.png`、`q5_1.png`）。

- 栈1（顶→底）：A,B,C,D,E；栈2 空
- **f**：栈1 pop → 栈2 push
- **g**：栈2 pop → 输出

**操作：** f×5（全部移入栈2，栈2 顶为 E）→ g×5  
**输出：** E,D,C,B,A ✓

**易错点：** 一次 f 一次 g 只能输出 A 开头；要 E 开头须先 f 全部再 g 全部。""")

_a(6, "二分木三种遍历的正确组合？", "ア", """**考点：** 先行/中間/後行遍历（见 `_q6_imgs/q6_1.png`、`q6_2.png`）。

树结构（根 R）：
- 左：S(U, V(∅,Y))
- 右：T(W, X(Z,∅))

| 遍历 | 序列 |
|------|------|
| 先行 | R,S,U,V,Y,T,W,X,Z |
| 中間 | U,S,V,Y,R,W,T,Z,X |
| 後行 | U,Y,V,S,W,Z,X,T,R |

与选项表对照 → **ア** ✓

**易错点：** 後行以根 R 结尾；勿把中間/後行顺序对调。""")

_a(7, "排序算法说明表：D 行对应哪种？", "クイックソート", """**考点：** 四种排序特征（见 `_q6_imgs/q7_0.png`）。

| 行 | 特征 | 算法 |
|----|------|------|
| A | 已排序区逐位插入 | 挿入ソート |
| B | 最左与余元比较交换 | 選択ソート |
| C | 相邻比较交换 | バブルソート |
| **D** | 按基准分两组递归 | **クイックソート** ✓ |

**易错点：** 快排=分区；冒泡=相邻交换。""")

_a(8, "索引寻址的有效地址公式？", "ADR＋XR", """**考点：** 寻址方式。

**索引寻址：** 有效地址 = **指令地址部 ADR + 索引寄存器 XR**

| 方式 | 公式 |
|------|------|
| 直接 | ADR |
| 相对 | IA+ADR |
| 基址+变址 | ADR+IA+XR |
| **索引** | **ADR+XR** ✓ |

**易错点：** IA 是 **指令存放地址**（PC 类），不是有效地址本身。""")

_a(9, "三 CPU 每秒平均指令数从多到少排序。", "CPU-1，CPU-2，CPU-3", """**考点：** IPS ≈ 时钟频率 ÷ CPI（见 `_q6_imgs/q9_0.png`）。

| CPU | f/CPI | 约 MIPS |
|-----|-------|---------|
| **CPU-1** | 3200/1.1 | **2909**（最高） |
| CPU-2 | 4300/1.7 | 2529 |
| CPU-3 | 4500/2.2 | 2045 |

顺序：**CPU-1 > CPU-2 > CPU-3**

**易错点：** 频率最高≠最快，看 **f÷CPI**。""")

_a(10, "三文件按 8 扇区/块分配，总扇区数？", "96", """**考点：** 块分配向上取整（见 `_q6_imgs/q10_0.png`）。

块大小 = 8×500 = **4000 字节/块**

| 文件 | 大小 | 块数 | 扇区 |
|------|------|------|------|
| FA | 9800 | ⌈9800/4000⌉=3 | 24 |
| FB | 13900 | 4 | 32 |
| FC | 16500 | 5 | 40 |
| **合计** | | | **96** |

**易错点：** 一文件可跨多块，一 block 不能共享；每块占满 8 扇区。""")

_a(11, "RAID 特征表中，属于ミラーリング的组合？", "特徴 B と特徴 C", """**考点：** 条带 vs 镜像（见 `_q6_imgs/q11_0.png`）。

| 特征 | 含义 | 对应 |
|------|------|------|
| A | RAID0 规定技术 | ストライピング |
| **B** | RAID1 规定技术 | **ミラーリング** ✓ |
| **C** | 同数据重复写多盘提可靠性 | **ミラーリング** ✓ |
| D | 分散写多盘提速 | ストライピング |

→ **B と C**

**易错点：** C 是镜像 **效果**；D 是条带 **效果**。""")

_a(12, "三系统 MTBF 从长到短排序。", "S3，S2，S1", """**考点：** 稼动率 A = MTBF/(MTBF+MTTR)（见 `_q6_imgs/q12_0.png`）。

MTBF = MTTR×A/(1−A)

| 系统 | MTTR | A | MTBF |
|------|------|---|------|
| S1 | 11h | 0.99 | 1089h |
| S2 | 37h | 0.97 | 1196h |
| **S3** | 65h | 0.95 | **1235h**（最长） |

顺序：**S3 > S2 > S1**

**易错点：** MTTR 短≠MTBF 长；本题问 **故障间隔**。""")

_a(13, "メモリコンパクション 的正确说明？", "散在小空闲区合并为连续大空闲区", """**考点：** 内存管理术语。

**内存紧缩（Compaction）：** 将主存中 **分散的小空闲区** 合并为 **连续大空闲区**。

| 选项 | 概念 |
|------|------|
| a | 虚拟内存/堆 |
| b | 内存泄漏 |
| **c** | **Compaction** ✓ |
| d | 存储体交错 |

**易错点：** 与分页/交换不同；目的是消除 **外部碎片**。""")

_a(14, "任务状态迁移的正确示例？", "実行→実行可能→実行→待ち→実行可能", """**考点：** 就绪/运行/等待三态。

合法迁移：
- 就绪 → 运行（调度分配 CPU）
- 运行 → 就绪（时间片到）
- 运行 → 等待（I/O 等）
- 等待 → 就绪（I/O 完成）

**选项 c** 符合上述序列 ✓

**易错点：** 等待不能直接→运行；须经 **就绪**。""")

_a(15, "编译器优化的说明。", "解析源程序生成更高效的目标程序", """**考点：** 语言处理程序。

**编译优化：** 分析源程序，生成 **执行时间更短、效率更高** 的目标代码。

**易错点：** b=交叉编译；c=编译器生成器；d=预处理器/宏。""")

_a(16, "DRAM/SDRAM/SRAM 存储单元结构？", "DRAM·SDRAM 用电容器，SRAM 用触发器", """**考点：** 半导体存储结构。

| 类型 | 单元 |
|------|------|
| DRAM | コンデンサ |
| SDRAM | コンデンサ（同步 DRAM） |
| SRAM | フリップフロップ |

**易错点：** SDRAM 仍是 **电容** 型 DRAM 族；不是触发器。""")

_a(17, "借贷方合计一致校验方式？", "バランスチェック", """**考点：** 输入校验四类。

**平衡检查（Balance Check）：** 成对项目（如借方/贷方）**合计是否一致**。

| 类型 | 用途 |
|------|------|
| Numeric | 数值属性 |
| **Balance** | **配对合计** ✓ |
| Format | 格式/位数 |
| Limit | 上下限 |

**易错点：** 勿与 **格式检查** 混淆。""")

_a(18, "DB 障害恢复填空 a,b 的组合？", "ウ（a=更新前，b=更新后）", """**考点：** Rollback / Roll-forward（见 `_q6_imgs/q18_0.png`、`q18_1.png`）。

- **Rollback：** 用日志 **更新前信息（before image）** 撤销 → **a=更新前**
- **Roll-forward：** 检查点恢复后用 **更新后信息（after image）** 重做 → **b=更新后**

→ 选项 **ウ** ✓

**易错点：** Rollback 不是 after；Roll-forward 不是 before。""")

_a(19, "讲座-会员 UML 数据模型（见条件）", "イ", """**考点：** UML 多重性（见 `_q6_imgs/q19_0.png`）。

(1) 1 会员 → 多讲座（会员侧 0..*）
(2) 1 讲座 → ≥1 会员（讲座侧 1..*）
(3) 可有 **不选课** 会员

→ 讲座 0..* — 会员 1..* 为 **イ** ✓

**易错点：** 条件(3) 排除会员侧 1..*；条件(2) 排除讲座侧 0..*。""")

_a(20, "データクレンジング 的说明。", "按规则修正既有数据的表记揺れ·误记·重复", """**考点：** 数据质量术语。

**数据清洗：** 对 **既有数据** 发现表记不一致、误记、重复，**统一/修正/正规化** 提升品质。

**易错点：** b=数据字典；c=数据集市；d=数据挖掘。""")

_a(21, "三事务锁表：哪种情况下不发生资源等待？", "T1 未结束前启动 T3，T3 不等待", """**考点：** 共享/排他锁兼容（见 `_q6_imgs/q21_0.png`）。

| 事务 | R1 | R2 | R3 |
|------|----|----|-----|
| T1 | X | S | — |
| T2 | — | X | — |
| T3 | — | S | X |

- T1+T2：T2 要 R2 的 X，T1 持 S → **T2 等待**
- **T1+T3**：T3 要 R2 的 S，与 T1 的 S **兼容**；R3 无冲突 → **T3 不等待** ✓
- T2+T1 / T2+T3：R2 上 X 与 S/X 冲突 → 等待

**易错点：** 同时运行上限 2 个；S 与 S 可共存，S 与 X 不可。""")

_a(22, "36Mbps 线路、864MB 数据、利用率 0.8，传输时间？", "240 秒", """**考点：** 传输时间 = 数据量 ÷ 有效带宽。

- 数据 = 864×10⁶×8 bit
- 有效带宽 = 36×10⁶×0.8 = 28.8×10⁶ bit/s
- 时间 = 6912/28.8 = **240 秒**

**易错点：** 字节×8；利用率乘在 **线路速率** 上。""")

_a(23, "OSI 物理层说明。", "比特级信号与传输媒体物理规格", """**考点：** OSI 分层。

**物理层：** 相邻设备间 **比特** 传输；规定 **媒体形状、电气/信号形式**。

| 层 | 职责 |
|----|------|
| **物理** | **比特+媒体** ✓ |
| 运输 | 端到端可靠 |
| 网络 | 路由中继 |
| 数据链路 | 相邻节点帧+误检 |

**易错点：** 误检/重传属 **数据链路** 或 **运输** 层。""")

_a(24, "给定 IP 与网络地址，求子网掩码。", "255.255.224.0", """**考点：** 子网划分（见 `_q6_imgs/q24_0.png`）。

- IP：172.28.**122**.50 → 122 = 011**110**10
- 网络：172.28.**96**.0 → 96 = 011**000**00
- 公共网络前缀：/19（第三 octet 前 3 bit + 前两 octet）

→ **255.255.224.0**（/19，范围 172.28.96.0～127.255）

**易错点：** /18 网络为 172.28.64.0；/24 无法得到 96.0 网络号。""")

_a(25, "RSA 数字签名流程填空 a,b？", "エ（发送者私钥加密，发送者公钥解密）", """**考点：** 数字签名（见 `_q6_imgs/q25_0.png`、`q25_1.png`）。

- **a**：摘要 → 签名 = **送信者の秘密鍵** 加密
- **b**：签名 → 摘要 = **送信者の公開鍵** 解密验证

→ **エ** ✓

**易错点：** 保密通信用 **接收者公钥**；签名验证用 **发送者公钥**。""")

_a(26, "ボットネット C&C 服务器角色？", "向被控多台 PC 下达攻击·窃密指示", """**考点：** 恶意软件基础设施。

**C&C 服务器：** 控制 **被入侵的多台计算机**，下达 **DDoS 等攻击** 或 **非法信息收集** 指令。

**易错点：** a=路径遍历；b=钓鱼；d=SQL 注入。""")

_a(27, "不正三角形三要素的正确描述？", "動機=实施不正的诱因与必要性", """**考点：** 舞弊三角理论。

| 要素 | 含义 |
|------|------|
| 機会 | 易成功、难发现的环境 |
| **動機** | **诱因、必要性** ✓ |
| 正当化 | 自我合理化的借口 |

**易错点：** 三要素 **同时** 才易舞弊；单要素不足。""")

_a(28, "入侵检测中フォールスネガティブ 是？", "把不正访问判为正常", """**考点：** 检测混淆矩阵。

| 术语 | 含义 |
|------|------|
| FN（假阴性） | **攻击 → 判正常** ✓ |
| FP | 正常 → 判攻击 |
| TP/TN | 判对 |

**易错点：** False **Negative** = 漏报（该报未报）。""")

_a(29, "监控 Web 通信并阻断攻击模式？", "WAF", """**考点：** Web 安全产品。

**WAF（Web Application Firewall）：** 监控 Web 通信、记录日志、检测 **攻击模式** 并 **阻断**。

**易错点：** DKIM=邮件签名；IPsec=网络层 VPN；RADIUS=认证。""")

_a(30, "DFD 相关描述正确的一项？", "C1 と C2 はプロセス名", """**考点：** DFD 符号（见 `_q6_imgs/q30_0.png`）。

| 符号 | 含义 |
|------|------|
| A1,A2 矩形 | **外部实体** |
| B1～B7 箭头 | **数据流** |
| **C1,C2 圆** | **进程** ✓ |
| D1,D2 开口线 | **数据存储** |

**易错点：** 圆=进程；开口平行线=存储；矩形=外部。""")

_a(31, "UML 状态机图说明。", "用对象状态与触发事件/条件表示状态迁移", """**考点：** UML 行为图分类。

**状态机图：** **对象状态** + **变化条件/事件** → **状态迁移**。

**易错点：** 序列图=消息时序；活动图=流程；用例图=功能与参与者。""")

_a(32, "OOP 术语表：C 行是？", "カプセル化", """**考点：** 四种 OOP 概念（见 `_q6_imgs/q32_0.png`）。

| 行 | 描述 | 术语 |
|----|------|------|
| A | 按类定义生成对象 | インスタンス化 |
| B | 继承超类性质加子类性质 | 特化 |
| **C** | 数据与方法一体、隐藏细节 | **カプセル化** ✓ |
| D | 子类共性提取为超类 | 汎化 |

**易错点：** B=特化/继承；D=泛化（方向相反）。""")

_a(33, "测试说明表：D 行是？", "ブラックボックステスト", """**考点：** 测试类型（见 `_q6_imgs/q33_0.png`）。

| 行 | 描述 | 类型 |
|----|------|------|
| A | 峰值数据/全终端运行 | 負荷テスト |
| B | 吞吐/响应时间 | 性能テスト |
| C | 关注内部结构/分支 | ホワイトボックス |
| **D** | 只看输入输出 | **ブラックボックス** ✓ |

**易错点：** 黑盒 **不考虑** 内部结构。""")

_a(34, "验证修改未引入新缺陷的测试？", "リグレッションテスト", """**考点：** 测试分类。

**回归测试：** 部分修改后，验证 **其他正常部分未出现新缺陷**。

**易错点：** 自顶/自底/大爆炸= **结合** 测试策略。""")

_a(35, "箭线图项目最短完成日数？", "15 日", """**考点：** CPM 关键路径（见 `_q6_imgs/q35_0.png`）。

代表路径验算：
- B→F→H→G→J→M：2+3+2+3+2+3 = **15**
- A→C→D→L：4+3+4+4 = 15
- B→F→K：2+3+3 = 8

**最长路径 = 15 日** → 项目最短工期 **15**

**易错点：** 关键路径决定工期；并行分支取 **最长** 路径。""")

_a(36, "逐层分解作业为可管理单位的工具？", "WBS", """**考点：** 项目管理手法。

**WBS（Work Breakdown Structure）：** 作业 **大→小** 逐层分解至 **可管理单位**。

**易错点：** 甘特图=日程可视化；PDPC=过程决策；趋势图=时序。""")

_a(37, "服务台术语表：C 行是？", "ローカルサービスデスク", """**考点：** ITIL 服务台模式（见 `_q6_imgs/q37_0.png`）。

| 行 | 描述 | 模式 |
|----|------|------|
| A | 海外多 desk 24×365 | フォロー・ザ・サン |
| B | 分散人员、对用户像单一 desk | バーチャル |
| **C** | 各据点 **同地/近地** 设 desk | **ローカル** ✓ |
| D | 多数据点 **集中** 少数地点 | 中央 |

**易错点：** C=贴近用户；D=集中式。""")

_a(38, "业务改善术语表：W 行是？", "RPA", """**考点：** BPM/BPO/RPA/工作流（见 `_q6_imgs/q38_0.png`）。

| 行 | 描述 | 术语 |
|----|------|------|
| **W** | PC 事务由 **软件自动** 替代 | **RPA** ✓ |
| X | 流程 PDCA 持续改进 | BPM |
| Y | 业务流程 **外包** 给专业商 | BPO |
| Z | 输入→审批→下一工序 | ワークフロー |

**易错点：** RPA=机器人自动化；BPM=流程管理方法论。""")

_a(39, "以高附加值开拓无竞争新市场的战略？", "ブルーオーシャン戦略", """**考点：** 竞争战略。

**蓝海战略：** 以 **新价值** 创造 **无竞争新市场**（非争夺现有市场）。

**易错点：** 红海=现有市场竞争；Push/Pull=促销/拉式营销。""")

_a(40, "Web コンバージョン率 说明。", "访客中完成购买/注册等成果行为的比例", """**考点：** Web 营销指标。

**转化率（CVR）** = 成果行为人数（购买、注册等）÷ **访客数**。

**易错点：** a=跳出率；b=ROAS；c=CTR（点击/展示）。""")

_a(41, "PPM「花形」特征？", "高市场占有率 + 高市场增长率", """**考点：** 波士顿矩阵四象限。

**花形（Star）：** **高份额 + 高增长率**，需投资维持。

| 象限 | 份额 | 增长 |
|------|------|------|
| **花形** | 高 | **高** ✓ |
| 金牛 | 高 | 低 |
| 问题儿 | 低 | 高 |
| 败犬 | 低 | 低 |

**易错点：** 勿与「现金牛」混淆。""")

_a(42, "统合客户信息提升满意与收益的管理？", "CRM", """**考点：** 企业管理系统。

**CRM：** **Customer Relationship Management**，统合 **客户信息与全渠道互动**，提升 **满意度与收益**。

**易错点：** KM=知识；SCM=供应链；TQM=全面质量。""")

_a(43, "工程系统术语表：X 行是？", "CAE", """**考点：** CAD/CAE/CAM/MRP（见 `_q6_imgs/q43_0.png`）。

| 行 | 描述 | 术语 |
|----|------|------|
| W | 工作机械控制程序 | CAM |
| **X** | 计算机 **仿真·结构/动作分析** | **CAE** ✓ |
| Y | 资材所要量·发注 | MRP |
| Z | 外观·配线·设计图 | CAD |

**易错点：** CAE= **工程分析/仿真**；CAD=设计绘图。""")

_a(44, "e 商务术语表：Tc 是？", "ロングテール", """**考点：** EC 用语（见 `_q6_imgs/q44_0.png`）。

| 行 | 描述 | 术语 |
|----|------|------|
| Ta | EC 自动推荐 | レコメンデーション |
| Tb | Web 向不特定多数发包 | クラウドソーシング |
| **Tc** | 多品种少量 **长期销售合计占比大** | **ロングテール** ✓ |
| Td | 个人闲置资产互联网共享 | シェアリングエコノミー |

**易错点：** 长尾= **小众 SKU 聚合**；非推荐算法。""")

_a(45, "CGM 说明。", "消费者投稿·提供信息生成内容的 Web 等", """**考点：** Consumer Generated Media。

**CGM：** 一般消费者 **投稿/提供信息**，经整理 **生成** 网站内容（点评、UGC 等）。

**易错点：** a=XBRL；c=智慧城市；d=3PL 物流外包。""")

_a(46, "两散布图相关系数 r1,r2 不等式？", "−1 < r2 < 0 < r1 < 1", """**考点：** 相关系数与散布形态（见 `_q6_imgs/q46_0.png`）。

- 散布1（p-q）：**正相关** → **0 < r1 < 1**
- 散布2（s-t）：**负相关** → **−1 < r2 < 0**

→ **−1 < r2 < 0 < r1 < 1** ✓

**易错点：** 正相关 r>0；负相关 r<0；勿与斜率 k 选项混选。""")

_a(47, "ABC 分析：累计 70% 销售额的 A 群几种？", "3 品目", """**考点：** ABC（帕累托）分析（见 `_q6_imgs/q47_0.png`）。

合计 978（百万円），按销售额降序：

| 商品 | 销售额 | 构成比 | 累计 |
|------|--------|--------|------|
| S4 | 302 | 30.9% | 30.9% |
| S3 | 215 | 22.0% | 52.9% |
| S8 | 181 | 18.5% | **71.4%** ✓ |

累计 ≥70% → **S4,S3,S8 = 3 品目**

**易错点：** 须 **按金额排序**；不是按编号 S1～S10。""")

_a(48, "四策略期望利润最大者？", "P2", """**考点：** 期望值决策（见 `_q6_imgs/q48_0.png`）。

E = 0.3×好転 + 0.5×不变 + 0.2×悪化（百万円）

| 方策 | 计算 | E |
|------|------|---|
| P1 | 210+125+30 | 365 |
| **P2** | 180+150+40 | **370** ✓ |
| P3 | 60+175+110 | 345 |
| P4 | 30+200+120 | 350 |

**易错点：** 概率 30%/50%/20% 须 **加权求和**。""")

_a(49, "固定费 1500、变动费 2500、売上 7500 时的损益分岐点？", "2,250 万円", """**考点：** 损益分岐点 = 固定费 ÷ 限界利益率。

- 变动费率 = 2500/7500 = **1/3**
- 限界利益率 = 1 − 1/3 = **2/3**
- 分岐点 = 1500 ÷ (2/3) = **2250 万円**

**易错点：** 分岐点是 **销售额**；不是固定费+变动费简单相加。""")

_a(50, "著作权填空 a,b 的组合？", "ア（a=最後，b=できない）", """**考点：** 日本著作权法（见 `_q6_imgs/q50_0.png`、`q50_1.png`）。

(1) 共同著作物保护期 = **最后** 去世作者死亡起算 → **a=最後**
(2) 著作者人格权 **不可转让** → **b=できない**

→ 选项 **ア** ✓

**易错点：** 财产权可转让； **人格权** 专属作者、不能让渡。""")
