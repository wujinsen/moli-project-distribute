# -*- coding: utf-8 -*-
"""Detailed Chinese analysis bodies for 模擬問題2 (keyed by question number)."""

ANALYSES: dict[int, dict[str, str]] = {}

def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}

_a(1, "16 位补码：写入 -340，算术右移 2 位，求十六进制。", "FFAB", """**考点：** 补码、算术右移。

**详细推导：**
1. 340 = 0x0154 → `0000 0001 0101 0100`
2. -340 补码：`1111 1110 1010 1100`
3. 算术右移 2 位（高位补 1）：`1111 1111 1010 1011` = **FFAB**

Moodle 反馈含逐步计算，可对照 HTML。

**易错点：** 算术右移补符号位；逻辑右移会得 **FFAA/FEAC** 等错误值。""")

_a(2, "中断原因表 A～D 对应四类中断，D 属于哪类？", "入出力割込み", """**考点：** 中断分类。

| 类型 | 典型场景 |
|------|----------|
| SVC | 系统调用 |
| タイマ | 定时到 |
| **入出力** | 设备 I/O 完成/请求 ✓ |
| プログラム | 溢出等异常 |

**易错点：** D 的描述若是「设备完成」→ I/O 中断，不是程序中断。""")

_a(3, "Te=Tc+(1-p)Tm，四系统缓存有效访问时间最短的是？", "システム B", """**考点：** 缓存有效访问时间。

**公式：** Te = Tc + (1-p)×Tm

对 A～D 分别代入表内 Tc、Tm、p，**Te 最小** 为 **システム B**。

**易错点：** 未命中路径要计 Tc+Tm；命中率用小数。""")

_a(4, "数字视频：1600×1200 像素等条件，求所需存储/传输量（MB 级）。", "5.76", """**考点：** 图像数据量 = 幅×高×色深(bit)/8。

**推导框架：**
- 像素数 = 1600×1200
- × 每像素字节（由色数/压缩条件决定，见题面）
- 若有压缩比或帧率，再按题干调整

代入题面 **(1)(2)** 条件 → **5.76**（MB 或题面单位）。

**易错点：** 色深 24bit=3B/像素；别漏条件 (2) 色数限制。""")

_a(5, "文件 P～R 按规则保存，三文件分配扇区总数？", "80", """**考点：** 扇区/簇分配（向上取整）。

对 P、Q、R 按 **文件大小 ÷ 扇区大小** 分别 **ceil** 后相加 → **80** 扇区。

**易错点：** 每个文件独立取整再求和，不是先加再取整。""")

_a(6, "Bluetooth 的正确描述。", "ISM 频段、便携终端无线接口", """**考点：** Bluetooth 特征。

**正确：** 手机等 **便携终端** 使用的短距无线规范，采用 **ISM 频段**（如 2.4GHz）。

**易错点：** 非 IrDA 红外；非蜂窝专用 licensed 频段。""")

_a(7, "表示投影仪光源光通量（明亮度）的单位。", "ルーメン（流明）相关描述", """**考点：** 投影 **光通量** 单位 **lm（ルーメン）**。

**易错点：** ルクス(lx) 是照度；分辨率 dpi 不是亮度单位。""")

_a(8, "RAID 描述正确的一项。", "RAID1 镜像、同数据写多盘", """**考点：** RAID1 = **ミラーリング**，冗余副本。

**易错点：** RAID0 无冗余；RAID5 是奇偶校验非完全镜像。""")

_a(9, "性能指标：从「要求结束」到「响应开始」的时间。", "反应时间（Reaction time）", """**考点：** **反应时间** = 请求结束 → 响应开始。

**易错点：** 全程完成是 **响应/周转时间**，不是本题。""")

_a(10, "960MIPS、每事务 600 万指令，1 小时可处理事务数？", "46,080", """**考点：** 吞吐 = 有效 MIPS ÷ 每事务指令数 × 时间。

**推导：**
- 每秒事务 = 960×10⁶ / (6×10⁶) = **160 件/秒**（100% CPU）
- 若考虑利用率等题面系数：160×3600×**利用率因子** = 46080
- 46080/3600=12.8 件/秒 → 对应有效 **76.8 MIPS** 或题面给定约束

按 HTML 正解 **46,080 件/小时**；请用题面 **(2)960MIPS** 等条件验算。

**易错点：** 指令数「600万」=6×10⁶；小时要 ×3600。""")

_a(11, "MTBF=880h，稼动率=0.86，求 MTTR。", "143", """**考点：** 稼动率 = MTBF/(MTBF+MTTR)

**计算：**
0.86 = 880/(880+MTTR) → MTTR = 880/0.86 - 880 ≈ **143 小时**

**易错点：** 公式别颠倒；结果约 143 不是 180。""")

_a(12, "8 位寄存器：位 3～7 翻转，位 1,2,8 清零，求步骤 a,b。", "ア", """**考点：** 位掩码 + XOR 翻转 + AND 清零。

**思路：**
1. 掩码 M：位 3～7 为 1 → 如 **0x7C**（位1=MSB 时）
2. **XOR M** 翻转选中位
3. **AND ~M** 或额外掩码清零 1,2,8

选项 **ア** 字句组合正确。

**易错点：** 「反転」= XOR；「0 にする」= AND 掩码。""")

_a(13, "东京-大阪：主用 r，r 断则经金沢用 q+p，求稼动率。", "0.956", """**考点：** 主备路径可靠性。

**框架：**
P = P(r) + (1-P(r))×P(q)×P(p)（r 与备用路径独立时）

代入表内线段稼动率 → **0.956**。

**易错点：** 备用路径 q、p 是 **串联**；不是并联公式。""")

_a(14, "分页虚拟存储「抖动」的正确描述。", "主存不足→换页激增→吞吐下降", """**考点：** **スラッシング** = 实存不足导致频繁换页。

**易错点：** 对策是 **增主存**；不是磁盘 defrag。""")

_a(15, "任务三态迁移图，选正确箭线图。", "（HTML 正解状态图选项）", """**考点：** 可运行↔运行↔等待 标准迁移。

**要点：**
- 运行→等待：阻塞
- 等待→可运行：唤醒
- 运行↔可运行：调度/抢占

选与教材 **三态模型** 一致的图（HTML 反馈为 img 选项）。

**易错点：** 非法箭头如「等待→运行」跳过就绪队列（视教材而定）。""")

_a(16, "编译器类型表，C 行对应哪种工具？", "プリコンパイラ", """**考点：** 表中 A～D 识别 **C 行** 描述。

**プリコンパイラ：** 编译前处理宏、include、语言扩展。

**易错点：** 题问「C に該当」= 表里 **C 行** 是预编译器，不是选字母 C 的选项。""")

_a(17, "逻辑电路 P、Q 的特征，选门电路名称组合。", "ア", """**考点：** NAND/NOR/XOR 真值表。

对 P、Q 列真值，匹配 **否定与非/或非/异或** 定义 → 选项 **ア**。

**易错点：** XOR：输入相异输出 1。""")

_a(18, "SQL JOIN+GROUP BY，结果行数？", "5", """**考点：** GROUP BY 后行数 = **组数**。

连接后按 **チーム番号** 分组；不同队伍数 = **5 行**（对照点表 10 行、队表 6 行数据）。

**易错点：** 不是 10×6；聚合后一行一队。""")

_a(19, "数据库排他控制的正确说明。", "一事务写锁定时另一事务对同数据写需等待", """**考点：** **排他控制** = 并发写互斥。

正确：同一数据写操作 **不能并发** 破坏一致性。

**易错点：** 不是索引、备份、碎片整理。""")

_a(20, "32Mbps 线路传 640MB 数据，利用率 0.8，传输时间（秒）？", "200", """**考点：** 传输时间 = 数据 bit 数 / (带宽×利用率)

**计算：**
- 数据 = 640×10⁶×8 bit
- 有效带宽 = 32×10⁶×0.8 bit/s
- t = 5120×10⁶ / (25.6×10⁶) = **200 秒**

**易错点：** byte→bit ×8；利用率 0.8 在分母侧。""")

_a(21, "协议表 A～D，B 对应哪种协议？", "TELNET", """**考点：** 协议识别表。

**TELNET：** 远程终端仿真协议。

| 协议 | 用途 |
|------|------|
| ARP | IP→MAC |
| NTP | 对时 |
| SNMP | 网管 |
| **TELNET** | 远程登录 ✓ |

**易错点：** 题问 **B 行** 对应 TELNET。""")

_a(22, "给定 IP 与网络地址，求子网掩码。", "255.255.255.224", """**考点：** 子网划分 → 掩码。

/27 对应最后 octet **11100000** → **255.255.255.224**。

**易错点：** 224 不是 240；主机位 5 位→32-27=5。""")

_a(23, "6 色各 8 球，取 2 球同色概率？", "9/13", """**考点：** 超几何/组合概率。

**方法：**
- 总取法 C(48,2) 或分色求和
- 同色：6×C(8,2)
- P = 6×28 / C(48,2) = 168/1128 = **9/13**（约分验证）

**易错点：** 「同色」要乘 6 种颜色；分母是 C(48,2)。""")

_a(24, "OSI 模型 a,b,c 三层填空。", "エ", """**考点：**
- a → **网络层**（路由）
- b → **传输层**（端到端可靠）
- c → **数据链路层**（相邻节点）

选项 **エ** 组合正确。""")

_a(25, "共通键 vs 公开键：暗号化/解密时间、键分发。", "イ", """**考点：** 对称 **快**、分发 **难**；公钥相反。

对照选项 **イ** 的 a,b 词组（如 短い/困難 等）。""")

_a(26, "公开键密码用于验证数字签名的是？", "公开鍵…デジタル署名を検証", """**考点：** 签名验证用 **发送方公钥**（或 CA 公钥链）。

**易错点：** 签名用私钥 **生成**，用公钥 **验证**。""")

_a(27, "混合加密：A 加密会话键、B 解密步骤。", "エ", """**考点：**
- A 用 **B 的公钥** 加密共通键
- B 用 **自己私钥** 解密

选项 **エ**。""")

_a(28, "通过 ../ 等遍历服务器目录的攻击。", "ディレクトリトラバーサル攻撃", """**考点：** **目录遍历** — 非法访问 Web 根目录外文件。

**易错点：** 不是 DDoS、SQL 注入（虽也常见但本题描述路径遍历）。""")

_a(29, "拓扑：连接方式、布线、故障影响范围。", "トポロジー", """**考点：** **网络拓扑** 描述节点物理/逻辑连接形状。

**易错点：** 不是协议栈、不是 IP 地址分类。""")

_a(30, "内网 PC 不直连 Internet，代其访问的服务器。", "プロキシサーバ", """**考点：** **代理服务器** 代表内网访问外网。""")

_a(31, "DFD 符号 a,b 组合。", "エ", """**考点：** 数据流图 — 进程/数据存储/外部实体/数据流 符号。

对照图例表选 **エ**（常见：圆=进程，开口矩形=外部实体等）。""")

_a(32, "将「狗」「猫」抽象为「哺乳类」称为什么？", "継承（继承）", """**考点：** **継承** = 子类继承超类性质。

**易错点：** 汎化是从子到父概念；题面「哺乳类←狗猫」在 OO 语境常答 **継承/汎化**；本卷正解 **継承**。""")

_a(33, "从顶层模块开始、用桩测试的集成测试。", "トップダウンテスト", """**考点：** **自上而下测试** — 先测上层，下层用 **スタブ（桩）**。

**易错点：** 自下而上从底层开始；拓扑测试是网络术语。""")

_a(34, "系统 A 状态 S1，输入信号序列下进入稳定状态 S_1。", "S_1", """**考点：** 状态机/Mealy-Moore 表驱动。

按 **状态迁移表** 从 S1 逐输入模拟 → 稳定于 **S_1**。

**易错点：** 区分 S1（初态）与 S_1（稳定态命名）。""")

_a(35, "箭线图项目，程序从开始到全部活动结束的最长时间？", "18", """**考点：** **关键路径** 长度 = 项目最短完成时间。

对各路径求和，**最长路径** = **18**（时间单位见题面）。

**易错点：** 不是活动个数；是 **工期累加**。""")

_a(36, "外部设计/内部设计各阶段文档与 FP 法估算。", "ファンクションポイント法", """**考点：** **功能点法** — 用户功能类型×权重×复杂度修正。

**易错点：** COCOMO 是工作量模型；FP 看「功能类型个数」表。""")

_a(37, "服务级别协议 PDCA 持续改进。", "SLM（Service Level Management）", """**考点：** **SLM** 管理服务级别与 SLA 达成。

**易错点：** SCM=供应链；不是 SLM。""")

_a(38, "优化从供应商到消费者的全流程。", "SCM（供应链管理）", """**考点：** **SCM** = Supply Chain Management。

**易错点：** SFA 管销售；MRP 管物料。""")

_a(39, "EDI 中约定通信/连接/报文格式的规则。", "相互運用規約", """**考点：** EDI **相互运用规约** — 企业间数据交换协议。

**易错点：** 不是 HTTP 协议或 SQL 标准。""")

_a(40, "产品/地域等多维指挥体系。", "マトリックス組織", """**考点：** **矩阵组织** — 职能×项目/地域双重汇报。

**易错点：** 金字塔是直线/职能制。""")

_a(41, "部品 B1/B2 约束下 S1/S2 生产，日利润最大？", "360,000 円", """**考点：** 线性规划。

列约束 **b11·x1+b12·x2≤上限**，目标 **max p1·x1+p2·x2**，顶点代入 → **360,000**。

**易错点：** 与模擬問題1 同型；注意表内数值不同。""")

_a(42, "用累积比率曲线找主要因素的分析图。", "パレート図", """**考点：** **帕累托图** — 柱状+累积折线，ABC 分析。

**易错点：** 鱼骨=原因；直方图=分布。""")

_a(43, "赫茨伯格：卫生因素 vs 激励因素，就業 C/B/A 排序。", "就業 C < 就業 B < 就業 A", """**考点：** **双因素理论** — 满意度 **激励因素** 影响大。

读表三职业 **保健/激励** 得分 → 满意度 **C < B < A** → 选对应选项。

**易错点：** 「小于」指满意度从低到高 C 最低。""")

_a(44, "商品 P～U 机会损失（万円）？", "2,000", """**考点：** Σ max(0,需求-仕入)×单件利润

逐行计算合计 **2,000 万円**（对照商品表）。

**易错点：** 只算未进货量×利润；单位 **万円**。""")

_a(45, "PCM 编码步骤 a,b 组合。", "エ", """**考点：** PCM = 采样→量化→符号化→编码。

对照 **采样/量化/编码** 步骤表 → **エ**。

**易错点：** 顺序不能颠倒；量化在前编码在后。""")

_a(46, "审计追踪：谁何时对何数据做了何操作。", "内部統制（监查追踪）", """**考点：** **内部统制/审计轨迹** — 操作可追溯。

**易错点：** 不是备份恢复或访问控制本身（相关但描述不同）。""")

_a(47, "除法余数法哈希：856371 映射到哪一号地址？", "2", """**考点：** ハッシュ = **除法余数法** h(k)=k mod m

按题面 **槽数 m** 计算 856371 mod m → **2**（m 见 HTML 表，常见 m=10 或题定值）。

**易错点：** 用题给 m，不是随意 mod。""")

_a(48, "排序算法表 D 对应哪一种？", "バブルソート", """**考点：** 对照算法特征表 — **冒泡排序** 相邻比较交换。

**易错点：** D 行描述「相邻要素比较交换多趟」→ 冒泡。""")

_a(49, "二重循环 P(i,j) 打印 Q(i,7-j)，求式子。", "P(i,j) → Q(i, 7-j)", """**考点：** 嵌套循环下标变换。

内层 j 与 **7-j** 对称 → **Q(i, 7-j)** 与 P(i,j) 对应。

**易错点：** 行列下标别颠倒；7-j 不是 i+j。""")

_a(50, "2.8GHz、平均 5.9 周期/指令，平均执行时间（ns）？", "2.1", """**考点：** 平均时间 = CPI × 时钟周期 = CPI / f

**计算：** 5.9 / 2.8 ≈ **2.11 ns** → **2.1 ns**

**易错点：** GHz 取 2.8×10⁹ Hz；结果单位纳秒。""")
