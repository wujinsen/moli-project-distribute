# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for 模擬問題3 (22 questions in saved HTML)."""

ANALYSES: dict[int, dict[str, str]] = {}

def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}

_a(1, "哪条算式（十进制幂次）等于十六进制 34.62？", "イ 2^5＋2^4＋2^2＋2^-2＋2^-3＋2^-7", """**考点：** 十六进制 ↔ 十进制幂次对照。

**详细推导：**
- 34₁₆ = 3×16+4 = **52**
- 0.62₁₆ = 6/16 + 2/256 = 0.375 + 0.0078125 = **0.3828125**
- 合计 **52.3828125**

验算选项 **イ**：32+16+4+0.25+0.125+0.0078125 = **52.3828125** ✓

**易错点：** 小数部分按 **16 负幂** 展开，不是十进制 0.62。""")

_a(2, "16 位补码寄存器写入 -303，求十六进制（小数点在最右位右侧）。", "エ FED1", """**考点：** 负数补码表示。

**详细推导（HTML 反馈）：**
1. 303 = `0000 0001 0010 1111`
2. -303 补码：`1111 1110 1101 0001` = **FED1**

**易错点：** FED0 是 -304；812F 把符号位当数据位。""")

_a(3, "8E9D 经某种 2 位移位得 E3A7，空格应填何种移位？", "ウ 右に算術シフト", """**考点：** 算术移位 vs 逻辑移位。

**验证：** 0x8E9D 最高位为 1（负数补码），**算术右移 2 位** 符号位填充 → **0xE3A7** ✓

**易错点：**
- 逻辑右移补 0 → 结果不同
- 左移会改变符号，不会得到 E3A7""")

_a(4, "浮点运算 A～C，哪组对应「桁落ち」与「情報落ち」？", "エ", """**考点：** 浮点误差类型。

| 运算 | 现象 |
|------|------|
| **A** 相近大数相减 | **桁落ち**（有效位丢失） |
| **B** 极小减极大 | **情報落ち**（小数被吞没） |
| C 同量级相加 | 一般不典型 |

对照选项表 → **エ**（A=桁落ち、B=情報落ち 组合）。

**易错点：** 含图题请对照 HTML 选项表字母组合。""")

_a(5, "10 位中恰有 7 个 1、3 个 0 的位模式有多少种？", "ア 120", """**考点：** 组合数 C(n,k)。

**计算：** C(10,7) = C(10,3) = 10×9×8/(3×2×1) = **120**

**易错点：** 128=2^7 是「至少 7 个 1」误用；240 无此组合意义。""")

_a(6, "按基准值分「≥基准」与「<基准」两组反复排序的算法。", "ア クイックソート", """**考点：** 排序算法特征。

**快速排序：** 选 pivot，分成 ≥ 与 < 两组 **递归**。

**易错点：** 冒泡=相邻比较；希尔=间隔插入；堆=堆树。""")

_a(7, "二叉树三种遍历，哪条描述正确（中序输出 QVSPTRU）？", "ウ 中間順…QVSPTRU", """**考点：** 先序/中序/后序遍历。

HTML 反馈：
- 先序 → PQSVRTU
- **中序 → QVSPTRU** ✓
- 后序 → VSQTURP

**易错点：** 把先序/后序结果与中序混淆。""")

_a(8, "程序性质表：B 行对应哪种程序？", "ア 再帰的プログラム", """**考点：** 再帰/再使用/再入/再配置 四类。

HTML 反馈对照表：
- A→再使用可能 B→**再帰的** C→再配置 D→再入可能

题问 **B 行** → 选 **ア 再帰的プログラム**。

**易错点：** 题问的是 **表中 B 行**，不是选项字母 B。""")

_a(9, "CISC/RISC 特征表，RISC 特征组合？", "イ 特徴 A と特徴 D", """**考点：** RISC vs CISC。

HTML 反馈：**RISC = A,D**；CISC = B,C → **イ**

**易错点：** RISC 指令少、固定长度、Load/Store；CISC 复杂寻址、微码。""")

_a(10, "基址寻址：求有效地址。", "ア 8200", """**考点：** EA = 基址寄存器 + 地址常数。

**计算：** 6800 + 1400 = **8200**（HTML 反馈）

**易错点：** 勿与变址/相对寻址公式混用。""")

_a(11, "三 CPU 每秒平均指令数从多到少排序。", "ウ CPU-3，CPU-1，CPU-2", """**考点：** 每指令时间 = CPI / 时钟频率；IPS = 1/时间。

| CPU | CPI/频率 | 约 ns/指令 |
|-----|----------|------------|
| CPU-3 | 1.7/2.7 | **0.63**（最快） |
| CPU-1 | 1.2/1.8 | 0.67 |
| CPU-2 | 1.6/2.3 | 0.70 |

顺序：**CPU-3 > CPU-1 > CPU-2** → **ウ**

**易错点：** 频率高不等于快，要看 **CPI/频率** 比值。""")

_a(12, "哪一项是 I/O 中断原因？", "エ ファイル中のレコードの読込みが終了した", """**考点：** 外部 I/O 中断 vs 内部异常。

| 选项 | 分类 |
|------|------|
| ア OS 读文件 **请求** | SVC/软件中断 |
| イ 定点溢出 | 程序中断 |
| ウ 时间片到 | 定时中断 |
| **エ 读记录完成** | **I/O 中断** ✓ |

**易错点：** 「请求读」≠「读完」；完成才触发 I/O 中断。""")

_a(13, "1400×800、16777216 色，视频内存最少多少 MB？", "イ 3.36", """**考点：** 像素×色深/8；1MB=10^6 B（题设）。

**计算：**
- 2^24 色 → 24 bit = 3 B/像素
- 1400×800×3 = 3,360,000 B = **3.36×10^6 B = 3.36 MB**

**易错点：** 题设 1M=10^6 非 1024²；勿漏 ×3 字节。""")

_a(14, "四系统缓存，有效访问时间最短？", "イ システムB", """**考点：** Te = Tc×p + Tm×(1-p)

HTML 逐步计算：
- A: 70.4 ns
- **B: 64.8 ns** ✓ 最短
- C: 79.2 ns
- D: 76.23 ns

**易错点：** 命中率 p 用小数；未命中要加 Tm。""")

_a(15, "磁盘表1+表2，1 块平均访问时间（ms）？", "ウ 16", """**考点：** 平均访问 = 寻道 + 旋转延迟 + 传输。

HTML 反馈逐步：
1. 旋转延迟 = 60000/7500/2 = **4 ms**
2. 传输率 = 28000×7500/60000 = 3500 B/ms
3. 传输 = 7000/3500 = **2 ms**
4. 寻道 **10 ms** + 4 + 2 = **16 ms**

**易错点：** 块大小 = 700×10 字节；旋转延迟是半圈时间。""")

_a(16, "860MIPS、利用率 0.8、688 万指令/事务，每事务时间？", "イ 10 ms", """**考点：** 时间 = 指令数 / (MIPS×10^6×利用率)

**计算：**
6.88×10^6 / (860×10^6×0.8) = 0.01 s = **10 ms**

**易错点：** MIPS=10^6 指令/秒；688 万 = 6.88×10^6。""")

_a(17, "RAID 填空 a,b 组合（镜像冗余）。", "エ", """**考点：** RAID 术语。

**a：** 同一数据 **重复写入多盘** → **ミラーリング**
**b：** 规定于 **RAID1**

对照选项 **エ**（具体字句见 HTML 选项表）。

**易错点：** 条带化=RAID0/5；镜像=RAID1。""")

_a(18, "三系统 MTBF 从长到短排序。", "ア システム B，A，C", """**考点：** MTBF = MTTR/(1-稼动率)

HTML 计算：
- A: 35/0.04 = **875 h**
- B: 27/0.03 = **900 h**（最长）
- C: 17/0.02 = **850 h**

顺序 **B > A > C** → **ア**

**易错点：** 稼动率越高 MTBF 越长（MTTR 固定时）。""")

_a(19, "内存紧缩（compaction）的正确说明。", "ウ 散在小空闲区合并为大连续区", """**考点：** **メモリコンパクション**。

**正确：** 把主存中 **分散的小空闲块** 合并为 **连续大空闲区**。

**易错点：**
- ア=堆/动态分配描述
- イ=内存泄漏
- エ=交错存取（bank）""")

_a(20, "换页时替换「最久未引用」页的算法。", "エ LRU 方式", """**考点：** 页面置换算法。

**LRU（Least Recently Used）：** 替换 **上次引用以来时间最长** 的页。

**易错点：** FIFO=最先进入；LFU=使用频率最低；LIFO 非标准置换。""")

_a(21, "任务 I/O 阻塞：a,b,c 状态迁移填空。", "エ", """**考点：** 三态 + I/O 阻塞。

**(1)** **実行中** 任务请求读文件 → **待機（等待）**
**(2)** I/O 完成 → **実行可能（就绪）**（日本教材常见；部分写「実行」）

对照状态迁移表选项 → **エ**

**易错点：** 含状态图，务必对照 HTML 插图选项字句。""")

_a(22, "校验方式表：A 行对应哪种 check？", "シーケンスチェック（序列检查）", """**考点：** 输入校验四类。

| 类型 | 含义 |
|------|------|
| **シーケンス** | 记录号/日期 **顺序** 连续 |
| ニューメリック | 纯数字 |
| フォーマット | 格式/位数 |
| リミット | 上下限范围 |

**A 行** → **シーケンスチェック**。

**注意：** 当前 HTML **未含 Q22 选项与正解**（页面截断）。请在 Moodle 勾选「Show all questions on one page」后重新另存为，再对照选项字母。""")
