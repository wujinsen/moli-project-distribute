# -*- coding: utf-8
"""Supplement analyses keyed by (HTML stem, question number)."""
from __future__ import annotations

BY_SOURCE: dict[str, dict[int, dict[str, str]]] = {}


def _a(source: str, n: int, stem: str, answer: str, body: str) -> None:
    BY_SOURCE.setdefault(source, {})[n] = {
        "stem_zh": stem,
        "answer": answer,
        "body": body.strip(),
    }


SRC_R3 = "ランダム問題（模擬問題）3"
SRC_DB = "技術要素(DB)"

_a(SRC_DB, 9, "销售曜日是否为日～土之一？", "論理チェック", """**考点：** 输入校验五类。

**論理チェック（逻辑检查）：** 验证数据是否符合 **业务规则/逻辑关系**（如曜日 ∈ {日,月,火,水,木,金,土}）。

| 类型 | 用途 |
|------|------|
| Numeric | 是否为数值 |
| Format | 格式/位数 |
| Limit | 上下限 |
| Balance | 配对合计 |
| **Logic** | **业务逻辑/枚举** ✓ |

**易错点：** 不是 Format（格式） alone；是 **值域/逻辑** 校验。""")

_a(SRC_R3, 2, "三事务锁表：T2 未结束前启动 T3 是否等待？", "T2 未结束前启动 T3，T3 不等待", """**考点：** 共享/排他锁兼容（见 HTML 锁表）。

对照表分析 **R2** 冲突点：
- 若 **T2 先** 占 R2 的 **排他锁**，T3 需 R2 的 **共享锁** → 不兼容 → T3 **会** 等待 ✗
- 正确项：**T2 运行中启动 T3 且 T3 不等待** → 说明 T3 所需资源与 T2 **无冲突**（R1/R3 等）

→ 选项 **エ** ✓

**易错点：** 同时运行上限 2 个；S 与 S 可共存，X 与 S/X 不可。""")

_a(SRC_R3, 3, "500 品目四工程检验项目，当前进度率？", "55%", """**考点：** 进度率 = 已完成工数 / 计划总工数（见 HTML 表）。

**步骤：**
1. 各工程 **工数比率 × 完成品目数/500** 得完成工数贡献
2. 四工程贡献 **相加** 得总进度

对照表计算 → **55%** ✓

**易错点：** 比率分母是 **计划总工数 100%**；未完成品目按表内完成数比例计入。""")

_a(SRC_R3, 4, "著作权描述正确的一项？", "信息选择/体系构成具创作性的 DB 受保护", """**考点：** 日本著作权法。

**正确：** 经 **选择或体系构成** 而具 **创作性** 的数据库 → **著作物** 保护 ✓

**易错点：**
- 共同著作保护期 = **最后** 去世作者起算（非最初）
- 人格权 **不可转让**（非可）。""")

_a(SRC_R3, 7, "L2/L3 交换机 a,b 层填空？", "a=データリンク　b=ネットワーク", """**考点：** OSI 层与交换机（见 HTML 选项表）。

| 设备 | 层 | 依据 |
|------|-----|------|
| **L2 交换机** | **データリンク** | **MAC** 地址转发 |
| **L3 交换机** | **ネットワーク** | **IP** 地址路由 |

→ **a=データリンク层，b=ネットワーク层** → 选项 **イ**

**易错点：** L2≠物理层；L3 在 L2 之上做 **IP 中继**。""")

_a(SRC_R3, 8, "目录遍历攻击说明？", "用特殊字符篡改路径读取本不可访问文件", """**考点：** Web 攻击类型。

**目录遍历（Directory Traversal）：** 向 Web 程序传入含 **`../` 等路径特殊字符** 的参数，**篡改路径** 读取 **本不可访问** 的文件。

**易错点：** 不是 SQL 注入；不是钓鱼；是 **路径遍历**。""")

_a(SRC_R3, 10, "协议表 A 行是？", "ARP", """**考点：** 协议对照（见 HTML 表）。

**ARP：** 已知 **IP 地址** 解析 **MAC 地址**（局域网）。

| 协议 | 用途 |
|------|------|
| **ARP** | IP→MAC ✓ |
| NTP | 时刻同步 |
| SNMP | 网络管理 |
| TELNET | 远程终端 |

**易错点：** ARP 工作在 **数据链路层附近**；不是传输层。""")

_a(SRC_R3, 12, "DFD 中 s1、s2 表示？", "データストア名", """**考点：** DFD 符号。

**s1、s2** 为 **开口平行线** 符号 → **データストア（数据存储）名** ✓

| 符号 | 含义 |
|------|------|
| 矩形 | 外部实体 |
| 圆 | 进程 |
| **平行线** | **数据存储** |
| 箭头 | 数据流 |

**易错点：** 不是进程名；不是外部名。""")

_a(SRC_R3, 14, "回归测试说明？", "修改后验证其他正常部分未引入新缺陷", """**考点：** 测试分类。

**リグレッションテスト：** 系统 **修正/变更** 后，确认 **其他原本正常的部分** 未产生 **新缺陷** ✓

**易错点：** 不是自顶/自底结合测试；是 **变更影响** 验证。""")

_a(SRC_R3, 16, "损益计算式 a,b,c 填空？", "a=営業利益　b=経常利益　c=税引前当期純利益", """**考点：** 日本损益表结构（见 HTML 选项）。

| 符号 | 公式 |
|------|------|
| **a** | 売上高 − (売上原価 + 販管費) = **営業利益** |
| **b** | a + 営業外収益 − 営業外費用 = **経常利益** |
| **c** | b + 特別利益 − 特別損失 = **税引前当期純利益** |

→ 选项 **ア** ✓

**易错点：** 经常利益含 **营业外** 收支；营业利润不含。""")

_a(SRC_R3, 17, "PNG 说明？", "48bit RGB、8bit 索引色、16bit 灰度，面向 Web", """**考点：** 图像文件格式。

**PNG：** 支持 **48bit RGB**、**8bit 索引色**、**16bit 灰度**；面向 **Web** 广泛使用；**无损** 压缩。

**易错点：** JPEG 有损；GIF 256 色；BMP 未压缩。""")

_a(SRC_R3, 18, "数据挖掘说明？", "大数据统计发现规律/相关/趋势", """**考点：** 数据分析术语。

**データマイニング：** 对 **大量数据** 用 **统计等方法** 分析，发现 **新规律、相关性、趋势** ✓

**易错点：** 数据字典=元数据；数据集市=部门分析库；清洗=质量修正。""")

_a(SRC_R3, 19, "损益分岐点销售额？（见表）", "450 百万円", """**考点：** 分岐点 = 固定费 ÷ 限界利益率。

对照 HTML 表代入：
- 限界利益率 = (売上 − 変動費) / 売上
- 分岐点 = 固定費 / 限界利益率 → **450** 百万円 ✓

**易错点：** 分岐点是 **销售额**；先算 **限界利益率** 再除固定费。""")

_a(SRC_R3, 20, "PMBOK 机会（正面）风险四策略之一？", "共有（シェア）", """**考点：** PMBOK 风险应对。

**威胁（负）四策略：** 回避、转移、减轻、接受  
**机会（正）四策略：** **提高、分享（共有）、开拓、接受**

「好机」→ **シェア/共有** ✓

**易错点：** 转移/回避多用于 **威胁**；机会用 **share/exploit**。""")

_a(SRC_R3, 21, "共通键/公开键特征表填空？", "ウ（见 HTML 对照表）", """**考点：** 对称 vs 非对称加密特征（见 HTML 表）。

典型对应：
- **共通键：** 加解密 **同一密钥**；速度快；密钥配送问题
- **公开键：** **公钥加密/私钥解密**（或反向签名）；密钥配送容易；速度较慢

对照表 a/b 空格 → 选项 **ウ** ✓

**易错点：** 数字签名用 **私钥签、公钥验**；保密通信用 **接收者公钥**。""")

_a(SRC_R3, 26, "类与对象对应？", "（駅，東京駅）", """**考点：** OOP 类 vs 实例。

**类** = 模板（駅）  
**对象/实例** = 具体个体（**東京駅**）

→ **（駅，東京駅）** ✓

**易错点：** （東京駅，駅）顺序反了；类是 **型**，对象是 **实例**。""")

_a(SRC_R3, 28, "修正补丁发布前利用漏洞的攻击？", "ゼロデイ攻撃", """**考点：** 攻击分类。

**零日攻击：** 漏洞公开/补丁可用 **之前** 被恶意利用 ✓

**易错点：** 字典攻击=穷举密码；DoS=拒绝服务；中间人=通信窃听/篡改。""")

_a(SRC_R3, 30, "校验表 A 行？", "シーケンスチェック", """**考点：** 输入校验（见 HTML 表）。

**序列检查：** 确认 **日期、编号** 等 **顺序关系** 合理（如结束日≥开始日）。

**易错点：** Numeric=数值；Format=格式；Limit=范围。""")

_a(SRC_R3, 31, "按 BOM/库存算所要量与发注？", "MRP", """**考点：** 生产管理系统。

**MRP（物料需求计划）：** 据 **生产计划、BOM、库存** 计算 **部品所要量与发注时机** ✓

**易错点：** PDM=产品数据；SFA=营业支援；CAM=制造。""")

_a(SRC_R3, 32, "S/MIME 说明？", "邮件中用 RSA 的加密与数字签名", """**考点：** 邮件安全扩展。

**S/MIME：** 电子邮件 **RSA 公开键** 实现 **加密通信** 与 **数字签名** ✓

**易错点：** SSL/TLS=传输层；IPsec=网络层；不是认证服务器本身。""")

_a(SRC_R3, 34, "336Mbps、21×10⁵B/件、利用率 0.7，1 分钟最多几件？", "840 件", """**考点：** 传输吞吐。

- 有效带宽 = 336×10⁶×0.7 = **235.2×10⁶ bit/s**
- 每件 = 21×10⁵×8 = **16.8×10⁶ bit**
- 每件时间 = 16.8/235.2 ≈ **0.0714 s**
- 1 分钟 = 60/0.0714 ≈ **840 件** ✓

**易错点：** 字节×8；利用率乘在带宽上。""")

_a(SRC_R3, 36, "VPN 说明？", "安全技术+隧道，公用网如专用网", """**考点：** VPN 定义。

**VPN：** **Security + Tunneling**，在 **公网** 上建立 **逻辑专用通道** ✓

**易错点：** 不是简单 Web 中继；不是仅 IP 伪装（那是 NAT）。""")

_a(SRC_R3, 37, "通配符匹配：含 A?B 的字符串？", "ABBAA", """**考点：** `?` 通配符 = **任意 1 字符**（见 HTML 表）。

模式 **A?B** 在字符串中匹配 **ABB**（?→B）等；对照选项逐条验证 → **ABBAA** ✓

**易错点：** `*` 才是任意长度；`?` 只匹配 **一个** 字符。""")

_a(SRC_R3, 40, "决策矩阵/方策 P～R 填空？", "ア（见 HTML 决策表）", """**考点：** 决策分析（见 HTML 利益表与记号说明）。

对照 **P～R** 三方案在好転/不变/悪化下利润，以及 **最大最小/后悔值** 等记号定义 → 选项 **ア** ✓

**易错点：** 须读清是 **乐观/悲观/后悔矩阵** 哪一种。""")

_a(SRC_R3, 42, "散布图 x-y 关系描述？", "点分布呈正相关", """**考点：** 散布图读图。

图中点列 **随 x 增 y 增** → **正相关** ✓

**易错点：** 勿与回归斜率符号混淆；看图整体 **趋势**。""")

_a(SRC_R3, 44, "PDCA 维持提升服务水平的约定管理？", "SLM（Service Level Management）", """**考点：** ITIL/服务管理。

**SLM：** 就 **SLA** 用 **PDCA** 维持/提升 **服务级别** ✓

**易错点：** CRM=客户；CIM=生产信息；SCM=供应链。""")

_a(SRC_R3, 45, "混合加密共通键交换步骤填空？", "エ（见 HTML 手顺）", """**考点：** 混合加密密钥配送。

典型手顺：
1. X 生成共通键
2. 用 **Y 的公开键** 加密共通键发送
3. Y 用 **私钥** 解密得共通键

对照 a/b 空格 → **エ** ✓

**易错点：** 共通键本身不用 RSA「加密整个消息」；只加密 **共通键**。""")

_a(SRC_R3, 46, "DB 障害恢复 a,b 填空？", "エ（见 HTML 选项）", """**考点：** 备份/恢复类型（见 HTML）。

- **a 处理：** 正常磁盘 **重新定义 DB** + 备份还原 + 日志 **更新后** 前滚 → **フォワード/ロールフォワード** 类
- **b 处理：** 用日志 **更新前** 信息 **回滚** 到事务开始前

对照选项表 → **エ** ✓

**易错点：** Rollback=更新**前**；Roll-forward=更新**后**。""")

_a(SRC_R3, 47, "ERP 导入目的？", "经营资源有效活用与效率化", """**考点：** ERP 定义。

**ERP 目的：** **Enterprise Resource Planning** — 统合 **经营资源**（人财物信息）**有效活用**、**提升经营效率** ✓

**易错点：** 不是仅库存时点优化（那是 SCM 一部分）。""")

_a(SRC_R3, 48, "IP/网络地址求子网掩码？", "255.255.248.0", """**考点：** 子网划分（见 HTML 表）。

将 IP 与网络地址 **第三 octet 二进制** 对照，求 **最长公共网络前缀** → **/21** → **255.255.248.0** ✓

**易错点：** 网络地址主机位必须为 0；掩码与 **/21** 一致。""")


def lookup_supplement(source_stem: str, qnum: int) -> dict[str, str] | None:
    return BY_SOURCE.get(source_stem, {}).get(qnum)
