# -*- coding: utf-8 -*-
"""Detailed Chinese analysis for マネジメント・ストラテジ (39 questions)."""

ANALYSES: dict[int, dict[str, str]] = {}


def _a(n, stem, answer, body):
    ANALYSES[n] = {"stem_zh": stem, "answer": answer, "body": body}


_a(1, "将各作业项的计划与实绩用横条对比的项目进度图？", "ガントチャート", """**考点：** 项目管理常用图表。

**推导：** 题干关键词——「予定と実績」「帯（横棒）」「対比」= **ガントチャート（甘特图）**。

**选项分析：**
- レーダーチャート：多维指标雷达图，非进度横条。
- トレンドチャート：费用/工程消化折线对比。
- PDPC：过程决策程序图，用于事态推演与对策链。

**易错点：** 与 WBS（工作分解）混淆；WBS 是层级任务树，不是时间横条。""")

_a(2, "销售星期字段须为「日/月/火/水/木/土」之一，属于哪种校验？", "論理チェック", """**考点：** 输入校验五分类。

**推导：** 校验的是 **业务规则/合法取值集合**（星期枚举），不是格式结构、也不是数值范围 → **論理チェック（逻辑校验）**。

**选项分析：**
- ニューメリック：仅确认是数字。
- フォーマット：记录样式/长度/属性是否符合模板。
- リミット：上下限/范围（如 0~100）。

**易错点：** 题面列举的是离散枚举值，典型逻辑校验；「金」未列入合法集合亦属业务规则。""")

_a(3, "数据库规范化的主要目的？", "データの重複を避けて一元管理することにより，データの保守及び管理が容易になる。", """**考点：** 关系数据库规范化目的。

**推导：** 规范化核心——消除冗余、避免更新异常、便于维护 → 选 **b**。

**选项分析：**
- a：反了；规范化是 **拆分** 表，不是把所有数据塞进一张表。
- c：安全性靠权限/加密，非规范化直接目的。
- d：规范化 **减少** 重复，不是增加重复以便恢复。

**易错点：** 「検索高速化」有时附带发生，但 **官方目的** 是冗余消除与易维护。""")

_a(4, "频繁增删改导致响应变慢，有效的对策？", "データベースの再編成を実施する。", """**考点：** DB 性能与碎片整理。

**推导：** 频繁 DML → 存储碎片/页利用率下降 → **再編成（重组）** 可整理物理存储、改善性能。

**选项分析：**
- b/d：重装程序/DBMS 不解决数据层碎片问题。
- c：增加备份频率与查询性能无关。

**易错点：** 官方反馈明确：本体问题是数据库文件，非应用或 DBMS 安装。""")

_a(5, "唯一标识表中一行的列（或列组合）叫什么？", "主キー", """**考点：** 关系模型基本术语。

**推导：** 行级唯一标识 = **主キー（Primary Key）**。

**选项分析：**
- シノニム：同义词/别名。
- 外部キー：引用他表主键，本身不必然唯一标识本行。
- リレーション：表间关系，非列名。

**易错点：** 复合主键仍是「主キー」；外部键用于参照完整性。""")

_a(6, "在服务器预置高频 SQL 命令群以减少网络传输？", "ストアドプロシージャ", """**考点：** C/S 架构 DB 访问优化。

**推导：** 常用命令在 **DB 服务器端** 预编译/存储，客户端只传调用 → **ストアドプロシージャ**。

**选项分析：**
- インデックス：加速检索，不减少 SQL 往返次数。
- クレンジング/マイニング：数据质量/分析，与网络负载无关。

**易错点：** 「命令群在サーバ上」是 SP 的典型场景。""")

_a(7, "入庫+出庫=在庫 等式关系校验属于？", "バランスチェック", """**考点：** 输入校验——平衡校验。

**推导：** 多字段间 **会计/数量平衡关系** → **バランスチェック**。

**选项分析：**
- リミット：单字段上下限。
- フォーマット：记录样式。
- ニューメリック：是否数字。

**易错点：** 与論理チェック（枚举/规则）不同；平衡校验强调 **算术关系**。""")

_a(8, "服务提供者与客户就服务范围、质量及违约罚则的协议？", "SLA", """**考点：** IT 服务管理术语。

**推导：** Service Level Agreement = **SLA（服务等级协议）**。

**选项分析：**
- CIO：首席信息官职位。
- TCO：总拥有成本。
- SFA：销售 force 自动化。

**易错点：** SLA 含可量化质量指标与未达标时的罚则/补偿。""")

_a(9, "事件表×地区表 JOIN 后 GROUP BY 地区，结果行数？", "5", """**考点：** SQL 聚合与 GROUP BY。

**推导（对照 HTML 插图）：**
```sql
SELECT イベント表.地区番号, 地区名, SUM(参加人数) ...
FROM イベント表, 地区表
WHERE イベント表.地区番号 = 地区表.地区番号
GROUP BY イベント表.地区番号, 地区名
```
イベント表出现的地区：A1,A2,A3,A5,A6（共 **5** 个）；A4 无事件不出现在结果中 → **5 行**。

**易错点：** 勿选 12（事件行数）或 6（地区表行数）；GROUP BY 后一行一地。""")

_a(10, "按规则统一表记、修正误记/重复以提升数据质量？", "データクレンジング", """**考点：** 数据质量术语。

**推导：** 发现揺れ/誤記/重複 → 统一/修正/删除 = **データクレンジング（清洗）**。

**选项分析：**
- マイニング：从大量数据 **发现规律**。
- マート：面向主题的 **小型分析库**。
- ディクショナリ：数据 **定义/metadata** 目录。

**易错点：** 「品質を高める」+「表記統一」是 cleansing 的标准表述。""")

_a(11, "支持 48bit RGB、8bit 索引色、16bit 灰度、面向 Web 的格式？", "PNG", """**考点：** 图像文件格式特性。

**推导：** **PNG** 支持真彩/索引/灰度，Web 常用且无损。

**选项分析：**
- GIF：256 色索引为主，非 48bit RGB 场景。
- TIFF：印刷/扫描常用，非「面向 Web 广泛普及」。
- BMP：未压缩体积大，Web 少用。

**易错点：** 题面三个 bit 深度特征与 PNG 规范一致。""")

_a(12, "从海量数据按需求抽取构成的小型主题库？", "データマート", """**考点：** 数据仓库体系结构。

**推导：** 大型库 → 按用户需求抽取 → 小规模 = **データマート（数据集市）**。

**选项分析：**
- マイニング：分析 **手法/过程**。
- モデリング：建模 **活动**。
- ディクショナリ：元数据 **辞典**。

**易错点：** DW 是企业级；Mart 是部门/主题级子集。""")

_a(13, "箭线图项目最短工期（日）？", "18", """**考点：** PERT/箭线图——关键路径长度。

**推导（对照 HTML 插图）：**
官方反馈给出关键路径 **C→F→H→J** = 3+2+5+8 = **18 日**。

**路径验算：** 从起点经 C(3)→F(2)→H(5)→J(8) 到终点，总 18 为最长路径。

**易错点：** 勿漏算并行支路；必须找 **最长** 路径而非任意通路。""")

_a(14, "代码 56243 的校验位（按给定 mod 公式）？", "1", """**考点：** チェックデジット 计算。

**推导：** a=5,b=6,c=2,d=4,e=3
- R = mod(5×5+4×6+3×2+2×4+1×3, 11) = mod(66, 11) = **0**
- C = mod(11−R, 10) = mod(11, 10) = **1**

**易错点：** 权重是 5,4,3,2,1 依次对应 a~e；第二步是 mod(11−R,10) 不是 mod(R,10)。""")

_a(15, "将项目作业逐层分解到可管理单元的结构？", "WBS", """**考点：** 项目范围分解。

**推导：** Work Breakdown Structure = **WBS（工作分解结构）**。

**选项分析：** ガント/トレンド/レーダー均为 **图表**，不是分解方法。

**易错点：** WBS 输出是树形任务清单，不是进度横条。""")

_a(16, "多程序同时更新同一数据时防止矛盾的控制？", "排他制御", """**考点：** 并发控制。

**推导：** 同时更新冲突 → **排他制御（互斥/锁）** 保证一致性。

**选项分析：**
- チェックポイント：恢复时点。
- コミット/ロールバック：事务结束/撤销 **动作**，非并发仲裁机制名称。

**易错点：** 题干强调「同時に更新」「矛盾が発生しないように」→ 排他。""")

_a(17, "导入费+运维/保守/教育等全部成本的总称？", "TCO", """**考点：** 系统成本术语。

**推导：** Total Cost of Ownership = **TCO（总拥有成本）**。

**选项分析：** TQC=全面质量；TLO=技术许可组织；TPC=事务处理性能委员会。

**易错点：** TCO 强调 **生命周期** 成本，非仅采购价。""")

_a(18, "用外部输入/输出/查询/文件数与复杂度系数估算规模？", "ファンクションポイント法", """**考点：** 软件规模估算方法。

**推导：** 计数 FP 要素（EI/EO/EQ/ILF/EIF）× 复杂度 = **ファンクションポイント法**。

**选项分析：**
- COCOMO：按 **源代码行数** 与模式系数估工数。
- 標準タスク法/プログラムステップ法：按任务或步数分解估工。

**易错点：** 题干列出的五类计数项是 FP 法标志。""")

_a(19, "通配符 X*Y?X* 匹配哪个字符串？", "XYYXYXYX", """**考点：** 通配符匹配（* = 0+ 任意字符，? = 恰好 1 字符）。

**推导：** 模式 `X * Y ? X *`
- **XYYXYXYX**：X + (空) + Y + Y + X + YXYX → 即 X + Y + Y + X + … ✓

**验算 a：** X | *→空 | Y | ?→Y | X | *→YXYX → **XYYXYXYX**

**易错点：** 首字符固定为 X；? 只占 **一个** 字符位。""")

_a(20, "UML 类图：行事—会員「参加する」关系的正确解读？", "どの行事にも参加しない会員が存在する。", """**考点：** UML 多重性（对照 HTML 类图）。

**推导（对照插图）：**
- 会員侧 **0..***：一名会员可参加 0~多个行事 → **存在不参加任何行事的会员** ✓
- 行事侧 **1..***：一个行事至少 1 名会员 → 不存在「零人参加」的行事

**选项分析：**
- a/c：与 1..* / 0..* 约束矛盾。
- b：「无人参加的行事」与行事端 1..* 矛盾。

**易错点：** 多重性读在 **关联另一端** 的实例数量上。""")

_a(21, "边收边播以缩短视频/音频等待开始时间？", "ストリーミング", """**考点：** 网络多媒体传输方式。

**推导：** 缓冲一定量即播放、边下载边播放 = **ストリーミング**。

**选项分析：**
- ミラーリング：磁盘镜像/复制。
- テザリング：手机共享网络。

**易错点：** a 选项「ストラィヒ°ング」为干扰拼写。""")

_a(22, "障害恢复描述中 □ 的正确字句组合？", "ウ", """**考点：** ログ与ロールバック/ロールフォワード。

**推导（对照 HTML 填空表）：**
- (1) ロールバック：用日志中 **更新前情報** 回到事务开始前 → a=更新前情報
- (2) ロールフォワード：恢复备份/チェックポイント后，用 **更新後情報** 重做 → b=更新後情報
- 组合 **ウ**：a=更新前情報，b=更新後情報

**易错点：**  rollback=前像；rollforward=后像；勿颠倒。""")

_a(23, "大数据中用统计等方法发现规律/相关性/趋势？", "データウェアハウス", """**考点：** 数据分析 vs 存储术语（⚠️ 本题语义与标答不一致）。

**中文题意：** 题干描述的是 **データマイニング（数据挖掘）**——分析发现规律。

**标答说明：** Moodle 标答为 **データウェアハウス**，但 DW 是 **存储整合** 系统，不是「分析手法」本身。备考以 **标答 d** 为准，理解上应区分：
- **マイニング** = 分析过程
- **ウェアハウス** = 时序/主题数据存储

**易错点：** 与 Q29 定义相近，两题选项可能互换；背 **日文定义** 而非中文直觉。""")

_a(24, "箭线图项目的クリティカルパス？", "C→ H → J→ L", """**考点：** 关键路径识别。

**推导（官方反馈路径时长）：**
| 路径 | 计算 | 合计 |
|------|------|------|
| A→F→K | 3+7+7 | 17 |
| B→G→L | 6+5+2 | 13 |
| C→E→G→L | 2+5+5+2 | 14 |
| **C→H→J→L** | **2+8+6+2** | **18** ← 最长 |

**正确答案：** **C→H→J→L**（最长路径即关键路径）。

**易错点：** 箭头图 Q13 与 Q24 网络不同，勿混用路径。""")

_a(25, "属性可取值的集合在关系模型中称？", "ドメイン", """**考点：** 关系数据库术语。

**推导：** 属性值域 = **ドメイン（域）**。

**选项分析：** DML=操纵语言；インデックス=索引结构；シリンダ=磁盘柱面（物理）。

**易错点：** ドメイン是 **逻辑** 层概念，非物理存储单位。""")

_a(26, "16bit×12500Hz 无压缩音频，128GB U 盘最多录几小时？", "1,422", """**考点：** 音频数据量估算。

**推导：**
- 每秒字节 = 12500 × (16/8) = **25000 B/s**
- 128 GB = 128×10⁹ B（题设 1G=10⁹）
- 秒数 = 128×10⁹ / 25000 = 5.12×10⁶ s
- 小时 = 5.12×10⁶ / 3600 ≈ **1422 小时**

**易错点：** 勿用 1024 进制；题面明确 1G=10⁹。""")

_a(27, "操作 A/B/C 与選択/射影/結合的对应？", "エ", """**考点：** 关系代数基本操作。

**推导：**
| 操作 | 含义 | 术语 |
|------|------|------|
| A | 按条件取 **行** | **選択** |
| B | 取 **列** | **射影** |
| C | 同值 **拼表** | **結合** |

→ 选项 **エ**：A=選択，B=射影，C=結合

**易错点：** 選択=σ，射影=π，結合=⋈/JOIN；勿与 SQL WHERE/GROUP BY 直接等同。""")

_a(28, "用日志中更新前信息回到事务开始前？", "ロールバック", """**考点：** 事务故障恢复。

**推导：** 异常终止 → 用 **更新前情報** 撤销 → **ロールバック**。

**选项分析：**
- コミット：确认写入。
- ロールフォワード：用后像 **重做**。
- フォールバック：非标准 DB 恢复术语（干扰项）。

**易错点：** 与 Q33 对比：Q28 是事务级 undo；Q33 是介质故障后 redo。""")

_a(29, "跨系统汇集交易数据、时序记录、用于决策分析？", "データディクショナリ", """**考点：** 数据仓库 vs 辞典（⚠️ 语义与标答需对照）。

**中文题意：** 题干描述 **データウェアハウス（数据仓库）**——多系统汇集、时序、决策支持。

**标答说明：** Moodle 标答 **データディクショナリ**；而 **ディクショナリ** 常指数据 **定义/metadata 目录**（见 Q38）。本题与 Q23 可能选项/答案对调，备考以 **标答 a** 为准。

**正确概念对照：**
- **ウェアハウス** = 历史交易集成、OLAP
- **ディクショナリ** = 项名/型/含义/所在 的元数据登记

**易错点：** 读清「时系列に記録」「意思決定」关键词——语义上更像 DW。""")

_a(30, "木曜障害后恢复到水曜业务结束，需哪些备份磁带？", "‘‘日曜日，月曜日，火曜日，水曜日’'のテープだけ", """**考点：** フル + 増分备份恢复策略。

**推导（官方反馈）：**
- 日曜：フルバックアップ（基线）
- 月〜水：各做 **増分**（自上次备份以来的变更）
- 目标：水曜 **业务结束后** 状态
- 木曜障害 → 尚未用到木曜磁带

**恢复组合：** 日曜全量 + 月 + 火 + 水 增量 = 选项 **a**

**易错点：**
- 仅水曜增量 **不够**（缺基线与中间增量链）
- 日+水 **不够**（缺月、火增量）""")

_a(31, "确认字段排列/长度/属性符合规定记录样式？", "フォーマットチェック", """**考点：** 输入校验——格式校验。

**推导：** レコード様式（布局/长度/属性）→ **フォーマットチェック**。

**易错点：** 与論理（业务规则）、リミット（数值界）区分。""")

_a(32, "120 项/24 日计划，15 日完成 60 项，进度落后几天？", "3", """**考点：** 项目进度偏差。

**推导：**
- 计划速率：120÷24 = **5 项/日**
- 15 日应完成：15×5 = **75 项**（或：60 项相当于 60÷5=**12 日** 工作量）
- 落后：15 − 12 = **3 日**

**易错点：** 「遅れ」= 实际日历日 − 按完成量折算应收工日。""")

_a(33, "磁盘故障后先恢复检查点/备份，再用日志更新后信息还原？", "ロールフォワード", """**考点：** 介质故障恢复——前滚。

**推导：** 备份/チェックポイント + ログ **更新後情報** 重做 = **ロールフォワード**。

**易错点：** Q28 ロールバック 用 **前像**；本题用 **后像** 向前恢复。""")

_a(34, "确认销售数量为数值？", "ニューメリックチェック", """**考点：** 数值校验。

**推导：** 是否 **数字** → **ニューメリックチェック**。

**易错点：** リミット 还检查是否在范围内；本题只问「数値であること」。""")

_a(35, "每次 DB 更新都输出、含更新前后信息的文件？", "ログファイル", """**考点：** 日志文件作用。

**推导：** 事务日志记录 before/after image → **ログファイル**。

**选项分析：** ワーク=中间工作；バックアップ=时点副本；パターン=模式文件（干扰）。

**易错点：** 日志是 **连续增量**；备份是 **周期性快照**。""")

_a(36, "共享锁/专有锁的正确描述？", "ジョブAによって共有ロックがかけられている資源に対して，ジョプBから共有ロックをかけることは可能である。", """**考点：** 读写锁语义。

**推导：**
- **共有ロック（S）**：多读兼容 → A 持 S 时 B 也可加 S ✓（选项 d）
- **专有ロック（X）**：写独占 → X 存在时不能再加 S 或 X

**选项分析：**
- a：双 X 互斥，不可能。
- b：A 持 S 时 B 不能加 X（写需独占）。
- c：A 持 X 时 B 不能加 S。

**易错点：** 「ジョプ」为「ジョブ」笔误；S-S 可并存，S-X / X-X 不可。""")

_a(37, "学生表 JOIN 得点表，筆記/実技均≥平均，排序后的结果？", "エ", """**考点：** SQL 子查询 + 排序。

**推导（对照 HTML 表与 SQL）：**
- 平均：筆記=70，実技=60
- 满足：S103(70,70)、S105(80,60)
- `ORDER BY 筆記 DESC, 実技 DESC` → **S105 在前，S103 在后**
- 对应选项图 **エ**

**易错点：** ウ 与 エ 数据相同但 **行序** 不同；必须看 ORDER BY。""")

_a(38, "集中记录数据项种类/名称/型/含义/所在的开发共享物？", "データディクショナリ", """**考点：** 数据辞典定义。

**推导：** 元数据统一定义 = **データディクショナリ**。

**易错点：** 与 Q29 不同，本题明确是 **定义信息的一元管理**，非时序交易库。""")

_a(39, "确认得分未超过上限值？", "リミットチェック", """**考点：** 上下限校验。

**推导：** 上限値を超えていない = 范围/界限制 → **リミットチェック**。

**易错点：** ニューメリック 只验数字；リミット 验 **阈值**。""")
