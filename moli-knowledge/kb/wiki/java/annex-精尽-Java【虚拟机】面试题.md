---
title: 精尽 Java【虚拟机】面试题.note（原文插图 annex）
slug: annex-精尽-Java【虚拟机】面试题
type: article
status: active
tags: [wujinsen, annex, 插图]
sources:
  - raw/wujinsen_markdown/面试笔试/精尽面试题/JVM/精尽 Java【虚拟机】面试题.note.md
related: [jvm-面试题]
created: 2026-07-05
updated: 2026-07-05
---

htp:/svip.iocoder.cn/Java/VirtualMachine/Interview/

# 精尽 Java【虚拟机】⾯试题

以下⾯试题，基于⽹络整理，和⾃⼰编辑。具体参考的⽂章，会在⽂末给出所有的链接。 如果胖友有⾃⼰的疑问，欢迎在星球提问，我们⼀起整理吊吊的 Java【虚拟机】⾯试题的⼤保健。 ⽽题⽬的难度，艿艿尽量按照从容易到困难的顺序，逐步下去。 因为 Java 并发涉及到的内容会⾮常多，本⾯试题可能很难覆盖到所有的知识点，所以推荐

《深⼊拆 解 Java 虚拟机》

。并且，本⽂会将⾯试题和该书的章节，⼤体保持⼀致。嘻嘻~ 另外，本⽂涉及的⾯试题会超级超级超级多，所以艿艿已经分了⼩节，胖友要注意哟。 对于⼤部分 JVM 的⾯试题，基本都是概念题，所以艿艿⼀些⾯试题如果和

《深⼊拆解 Java 虚拟 机》

中，可以找到对应的章节，会进⾏注明。毕竟，看书可以理解更加深刻。

艿艿：因为和字节码、指令级，理解起来相对复杂，实际⾯试问的也相对 少。所以，本⽂暂时先不写。当然，未来肯定要补的，这么有趣的东⻄， 胖友说是不是。

# ⾛近 Java

什么是虚拟机？

Java 虚拟机，是⼀个可以执⾏ Java 字节码的虚拟机进程。Java 源⽂件被编译成能被 Java 虚拟机执 ⾏的字节码⽂件( .class )。 Java 被设计成允许应⽤程序可以运⾏在任意的平台，⽽不需要程序员为每⼀个平台单独重写或者是重 新编译。Java 虚拟机让这个变为可能，因为它知道底层硬件平台的指令⻓度和其他特性。 但是，跨平台的是 Java 程序(包括字节码⽂件)，，⽽不是 JVM。JVM 是⽤ C/C++ 开发的，是编译后 的机器码，不能跨平台，不同平台下需要安装不同版本的 JVM 。

![image 1](assets/imageFile1.png)

不同平台，不同 JVM

也就是说，JVM 能够跨计算机体系结构来执⾏ Java 字节码，主要是由于 JVM 屏蔽了与各个计算机 平台相关的软件或者硬件之间的差异，使得与平台相关的耦合统⼀由 JVM 提供者来实现。

## JVM 由哪些部分组成？

考察对 JVM 体系结构的掌握 的 「2.2 运⾏时数据区域」 。

### 《深⼊拆解 Java 虚拟机》

JVM 的结构基本上由 4 部分组成：

注意打钩的 4 个地⽅。

![image 2](assets/imageFile2.png)

类加载器，在 JVM 启动时或者类运⾏时将需要的 class 加载到 JVM 中。

内存区，将内存划分成若⼲个区以模拟实际机器上的存储、记录和调度功能模块，如实际机器上的 各种功能的寄存器或者 PC 指针的记录器等。

关于这⼀块，我们在 「Java 内存区域与内存溢出异常」 也会细看。

执⾏引擎，执⾏引擎的任务是负责执⾏ class ⽂件中包含的字节码指令，相当于实际机器上的 CPU 。

本地⽅法调⽤，调⽤ C 或 C++ 实现的本地⽅法的代码返回结果。

怎样通过 Java 程序来判断 JVM 是 32 位 还是 64 位？

Sun 有⼀个 Java System 属性来确定JVM的位数：32 或 64。

sun.arch.data.model=32 // 32 bit JVM sun.arch.data.model=64 // 64 bit JVM

我可以使⽤以下语句来确定 JVM 是 32 位还是 64 位：

<table>
  <tr>
    <th> </th>
  </tr>
</table>


System.getProperty("sun.arch.data.model")

🦅 32 位 JVM 和 64 位 JVM 的最⼤堆内存分别是多数？ 理论上说上 32 位的 JVM 堆内存可以到达 2^32，即 4GB ，但实际上会⽐这个⼩很多。不同操作系统 之间不同，如 Windows 系统⼤约 1.5 GB，Solaris ⼤约 3GB。 64 位 JVM 允许指定最⼤的堆内存，理论上可以达到 2^64 ，这是⼀个⾮常⼤的数字，实际上你可以指 定堆内存⼤⼩到 100GB 。甚⾄有的 JVM，如 Azul ，堆内存到 1000G 都是可能的。 🦅 64 位 JVM 中，int 的⻓度是多数？ Java 中，int 类型变量的⻓度是⼀个固定值，与平台⽆关，都是 32 位或者 4 个字节。意思就是说，在 32 位 和 64 位 的Java 虚拟机中，int 类型的⻓度是相同的。

# Java 内存区域与内存溢出异常

## JVM 运⾏内存的分类？

### 《深⼊拆解 Java 虚拟机》

的 「2.3.1 对象的创建」 。

JVM 运⾏内存的分类如下图所示：

注意，这个图是基于 JDK6 版本的运⾏内存的分类。

![image 3](assets/imageFile3.png)

程序计数器： Java 线程私有，类似于操作系统⾥的 PC 计数器，它可以看做是当前线程所执⾏的 字节码的⾏号指示器。

如果线程正在执⾏的是⼀个 Java ⽅法，这个计数器记录的是正在执⾏的虚拟机字节码指令的地 址；如果正在执⾏的是 Native ⽅法，这个计数器值则为空（Undefined）。

此内存区域是唯⼀⼀个在 Java 虚拟机规范中没有规定任何 OutOfMemoryError 情况的区域。

虚拟机栈（栈内存）：Java线程私有，虚拟机栈描述的是 Java ⽅法执⾏的内存模型：

每个⽅法在执⾏的时候，都会创建⼀个栈帧⽤于存储局部变量、操作数、动态链接、⽅法出⼝ 等信息。

每个⽅法调⽤都意味着⼀个栈帧在虚拟机栈中⼊栈到出栈的过程。

本地⽅法栈 ：和 Java 虚拟机栈的作⽤类似，区别是该区域为 JVM 提供使⽤ Native ⽅法的服务。

堆内存（线程共享）：所有线程共享的⼀块区域，垃圾收集器管理的主要区域。

⽬前主要的垃圾回收算法都是分代收集算法，所以 Java 堆中还可以细分为：新⽣代和⽼年代； 再细致⼀点的有 Eden 空间、From Survivor 空间、To Survivor 空间等，默认情况下新⽣代按 照 8:1:1的⽐例来分配。

根据 Java 虚拟机规范的规定，Java 堆可以处于物理上不连续的内存空间中，只要逻辑上是连 续的即可，就像我们的磁盘⼀样。

⽅法区（线程共享）：各个线程共享的⼀个区域，⽤于存储虚拟机加载的类信息、常量、静态变 量、即时编译器编译后的代码等数据。

虽然 Java 虚拟机规范把⽅法区描述为堆的⼀个逻辑部分，但是它却有⼀个别名叫做 Non-Heap （⾮堆），⽬的应该是与 Java 堆区分开来。

运⾏时常量池：是⽅法区的⼀部分，⽤于存放编译器⽣成的各种字⾯量和符号引⽤。

🦅 直接内存是不是虚拟机运⾏时数据区的⼀部分？

《JVM 直接内存》

参⻅ ⽂章。 直接内存(Direct Memory)，并不是虚拟机运⾏时数据区的⼀部分，也不是 Java 虚拟机规范中农定义的 内存区域。在 JDK1.4 中新加⼊了 NIO(New Input/Output) 类，引⼊了⼀种基于通道(Channel)与缓冲 区（Buffer）的 I/O ⽅式，它可以使⽤ native 函数库直接分配堆外内存，然后通脱⼀个存储在 Java 堆 中的 DirectByteBuffer 对象作为这块内存的引⽤进⾏操作。这样能在⼀些场景中显著提⾼性能，因为避 免了在 Java 堆和 Native 堆中来回复制数据。

本机直接内存的分配不会受到 Java 堆⼤⼩的限制，受到本机总内存⼤⼩限制。

配置虚拟机参数时，不要忽略直接内存，防⽌出现 OutOfMemoryError 异常。

🦅 直接内存（堆外内存）与堆内存⽐较？

- 1.
- 2.


直接内存申请空间耗费更⾼的性能，当频繁申请到⼀定量时尤为明显。 直接内存 IO 读写的性能要优于普通的堆内存，在多次读写操作的情况下差异明显。

🦅 实际上，后续的版本，主要对【⽅法区】做了⼀定的调整

- JDK7 的改变

存储在永久代的部分数据就已经转移到了 Java Heap 或者是 Native Heap。但永久代仍存在于 JDK7 中，但是并没完全移除。

常量池和静态变量放到 Java 堆⾥。

- JDK8 的改变


废弃 PermGen（永久代），新增 Metaspace（元数据区）。

那么⽅法区还在么？FROM 狼哥 的解答：⽅法区在 Metaspace 中了，⽅法区都是⼀个概念的 东⻄。😈 狼哥通过撸源码获得该信息。

### 因为，《Java 虚拟机规范》只是规定了有⽅法区这么个概念和它的作⽤， 并没有规定如何去实现它。那么，在不同的 JVM 上⽅法区的实现肯定是不 同的了。 同时，⼤多数⽤的 JVM 都是 Sun 公司的 HotSpot 。在 HotSpot 上把 GC 分 代收集扩展⾄⽅法区，或者说使⽤永久带来实现⽅法区。

参考⽂章

《JVM 内存堆布局图解分析》

《笔记之 JVM ⽅法区：永久带 VS 元空间》

《JVM —— 移除永久代》

《JDK 源码剖析五：JDK8 —— 废弃永久代（PermGen）迎来元空间（Metaspace）》

《Java8 内存模型 —— 永久代(PermGen)和元空间(Metaspace)》

🦅 JDK8 之后 Perm Space 有哪些变动? MetaSpace ⼤⼩默认是⽆限的么? 还是你们会通过什么⽅式 来指定⼤⼩?

JDK8 后⽤元空间替代了 Perm Space ；字符串常量存放到堆内存中。

MetaSpace ⼤⼩默认没有限制，⼀般根据系统内存的⼤⼩。JVM 会动态改变此值。

可以通过 JVM 参数配置

- -XX:MetaspaceSize ： 分配给类元数据空间（以字节计）的初始⼤⼩（Oracle 逻辑存储上 的初始⾼⽔位，the initial high-water-mark）。此值为估计值，MetaspaceSize 的值设置的过⼤ 会延⻓垃圾回收时间。垃圾回收过后，引起下⼀次垃圾回收的类元数据空间的⼤⼩可能会变 ⼤。

- -XX:MaxMetaspaceSize ：分配给类元数据空间的最⼤值，超过此值就会触发Full GC 。此 值默认没有限制，但应取决于系统内存的⼤⼩，JVM 会动态地改变此值。


🦅 为什么要废弃永久代？

- 1）现实使⽤中易出问题。 由于永久代内存经常不够⽤或发⽣内存泄露，爆出异常 java.lang.OutOfMemoryError: PermGen 。

- 2）永久代会为 GC 带来不必要的复杂度，并且回收效率偏低。
- 3）Oracle 可能会将HotSpot 与 JRockit 合⼆为⼀。 参照 JEP122 ： ，原⽂截取：


字符串存在永久代中，容易出现性能问题和内存溢出。

类及⽅法的信息等⽐较难确定其⼤⼩，因此对于永久代的⼤⼩指定⽐较困难，太⼩容易出现永久代 溢出，太⼤则容易导致⽼年代溢出。

http://openjdk.java.net/jeps/122

Motivation This is part of the JRockit and Hotspot convergence effort. JRockit customers do not need to configure the permanent generation (since JRockit does not have a permanent generation) and are accustomed to not configuring the permanent generation.

即：移除永久代是为融合 HotSpot JVM 与 JRockit VM ⽽做出的努⼒，因为 JRockit 没有永久代，不 需要配置永久代。

Java 内存堆和栈区别？

栈内存⽤来存储基本类型的变量和对象的引⽤变量；堆内存⽤来存储Java中的对象，⽆论是成员变 量，局部变量，还是类变量，它们指向的对象都存储在堆内存中。

栈内存归属于单个线程，每个线程都会有⼀个栈内存，其存储的变量只能在其所属线程中可⻅，即 栈内存可以理解成线程的私有内存；堆内存中的对象对所有线程可⻅。堆内存中的对象可以被所有 线程访问。

如果栈内存没有可⽤的空间存储⽅法调⽤和局部变量，JVM 会抛 出 java.lang.StackOverFlowError 错误；如果是堆内存没有可⽤的空间存储⽣成的对象， JVM 会抛出 java.lang.OutOfMemoryError 错误。

栈的内存要远远⼩于堆内存，如果你使⽤递归的话，那么你的栈很快就会充满。-Xss 选项设置栈 内存的⼤⼩，-Xms 选项可以设置堆的开始时的⼤⼩。

当然，如果你记不住这个些，只要记住如下即可：

JVM 中堆和栈属于不同的内存区域，使⽤⽬的也不同。栈常⽤于保存⽅法 帧和局部变量，⽽对象总是在堆上分配。栈通常都⽐堆⼩，也不会在多个 线程之间共享，⽽堆被整个 JVM 的所有线程共享。

![image 4](assets/imageFile4.png)

## JAVA 对象创建的过程？

的 「2.3.1 对象的创建」 。 注意，加粗的⽂字部分。

### 《深⼊拆解 Java 虚拟机》

JAVA 对象创建的过程，如下图所示：

![image 5](assets/imageFile5.png)

JAVA 对象创建的过程

Java 中对象的创建就是在堆上分配内存空间的过程，此处说的对象创建仅限于 new 关键字创建的 普通 Java 对象，不包括数组对象的创建。

- 1）检测类是否被加载 当虚拟机遇到 new 指令时，⾸先先去检查这个指令的参数是否能在常量池中定位到⼀个类的符号引 ⽤，并且检查这个符号引⽤代表的类是否已被加载、解析和初始化过。如果没有，就执⾏类加载过 程。

- 2）为对象分配内存 类加载完成以后，虚拟机就开始为对象分配内存，此时所需内存的⼤⼩就已经确定了。只需要在堆上 分配所需要的内存即可。 具体的分配内存有两种情况：第⼀种情况是内存空间绝对规整，第⼆种情况是内存空间是不连续的。

多线程并发时会出现正在给对象 A 分配内存，还没来得及修改指针，对象 B ⼜⽤这个指针分配内存， 这样就出现问题了。解决这种问题有两种⽅案：

- 3）为分配的内存空间初始化零值 对象的内存分配完成后，还需要将对象的内存空间都初始化为零值，这样能保证对象即使没有赋初 值，也可以直接使⽤。
- 4）对对象进⾏其他设置 分配完内存空间，初始化零值之后，虚拟机还需要对对象进⾏其他必要的设置，设置的地⽅都在对象 头中，包括这个对象所属的类，类的元数据信息，对象的 hashcode ，GC 分代年龄等信息。
- 5）执⾏ init ⽅法 执⾏完上⾯的步骤之后，在虚拟机⾥这个对象就算创建成功了，但是对于 Java 程序来说还需要执⾏ init ⽅法才算真正的创建完成，因为这个时候对象只是被初始化零值了，还没有真正的去根据程序中的 代码分配初始值，调⽤了 init ⽅法之后，这个对象才真正能使⽤。 到此为⽌⼀个对象就产⽣了，这就是 new 关键字创建对象的过程。过程如下：


对于内存绝对规整的情况相对简单⼀些，虚拟机只需要在被占⽤的内存和可⽤空间之间移动指针即 可，这种⽅式被称为“指针碰撞”。

对于内存不规整的情况稍微复杂⼀点，这时候虚拟机需要维护⼀个列表，来记录哪些内存是可⽤ 的。分配内存的时候需要找到⼀个可⽤的内存空间，然后在列表上记录下已被分配，这种⽅式成为 “空闲列表”。

第⼀种，是采⽤同步的办法，使⽤ CAS 来保证操作的原⼦性。

另⼀种，是每个线程分配内存都在⾃⼰的空间内进⾏，即是每个线程都在堆中预先分配⼀⼩块内 存，称为本地线程分配缓冲（Thread Local Allocation Buffer, TLAB），分配内存的时候再TLAB上 分配，互不⼲扰。可以通过 -XX:+/-UseTLAB 参数决定。

![image 6](assets/imageFile6.png)

JAVA 对象创建的过程

另外，这个问题，⾯试官可能引申成 “A a = new A() 经历过什么过程” 的问题。

## 对象的内存布局是怎样的？

### 《深⼊拆解 Java 虚拟机》

### 的 「2.3.2 对象的内存布局」 。

对象的内存布局包括三个部分：

对象头：对象头包括两部分信息。

第⼀部分，是存储对象⾃身的运⾏时数据，如哈希码，GC 分代年龄，锁状态标志，线程持有的 锁等等。

第⼆部分，是类型指针，即对象指向类元数据的指针。

实例数据：就是数据。

对⻬填充：不是必然的存在，就是为了对⻬。

对象是如何定位访问的？

### 《深⼊拆解 Java 虚拟机》

### 的 「2.3.3 对象的访问定位」 。

对象的访问定位有两种：

句柄定位：Java 堆会画出⼀块内存来作为句柄池，reference 中存储的就是对象的句柄地址，⽽句 柄中包含了对象实例数据与类型数据各⾃的具体地址信息。

![image 7](assets/imageFile7.png)

句柄定位

直接指针访问：Java 堆对象的不居中就必须考虑如何放置访问类型数据的相关信息，⽽ reference 中存储的直接就是对象地址。

![image 8](assets/imageFile8.png)

直接指针访问 🦅 对⽐两种⽅式？ 这两种对象访问⽅式各有优势。

使⽤句柄来访问的最⼤好处，就是 reference 中存储的是稳定的句柄地址，在对象被移动（垃圾收 集时移动对象是⾮常普遍的⾏为）时只会改变句柄中的实例数据指针，⽽ reference 本身不需要修 改。

使⽤直接指针访问⽅式的最⼤好处就是速度更快，它节省了⼀次指针定位的时间开销，由于对象的 访问在 Java 中⾮常频繁，因此这类开销积少成多后也是⼀项⾮常可观的执⾏成本。

我们⽬前主要虚拟机 Sun HotSpot ⽽⾔，它是使⽤第⼆种⽅式进⾏对象访问的，但从整个软件开发的 范围来看，各种语⾔和框架使⽤句柄来访问的情况也⼗分常⻅。

## 有哪些 OutOfMemoryError 异常？

### 《深⼊拆解 Java 虚拟机》

的 「2.4 实战：OutOfMemoryError 异常」。

在 Java 虚拟机中规范的描述中，除了程序计数器外，虚拟机内存的其它⼏个运⾏时区域都有发⽣的 OutOfMemoryError(简称为“OOM”) 异常的可能。

Java 堆溢出

虚拟机栈和本地⽅法栈溢出 ⽅法区和运⾏时常量池溢出

### 从 JDK8 开始，就变成元数据区的内存溢出。

本机直接内存溢出

- 1）Java 堆溢出 重现⽅式，参⻅ ⽂章。 另外，Java 堆溢出的原因，有可能是内存泄露，可以使⽤ MAT 进⾏分析。
- 2）虚拟机栈和本地⽅法栈溢出 由于在 HotSpot 虚拟机中并不区分虚拟机栈和本地⽅法栈，因此，对于 HotSpot 来说，虽然 Xoss 参数（设置本地⽅法栈⼤⼩）存在，但实际上是⽆效的，栈容量只由 -Xss参数设定。 关于虚拟机栈和本地⽅法栈，在 Java 虚拟机规范中描述了两种异常：

StackOverflowError 不属于 OOM 异常哈。

重现⽅式，参⻅ ⽂章。

- 3）运⾏时常量池溢出

- 因为 JDK7 将常量池和静态变量放到 Java 堆⾥，所以⽆法触发运⾏时常量池溢出。如果想要触发，可 以使⽤ JDK6 的版本。 重现⽅式，参⻅ ⽂章。

4）⽅法区的内存溢出

- 因为 JDK8 将⽅法区溢出，所以⽆法触发⽅法区的内存溢出溢出。如果想要触发，可以使⽤ JDK7 的 版本。 重现⽅式，参⻅ ⽂章。


- 5）元数据区的内存溢出 实际上，⽅法区的内存溢出在 JDK8 中，变成了元数据区的内存溢出。所以，重现⽅式，还是参⻅

⽂章，只是说，需要增加 -XX:MaxMetaspaceSize=10m VM 配置项。

- 6）本机直接内存溢出 重现⽅式，参⻅ ⽂章。


《Java 堆溢出》

如果线程请求的栈深度⼤于虚拟机所允许的最⼤深度，将抛出 StackOverflowError 异常。

如果虚拟机在扩展栈时⽆法申请到⾜够的内存空间，则抛出 OutOfMemoryError 异常。 《OutOfMemoryError 异常 —— 虚拟机栈和本地⽅法栈溢出》

《JVM 内存溢出 - ⽅法区及运⾏时常量池溢出》

《Java ⽅法区溢出》

《J ava ⽅法区溢出》

《JVM 内存溢出 —— 直接内存溢出》

《Java 内存溢出(OOM)异常完全 指南》

另外，⾮常推荐⼀篇⽂章，胖友耐⼼阅读，提供了更多有趣的案例， 。 🦅 当出现了内存溢出，你怎么排错？

- 1、⾸先，控制台查看错误⽇志。

- 2、然后，使⽤ JDK ⾃带的 jvisualvm ⼯具查看系统的堆栈⽇志。

- 3、定位出内存溢出的空间：堆，栈还是永久代（JDK8 以后不会出现永久代的内存溢出）。


如果是堆内存溢出，看是否创建了超⼤的对象。

如果是栈内存溢出，看是否创建了超⼤的对象，或者产⽣了死循环。

Java 中会存在内存泄漏吗？

理论上 Java 因为有垃圾回收机制（GC）不会存在内存泄露问题（这也是 Java 被⼴泛使⽤于服务器端 编程的⼀个重要原因）。然⽽在实际开发中，可能会存在⽆⽤但可达的对象，这些对象不能被 GC 回 收也会发⽣内存泄露。例如说：

Hibernate 的 Session（⼀级缓存）中的对象属于持久态，垃圾回收器是不会回收这些对象的，然⽽ 这些对象中可能存在⽆⽤的垃圾对象。

使⽤ Netty 的堆外的 ByteBuf 对象，在使⽤完后，并未归还，导致使⽤的⼀点⼀点在泄露。

# 垃圾收集器与内存分配策略

## 什么是垃圾回收机制？

《深⼊拆解 Java 虚拟机》

### 的 「3.1 概述」 。

Java 中对象是采⽤ new 或者反射的⽅法创建的，这些对象的创建都是在堆(Heap)中分配的，所有 对象的回收都是由 Java 虚拟机通过垃圾回收机制完成的。GC 为了能够正确释放对象，会监控每个 对象的运⾏状况，对他们的申请、引⽤、被引⽤、赋值等状况进⾏监控。

Java 程序员不⽤担⼼内存管理，因为垃圾收集器会⾃动进⾏管理。

可以调⽤下⾯的⽅法之⼀：System#gc() 或 Runtime#getRuntime()#gc() ，但 JVM 也可以 屏蔽掉显示的垃圾回收调⽤。

🦅 为什么不建议在程序中显式的声明 System.gc() ？ 因为显式声明是做堆内存全扫描，也就是 Full GC ，是需要停⽌所有的活动的(Stop The World Collection)，对应⽤很⼤可能存在影响。 另外，调⽤ System.gc() ⽅法后，不会⽴即执⾏ Full GC ，⽽是虚拟机⾃⼰决定的。 🦅 如果⼀个对象的引⽤被设置为 null , GC 会⽴即释放该对象的内存么? 不会, 这个对象将会在下⼀次 GC 循环中被回收。 🦅 #finalize() ⽅法什么时候被调⽤？它的⽬的是什么？

### 《深⼊拆解 Java 虚拟机》

的 「3.2.4 ⽣存还是死亡」 。

#finallize() ⽅法，是在释放该对象内存前由 GC (垃圾回收器)调⽤。

通常建议在这个⽅法中释放该对象持有的资源，例如持有的堆外内存、和远程服务的⻓连接。

⼀般情况下，不建议重写该⽅法。

对于⼀个对象，该⽅法有且仅会被调⽤⼀次。

## 如何判断⼀个对象是否已经死去？

### 《深⼊拆解 Java 虚拟机》

的 「3.2 对象已死吗」 。

有两种⽅式：

- 1.
- 2.


引⽤计数 可达性分析

- 1）引⽤计数 每个对象有⼀个引⽤计数属性，新增⼀个引⽤时计数加 1 ，引⽤释放时计数减 1 ，计数为 0 时可以回 收。此⽅法简单，⽆法解决对象相互循环引⽤的问题。⽬前在⽤的有 Python、ActionScript3 等语⾔。
- 2）可达性分析（Reachability Analysis） 从 GC Roots 开始向下搜索，搜索所⾛过的路径称为引⽤链。当⼀个对象到 GC Roots 没有任何引⽤ 链相连时，则证明此对象是不可⽤的。不可达对象。⽬前在⽤的有 Java、C# 等语⾔。 🦅 如果 A 和 B 对象循环引⽤，是否可以被 GC？ 可以，因为 Java 采⽤可达性分析的判断⽅式。 🦅 在 Java 语⾔⾥，可作为 GC Roots 的对象包括以下⼏种？


- 1.
- 2.
- 3.
- 4.


虚拟机栈（栈帧中的本地变量表）中引⽤的对象。 ⽅法区中的类静态属性引⽤的对象。 ⽅法区中常量引⽤的对象。 本地⽅法栈中 JNI(即⼀般说的 Native ⽅法)中引⽤的对象。

🦅 ⽅法区是否能被回收？ ⽅法区可以被回收，但是价值很低，主要回收废弃的常量和⽆⽤的类。 如何判断⽆⽤的类，需要完全满⾜如下三个条件：

- 1.
- 2.
- 3.


该类所有实例都被回收（Java 堆中没有该类的对象）。 加载该类的 ClassLoader 已经被回收。 该类对应的 java.lang.Class 对象没有在任何地⽅被引⽤，⽆法在任何地⽅利⽤反射访问该 类。

## Java 对象有哪些引⽤类型？

### 《深⼊拆解 Java 虚拟机》

的 「3.2.3 再谈引⽤类型」 。

Java ⼀共有四种引⽤类型：

强引⽤

软引⽤（SoftReference）

弱引⽤（WeakReference）

虚引⽤（PhantomReference）

- 1）强引⽤ 以前我们使⽤的⼤部分引⽤实际上都是强引⽤，这是使⽤最普遍的引⽤。如果⼀个对象具有强引⽤， 那就类似于必不可少的⽣活⽤品，垃圾回收器绝不会回收它。当内存空间不⾜，Java 虚拟机宁愿抛出 OutOfMemoryError 错误，使程序异常终⽌，也不会靠随意回收具有强引⽤的对象来解决内存不⾜问 题。

艿艿：不然，代码都没法写了 😈

- 2）软引⽤（SoftReference） 如果⼀个对象只具有软引⽤，那就类似于可有可物的⽣活⽤品。如果内存空间⾜够，垃圾回收器就不 会回收它，如果内存空间不⾜了，就会回收这些对象的内存。只要垃圾回收器没有回收它，该对象就 可以被程序使⽤。软引⽤可⽤来实现内存敏感的⾼速缓存。 软引⽤可以和⼀个引⽤队列（ReferenceQueue）联合使⽤，如果软引⽤所引⽤的对象被垃圾回收， JAVA 虚拟机就会把这个软引⽤加⼊到与之关联的引⽤队列中。 使⽤示例，⻅ 的 「2.10 SoftCache」 ⼩节。
- 3）弱引⽤（WeakReference） 如果⼀个对象只具有弱引⽤，那就类似于可有可物的⽣活⽤品。弱引⽤与软引⽤的区别在于：只具有 弱引⽤的对象拥有更短暂的⽣命周期。在垃圾回收器线程扫描它 所管辖的内存区域的过程中，⼀旦发 现了只具有弱引⽤的对象，不管当前内存空间⾜够与否，都会回收它的内存。不过，由于垃圾回收器 是⼀个优先级很低的线程， 因此不⼀定会很快发现那些只具有弱引⽤的对象。 弱引⽤可以和⼀个引⽤队列（ReferenceQueue）联合使⽤，如果弱引⽤所引⽤的对象被垃圾回收， Java虚拟机就会把这个弱引⽤加⼊到与之关联的引⽤队列中。 使⽤示例，⻅
- 4）虚引⽤（PhantomReference） “虚引⽤”顾名思义，就是形同虚设，与其他⼏种引⽤都不同，虚引⽤并不会决定对象的⽣命周期。如果 ⼀个对象仅持有虚引⽤，那么它就和没有任何引⽤⼀样，在任何时候都可能被垃圾回收。 虚引⽤主要⽤来跟踪对象被垃圾回收的活动。虚引⽤与软引⽤和弱引⽤的⼀个区别在于：虚引⽤必须 和引⽤队列（ReferenceQueue）联合使⽤。当垃 圾回收器准备回收⼀个对象时，如果发现它还有虚 引⽤，就会在回收对象的内存之前，把这个虚引⽤加⼊到与之关联的引⽤队列中。程序可以通过判断 引⽤队列中是否已经加⼊了虚引⽤，来了解被引⽤的对象是否将要被垃圾回收。程序如果发现某个虚 引⽤已经被加⼊到引⽤队列，那么就可以在所引⽤的对象的内存被回收之前采取必要的⾏动。


《精尽 MyBatis 源码分析 —— 缓存模块》

《精尽 MyBatis 源码分析 —— 缓存模块》 的 「2.9 WeakCache」 ⼩节。

的 「4. ResourceLeakDetector」 ⼩节。

《精尽 Netty 源码解析 —— Buffer 之 ByteBuf（三）内存泄露检测》

如果胖友想看看各种引⽤在 GC 下的效果，可以看看 提供的代码示例。 🦅 WeakReference 与 SoftReference的区别？ 虽然 WeakReference 与 SoftReference 都有利于提⾼ GC 和 内存的效率。

《Java 中的四种引⽤类型》

但是 WeakReference ⼀旦失去最后⼀个强引⽤，就会被 GC 回收

⽽ SoftReference 虽然不能阻⽌被回收，但是可以延迟到 JVM 内存不⾜的时候。

🦅 为什么要有不同的引⽤类型？ 不像 C 语⾔，我们可以控制内存的申请和释放，在 Java 中有时候我们需要适当的控制对象被回收的 时机，因此就诞⽣了不同的引⽤类型，可以说不同的引⽤类型实则是对 GC 回收时机不可控的妥协。 有以下⼏个使⽤场景可以充分的说明：

利⽤软引⽤和弱引⽤解决 OOM 问题。⽤⼀个 HashMap 来保存图⽚的路径和相应图⽚对象关联的 软引⽤之间的映射关系，在内存不⾜时，JVM 会⾃动回收这些缓存图⽚对象所占⽤的空间，从⽽有 效地避免了 OOM 的问题.

通过软引⽤实现 Java 对象的⾼速缓存。⽐如我们创建了⼀ Person 的类，如果每次需要查询⼀个⼈ 的信息，哪怕是⼏秒中之前刚刚查询过的，都要重新构建⼀个实例，这将引起⼤量 Person 对象的 消耗，并且由于这些对象的⽣命周期相对较短，会引起多次 GC 影响性能。此时，通过软引⽤和 HashMap 的结合可以构建⾼速缓存，提供性能。

## JVM 垃圾回收算法？

### 《深⼊拆解 Java 虚拟机》

的 「3.3 垃圾回收算法」 。

有四种算法：

- 1.
- 2.
- 3.
- 4.


标记-清除算法 标记-整理算法 复制算法 分代收集算法

- 1）标记-清除算法 标记-清除（Mark-Sweep）算法，是现代垃圾回收算法的思想基础。 标记-清除算法将垃圾回收分为两个阶段：标记阶段和清除阶段。 ⼀种可⾏的实现是，在标记阶段，⾸先通过根节点，标记所有从根节点开始的可达对象。因此，未被 标记的对象就是未被引⽤的垃圾对象（好多资料说标记出要回收的对象，其实明⽩⼤概意思就可以 了）。然后，在清除阶段，清除所有未被标记的对象。


![image 9](assets/imageFile9.png)

标记-清除算法

缺点：

- 1、效率问题，标记和清除两个过程的效率都不⾼。

- 2、空间问题，标记清除之后会产⽣⼤量不连续的内存碎⽚，空间碎⽚太多可能会导致以后在程 序运⾏过程中需要分配较⼤的对象时，⽆法找到⾜够的连续内存⽽不得不提前触发另⼀次垃圾 收集动作。


- 2）标记-整理算法 标记整理算法，类似与标记清除算法，不过它标记完对象后，不是直接对可回收对象进⾏清理，⽽是 让所有存活的对象都向⼀端移动，然后直接清理掉边界以外的内存。

标记-整理算法

- 3）复制算法 复制算法，可以解决效率问题，它将可⽤内存按容量划分为⼤⼩相等的两块，每次只使⽤其中的⼀ 块，当这⼀块内存⽤完了，就将还存活着的对象复制到另⼀块上⾯，然后再把已经使⽤过的内存空间 ⼀次清理掉，这样使得每次都是对整个半区进⾏内存回收，内存分配时也就不⽤考虑内存碎⽚等复杂 情况，只要移动堆顶指针，按顺序分配内存即可（还可使⽤TLAB进⾏⾼效分配内存）。


![image 10](assets/imageFile10.png)

优点：

- 1、相对标记清除算法，解决了内存碎⽚问题。

- 2、没有内存碎⽚后，对象创建内存分配也更快速了（可以使⽤TLAB进⾏分配）。


缺点：

1、效率问题，（同标记清除算法）标记和整理两个过程的效率都不⾼。

![image 11](assets/imageFile11.png)

复制算法

图的上半部分是未回收前的内存区域，图的下半部分是回收后的内存区域。通过图，我们发现不管 回收前还是回收后都有⼀半的空间未被利⽤。

优点：

1、效率⾼，没有内存碎⽚。

缺点：

- 1、浪费⼀半的内存空间。

- 2、复制收集算法在对象存活率较⾼时就要进⾏较多的复制操作，效率将会变低。


- 4）分代收集算法 当前商业虚拟机都是采⽤分代收集算法，它根据对象存活周期的不同将内存划分为⼏块，⼀般是把 Java 堆分为新⽣代和⽼年代，然后根据各个年代的特点采⽤最适当的收集算法。


在新⽣代中，每次垃圾收集都发现有⼤批对象死去，只有少量存活，就选⽤复制算法。

⽽⽼年代中，因为对象存活率⾼，没有额外空间对它进⾏分配担保，就必须使⽤“标记清理”或者“标 记整理”算法来进⾏回收。

![image 12](assets/imageFile12.png)

分代收集算法

图的左半部分是未回收前的内存区域，右半部分是回收后的内存区域。

对象分配策略：

对象优先在 Eden 区域分配，如果对象过⼤直接分配到 Old 区域。

⻓时间存活的对象进⼊到 Old 区域。

改进⾃复制算法

现在的商业虚拟机都采⽤这种收集算法来回收新⽣代，IBM 公司的专⻔研究表明，新⽣代中的 对象 98% 是“朝⽣夕死”的，所以并不需要按照 1:1 的⽐例来划分内存空间，⽽是将内存分为⼀ 块较⼤的 Eden 空间和两块较⼩的 Survivor 空间，每次使⽤ Eden 和其中⼀块 Survivor 。当回 收时，将 Eden 和 Survivor 中还存活着的对象⼀次性地复制到另外⼀块 Survivor 空间上，最后 清理掉 Eden 和刚才⽤过的 Survivor 空间。

HotSpot 虚拟机默认 Eden 和 2 块 Survivor 的⼤⼩⽐例是 8:1:1，也就是每次新⽣代中可⽤内 存空间为整个新⽣代容量的 90%（80%+10%），只有 10% 的内存会被“浪费”。当然，98% 的 对象可回收只是⼀般场景下的数据，我们没有办法保证每次回收都只有不多于 10% 的对象存 活，当 Survivor 空间不够⽤时，需要依赖其他内存（这⾥指⽼年代）进⾏分配担保（Handle Promotion）。

## 什么是安全点？

### 《深⼊拆解 Java 虚拟机》

的 「3.4.2 安全点」 。

SafePoint 安全点，顾名思义是指⼀些特定的位置，当线程运⾏到这些位置时，线程的⼀些状态可以被 确定(the thread’s representation of it’s Java machine state is well described)，⽐如记录OopMap 的状 态，从⽽确定 GC Root 的信息，使 JVM 可以安全的进⾏⼀些操作，⽐如开始 GC 。

SafePoint 指的特定位置主要有：

- 1. 循环的末尾 (防⽌⼤循环的时候⼀直不进⼊ Safepoint ，⽽其他线程在等待它进⼊ Safepoint )。


2. 3. 4.

⽅法返回前。 调⽤⽅法的 Call 之后。 抛出异常的位置。

《深⼊学习 JVM-JVM 安全点和安全区域》

详细的内容，可以看看 。

如何使线程中断

主动式

主动式 JVM 设置⼀个全局变量，线程去按照某种策略检查这个变量⼀旦发 现是 SafePoint 就主动挂起。 HostSpot 虚拟机采⽤的是主动式使线程中断。

被动式

被动式就是发个信号，例如关机、Control+C ，带来的问题就是不可控，发 信号的时候不知道线程处于什么状态。

安全区域

如果程序⻓时间不执⾏，⽐如线程调⽤的 sleep ⽅法，这时候程序⽆法响应 JVM 中断请求这时候线程⽆法到达安全点，显然 JVM 也不可能等待程序 唤醒，这时候就需要安全区域了。 安全区域是指⼀段代码⽚中，引⽤关系不会发⽣变化，在这个区域任何地 ⽅ GC 都是安全的，安全区域可以看做是安全点的⼀个扩展。

线程执⾏到安全区域的代码时，⾸先标识⾃⼰进⼊了安全区域，这样 GC 时就不⽤管进⼊安全区域的线程了.

线程要离开安全区域时就检查 JVM 是否完成了 GC Roots 枚举（或者 整个 GC 过程），如果完成就继续执⾏，如果没有完成就等待直到收 到可以安全离开的信号。

## JVM 垃圾收集器有哪些？

《深⼊拆解 Java 虚拟机》

的 「3.5 垃圾收集器」 。

如果说收集算法是内存回收的⽅法论，那么垃圾收集器就是内存回收的具体实现。

新⽣代收集器

Serial 收集器

ParNew 收集器

ParNew 收集器，是 Serial 收集器的多线程版。

Parallel Scavenge 收集器

⽼年代收集器

Serial Old 收集器

Serial Old 收集器，是 Serial 收集器的⽼年代版本。

Parallel Old 收集器

Parallel Old 收集器，是 Parallel Scavenge 收集器的⽼年代版本。

CMS 收集器

新⽣代 + ⽼年代收集器

G1 收集器

ZGC 收集器

⼩结表格如下：

<table>
  <tr>
    <th>收集器</th>
    <th>串⾏、并⾏or 并发</th>
    <th>新⽣代/⽼年代</th>
    <th>算法</th>
    <th>⽬标</th>
    <th>适⽤场景</th>
  </tr>
  <tr>
    <td>Serial</td>
    <td>串⾏</td>
    <td>新⽣代</td>
    <td>复制算法</td>
    <td>响应速度优先</td>
    <td>单CPU环境下 模式</td>
  </tr>
  <tr>
    <td>Serial Old</td>
    <td>串⾏</td>
    <td>⽼年代</td>
    <td>标记-整理</td>
    <td>响应速度优先</td>
    <td>的Client 单CPU环境下 的Client模式、 CMS的后备预 案</td>
  </tr>
  <tr>
    <td>ParNew</td>
    <td>并⾏</td>
    <td>新⽣代</td>
    <td>复制算法</td>
    <td>响应速度优先</td>
    <td>多CPU环境时 在Server模式<br><br>配合</td>
  </tr>
  <tr>
    <td>Paralel Scavenge</td>
    <td>并⾏</td>
    <td>新⽣代</td>
    <td>复制算法</td>
    <td>吞吐量优先</td>
    <td>下与CMS<br><br>在后台运算⽽ 不需要太多交 互的任务</td>
  </tr>
  <tr>
    <td>Paralel Old</td>
    <td>并⾏</td>
    <td>⽼年代</td>
    <td>标记-整理</td>
    <td>吞吐量优先</td>
    <td>在后台运算⽽ 不需要太多交 互的任务</td>
  </tr>
  <tr>
    <td>CMS</td>
    <td>并发</td>
    <td>⽼年代</td>
    <td>标记-清除</td>
    <td>响应速度优先</td>
    <td>集中在互联⽹ 站或B/S系统服 务端上的Java 应⽤</td>
  </tr>
  <tr>
    <td>G1</td>
    <td>并发</td>
    <td>both</td>
    <td>标记-整理+复 制算法</td>
    <td>响应速度优先</td>
    <td>⾯向服务端应 ⽤，将来替换</td>
  </tr>
</table>


CMS

关于每种垃圾收集器的说明，请看 如下⽂章：

《深⼊理解 JVM(3) —— 7 种垃圾收集器》

《⼀⽂读懂 Java 11 的 ZGC 为何如此⾼效》

🦅 G1 和 CMS 的区别？

CMS ：并发标记清除。他的主要步骤有：初始收集，并发标记，重新标记，并发清除（删除）、重 置。

G1：主要步骤：初始标记，并发标记，重新标记，复制清除（整理）

CMS 的缺点是对 CPU 的要求⽐较⾼。G1是将内存化成了多块，所有对内段的⼤⼩有很⼤的要求。

CMS是清除，所以会存在很多的内存碎⽚。G1是整理，所以碎⽚空间较⼩。

G1 和 CMS 都是响应优先把，他们的⽬的都是尽量控制 STW 时间。

G1 和 CMS 的 Full GC 都是单线程 mark sweep compact 算法，直到 JDK10 才优化为并⾏的。

《GC 优化的⼀些总结》

感兴趣的胖友，可以看看 的分析。 🦅 CMS 算法的过程，CMS 回收过程中 JVM 是否需要暂停？ 会有短暂的停顿。详细的，可以看看 。 🦅 如何使⽤指定的垃圾收集器

《[jvm][⾯试] 并发收集器 CMS(Concurrent Mark-Sweep)》

<table>
  <tr>
    <th>配置</th>
    <th>描述</th>
  </tr>
  <tr>
    <td>-X:+UserSerialGC</td>
    <td>串⾏垃圾收集器</td>
  </tr>
  <tr>
    <td>-X:+UserParalelGC</td>
    <td>并⾏垃圾收集器</td>
  </tr>
  <tr>
    <td>-X:+UseConcMarkSwepGC</td>
    <td>并发标记扫描垃圾回收器</td>
  </tr>
  <tr>
    <td>-X ParalelCMSThreads</td>
    <td>并发标记扫描垃圾回收器 =为使⽤的线程数量</td>
  </tr>
  <tr>
    <td> </td>
    <td>垃圾回收器</td>
  </tr>
</table>


-X:+UseG1GC G1

## 对象分配规则是什么？

《深⼊拆解 Java 虚拟机》

的 「3.6 对象分配与回收策略」 。

对象优先分配在 Eden 区。

如果 Eden 区⽆法分配，那么尝试把活着的对象放到 Survivor0 中去（Minor GC）

### 如果 Survivor0 可以放⼊，那么放⼊之后清除 Eden 区。 如果 Survivor0 不可以放⼊，那么尝试把 Eden 和 Survivor0 的存活对 象放到 Survivor1 中。

### 如果 Survivor1 可以放⼊，那么放⼊ Survivor1 之后清除 Eden 和 Survivor0 ，之后再把 Survivor1 中的对象复制到 Survivor0 中，保 持 Survivor1 ⼀直为空。 如果 Survivor1 不可以放⼊，那么直接把它们放⼊到⽼年代中，并 清除 Eden 和 Survivor0 ，这个过程也称为分配担保。

ps：清除 Eden、Survivor 区，就是 Minor GC 。 总结来说，分配的顺序是：新⽣代（Eden => Survivor0 => Survivor1）=> ⽼年代

⼤对象直接进⼊⽼年代（⼤对象是指需要⼤量连续内存空间的对象）。

这样做的⽬的是，避免在 Eden 区和两个 Survivor 区之间发⽣⼤量的内存拷 ⻉（新⽣代采⽤复制算法收集内存）。

⻓期存活的对象进⼊⽼年代。

虚拟机为每个对象定义了⼀个年龄计数器，如果对象经过了 1 次 Minor GC 那么对象会进⼊ Survivor 区，之后每经过⼀次 Minor GC 那么对象的年龄 加 1 ，知道达到阀值对象进⼊⽼年区。

动态判断对象的年龄。

为了更好的适⽤不同程序的内存情况，虚拟机并不是永远要求对象的年龄 必须达到 MaxTenuringThreshold 才能晋升⽼年代。 如果 Survivor 区中相同年龄的所有对象⼤⼩的总和⼤于 Survivor 空间的⼀ 半，年龄⼤于或等于该年龄的对象可以直接进⼊⽼年代。

空间分配担保。

每次进⾏ Minor GC 时，JVM 会计算 Survivor 区移⾄⽼年区的对象的平均 ⼤⼩，如果这个值⼤于⽼年区的剩余值⼤⼩则进⾏⼀次 Full GC ，如果⼩ 于检查 HandlePromotionFailure 设置，如果 true则只进⾏ Monitor GC ，如 果 false 则进⾏ Full GC 。

如下是⼀张对象创建时，分配内存的图：

![image 13](assets/imageFile13.png)

内存分配 🦅 为什么新⽣代内存需要有两个 Survivor 区？ 详细的原因，可以看 ⽂章。

《为什么新⽣代内存需要有两个 Survivor 区》

## 什么是新⽣代 GC 和⽼年代 GC？

GC 经常发⽣的区域是堆区，堆区还可以细分为

![image 14](assets/imageFile14.png)

堆

新⽣代

⼀个 Eden 区

两个 Survivor 区

⽼年代

默认新⽣代(Young)与⽼年代(Old)的⽐例的值为 1:2 (该值可以通过参数 – XX:NewRatio 来指定)。 默认的 Eden:from:to=8:1:1 (可以通过参数 –XX:SurvivorRatio 来 设定)。

新⽣代GC（MinorGC/YoungGC）：指发⽣在新⽣代的垃圾收集动作，因为 Java 对象⼤多都具备朝 ⽣夕灭的特性，所以 MinorGC ⾮常频繁，⼀般回收速度也⽐较快。 ⽼年代GC（MajorGC/FullGC）：指发⽣在⽼年代的 GC，出现了 MajorGC，经常会伴随⾄少⼀次的 MinorGC（但⾮绝对的，在 Parallel Scavenge 收集器的收集策略⾥就有直接进⾏ MajorGC 的策略选 择过程）。MajorGC 的速度⼀般会⽐ MinorGC 慢 10 倍以上。 🦅 什么情况下会出现 Young GC？ 对象优先在新⽣代 Eden 区中分配，如果 Eden 区没有⾜够的空间时，就会触发⼀次 Young GC 。 🦅 什么情况下回出现 Full GC？ Full GC 的触发条件有多个，FULL GC 的时候会 STOP THE WORD 。

- 1、在执⾏ Young GC 之前，JVM 会进⾏空间分配担保——如果⽼年代的连续空间⼩于新⽣代对象 的总⼤⼩（或历次晋升的平均⼤⼩），则触发⼀次 Full GC 。

- 2、⼤对象直接进⼊⽼年代，从年轻代晋升上来的⽼对象，尝试在⽼年代分配内存时，但是⽼年代 内存空间不够。

- 3、显式调⽤ System#gc() ⽅法时。


# 虚拟机性能监控与故障处理⼯具

## JDK 的命令⾏⼯具有哪些可以监控虚拟机？

《深⼊拆解 Java 虚拟机》

的 「4.2 JDK 的命令⾏⼯具」 。

jps ：虚拟机进程状况⼯具

JVM Process Status Tool ，显示指定系统内所有的HotSpot虚拟机进程。

jstat ：虚拟机统计信息监控⼯具

JVM statistics Monitoring ，是⽤于监视虚拟机运⾏时状态信息的命令，它 可以显示出虚拟机进程中的类装载、内存、垃圾收集、JIT编译等运⾏数 据。

jinfo ：Java 配置信息⼯具

JVM Configuration info ，这个命令作⽤是实时查看和调整虚拟机运⾏参 数。

jmap ：Java 内存映射⼯具

JVM Memory Map ，命令⽤于⽣成 heap dump ⽂件。

jhat ：虚拟机堆转储快照分析⼯具

JVM Heap Analysis Tool ，命令是与 jmap 搭配使⽤，⽤来分析 jmap ⽣成的 dump ⽂件。jhat 内置了⼀个微型 的HTTP/HTML 服务器，⽣成 dump 的分 析结果后，可以在浏览器中查看。

jstack ：Java 堆栈跟踪⼯具

Java Stack Trace ，⽤于⽣成 Java 虚拟机当前时刻的线程快照。

HSDIS ：JIT ⽣成代码反编译

## JDK 的可视化⼯具有哪些可以监控虚拟机？

《深⼊拆解 Java 虚拟机》

的 「4.3 JDK 的可视化⼯具」 。

Java ⾃带

JConsole ：Java 监视与管理控制台

Java Monitoring and Management Console 是从 Java5 开始，在 JDK 中⾃带的 Java 监控和管理控制台，⽤于对 JVM 中内存，线程和类等的监控。

VisualVM ：多合⼀故障处理⼯具

JDK ⾃带全能⼯具，可以分析内存快照、线程快照、监控内存变化、GC变 化等。 特别是 BTrace 插件，动态跟踪分析⼯具。

第三⽅

MAT ：内存分析⼯具

Memory Analyzer Tool ，⼀个基于 Eclipse 的内存分析⼯具，是⼀个快速、 功能丰富的 Java heap 分析⼯具，它可以帮助我们查找内存泄漏和减少内存 消耗。

GChisto ：⼀款专业分析 GC ⽇志的⼯具。 SkyWalking Cat

另外，⼀些开源项⽬，例如 、 ，也提供了 JVM 监控的功能，更加适合⽣产环境，对 JVM 的监控。

## 怎么获取 Java 程序使⽤的内存？

可以通过 java.lang.Runtime 类中与内存相关⽅法来获取剩余的内存，总内存及最⼤堆内存。通过 这些⽅法你也可以获取到堆使⽤的百分⽐及堆内存的剩余空间。

Runtime#freeMemory() ⽅法，返回剩余空间的字节数。

Runtime#totalMemory() ⽅法，总内存的字节数。 Runtime#maxMemory() ⽅法，返回最⼤内存的字节数。

# 调优案例分析与实战

在 的 「第5章 调优案例分析与实战」 中，已经提供了⼀些案例，建议胖友 可以看看。

《深⼊拆解 Java 虚拟机》

常⻅ GC 的优化配置？

<table>
  <tr>
    <th>配置</th>
    <th>描述</th>
  </tr>
  <tr>
    <td>-Xms</td>
    <td>初始化堆内存⼤⼩</td>
  </tr>
  <tr>
    <td>-Xmx</td>
    <td>堆内存最⼤值</td>
  </tr>
  <tr>
    <td>-Xmn</td>
    <td>新⽣代⼤⼩</td>
  </tr>
  <tr>
    <td>-X PermSize</td>
    <td>初始化永久代⼤⼩</td>
  </tr>
  <tr>
    <td>-X MaxPermSize</td>
    <td>永久代最⼤容量</td>
  </tr>
  <tr>
    <td>-X SurvivorRatio</td>
    <td>设置年轻代中 Eden 区与 Survivor 区的⽐值</td>
  </tr>
  <tr>
    <td> </td>
    <td>设置年轻代⼤⼩</td>
  </tr>
</table>


-X Xmn

《JVM 调优》

另外，也可以看看 ⽂章。

如何排查线程 Full GC 频繁的问题？

《线上 Full GC 频繁的排查》

《触发 JVM 进⾏ Full GC 的情况及应对策略》

🦅 JVM 的永久代中会发⽣垃圾回收么？

Young GC 不会发⽣在永久代。

如果永久代满了或者是超过了临界值，会触发完全垃圾回收(Full GC)。如果我们仔细查看垃圾收集 器的输出信息，就会发现永久代也是被回收的。这就是为什么正确的永久代⼤⼩对避免 Full GC 是 ⾮常重要的原因。

Java8 ：从永久代到元数据区 (注：Java8 中已经移除了永久代，新加了⼀个 叫做元数据区的 native 内存区)。

有看过 GC ⽇志么？

艿艿：这个问题，⼀般⾯试不会问，加进来，主要让胖友知道，有这么个 知识点。

参⻅⽂章如下：

《[JVM]理解GC⽇志》

《GC ⽇志查看分析》

TODO JVM 线程案例

TODO 问晓峰 TODO 问阿⽜ TODO 问狼哥 TODO 问闪电侠 TODO 问超哥

# TODO 类⽂件结构 TODO 虚拟机类加载机制

类加载器，是⾯试的重点，所以要掌握好。当然，相对来说难度也不算上~

## 类加载器是有了解吗？

### 《深⼊拆解 Java 虚拟机》

的 「7.4 类加载器」 。

类加载器(ClassLoader)，⽤来加载 Java 类到 Java 虚拟机中。⼀般来说，Java 虚拟机使⽤ Java 类的 ⽅式如下：Java 源程序(.java ⽂件)在经过 Java 编译器编译之后就被转换成 Java 字节代码 (.class ⽂件)。 类加载器，负责读取 Java 字节代码，并转换成 java.lang.Class 类的⼀个实例。

每个这样的实例⽤来表示⼀个 Java 类。通过此实例的 Class#newInstance(...) ⽅法，就可以 创建出该类的⼀个对象。

实际的情况可能更加复杂，⽐如 Java 字节代码可能是通过⼯具动态⽣成的，也可能是通过⽹络下 载的。

## 类加载发⽣的时机是什么时候？

### 《深⼊拆解 Java 虚拟机》

的 「7.2 类加载的时机」 。

虚拟机严格规定，有且仅有 5 种情况必须对类进⾏加载：

注意，有些⽂章会称为对类进⾏“初始化”。

- 1、遇到 new、getstatic、putstatic、invokestatic 这四条字节码指令时，如果类还没进 ⾏初始化，则需要先触发其初始化。

- 2、使⽤ java.lang.reflect 包的⽅法对类进⾏反射调⽤的时候，如果类还没进⾏初始化，则需 要先触发其初始化。

- 3、当初始化了⼀个类的时候，如果发现其⽗类还没进⾏初始化，则需要先触发其⽗类的初始化。


- 4、当虚拟机启动时，⽤户需要指定⼀个执⾏的主类，即调⽤其 #main(String[] args) ⽅法， 虚拟机则会先初始化该主类。

- 5、当使⽤ JDK7 的动态语⾔⽀持时，如果⼀个 java.lang.invoke.MethodHandle 实例最后的 解析结果为 REF_getStatic、REF_putStatic、REF_invokeStatic 的⽅法句柄，并且这个⽅法句柄所 对应的类没有进⾏过初始化，则需要先触发其初始化。


## 类加载器是如何加载 Class ⽂件的？

### 《深⼊拆解 Java 虚拟机》

的 「7.2 类加载的时机」 。

下图所示是 ClassLoader 加载⼀个 .class ⽂件到 JVM 时需要经过的步骤：

![image 15](assets/imageFile15.png)

类加载器是如何加载 class ⽂件

第⼀个阶段，加载(Loading)，是找到 .class ⽂件并把这个⽂件包含的字节码加载到内存中。

第⼆阶段，连接(Linking)，⼜可以分为三个步骤，分别是字节码验证、Class 类数据结构分析及相 应的内存分配、最后的符号表的解析。

第三阶段，Initialization(类中静态属性和初始化赋值)，以及Using(静态块的执⾏)等。

注意，不包括卸载(Unloading)部分。

- 🦅 1）加载


加载是“类加载”过程的第⼀阶段，胖友不要混淆这两个名字。

在加载阶段，虚拟机需要完成以下三件事情：

通过⼀个类的全限定名来获取其定义的⼆进制字节流。

将这个字节流所代表的静态存储结构转化为⽅法区的运⾏时数据结构。

在Java堆中⽣成⼀个代表这个类的 java.lang.Class 对象，作为对⽅法区中这些数据的访问⼊ ⼝。

相对于类加载的其他阶段⽽⾔，加载阶段（准确地说，是加载阶段获取类的⼆进制字节流的动作）是 可控性最强的阶段，因为开发⼈员既可以使⽤系统提供的类加载器来完成加载，也可以⾃定义⾃⼰的 类加载器来完成加载。 加载阶段完成后，虚拟机外部的⼆进制字节流就按照虚拟机所需的格式存储在⽅法区之中，⽽且在 Java堆中也创建⼀个 java.lang.Class 类的对象，这样便可以通过该对象访问⽅法区中的这些数 据。

- 🦅 2）加载


- 2.1 验证：确保被加载的类的正确性 验证是连接阶段的第⼀步，这⼀阶段的⽬的是为了确保 Class ⽂件的字节流中包含的信息符合当前虚 拟机的要求，并且不会危害虚拟机⾃身的安全。 验证阶段⼤致会完成4个阶段的检验动作： 验证阶段是⾮常重要的，但不是必须的，它对程序运⾏期没有影响，如果所引⽤的类经过反复验证， 那么可以考虑采⽤ -Xverifynone 参数来关闭⼤部分的类验证措施，以缩短虚拟机类加载的时间。
- 2.2 准备：为类的静态变量分配内存，并将其初始化为默认值 准备阶段，是正式为类变量分配内存并设置类变量初始值的阶段，这些内存都将在⽅法区中分配。对 于该阶段有以下⼏点需要注意：


⽂件格式验证：验证字节流是否符合 Class ⽂件格式的规范。例如：是否以 0xCAFEBABE 开头、 主次版本号是否在当前虚拟机的处理范围之内、常量池中的常量是否有不被⽀持的类型。

元数据验证：对字节码描述的信息进⾏语义分析（注意：对⽐ javac 编译阶段的语义分析），以保 证其描述的信息符合 Java 语⾔规范的要求。例如：这个类是否有⽗类，除 了 java.lang.Object 之外。

字节码验证：通过数据流和控制流分析，确定程序语义是合法的、符合逻辑的。

符号引⽤验证：确保解析动作能正确执⾏。

- 1、这时候进⾏内存分配的仅包括类变量(static)，⽽不包括实例变量，实例变量会在对象实例化 时随着对象⼀块分配在 Java 堆中。

- 2、这⾥所设置的初始值通常情况下是数据类型默认的零值(如 0、0L、null、false 等），⽽不 是被在 Java 代码中被显式地赋予的值。


### 思考下，对于类本身，静态变量就是其属性。

假设⼀个类变量的定义为： public static int value = 3。那么静态变量 value 在准备阶段 过后的初始值为 0，⽽不是 3。因为这时候尚未开始执⾏任何 Java ⽅法，⽽把 value 赋值为 3 的 public static 指令是在程序编译后，存放于类构造器 <clinit>() ⽅法之中的，所以 把 value 赋值为 3 的动作将在初始化阶段才会执⾏。

这⾥还需要注意如下⼏点：

### 对基本数据类型来说，对于类变量(static)和全局变量，如果不显 式地对其赋值⽽直接使⽤，则系统会为其赋予默认的零值，⽽对于局 部变量来说，在使⽤前必须显式地为其赋值，否则编译时不通过。

对于同时被 static 和 final 修饰的常量，必须在声明的时候就为 其显式地赋值，否则编译时不通过；⽽只被 final修饰的常量则既 可以在声明时显式地为其赋值，也可以在类初始化时显式地为其赋 值，总之，在使⽤前必须为其显式地赋值，系统不会为其赋予默认零 值。 对于引⽤数据类型 reference 来说，如数组引⽤、对象引⽤等，如果 没有对其进⾏显式地赋值⽽直接使⽤，系统都会为其赋予默认的空 值，即 null 。 如果在数组初始化时没有对数组中的各元素赋值，那么其中的元素将 根据对应的数据类型⽽被赋予默认的“空”值。

3、如果类字段的字段属性表中存在 ConstantValue 属性，即同时被 final 和 static 修饰，那么 在准备阶段变量 value 就会被初始化为 ConstValue 属性所指定的值。

假设上⾯的类变量 value 被定义为： public static final int value = 3 。编译 时， javac 将会为 value ⽣成 ConstantValue 属性。在准备阶段虚拟机就会根据 ConstantValue 的 设置将 value 赋值为 3。我们可以理解为 static final 常量在编译期就将其结果放⼊了调⽤它的 类的常量池中。

- 2.3 解析：把类中的符号引⽤转换为直接引⽤


### 这个步骤，艿艿看的也有点懵逼。 R ⼤在 和 做过解答，看的还是懵逼。

### 《JVM 符号引⽤转换直接引⽤的过程?》 《JVM ⾥的符号引⽤如 何存储？》

解析阶段，是虚拟机将常量池内的符号引⽤替换为直接引⽤的过程。解析动作，主要针对类或接⼝、 字段、类⽅法、接⼝⽅法、⽅法类型、⽅法句柄和调⽤点限定符 7 类符号引⽤进⾏。

符号引⽤，就是⼀组符号来描述⽬标，可以是任何字⾯量。

直接引⽤，就是直接指向⽬标的指针、相对偏移量或⼀个间接定位到⽬标的句柄。

- 🦅 3）初始化 初始化，为类的静态变量赋予正确的初始值，JVM 负责对类进⾏初始化，主要对类变量进⾏初始化。 在 Java 中对类变量进⾏初始值设定有两种⽅式：


- 1、声明类变量是指定初始值。

- 2、使⽤静态代码块为类变量指定初始值。


JVM 初始化步骤：

1、假如这个类还没有被加载和连接，则程序先加载并连接该类。

- 2、假如该类的直接⽗类还没有被初始化，则先初始化其直接⽗类。

- 3、假如类中有初始化语句，则系统依次执⾏这些初始化语句。


## 什么是双亲委派模型（Parent Delegation Model）？

### 《深⼊拆解 Java 虚拟机》

的 「7.4.2 双亲委派模型」 。

类加载器 ClassLoader 是具有层次结构的，也就是⽗⼦关系，如下图所示：

![image 16](assets/imageFile16.png)

双亲委派模型

Bootstrap ClassLoader ：根类加载器，负责加载 Java 的核⼼类，它不 是 java.lang.ClassLoader的⼦类，⽽是由 JVM ⾃身实现。

此处，说的是 Hotspot 的情况下。

Extension ClassLoader ：扩展类加载器，扩展类加载器的加载路径是 JDK ⽬录 下 jre/lib/ext 。扩展加载器的 #getParent() ⽅法返回 null ，实际上扩展类加载器的⽗类 加载器是根加载器，只是根加载器并不是 Java 实现的。

System ClassLoader ：系统(应⽤)类加载器，它负责在 JVM 启动时加载来⾃ Java 命令的 classpath选项、java.class.path 系统属性或 CLASSPATH 环境变量所指定的 jar 包和类路 径。程序可以通过 #getSystemClassLoader() 来获取系统类加载器。系统加载器的加载路径是 程序运⾏的当前路径。

该模型要求除了顶层的 Bootstrap 启动类加载器外，其余的类加载器都应当有⾃⼰的⽗类加载器。 ⼦类加载器和⽗类加载器不是以继承（Inheritance）的关系来实现，⽽是通过组合（Composition） 关系来复⽤⽗加载器的代码。简略代码如下：

<table>
  <tr>
    <th>/ java.lang.ClasLoader<br><br>public abstract clas ClasLoader { / . 省略其它代码 / The parent clas loader for delegation / Note: VM hardcodedthe ofset of this field, thus alnew fields / must be aded *after* it.<br><br>private final ClasLoader parent;<br><br></th>
  </tr>
</table>


}

每个类加载器都有⾃⼰的命名空间（由该加载器及所有⽗类加载器所加载的类组成。

在同⼀个命名空间中，不会出现类的完整名字（包括类的包名）相同的两个类。

在不同的命名空间中，有可能会出现类的完整名字（包括类的包名）相同的两个类）。

类加载器负责加载所有的类，同⼀个类(⼀个类⽤其全限定类名(包名加类名)标志)只会被加载⼀ 次。

🦅 Java 虚拟机是如何判定两个 Java 类是相同的？ Java 虚拟机不仅要看类的全名是否相同，还要看加载此类的类加载器是否⼀样。只有两者都相同的情 况，才认为两个类是相同的。即便是同样的字节代码，被不同的类加载器加载之后所得到的类，也是 不同的。 ⽐如⼀个 Java 类 com.example.Sample ，编译之后⽣成了字节代码⽂件 Sample.class 。两个不 同的类加载器 ClassLoaderA 和 ClassLoaderB 分别读取了这个 Sample.class ⽂件，并定义出两 个 java.lang.Class 类的实例来表示这个类。这两个实例是不相同的。对于 Java 虚拟机来说，它 们是不同的类。试图对这两个类的对象进⾏相互赋值，会抛出运⾏时异常 ClassCastException 。 🦅 双亲委派模型的⼯作过程？

- 1、当前 ClassLoader ⾸先从⾃⼰已经加载的类中，查询是否此类已经加载，如果已经加载则直接 返回原来已经加载的类。

- 2、当前 ClassLoader 的缓存中没有找到被加载的类的时候


### 每个类加载器都有⾃⼰的加载缓存，当⼀个类被加载了以后就会放⼊缓 存，等下次加载的时候就可以直接返回了。

委托⽗类加载器去加载，⽗类加载器采⽤同样的策略，⾸先查看⾃⼰的缓存，然后委托⽗类的 ⽗类去加载，⼀直到 bootstrap ClassLoader。

当所有的⽗类加载器都没有加载的时候，再由当前的类加载器加载，并将其放⼊它⾃⼰的缓存 中，以便下次有加载请求的时候直接返回。

让我们来简单撸下源码。代码如下：

### 艿艿：要不要跟⾯试官吹下，⾃⼰看过源码得知~

<table>
  <tr>
    <th>/ java.lang.ClasLoader / 删除部分⽆关代码<br><br>protected Clas<?> loadClas(String name, bolean resolve) throws ClasNotFoundException { synchronized (getClasLoadingLock(name) {<br><br>/ ⾸先，从缓存中获得 name 对应的类 Clas<?> c = findLoadedClas(name); if (c = nul) {/ 获得不到<br><br>try {<br><br>/ 其次，如果⽗类⾮空，使⽤它去加载类 if (parent != nul) {<br><br>c = parent.loadClas(name,false);<br><br>/ 其次，如果⽗类为空，使⽤ Botstrap 去加载类 } else {<br><br>c = findBotstrapClasOrNul(name); }<br><br>catch (ClasNotFoundException e){<br><br>} if (c = nul) {/ 还是加载不到<br><br>/ 最差，使⽤⾃⼰去加载类 c = findClas(name);<br><br>} }<br><br>/ 如果要解析类，则进⾏解析 if (resolve) {<br><br>resolveClas(c);<br><br>} return c;<br><br>}<br><br></th>
  </tr>
</table>


}

🦅 为什么优先使⽤⽗ ClassLoader 加载类？

- 1、共享功能：可以避免重复加载，当⽗亲已经加载了该类的时候，⼦类不需要再次加载，⼀些 Framework 层级的类⼀旦被顶层的 ClassLoader 加载过就缓存在内存⾥⾯，以后任何地⽅⽤到都不 需要重新加载。

- 2、隔离功能：主要是为了安全性，避免⽤户⾃⼰编写的类动态替换 Java 的⼀些核⼼类，⽐如 String ，同时也避免了重复加载，因为 JVM 中区分不同类，不仅仅是根据类名，相同的 class ⽂件 被不同的 ClassLoader 加载就是不同的两个类，如果相互转型的话会 抛 java.lang.ClassCaseException 。


这也就是说，即使我们⾃⼰定义了⼀个 java.util.String类，也不会 被重复加载。

## 什么是破坏双亲委托模型？

### 《深⼊拆解 Java 虚拟机》

的 「7.4.3 破坏双亲委派模型」 。

正如我们上⾯看到的源码，破坏双亲委托模型，需要做的是，#loadClass(String name, boolean resolve) ⽅法中，不调⽤⽗ parent ClassLoader ⽅法去加载类，那么就成功了。那么 我们要做的仅仅是，错误的覆盖 ##loadClass(String name, boolean resolve) ⽅法，不去 使⽤⽗ parentClassLoader ⽅法去加载类即可。 想要深⼊的胖友，可以深⼊看看如下⽂章：

《Tomcat 类加载器之为何违背双亲委派模型》

《真正理解线程上下⽂类加载器（多案例分析）》 提供了多种打破双亲委托模型的案例。

《深⼊拆解 Java 虚拟机》 的 「第 9 章 类加载及执⾏⼦系统的案例与实战」

🦅 如何⾃定义 ClassLoader 类？ 直接参考 ⽂章即可。 🦅 OSGI 如何实现模块化热部署？

《Java ⾃定义 ClassLoader 实现 JVM 类加载》

艿艿：了解即可。

OSGI 实现模块化热部署的关键，是它⾃定义的类加载器机制的实现。每⼀个程序模块都有⼀个⾃⼰的 类加载器，当需要等换⼀个模块时，就把模块连同类加载器⼀起换掉以实现代码的热替换。

TODO 虚拟机字节码执⾏引擎 TODO 早期（编译期）优化

TODO JIT

有时我们会听到 JIT 这个概念，并说它是 JVM 的⼀部分，这让我们很困惑。JIT 是 JVM 的⼀部分，它 可以在同⼀时间编译类似的字节码来优化将字节码转换为机器特定语⾔的过程相似的字节码，从⽽将 优化字节码转换为机器特定语⾔的过程，这样减少转换过程所需要花费的时间。

TODO 晚期（运⾏期）优化

666. 彩蛋

总的来说，JVM 主要提问的点，如下脑图：

![image 17](assets/imageFile17.png)

#### 脑图
