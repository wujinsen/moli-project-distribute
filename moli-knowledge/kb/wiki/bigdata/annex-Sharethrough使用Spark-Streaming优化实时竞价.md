---
title: Sharethrough使用Spark Streaming优化实时竞价.note（原文插图 annex）
slug: annex-Sharethrough使用Spark-Streaming优化实时竞价
type: article
status: active
tags: [wujinsen, annex, 插图]
sources:
  - raw/wujinsen_markdown/BigData/spark(1)/Sharethrough使用Spark Streaming优化实时竞价.note.md
related: [spark-核心概念与实践]
created: 2026-07-05
updated: 2026-07-05
---

来⾃于 的数据基础设施⼯程师Rusel Cardulo和Michael Rugiero最近在Cloudera博客 上投递了⼀篇 ，分享了他们是如何使⽤ 解决复杂的实时问题的。下⾯是博⽂的具 体内容，如果您想查看英⽂原⽂，可以点击 。 Sharethrough是⼀家从事视频⼴告业务的初创公司，在过去的三年中（从Amazon EMR上迁移出来之 后）我们⼀直运⾏在CDH上⾯，主要是为了ETL。随着2013年早期交易平台的启动以及对优化实时内 容分发的需要，我们需要做出⼀些改变，但是CDH依然是我们基础设施中的⼀个重要部分。 在2013年年中，我们开始尝试使⽤基于流的⽅式访问管道中的点击流数据。我们问⾃⼰：我们是否能 够给⾃⼰的开发⼈员⼀个针对增量、⼩批处理任务做过优化的编程模型和框架并继续依赖于Cloudera 平台，⽽不是通过更频繁地运⾏那些更⼤的批处理任务“加热我们的冷路径”？在理想的情况下，我们 的⼯程师团队关注于数据本身，⽽不是担⼼具体细节，例如管道间的状态⼀致性或者故障恢复。

Sharethrough 博⽂ Spark Streaming 这⾥

# Spark（和Spark Streaming）

Apache Spark

是⼀个快速通⽤的⼤数据处理框架，它的编程模型⽐较适合于构建那些对常规的 MapReduce⽽⾔太过于复杂或者不太可⾏的应⽤程序。Cloudera Enterprise 5已经包含了Spark，并 且已经⽀持CDH 4.4及更新版本。借助于对内存持久化存储的抽象，Spark⽀持所有的MapReduce功 能，但是它执⾏地更快，因为它不需要进⾏数据复制、磁盘I/O和序列化。 因为 与Spark批处理和交互模式共享同样的API，所以我们现在可以使⽤Spark Streaming实时聚合关键的业务数据。⼀致的API意味着我们能够使⽤较为简单的批处理模式进⾏本地 的开发和测试，同时⼜能让这些⼯作⽆缝地在产品环境中运⾏。例如，我们现在可以使⽤与某个活动 相关的所有数据集优化实时竞价，不需要等运⾏频率较低的ETL流完成。此外，我们还能执⾏实时实验 并衡量结果。 使⽤Spark之前和之后 之前，我们的批处理系统是这样的：

Spark Streaming

- 1.
- 2.
- 3.
- 4.


Apache Flume基于最优的HDFS块⼤⼩（64MB）将⽂件写⼊到每⼩时的桶中 每天定时执⾏MapReduce (Scalding) 任务N次 Apache Sqop将结果移⼊数据仓库 延迟是~1⼩时之后，加上Hadop的处理时间

![image 1](assets/imageFile1.png)

# Sharethrough之前的批处理数据流

对我们这个特定⽤例，尽管那些运算结果依然是有价值的，但是这个批处理流程却⽆法让我们实时访 问绩效数据。例如，⼀个⼩时之后我们才能知道⽤户每天的预算是否花完，这意味着我们的⼴告商要 花⼀些冤枉钱，我们的内容商⽆法填满⾃⼰的需求。即使是在批处理任务只会花费⼏分钟的时候，流 量峰值也可能会减慢给定的批处理任务，导致它“撞”到新启动的任务。 对于这些⽤例，流式数据处理是可⾏的解决⽅案。

- 1.
- 2.
- 3.


Flume将点击流数据写⼊HDFS Spark每5秒钟从HDFS上读取数据 输出到⼀个键—值存储并更新预测模型

![image 2](assets/imageFile2.png)

Sharethrough新的基于Spark Streaming的数据流 在新模型中，我们的延迟时间仅仅是Spark的处理时间和Flume将⽂件传输到HDFS上的时间；在实际 情况下，这个⼯作⼤约会花费5秒钟。

# 在路上

在开始使⽤Spark Streaming的时候，我们开始的⾮常快，遇到的麻烦很少。为了更好地利⽤新的 Streaming任务，我们很快就调整成了Spark编程模型。 下⾯是我们在这个过程中发现的⼀些事情：

24 x 7流式应⽤的结构和按⼩时的批处理任务是不同的⸺你可能需要细粒度的警报，并对重复的错 误更有耐⼼。对于流式应⽤你需要良好的异常处理。准备好回答这样的问题：“如果Spark接收端不 可⽤怎么办？应⽤程序是否应该重试？是否应该忽略丢失的数据？是否应该给你报警？”

花时间验证输⼊对应的输出。例如，验证⼀个记录点击数的任务在测试时是否会返回符合你期望的 结果。

确认⽀持的对象被序列化。Scala DSL让我们能够容易地封装⾮可序列化的变量或者引⽤。在我们 的场景中，GeoCoder对象并没有被序列化，导致我们的应⽤运⾏的⾮常慢；它必须返回到原来的 驱动程序，⾮分布式对象。

Spark Streaming任务的输出仅和喂养Spark的队列⼀样可靠。如果⽣产队列落下了1%的消息，那么 你可能需要⼀个定期调整策略，例如将有损耗的“热路径”和“冷路径”持久数据进⾏合并。对于这种 类型的合并，当你确实需要精确可靠的关联计算时（例如计数）monoid抽象可能是有帮助的。如果 你想了解更多与此相关的信息，可以查看可合并的存储，例如Twiter的 或者Oscar Boykin的 。

Storehaus Algebra for Analytics

# 结论

Sharethrough的⼯程师打算使⽤Spark Streaming做更多的事情。我们的⼯程师可以交互式地制作⼀个 应⽤程序，测试它的批处理，然后将它转移到Streaming上并让它正常运⾏。我们⿎励其他对实时处理 感兴趣的⼈看看Spark Streaming。由于简洁的Spark API，熟悉MapReduce的⼯程师现在不需要学习 全新的编程模型就能构建Streaming应⽤程序。 Spark Streaming让你的组织能够攫取那些只能从分钟级的数据中获取的价值，或者以机器学习算法的 形式，或者是实时仪表盘：完全取决于你！

感谢 对本⽂的审校。 给InfoQ中⽂站投稿或者参与内容翻译⼯作，请邮件⾄ 。也欢迎⼤家通过新浪微 博（ ）或者腾讯微博（ ）关注我们，并与我们的编辑和其他读者朋友交流。 【QCon北京2015⼤会】“ ”专题，由新浪微博技术总监杨卫华出品，邀请不同 领域实践者，分享系统架构设计⽅⾯的思考及启发，探讨解决思路的共性。6场演讲已确认，涵盖： “基于SQL的秒杀解决⽅案”，“搜狗商业⼴告流式计算实践”，“你的⽹站是⾼可⽤的吗？”等话题。

⾟湜

editors@cn.infoq.com @InfoQ @InfoQ

可扩展、⾼可⽤架构设计

了解 详情

。
