---
title: 全局锁和表锁 ：给表加个字段怎么有这么多阻碍？.note（原文插图 annex）
slug: annex-全局锁和表锁-：给表加个字段怎么有这么多阻碍？
type: article
status: active
tags: [wujinsen, annex, 插图]
sources:
  - raw/wujinsen_markdown/DataBase/mysql/全局锁和表锁 ：给表加个字段怎么有这么多阻碍？.note.md
related: [mysql-innodb锁机制]
created: 2026-07-05
updated: 2026-07-05
---

htps:/mp.weixin.q.com/s/CAYehoWyJZulpljh9yqlvg

根据加锁的范围，MySQL ⾥⾯的锁⼤致可以分成全局锁、表级锁和⾏锁三类。下⾯看⼀下全局锁和表级锁。

# 全局锁

全局锁就是对整个数据库实例加锁。MySQL 提供了⼀个加全局读锁的⽅法，命令是 Flushtableswithreadlock(FTWRL)。执⾏这个命令之后，整个就库处于只读状态。 全局锁的典型使⽤场景是，做全库逻辑备份。也就是把整库每个表都 select 出来存成⽂本。但是使⽤这种⽅ 法时会有问题：

如果在主库上备份，那么在备份期间都不能执⾏更新，业务基本上就得停摆；

如果在从库上备份，那么备份期间从库不能执⾏主库同步过来的 binlog，会导致主从延迟。

下⾯看⼀个反例，如果备份不加锁，在备份过程允许写⼊，会出现什么问题。 假设现在要维护“极客时间”的购买系统，关注的是⽤户账户余额表和⽤户课程表。 现在发起⼀个逻辑备份。假设备份期间，有⼀个⽤户，他购买了⼀⻔课程，业务逻辑⾥就要扣掉他的余额， 然后往已购课程⾥⾯加上⼀⻔课。 如果时间顺序上是先备份账户余额表 (u_acount)，然后⽤户购买，然后备份⽤户课程表 (u_course)，会怎 么样呢？可以看⼀下这个图：

![image 1](assets/imageFile1.png)

可以看到，这个备份结果⾥，⽤户 A 的数据状态是“账户余额没扣，但是⽤户课程表⾥⾯已经多了⼀⻔课”。 也就是说，不加锁的话，备份系统备份的得到的库不是⼀个逻辑时间点，这个视图是逻辑不⼀致的。 加锁会出问题，不加锁⼜拿不到⼀致性视图。那么，有没有⼀种⽅法，可以既不加锁，⼜能够拿到⼀致性视 图呢？那就是在可重复读隔离级别下开启⼀个事务。

官⽅⾃带的逻辑备份⼯具是 mysqldump。当 mysqldump 使⽤参数 –single-transaction的时候，导数据之 前就会启动⼀个事务，来确保拿到⼀致性视图。⽽由于 MVC 的⽀持，这个过程中数据是可以正常更新的。 当然，数据库⽐如⽀持“可重复度”这个隔离级别，向MyISAM这样的引擎，就不得不使⽤FTWRL了。 既然FTWRL⽅法是将整个库设置为只读状态，那么可不可以使⽤ setglobalreadonly=true 的⽅式来代替 呢？最好不要，原因有两个：

⼀是，在有些系统中， readonly 的值会被⽤来做其他逻辑，⽐如⽤来判断⼀个库是主库还是备库 (在存在主从复制的库时，备库⼀般只⽤来读，所以会将其设置为只读状态)。因此，修改 global 变 量的⽅式影响⾯更⼤，不建议你使⽤。

⼆是，在异常处理机制上有差异。如果执⾏ FTWRL 命令之后由于客户端发⽣异常断开，那 么 MySQL 会⾃动释放这个全局锁，整个库回到可以正常更新的状态。⽽将整个库设置 为 readonly 之后，如果客户端发⽣异常，则数据库就会⼀直保持 readonly 状态，这样会导致整个 库⻓时间处于不可写状态，⻛险较⾼。

在数据库中，业务的更新不只是增删改数据（ DML)还有可能是加字段等修改表结构的操作（ DL）`。即使 没有被全局锁住，加字段也不是就能⼀帆⻛顺的，因为还有表级锁。 注意：

DML（data manipulation language）数据操纵语⾔：就是我们最经常⽤到的 SELECT、UPDATE、 INSERT、DELETE)。主要⽤来对数据库的数据进⾏⼀些操作)。

DL（data definition language）数据库定义语⾔：其实就是我们在创建表的时候⽤到的⼀些 sql， ⽐如说： CREATE、ALTER、DROP等。 DL主要是⽤在定义或改变表的结构。 DCL（DataControlLanguage）数据库控制语⾔：是⽤来设置或更改数据库⽤户或⻆⾊权限的语 句，包括（ grant,deny,revoke等）语句。

# 表级锁

MySQL ⾥⾯表级别的锁有两种：⼀种是表锁，⼀种是元数据锁（meta data lock，MDL)。 表锁的语法是 locktables…read/write。与 FTWRL 类似，可以⽤ unlock tables 主动释放锁，也可以在客户 端断开的时候⾃动释放。 需要注意， locktables 语法除了会限制别的线程的读写外，也限定了本线程接下来的操作对象。举个例⼦, 如果在某个线程 A 中执⾏ locktables t1 read,t2 write; 这个语句，则其他线程写 t1、读写 t2 的语句都会被 阻塞。同时，线程 A 在执⾏ unlock tables 之前，也只能执⾏读 t1、读写 t2 的操作。连写 t1 都不允许，⾃ 然也不能访问其他表。 另⼀类表级的锁是 MDL（metadata lock)。MDL主要解决在访问数据的时候对表做 DL操作从⽽导致访问 数据出错。MDL 不需要显式使⽤，在访问⼀个表的时候会被⾃动加上。 在 MySQL 5.5 版本中引⼊了 MDL，当对⼀个表做增删改查操作的时候，加 MDL 读锁；当要对表做结构变更 操作的时候，加 MDL 写锁。

读锁之间不互斥，因此你可以有多个线程同时对⼀张表增删改查。

读写锁之间、写锁之间是互斥的，⽤来保证变更表结构操作的安全性。因此，如果有两个线程要同 时给⼀个表加字段，其中⼀个要等另⼀个执⾏完才能开始执⾏。

但是，需要注意MDL的⼀个机制：事务中的 MDL 锁，在语句执⾏开始时申请，但是语句结束后并不会⻢上释 放，⽽会等到整个事务提交后再释放。 给⼀个表加字段，或者修改字段，或者加索引，需要扫描全表的数据。在对⼤表操作的时候，需要特别⼩ ⼼，以免对线上服务造成影响。⽽实际上，即使是⼩表，操作不慎也会出问题。 下⾯看⼀个例⼦，假设表 t 是⼀个⼩表(基于mysql5.6)：

![image 2](assets/imageFile2.png)

上图中， sesion A 先启动，这时候会对表 t 加⼀个 MDL 读锁。由于 sesion B 需要的也是 MDL 读锁，因 此可以正常执⾏。 之后 sesion C 会被 blocked，是因为 sesion A 的 MDL 读锁还没有释放，⽽ sesion C 需要 MDL 写锁， 因此只能被阻塞。<并且之后所有要在表 t 上新申请 MDL 读锁的请求也会被 sesion C 阻塞。 如果某个表上的查询语句频繁，⽽且客户端有重试机制，也就是说超时后会再起⼀个新 sesion 再请求的 话，这个库的线程很快就会爆满。

## 如何给⼀个⼩表加字段

⾸先要解决⻓事务，事务不提交，就会⼀直占着 MDL 锁。在 MySQL 的 information_schema库 的 i nodb_trx 表中，可以查到当前执⾏中的事务。如果要做 DL 变更的表刚好有⻓事务在执⾏， 要考虑先暂停 DL，或者 kil 掉这个⻓事务。

如果要变更的表是⼀个热点表，虽然数据量不⼤，但是上⾯的请求很频繁，这时候 kil 可能未必管 ⽤，因为新的请求⻢上就来了。⽐较理想的机制是，在 alter table 语句⾥⾯设定等待时间，如果在 这个指定的等待时间⾥⾯能够拿到 MDL 写锁最好，拿不到也不要阻塞后⾯的业务语句，先放弃。 之后开发⼈员或者 DBA 再通过重试命令重复这个过程。

对于第⼆种情况， MariaDB 和 AliSQL 具有这个功能，所以这两个开源分⽀⽬前都⽀持 DL NOWAIT/WAIT n 这个语法。

- 1.
- 2.


ALTER TABLE tbl_name NOWAIT add column ... ALTER TABLE tbl_name WAIT N add column ...

![image 3](assets/imageFile3.png)

- ● ⼀⽂带你学习Kafka核⼼概念
- ● ⼲货：分布式基础-RPC你值得了解⼀下
- ● ⼲货|今⽇头条都在⽤的ClickHouse技术你应该了解⼀下
- ● ⾯试：Spring--AOP如果⾯试官再问，直接怼回去
- ● ⾯试：你说你精通Java并发，给我讲讲Java并发之J.U.C
- ● 什么？OSS存储你还在⽤FastDFS？MinIO了解⼀下！！！
