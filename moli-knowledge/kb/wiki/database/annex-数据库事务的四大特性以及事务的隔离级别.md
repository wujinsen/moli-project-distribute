---
title: 数据库事务的四大特性以及事务的隔离级别.note（原文插图 annex）
slug: annex-数据库事务的四大特性以及事务的隔离级别
type: article
status: active
tags: [wujinsen, annex, 插图]
sources:
  - raw/wujinsen_markdown/DataBase/mysql/数据库事务的四大特性以及事务的隔离级别.note.md
related: [mysql-事务与锁]
created: 2026-07-05
updated: 2026-07-05
---

本篇讲诉数据库中事务的四⼤特性（ACID），并且将会详细地说明事务的隔离级别。 如果⼀个数据库声称⽀持事务的操作，那么该数据库必须要具备以下四个特性：

## ⑴ 原⼦性（Atomicity）

原⼦性是指事务包含的所有操作要么全部成功，要么全部失败回滚，这和前⾯两篇博客介绍事务的功能是 ⼀样的概念，因此事务的操作如果成功就必须要完全应⽤到数据库，如果操作失败则不能对数据库有任何影 响。

# ⑵ ⼀致性（Consistency）

⼀致性是指事务必须使数据库从⼀个⼀致性状态变换到另⼀个⼀致性状态，也就是说⼀个事务执⾏之前和 执⾏之后都必须处于⼀致性状态。

拿转账来说，假设⽤户A和⽤户B两者的钱加起来⼀共是5000，那么不管A和B之间如何转账，转⼏次 账，事务结束后两个⽤户的钱相加起来应该还得是5000，这就是事务的⼀致性。

## ⑶ 隔离性（Isolation）

隔离性是当多个⽤户并发访问数据库时，⽐如操作同⼀张表时，数据库为每⼀个⽤户开启的事务，不能被 其他事务的操作所⼲扰，多个并发事务之间要相互隔离。

即要达到这么⼀种效果：对于任意两个并发的事务T1和T2，在事务T1看来，T2要么在T1开始之前就已 经结束，要么在T1结束之后才开始，这样每个事务都感觉不到有其他事务在并发地执⾏。

关于事务的隔离性数据库提供了多种隔离级别，稍后会介绍到。

## ⑷ 持久性（Durability）

持久性是指⼀个事务⼀旦被提交了，那么对数据库中的数据的改变就是永久性的，即便是在数据库系统遇 到故障的情况下也不会丢失提交事务的操作。

例如我们在使⽤JDBC操作数据库时，在提交事务⽅法后，提示⽤户事务操作完成，当我们程序执⾏完成

直到看到提示后，就可以认定事务以及正确提交，即使这时候数据库出现了问题，也必须要将我们的事务完 全执⾏完成，否则就会造成我们看到提示事务处理完毕，但是数据库因为故障⽽没有执⾏事务的重⼤错误。

以上介绍完事务的四⼤特性(简称ACID)，现在重点来说明下事务的隔离性，当多个线程都开启事务操作 数据库中的数据时，数据库系统要能进⾏隔离操作，以保证各个线程获取数据的准确性，在介绍数据库提供 的各种隔离级别之前，我们先看看如果不考虑事务的隔离性，会发⽣的⼏种问题：

## 1，脏读

脏读是指在⼀个事务处理过程⾥读取了另⼀个未提交的事务中的数据。

当⼀个事务正在多次修改某个数据，⽽在这个事务中这多次的修改都还未提交，这时⼀个并发的事务来 访问该数据，就会造成两个事务得到的数据不⼀致。例如：⽤户A向⽤户B转账100元，对应SQL命令如下

update account set money=money+100 where name=’B’; (此时A通知B)

update account set money=money - 100 where name=’A’;

当只执⾏第⼀条SQL时，A通知B查看账户，B发现确实钱已到账（此时即发⽣了脏读），⽽之后⽆论第 ⼆条SQL是否执⾏，只要该事务不提交，则所有操作都将回滚，那么当B以后再次查看账户时就会发现钱其实 并没有转。

## 2，不可重复读

不可重复读是指在对于数据库中的某个数据，⼀个事务范围内多次查询却返回了不同的数据值，这是由于 在查询间隔，被另⼀个事务修改并提交了。

例如事务T1在读取某⼀数据，⽽事务T2⽴⻢修改了这个数据并且提交事务给数据库，事务T1再次读取该 数据就得到了不同的结果，发送了不可重复读。

不可重复读和脏读的区别是，脏读是某⼀事务读取了另⼀个事务未提交的脏数据，⽽不可重复读则是读 取了前⼀事务提交的数据。

在某些情况下，不可重复读并不是问题，⽐如我们多次查询某个数据当然以最后查询得到的结果为主。 但在另⼀些情况下就有可能发⽣问题，例如对于同⼀个数据A和B依次查询就可能不同，A和B就可能打起来 了……

## 3，虚读(幻读)

幻读是事务⾮独⽴执⾏时发⽣的⼀种现象。例如事务T1对⼀个表中所有的⾏的某个数据项做了从“1”修改 为“2”的操作，这时事务T2⼜对这个表中插⼊了⼀⾏数据项，⽽这个数据项的数值还是为“1”并且提交给数据 库。⽽操作事务T1的⽤户如果再查看刚刚修改的数据，会发现还有⼀⾏没有修改，其实这⾏是从事务T2中添 加的，就好像产⽣幻觉⼀样，这就是发⽣了幻读。

幻读和不可重复读都是读取了另⼀条已经提交的事务（这点就脏读不同），所不同的是不可重复读查询 的都是同⼀个数据项，⽽幻读针对的是⼀批数据整体（⽐如数据的个数）。

现在来看看MySQL数据库为我们提供的四种隔离级别：

- ① Serializable (串⾏化)：可避免脏读、不可重复读、幻读的发⽣。

- ② Repeatable read (可重复读)：可避免脏读、不可重复读的发⽣。

- ③ Read committed (读已提交)：可避免脏读的发⽣。

- ④ Read uncommitted (读未提交)：最低级别，任何情况都⽆法保证。


以上四种隔离级别最⾼的是Serializable级别，最低的是Read uncommitted级别，当然级别越⾼，执 ⾏效率就越低。像Serializable这样的级别，就是以锁表的⽅式(类似于Java多线程中的锁)使得其他的线程 只能在锁外等待，所以平时选⽤何种隔离级别应该根据实际情况。在MySQL数据库中默认的隔离级别为 Repeatable read (可重复读)。

在MySQL数据库中，⽀持上⾯四种隔离级别，默认的为Repeatable read (可重复读)；⽽在Oracle数 据库中，只⽀持Serializable (串⾏化)级别和Read committed (读已提交)这两种级别，其中默认的为 Read committed级别。

在MySQL数据库中查看当前事务的隔离级别：

select @@tx_isolation;

在MySQL数据库中设置事务的隔离 级别：

set [glogal | session] transaction isolation level 隔离级别名称;

set tx_isolation=’隔离级别名称;’

- 例1：查看当前事务的隔离级别：


![image 1](assets/imageFile1.png)

- 例2：将事务的隔离级别设置为Read uncommitted级别：


![image 2](assets/imageFile2.png)

或：

![image 3](assets/imageFile3.png)

记住：设置数据库的隔离级别⼀定要是在开启事务之前！

如果是使⽤JDBC对数据库的事务设置隔离级别的话，也应该是在调⽤Connection对象的 setAutoCommit(false)⽅法之前。调⽤Connection对象的setTransactionIsolation(level)即可设置当前 链接的隔离级别，⾄于参数level，可以使⽤Connection对象的字段：

![image 4](assets/imageFile4.png)

在JDBC中设置隔离级别的部分代码：

![image 5](assets/imageFile5.png)

后记：隔离级别的设置只对当前链接有效。对于使⽤MySQL命令窗⼝⽽⾔，⼀个窗⼝就相当于⼀个链 接，当前窗⼝设置的隔离级别只对当前窗⼝中的事务有效；对于JDBC操作数据库来说，⼀个Connection对 象相当于⼀个链接，⽽对于Connection对象设置的隔离级别只对该Connection对象有效，与其他链接 Connection对象⽆关。

参考博客：

http://www.zhihu.com/question/23989904 http://dev.mysql.com/doc/refman/5.6/en/set-transaction.html http://www.cnblogs.com/xdp-gacl/p/3984001.html
