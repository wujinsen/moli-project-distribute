---
title: B树和B+树的总结.note（原文插图 annex）
slug: annex-B树和B+树的总结
type: article
status: active
tags: [wujinsen, annex, 插图]
sources:
  - raw/wujinsen_markdown/面试笔试/树/B树和B+树的总结.note.md
related: [b-plus树与-innodb索引结构]
created: 2026-07-05
updated: 2026-07-05
---

## 为什么要B树

磁盘中有两个机械运动的部分，分别是盘⽚旋转和磁臂移动。盘⽚旋转就是我们市⾯上所提到的多少 转每分钟，⽽磁盘移动则是在盘⽚旋转到指定位置以后，移动磁臂后开始进⾏数据的读写。那么这就 存在⼀个定位到磁盘中的块的过程，⽽定位是磁盘的存取中花费时间⽐较⼤的⼀块，毕竟机械运动花 费的时候要远远⼤于电⼦运动的时间。当⼤规模数据存储到磁盘中的时候，显然定位是⼀个⾮常花费 时间的过程，但是我们可以通过B树进⾏优化，提⾼磁盘读取时定位的效率。 为什么B类树可以进⾏优化呢？我们可以根据B类树的特点，构造⼀个多阶的B类树，然后在尽量多的 在结点上存储相关的信息，保证层数尽量的少，以便后⾯我们可以更快的找到信息，磁盘的I/O操作也 少⼀些，⽽且B类树是平衡树，每个结点到叶⼦结点的⾼度都是相同，这也保证了每个查询是稳定的。

## 简介

这⾥的B树，也就是英⽂中的B-Tre，⼀个 m 阶的B树满⾜以下条件：

- 1.
- 2.
- 3.
- 4.
- 5.
- 6.


每个结点⾄多拥有m棵⼦树； 根结点⾄少拥有两颗⼦树（存在⼦树的情况下）； 除了根结点以外，其余每个分⽀结点⾄少拥有 m/2 棵⼦树； 所有的叶结点都在同⼀层上； 有 k 棵⼦树的分⽀结点则存在 k-1 个关键码，关键码按照递增次序进⾏排列； 关键字数量需要满⾜ceil(m/2)-1 <= n <= m-1；

举个栗⼦：

![image 1](assets/imageFile1.png)

B树上⼤部分的操作所需要的磁盘存取次数和B树的⾼度是成正⽐的，在B树中可以检查多个⼦结点， 由于在⼀棵树中检查任意⼀个结点都需要⼀次磁盘访问，所以B树避免了⼤量的磁盘访问。

操作

既然是树，那么必不可少的操作就是插⼊和删除，这也是B树和其它数据结构不同的地⽅，当然了，还 有必不可少的搜索，分享⼀个对B树的操作进⾏可视化的 ，它是由usfca提供的。 假定对⾼度为h的m阶B树进⾏操作。

⽹址

### 插⼊

新结点⼀般插在第h层，通过搜索找到对应的结点进⾏插⼊，那么根据即将插⼊的结点的数量⼜分为下 ⾯⼏种情况。

如果该结点的关键字个数没有到达m-1个，那么直接插⼊即可； 如果该结点的关键字个数已经到达了m-1个，那么根据B树的性质显然⽆法满⾜，需要将其进⾏分 裂。分裂的规则是该结点分成两半，将中间的关键字进⾏提升，加⼊到⽗亲结点中，但是这⼜可能 存在⽗亲结点也满员的情况，则不得不向上进⾏回溯，甚⾄是要对根结点进⾏分裂，那么整棵树都 加了⼀层。

其过程如下：

![image 2](assets/imageFile2.png)

![image 3](assets/imageFile3.png)

![image 4](assets/imageFile4.png)

![image 5](assets/imageFile5.png)

删除

同样的，我们需要先通过搜索找到相应的值，存在则进⾏删除，需要考虑删除以后的情况， 如果该结点拥有关键字数量仍然满⾜B树性质，则不做任何处理；

如果该结点在删除关键字以后不满⾜B树的性质（关键字没有到达ceil(m/2)-1的数量），则需要向兄 弟结点借关键字，这有分为兄弟结点的关键字数量是否⾜够的情况。

如果兄弟结点的关键字⾜够借给该结点，则过程为将⽗亲结点的关键字下移，兄弟结点的关键 字上移； 如果兄弟结点的关键字在借出去以后也⽆法满⾜情况，即之前兄弟结点的关键字的数量为 ceil(m/2)-1，借的⼀⽅的关键字数量为ceil(m/2)-2的情况，那么我们可以将该结点合并到兄弟 结点中，合并之后的⼦结点数量少了⼀个，则需要将⽗亲结点的关键字下放，如果⽗亲结点不 满⾜性质，则向上回溯；

其余情况参照BST中的删除。

其过程如下：

![image 6](assets/imageFile6.png)

![image 7](assets/imageFile7.png)

![image 8](assets/imageFile8.png)

# B+树

为什么要B+树

由于B+树的数据都存储在叶⼦结点中，分⽀结点均为索引，⽅便扫库，只需要扫⼀遍叶⼦结点即可， 但是B树因为其分⽀结点同样存储着数据，我们要找到具体的数据，需要进⾏⼀次中序遍历按序来扫， 所以B+树更加适合在区间查询的情况，所以通常B+树⽤于数据库索引，⽽B树则常⽤于⽂件索引。

简介

同样的，以⼀个m阶树为例：

- 1.
- 2.
- 3.
- 4.


根结点只有⼀个，分⽀数量范围为[2，m]； 分⽀结点，每个结点包含分⽀数范围为[ceil(m/2), m]； 分⽀结点的关键字数量等于其⼦分⽀的数量减⼀，关键字的数量范围为[ceil(m/2)-1, m-1]，关键字 顺序递增； 所有叶⼦结点都在同⼀层；

操作

其操作和B树的操作是类似的，不过需要注意的是，在增加值的时候，如果存在满员的情况，将选择结 点中的值作为新的索引，还有在删除值的时候，索引中的关键字并不会删除，也不会存在⽗亲结点的 关键字下沉的情况，因为那只是索引。

# B树和B+树的区别

这都是由于B+树和B具有这不同的存储结构所造成的区别，以⼀个m阶树为例。

- 1.


关键字的数量不同；B+树中分⽀结点有m个关键字，其叶⼦结点也有m个，其关键字只是起到了 ⼀个索引的作⽤，但是B树虽然也有m个⼦结点，但是其只拥有m-1个关键字。

- 存储的位置不同；B+树中的数据都存储在叶⼦结点上，也就是其所有叶⼦结点的数据组合起来就 是完整的数据，但是B树的数据存储在每⼀个结点中，并不仅仅存储在叶⼦结点上。 分⽀结点的构造不同；B+树的分⽀结点仅仅存储着关键字信息和⼉⼦的指针（这⾥的指针指的是 磁盘块的偏移量），也就是说内部结点仅仅包含着索引信息。 查询不同；B树在找到具体的数值以后，则结束，⽽B+树则需要通过索引找到叶⼦结点中的数据 才结束，也就是说B+树的搜索过程中⾛了⼀条从根结点到叶⼦结点的路径。
- 2.
- 3.
- 4.
