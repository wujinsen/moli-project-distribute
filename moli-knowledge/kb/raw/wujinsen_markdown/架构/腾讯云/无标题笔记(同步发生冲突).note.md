# CentOS 下部署 Nginx

htps:/cloud.tencent.com/document/product/214/ 3413

本⽂将为您详细介绍如何在 CentOS 系统下部署 Nginx 项⽬，适⽤于刚开始使⽤腾讯云的个⼈⽤户。

### 软件版本

本⽂在示例步骤中的软件版本如下，在实际操作时，请您以实际软件版本为准。

操作系统：CentOS 7.5

Nginx 版本：Nginx 1.16.1

## 安装 Nginx

- 1.
- 2.
- 3.


购买完成后，在云服务器的详情⻚⾯，单击登录，可以直接登录云服务器，输⼊⾃⼰的⽤户名密 码后，开始搭建 Nginx 环境。有关如何创建云服务器实例，请参⻅ 创建云服务器实例 。

# 安装 Nginx： yum -yinstal nginx # 查看 Nginx 版本 nginx -v # 查看 Nginx 安装⽬录 rpm -ql nginx # 启动 Nginx service nginx start

访问该云服务器的公⽹ IP 地址，出现如下⻚⾯则表示 Nginx 部署完成：

![image 1](<无标题笔记(同步发生冲突).note_images/imageFile1.png>)

Nginx 的默认根⽬录 root 是/usr/share/nginx/html，直接修改 html 下的 index.html静态⻚⾯，⽤ 来标识这个⻚⾯的特殊性，相关操作如下：

- a.
- b.
- c.


执⾏如下命令，进⼊html 下的 index.html 静态⻚⾯：

vim /usr/share/nginx/html/index.html

按“i”进⼊编辑模式，请在<body></body>标签内输⼊：

# 建议直接在<body>下⽅输⼊ Helo nginx , This is rs-1! URL is index.html

![image 2](<无标题笔记(同步发生冲突).note_images/imageFile2.png>)

按 “Esc”，输⼊:wq 保存编辑。 负载均衡（原“应⽤型负载均衡”）可以根据后端服务器的路径来进⾏请求转发，在/image路径下部 署静态⻚⾯，相关操作如下：

4.

- a.
- b.
- c.
- d.


依次执⾏如下命令，新建⽬录 image 并进⼊该⽬录：

mkdir /usr/share/nginx/html/image cd /usr/share/nginx/html/image

执⾏如下命令，在image⽬录下创建 index.html静态⻚⾯：

vimindex.html

按“i”进⼊编辑模式，在⻚⾯中输⼊：

Helo nginx , This is rs-1! URL is image/index.html

按 “Esc”，输⼊:wq保存编辑。

注意： Nginx 的默认端⼝是80，如果想修改端⼝请修改配置⽂件并重启 Nginx。

### 验证 Nginx 服务

访问云服务器的公⽹ IP + 路径，如果可以显示出已部署好的静态⻚⾯，则证明 Nginx 部署成功。

rs-1 的 index.html ⻚⾯：

![image 3](<无标题笔记(同步发生冲突).note_images/imageFile3.png>)

rs-1 的 /image/index.html ⻚⾯：

![image 4](<无标题笔记(同步发生冲突).note_images/imageFile4.png>)

