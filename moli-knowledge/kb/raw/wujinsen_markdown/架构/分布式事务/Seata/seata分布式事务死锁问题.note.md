这个我能在我的项⽬中可以复现： 进⼊事务⽅法->分布锁枷锁->业务代码， 然后压测：）， 当压测请求的分布锁（zk的）获取时间（如 超时10秒）⼩于整个压测请求执⾏时间（如需20秒），这时就会出现死锁（lock_table锁数据），现 象就是seata提示获取不到global lock. ⼀般正常⾛业务是没问题。对特定业务并⾼的，需要提前预估做出预估兵法，加⼤业务服务处理能⼒ （可以增加业务模块实例数，或增⼤zk获取锁时间）。。。。

