- 1. 下载nety源码，

- 2. 利⽤maven编译nety项⽬，通常不嫩直接执⾏ mvn clean instal ，因为checkstyle 的缘故（具体原 因可以参看：

）⽽是使⽤mvn compile -Dcheckstyle.skip=true （⽬的是跳过checkstyle ）。

- 3. 为了⽅⾯还可以跳过测试，即加上 -DskipTest 如果还有问题（对于windows 直接使⽤nety官⽹下载的源码，可能存在问题）。 博主遇到的问题和解决⽅案如下：


htp:/ w.4byte.cn/question/32564/mvn-compile-eror-invalid-newline-character.h tml#

- 1. 如果是提示 nety-tcnative 的问题 需要修改如下四个⽬录下的pom.xml⽂件 将tcnative 下对应的 <clasifier>${os.detected.clasifier}</clasifier>注释。四个⽬录分别是：主⽬录，handler,example, 以及testsuite。（如果还有其他项⽬报错，估计也是要修改其对应的pom.xml ⽂件，将其上⾯提到 的 <clasifier>${os.detected.clasifier}</clasifier> 注释） 然后再执⾏⼀个 mvn clean compile -Dcheckstyle.skip=true -Dtest

- 2. 如果还是报错 那么试⼀下 mvn instal -DskipTests -Dcheckstyle.skip=true （博主尝试了⼀下⽤ mvn instal -Dcheckstyle.skip=true -Dmaven.test.skip=true 依然报错，更多有关-DskipTests和Dmaven.test.skip=true 参考： ）


htp:/zephiruswt.blog.51cto.com/5193151/130270

