以前在⾳乐做过⼀些实时投票，积分排名；单曲、专辑等排⾏榜；游戏中也有类似的战⽃⼒排⾏； SNS的游戏⼜有好友排⾏等，对于此类的排⾏算法在此做个总结。

# 需求背景：

查看前top N的排名⽤户

查看⾃⼰的排名

⽤户积分变更后，排名及时更新

# ⽅案⼀：

利⽤MySQL来实现，存放⼀张⽤户积分表user_score，结构如下：

![image 1](<游戏排行榜算法设计实现比较.note_images/imageFile1.png>)

游戏排⾏榜算法设计实现⽐较

取前top N，⾃⼰的排名都可以通过简单的sql语句搞定。

算法简单，利⽤sql的功能，不需要其他复杂逻辑，对于数据量⽐较少、性能要求不⾼，可以使 ⽤。但是对于海量数据，性能是⽆法接受的。

⽅案⼆：积分排名数组实现

如有1百万⽤户进⾏排名，就⽤⼀个⼤⼩为1， 0， 0的数组表示积分和排名的对应关系，其中

rank[ s ]表示积分s所对应的排名。初始化时，rank数组可以由user_score表在O(n)的复杂度内计算⽽ 来。⽤户排名的查询和更新基于这个数组来进⾏。查询积分s所对应的排名直接返回rank[ s ]即可，复 杂度为O(1)；当⽤户积分从s变为s+n，只需要把rank[ s ]到rank[s+n-1]这n个元素的值增加1即可，复 杂度为O(n)。

⽅案三：⽤GC的pb_ds库中有asoc_container来进⾏实现。

tre_order_statistics.c

参考如下：

存取效率都可以达到O(log(n)，不⾜就是程序重启后数据会丢失。

⽅案四：⾃⼰实现排序树

⼤致实现思路如下：

我们可以把[0, 1, 0, 0)作为⼀级区间；再把⼀级区间分为两个2级区间[0, 50, 0), [50, 0, 1, 0, 0)，然后把⼆级区间⼆分为4个3级区间[0, 250, 0), [250, 0, 50, 0), [50, 0, 750, 0), [750, 0, 1, 0, 0)，依此类推，最终我们会得到1, 0, 0个21级区间[0,1), [1,2) … [ 9, 9, 1, 0, 0)。这实际上是把区间组织成了⼀种平衡⼆叉树结构，根结点代表⼀级区间，每个 ⾮叶⼦结点有两个⼦结点，左⼦结点代表低分区间，右⼦结点代表⾼分区间。树形分区结构需要在更 新时保持⼀种不变量，⾮叶⼦结点的count值总是等于其左右⼦结点的count值之和。

以后，每次⽤户积分有变化所需要更新的区间数量和积分变化量有关系，积分变化越⼩更新的区 间层次越低。总体上，每次所需要更新的区间数量是⽤户积分变量的log(n)级别的，也就是说如果⽤户 积分⼀次变化在百万级，更新区间的数量在⼆⼗这个级别。在这种树形分区积分表的辅助下查询积分 为s的⽤户排名，实际上是⼀个在区间树上由上⾄下、由粗到细⼀步步明确s所在位置的过程。⽐如， 对于积分49, 0，我们⽤⼀个初值为0的排名变量来做累加；⾸先，它属于1级区间的左⼦树[0, 50, 0)，那么该⽤户排名应该在右⼦树[50, 0, 1, 0, 0)的⽤户数count之后，我们把该count 值累加到该⽤户排名变量，进⼊下⼀级区间；其次，它属于3级区间的[250, 0, 50, 0)，这是2级 区间的右⼦树，所以不⽤累加count到排名变量，直接进⼊下⼀级区间；再次，它属于4级区间的…； 直到最后我们把⽤户积分精确定位在21级区间[49, 0, 49,01)，整个累加过程完成，得出排名！

虽然，本算法的更新和查询都涉及到若⼲个操作，但如果我们为区间的from_score和to_score建 ⽴索引，这些操作都是基于键的查询和更新，不会产⽣表扫描，因此效率更⾼。另外，本算法并不依 赖于关系数据模型和SQL运算，可以轻易地改造为NoSQL等其他存储⽅式，⽽基于键的操作也很容易 引⼊缓存机制进⼀步优化性能。进⼀步，我们可以估算⼀下树形区间的数⽬⼤约为2, 0, 0，考虑每 个结点的⼤⼩，整个结构只占⽤⼏⼗M空间。所以，我们完全可以在内存建⽴区间树结构，并通过 user_score表在O(n)的时间内初始化区间树，然后排名的查询和更新操作都可以在内存进⾏。⼀般来 讲，同样的算法，从数据库到内存算法的性能提升常常可以达到10^5以上；因此，本算法可以达到⾮ 常⾼的性能。

算法特点

优点：结构稳定，不受积分分布影响；每次查询或更新的复杂度为积分最⼤值的O(log(n)级别， 且与⽤户规模⽆关，可以应对海量规模；不依赖于SQL，容易改造为NoSQL或内存数据结构。

缺点：算法相对更复杂。

⽅案五：skiplist的实现

实现⽅案四的时候，发现代码⽐较复杂，调试起来特别不⽅便。游戏这边有个同事也实现了个， 代码地址：htp: /km.oa.com/articles/show/158740

于是就想到的跳表，发现⽤这个实现起来⽐较简单；⽤hashmap来存储具体的对象；⽤skiplist⽤ 来排序。也可以简单的⽤⼀个map和set来实现。Map内⾯存具体对象，set⽤来排序。

关于skip list这⾥简单介绍下：skip list是链表的⼀种特殊形式，对链表的⼀种优化；保证INSERT 和REMOVE操作是O(logn)，⽽通⽤链表的复杂度为O(n);

优点：实现较简单，效率基本上O(log(N)

缺点：当达到亿级别时的数据时，性能会急剧下降

⽅案六：基于redis的 sort set的实现

后来看redis发现redis的zset天⽣是⽤来做排⾏榜的、好友列表, 去重, 历史记录等业务需求。接⼝ 使⽤⾮常简单。接⼝⾮常丰富，基本上需要的实现都能满⾜，说明如下：

ZAd/ZRem是O(log(N)，ZRangeByScore/ZRemRangeByScore是O(log(N)+M)，N是Set⼤⼩， M是结果/操作元素的个数。

ZSET的实现⽤到了两个数据结构：hash table 和 skip list(跳跃表)，其中hash table是具体使⽤ redis中的dict来实现的，主要是为了保证查询效率为O(1) ，⽽skip list(跳跃表)主要是保证元素有序并 能够保证INSERT和REMOVE操作是O(logn)的复杂度。

⾳乐现在的通⽤投票排名系统就是基于redis来实现的，运⾏还不错。

优点：基于redis开发，速度快；使⽤redis相关特性

缺点：当达到亿级别时的数据时，性能会急剧下降 来实现排⾏榜的⽅法很多，可以根据⾃⼰的具体需求，参考选⽤。

