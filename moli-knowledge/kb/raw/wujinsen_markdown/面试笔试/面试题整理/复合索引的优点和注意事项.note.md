概念: 单⼀索引是指索引列为⼀列的情况,即新建索引的语句只实施在⼀列上; ⽤户可以在多个列 上建⽴索引,这种索引叫做复合索引(组合索引); 复合索引在数据库操作期间所需的开销更⼩,可以代 替多个单⼀索引; 同时有两个概念叫做窄索引和宽索引,窄索引是指索引列为1-2列的索引,宽索引也 就是索引列超过2列的索引; 设计索引的⼀个重要原则就是能⽤窄索引不⽤宽索引,因为窄索引往往 ⽐组合索引更有效; 使⽤: 创建索引 create index idx1 on table1(col1,col2,col3) 查询 select * from table1 where col1= A and col2= B and col3 = C 这时候查询优化器,不在扫描表了,⽽是直接 的从索引中拿数据,因为索引中有这些数据,这叫覆盖式查询,这样的查询速度⾮常快;

注意事项: 1、对于复合索引,在查询使⽤时,最好将条件顺序按找索引的顺序,这样效率最⾼; select * from table1 where col1=A AND col2=B AND col3=D 如果使⽤ where col2=B AND col1=A 或者 where col2=B 将不会使⽤索引

- 2、何时是⽤复合索引 根据where条件建索引是极其重要的⼀个原则; 注意不要过多⽤索引,

否则对表更新的效率有很⼤的影响,因为在操作表的时候要化⼤量时间花在创建索引中

- 3、复合索引会替代单⼀索引么 如果索引满⾜窄索引的情况下可以建⽴复合索引,这样可以节约


空间和时间 备注: 对⼀张表来说,如果有⼀个复合索引 on (col1,col2),就没有必要同时建⽴⼀个单索引 on col1; 如果查询条件需要,可以在已有单索引 on col1的情况下,添加复合索引on (col1,col2),对于 效率有⼀定的提⾼ 同时建⽴多字段(包含5、6个字段)的复合索引没有特别多的好处,相对⽽⾔,建⽴ 多个窄字段(仅包含⼀个,或顶多2个字段)的索引可以达到更好的效率和灵活性

![image 1](<复合索引的优点和注意事项.note_images/imageFile1.png>)

