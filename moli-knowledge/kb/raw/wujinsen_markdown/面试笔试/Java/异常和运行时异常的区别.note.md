Throwable是所有Java程序中错误处理的⽗类，有两种资类：Eror和Exception。

Eror：表示由JVM所侦测到的⽆法预期的错误，由于这是属于JVM层次的严重错误，导致JVM⽆法继续执⾏，因 此，这是不可捕捉到的，⽆法采取任何恢复的操作，顶多只能显示错误信息。

Exception：表示可恢复的例外，这是可捕捉到的。

Java提供了两类主要的异常:runtime exception和checked exception。checked 异常也就是我们经常遇到的IO异 常，以及SQL异常都是这种异常。对于这种异常，JAVA编译器强制要求我们必需对出现的这些异常进⾏catch。所 以，⾯对这种异常不管我们是否愿意，只能⾃⼰去写⼀⼤堆catch块去处理可能的异常。

但是另外⼀种异常：runtime exception，也称运⾏时异常，我们可以不处理。当出现这样的异常时，总是由虚拟 机接管。⽐如：我们从来没有⼈去处理过NulPointerException异常，它就是运⾏时异常，并且这种异常还是最常⻅ 的异常之⼀。

出现运⾏时异常后，系统会把异常⼀直往上层抛，⼀直遇到处理代码。如果没有处理块，到最上层，如果是多线程 就由Thread.run()抛出，如果是单线程就被main()抛出。抛出之后，如果是线程，这个线程也就退出了。如果是主程 序抛出的异常，那么这整个程序也就退出了。运⾏时异常是Exception的⼦类，也有⼀般异常的特点，是可以被Catch 块处理的。只不过往往我们不对他处理罢了。也就是说，你如果不对运⾏时异常进⾏处理，那么出现运⾏时异常之 后，要么是线程中⽌，要么是主程序终⽌。

如果不想终⽌，则必须扑捉所有的运⾏时异常，决不让这个处理线程退出。队列⾥⾯出现异常数据了，正常的处理 应该是把异常数据舍弃，然后记录⽇志。不应该由于异常数据⽽影响下⾯对正常数据的处理。在这个场景这样处理可 能是⼀个⽐较好的应⽤，但并不代表在所有的场景你都应该如此。如果在其它场景，遇到了⼀些错误，如果退出程序 ⽐较好，这时你就可以不太理会运⾏时异常，或者是通过对异常的处理显式的控制程序退出。

异常处理的⽬标之⼀就是为了把程序从异常中恢复出来。

<table>
  <tr>
    <th>runtime异常是运⾏是异常，如果抛出runtime异常，程序会停⽌运⾏， ⼀般异常 抛出程序不会停⽌，甚⾄有事如果程序做了try catch 处理，catch块⾥是空的，即使程序发⽣异常， 也会继续运⾏，且不能发现程序发⽣异常，所以在写程序的时候不建议，写空的catch块，不便于程<br><br>异常⼀旦抛出程序会即刻停⽌运⾏</th>
  </tr>
</table>


# 序的维护；⽽runtime

