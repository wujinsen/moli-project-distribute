通过它，你可以遍历对象、数组的属性值并进⾏处理。 使⽤说明 each函数根据参数的类型实现的效果不完全⼀致：

- 1、遍历对象(有附加参数)
- 2、遍历数组(有附件参数)
- 3、遍历对象(没有附加参数)
- 4、遍历数组(没有附加参数)


<table>
  <tr>
    <th>$.each(Object, function(p1, p2) { this; /这⾥的this指向每次遍历中Object的当前属性值 p1; p2; /访问附加参数</th>
  </tr>
</table>


}, ['参数1', '参数2']);

<table>
  <tr>
    <th>$.each(Aray, function(p1, p2){ this; /这⾥的this指向每次遍历中Aray的当前元素 p1; p2; /访问附加参数</th>
  </tr>
</table>


}, ['参数1', '参数2']);

<table>
  <tr>
    <th>$.each(Object, function(name, value) { this; /this指向当前属性的值 name; /name表示Object当前属性的名称 value; /value表示Object当前属性的值</th>
  </tr>
</table>


});

<table>
  <tr>
    <th>$.each(Aray, function(i, value) { this; /this指向当前元素 i; /i表示Aray当前下标 value; /value表示Aray当前元素</th>
  </tr>
</table>


});

下⾯提⼀下jQuery的each⽅法的⼏种常⽤的⽤法 Js代码

<table>
  <tr>
    <th>var ar = [ "one", "two", "thre", "four"]; $.each(ar, function(){ alert(this); });<br><br>/上⾯这个each输出的结果分别为：one,two,thre,four<br><br>var ar1 =[1, 4, 3], [4, 6, 6], [7, 20, 9] $.each(ar1, function(i, item){ alert(item[0]); });<br><br>/其实ar1为⼀个⼆维数组，item相当于取每⼀个⼀维数组， /item[0]相对于取每⼀个⼀维数组⾥的第⼀个值 /所以上⾯这个each输出分别为：1 4 7<br><br>var obj = { one:1, two:2, thre:3, four:4}; $.each(obj, function(key, val) { alert(obj[key]); });<br><br>/这个each就有更厉害了，能循环每⼀个属性</th>
  </tr>
</table>


/输出结果为：1 2 3 4

天⽣会嫉妒两种⼈，⼀种⼈是艺术狂⼈，⼀种⼈是代码狂⼈ … 嫉妒是让我前进的动⼒

