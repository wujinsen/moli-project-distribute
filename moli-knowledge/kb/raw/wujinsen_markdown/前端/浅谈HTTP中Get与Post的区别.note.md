Htp定义了与服务器交互的不同⽅法，最基本的⽅法有4种，分别是GET，POST，PUT，DELETE。 URL全称是资源描述符，我们可以这样认为：⼀个URL地址，它⽤于描述⼀个⽹络上的资源，⽽HTP 中的GET，POST，PUT，DELETE就对应着对这个资源的查，改，增，删4个操作。到这⾥，⼤家应该 有个⼤概的了解了，GET⼀般⽤于获取/查询资源信息，⽽POST⼀般⽤于更新资源信息。

1.根据HTP规范，GET⽤于信息获取，⽽且应该是安全的和幂等的。

- (1).所谓安全的意味着该操作⽤于获取信息⽽⾮修改信息。换句话说，GET 请求⼀般不应产⽣副作

⽤。就是说，它仅仅是获取资源信息，就像数据库查询⼀样，不会修改，增加数据，不会影响资源的 状态。

* 注意：这⾥安全的含义仅仅是指是⾮修改信息。

- (2).幂等的意味着对同⼀URL的多个请求应该返回同样的结果。这⾥我再解释⼀下幂等这个概念：


幂等（idempotent、idempotence）是⼀个数学或计算机学概念，常⻅于抽象代数中。 幂等有⼀下⼏种定义： 对于单⽬ 运算，如果⼀个运算对于在范围内的所有的⼀个数多次进⾏该运算所得的结果和进⾏⼀次该运算所得的结果是⼀样的，那么我们就称该 运算是幂等的。⽐如绝对值运算就是⼀个例⼦，在实数集中，有abs(a)=abs(abs(a))。 对于双⽬运算，则要求当参与运算的两个 值是等值的情况下，如果满⾜运算结果与参与运算的两个值相等，则称该运算幂等，如求两个数的最⼤值的函数，有在在实数集中幂 等，即max(x,x) = x。

看完上述解释后，应该可以理解GET幂等的含义了。 但在实际应⽤中，以上2条规定并没有这么严格。引⽤别⼈⽂章的例⼦：⽐如，新闻站点的头版不

断更新。虽然第⼆次请求会返回不同的⼀批新闻，该操作仍然被认为是安全的和幂等的，因为它总是 返回当前的新闻。从根本上说，如果⽬标是当⽤户打开⼀个链接时，他可以确信从⾃身的⻆度来看没 有改变资源即可。

2.根据HTP规范，POST表示可能修改变服务器上的资源的请求。继续引⽤上⾯的例⼦：还是新 闻以⽹站为例，读者对新闻发表⾃⼰的评论应该通过POST实现，因为在评论提交后站点的资源已经不 同了，或者说资源被修改了。

上⾯⼤概说了⼀下HTP规范中GET和POST的⼀些原理性的问题。但在实际的做的时候，很多⼈ 却没有按照HTP规范去做，导致这个问题的原因有很多，⽐如说：

- 1.很多⼈贪⽅便，更新资源时⽤了GET，因为⽤POST必须要到FORM（表单），这样会麻烦⼀

点。

- 2.对资源的增，删，改，查操作，其实都可以通过GET/POST完成，不需要⽤到PUT和DELETE。
- 3.另外⼀个是，早期的Web MVC框架设计者们并没有有意识地将URL当作抽象的资源来看待和设


计，所以导致⼀个⽐较严重的问题是传统的Web MVC框架基本上都只⽀持GET和POST两种HTP⽅ 法，⽽不⽀持PUT和DELETE⽅法。

* 简单解释⼀下MVC：MVC本来是存在于Desktop程序中的，M是指数据模型，V是指⽤户界⾯， C则是控制器。使⽤MVC的⽬的是将M和V的实现代码分离，从⽽使同⼀个程序可以使⽤不同的表现形 式。

以上3点典型地描述了⽼⼀套的⻛格（没有严格遵守HTP规范），随着架构的发展，现在出现 REST(Representational State Transfer)，⼀套⽀持HTP规范的新⻛格，这⾥不多说了，可以参考 《RESTful Web Services》。

说完原理性的问题，我们再从表⾯现像上⾯看看GET和POST的区别： 1.GET请求的数据会附在URL之后（就是把数据放置在HTP协议头中），以?分割URL和传输数

据，参数之间以&相连，如：login.action? name=hy d&pasword=idontknow&verify=%E4%BD%A0%E5%A5%BD。如果数据是英⽂字⺟/数 字，原样发送，如果是空格，转换为+，如果是中⽂/其他字符，则直接把字符串⽤BASE64加密，得出 如：%E4%BD%A0%E5%A5%BD，其中％ X中的 X为该符号以16进制表示的ASCI。

POST把提交的数据则放置在是HTP包的包体中。 2."GET⽅式提交的数据最多只能是1024字节，理论上POST没有限制，可传较⼤量的数据， IS4中

最⼤为80KB， IS5中为10KB"？？！ 以上这句是我从其他⽂章转过来的，其实这样说是错误的，不准确的：

(1).⾸先是"GET⽅式提交的数据最多只能是1024字节"，因为GET是通过URL提交数据，那么GET 可提交的数据量就跟URL的⻓度有直接关系了。⽽实际上，URL不存在参数上限的问题，HTP协议规 范没有对URL⻓度进⾏限制。这个限制是特定的浏览器及服务器对它的限制。IE对URL⻓度的限制是 2083字节(2K+35)。对于其他浏览器，如Netscape、FireFox等，理论上没有⻓度限制，其限制取决于 操作系统的⽀持。

注意这是限制是整个URL⻓度，⽽不仅仅是你的参数值数据⻓度。[⻅参考资料5]

(2).理论上讲，POST是没有⼤⼩限制的，HTP协议规范也没有进⾏⼤⼩限制，说“POST数据量存 在80K/10K的⼤⼩限制”是不准确的，POST数据是没有限制的，起限制作⽤的是服务器的处理程序的 处理能⼒。

对于ASP程序，Request对象处理每个表单域时存在10K的数据⻓度限制。但如果使⽤ Request.BinaryRead则没有这个限制。

由这个延伸出去，对于 IS 6.0，微软出于安全考虑，加⼤了限制。我们还需要注意：

- 1).IS 6.0默认ASP POST数据量最⼤为20KB，每个表单域限制是10KB。
- 2).IS 6.0默认上传⽂件的最⼤⼤⼩是4MB。
- 3).IS 6.0默认最⼤请求头是16KB。


IS 6.0之前没有这些限制。[⻅参考资料5]

所以上⾯的80K，10K可能只是默认值⽽已(注：关于 IS4和 IS5的参数，我还没有确认)，但肯定 是可以⾃⼰设置的。由于每个版本的 IS对这些参数的默认值都不⼀样，具体请参考相关的 IS配置⽂ 档。

- 3.在ASP中，服务端获取GET请求参数⽤Request.QueryString，获取POST请求参数⽤ Request.Form。在JSP中，⽤request.getParameter(\" X\")来获取，虽然jsp中也有 request.getQueryString()⽅法，但使⽤起来⽐较麻烦，⽐如：传⼀个test.jsp? name=hy d&pasword=hy d，⽤request.getQueryString()得到的是： name=hy d&pasword=hy d。在PHP中，可以⽤$_GET和$_POST分别获取GET和POST中的数 据，⽽$_REQUEST则可以获取GET和POST两种请求中的数据。值得注意的是，JSP中使⽤request和 PHP中使⽤$_REQUEST都会有隐患，这个下次再写个⽂章总结。
- 4.POST的安全性要⽐GET的安全性⾼。注意：这⾥所说的安全性和上⾯GET提到的“安全”不是同 个概念。上⾯“安全”的含义仅仅是不作数据修改，⽽这⾥安全的含义是真正的Security的含义，⽐如： 通过GET提交数据，⽤户名和密码将明⽂出现在URL上，因为(1)登录⻚⾯有可能被浏览器缓存，(2)其 他⼈查看浏览器的历史纪录，那么别⼈就可以拿到你的账号和密码了，除此之外，使⽤GET提交数据 还可能会造成Cros-site request forgery攻击。


总结⼀下，Get是向服务器发索取数据的⼀种请求，⽽Post是向服务器提交数据的⼀种请求，在 FORM（表单）中，Method默认为"GET"，实质上，GET和POST只是发送机制不同，并不是⼀个取⼀ 个发！

纯属hy d个⼈总结，如有错漏请指出。:>

参考资料： htp:/hi.baidu.com/liuzd03/blog/item/7bfecbfa6ea94ed8b58f318c.html htp:/ w.blogjava.net/onlykeke/archive/206/08/23/65285.aspx htp:/baike.baidu.com/view/2067025.htm htp:/ w.chxwei.com/article.asp?id=373 htp:/blog.csdn.net/somat/archive/204/10/29/158707.aspx

- [1].
- [2].
- [3].
- [4].
- [5]. 转载请说明出处，谢谢[hy d( ]


htp:/ w.cnblogs.com/hy d/)

