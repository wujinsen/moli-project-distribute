“并⾏程序易于产⽣ bug 不是什么秘密。编写这种程序是⼀种挑战，并且在编程过程中悄悄产⽣的 bug 不容易被发现。许多并⾏ bug 只有在系统测试、功能测试时才能被发现或由⽤户发现。到那时修复它 们需要⾼昂的费⽤ - 假设能够修复它们 - 因为它们是如此难于调试。”以上论述来⾃IBM论坛中关于 ConTest 的⼀篇介绍⽂章，并且，我还要补充⼀点，这种 BUG 通常根本⽆法重现，以⾄于要找到发⽣ BUG 的原因都是⾮常的困难；即使幸运的找到了可能原因，修改了代码，要确认问题确实得到了解 决，依然是⾮常的困难，因为根本⽆法进⾏有效的测试。

JUnit提供了很好的单元测试框架，但是对于多线程的并发测试，却⽆能为⼒。经过⼀番搜索，找到了 ⼀个叫 GroboUtils 的东⻄，关于它的介绍不少，这⾥不再重复，GroboUtils 解决了启动多线程以及当 某个线程出错时将错误返回给 JUnit 的问题，现在我们可以⽐较简单启动多个线程并验证结果了。具 体介绍请⾃⼰ gogle ⼀下，我这⾥简单的贴⼀个 Demo 代码，当然和官⽅以及⼀般的例⼦不同，我⽐ 较喜欢使⽤内部匿名类来启动线程。这个测试很简单，启动 5 个线程，并发的进⾏ i + 运算，我有意 的注释掉了同步代码，这样应该会导致最后测试⽆法通过。

htp:/ w.CodeHighlighter.co m/-

Code highlighting produced by Actipro CodeHighlighter (freware) > clas ParalInc {

public int i=0; }

public clas ParalIncTest {

public static clas Incrementor extends Thread { public void run() { }

}

@Test public void testInc() throws Throwable {

for (int i = 0; i < 1 0; i +) { final ParalInc target = new ParalInc();

TestRunable r = new TestRunable() {

public void runTest() throws Throwable { / synchronized(ParalIncTest.clas) {

target.i +; / }

}

};

TestRunable[] tcs = {r, r, r, r, r}; int threadCount = tcs.length;

MultiThreadedTestRuner mtr = new MultiThreadedTestRuner( tcs ); mtr.runTestRunables( 2 * 60 * 1 0 );

System.out.println("final value: " + target.i);

if (target.i != threadCount) {

System.er.println(" Bug - at l op " + i); }

asertEquals(threadCount, target.i); }

}

} 开始的时候我并没有添加最外层循环，然⽽测试的结果很遗憾，怎么都⽆法让这段代码出错。加上循 环以后，才偶尔会出错，也⼀般要循环到数百次以后才会发⽣错误。导致这种结果的原因，估计是线 程的切换其实并发象我们想象的那么频繁，⽽且要恰恰在共享冲突点发⽣切换，这个概率也确实⾮常 的⼩。如果能够控制虚拟机的线程切换，使得共享冲突概率更⼤⼀些那该是多么好啊。

因此，我找到了这篇⽂章 ，ConTest，这 真是个好东⻄啊，配置好以后，还是上⾯那段测试代码，但是测试结果⼏乎都会检测到冲突，⽽且通 常是在循环了2-3次后就检测到了。

htp:/ w.ibm.com/developerworks/cn/java/j-contest.html

ConTest 下载链接在从上⾯的⽂章中可直接找到，可以⽤ Eclipse plug-in ⽅式安装，update 地址

ht p:/awebx04.alphaworks.ibm.com/etktechnologies/updates

。但是我实在没发现它给 Eclipse 添加 了什么功能，其实我觉得主要还是只要下载到那个 ZIP 包就可以了，并不需要安装为 plug-in。

下载下来以后，ConTest 并不能⾃动的开始⼯作，需要⼀点点配置⼯作，这有点烦⼈。

⾸先要将 ConTest.jar 包拷⻉到⼀个便于指定位置的地⽅，我⼀般倾向于将它放在⼯程⽬录的lib⼦⽬录 下，另外 ZIP 包的 LIB ⽬录下还有⼀个 KingProperties ⽂件，要拷⻉到和 ConTest.jar 相同的⽬录中。 修改 KingProperties ⽂件，当然也可以不改，我主要是修改 output ⽬录，为 output = target，和 maven 保持⼀致，⼀切都输出到那⾥去。

这还只是⼀些准备活动，最后，要让 ConTest 开始⼯作，还需要修改 Run|RunConfigurations，在单元 测试的 jvm 启动参数中指定启动 ConTest , 既添加 JVM 参数：

-javagent:lib/ConTest.jar -Dcontest.targetClases=demo/ParalIncTest 这⾥ -Dcontest.targetClases=demo/ParalIncTest 既指定要进⾏并⾏性分析的类名，可以⽤逗号分隔 指定多个类，当然这个参数本身也可以 KingProperties ⽂件中指定的，但是我觉得在命令⾏测试⽐较 ⽅便⼀些，毕竟从单元测试的⻆度来说，⼀次应该只测试⼀个类。在命令⾏测试，就不需要多套 KingProperties 配置⽂件。

⾄此，所有配置⼯作完成，依然像以前⼀样启动单元测试，ConTest 作为 javagent 装载，因此它其实 是在 ClasLoader 加载类字节码的时候介⼊，修改字节码在其中加⼊⼀些线程切换的代码，这样线程 切换更频繁了，特别在⼀些同步块中，发⽣同步冲突的概率⾃然就⼤很多了。

总的来说，将 GroboUtils 和 ConTest 结合起来使⽤，效果还是相当不错的，并发测试变得简单多了， ⽽且检测到冲突的可能性⾮常的⼤。

# 使⽤ ConTest进⾏多线程单元测试

为什么 并 ⾏ 测 试 很 困 难 以 及 如 何使 ⽤ ConTest 辅 助 测 试

Yarden Nir-Buchbinderyarden@il.ibm.com Shmuel Urur@il.ibm.com

( ), 研究科学家, IBM ( ), 研究科学家, IBM

简介： 众所周知并⾏程序设计易于产⽣ bug。更为严重的是，往往在开发过程的晚期当这些并⾏ bug 引起严重的损害时才能发现它们并且难于调试它们。即使彻底地对它们进⾏了调试，常规的单元测试 实践也很可能遗漏并⾏ bug。在本⽂中，并⾏专家 Shmuel Ur 和 Yarden Nir-Buchbinder 解释了为什 么并⾏ bug 如此难于捕获并且介绍了 IBM Research 的⼀种新的解决⽅案。 发布⽇期： 206 年 5 ⽉ 1 ⽇ 级别： 初级 访问情况 ： 590 次浏览 评论： 2 (查看 | 添加评论 - 登录)

![image 1](<多线程单元测试.note_images/imageFile1.png>)

平均分 (5个评分)

为本⽂评分

并⾏程序易于产⽣ bug 不是什么秘密。编写这种程序是⼀种挑战，并且在编程过程中悄悄产⽣的 bug 不容易被发现。许多并⾏ bug 只有在系统测试、功能测试时才能被发现或由⽤户发现。到那时修复它 们需要⾼昂的费⽤ - 假设能够修复它们 - 因为它们是如此难于调试。 在本⽂中，我们介绍了 ConTest，⼀种⽤于测试、调试和测量并⾏程序范围的⼯具。正如您将很快看 到的，ConTest 不是单元测试的取代者，但它是处理并⾏程序的单元测试故障的⼀种补充技术。 注意本⽂中包含了⼀个 ，⼀旦理解了有关 ConTest 如何⼯作的基本知识，您就可以⾃⼰使 ⽤该软件包进⾏试验。

示例软件包

## 为什么单元测试还不够

当问任何 Java™ 开发者时，他们都会告诉您单元测试是⼀种好的实践。在单元测试上做适当的投⼊， 随后将得到回报。通过单元测试，能较早地发现 bug 并且能⽐不进⾏单元测试更容易地修复它们。但 是普通的单元测试⽅法（即使当彻底地进⾏了测试时）在查找并⾏ bug ⽅⾯不是很有效。这就是为什 么它们能逃到程序的晚期 。 为什么单元测试经常遗漏并⾏ bug？通常的说法是并⾏程序（和 bug）的问题在于它们的不确定性。 但是对于单元测试⽬的⽽⾔，荒谬性在于并⾏程序是⾮ 常 确定的。下⾯的两个示例解释了这⼀点。

### ⽆修饰的 NamePrinter

第⼀个例⼦是⼀个类，该类除了打印由两部分构成的名字之外，什么也不做。出于教学⽬的，我们把 此任务分在三个线程中：⼀个线程打印⼈名，⼀个线程打印空格，⼀个线程打印姓和⼀个新⾏。⼀个 包括对锁进⾏同步和调⽤ wait() 和 notifyAll() 的成熟的同步协议能保证所有事情以正确的顺序 发⽣。正如您在清单 1 中看到的， main() 充当单元测试，⽤名字 "Washington Irving" 调⽤此类：

- 清单 1. NamePrinter


- 1 public class NamePrinter {

- 2 private final String firstName;

- 3 private final String surName;

- 4 private final Object lock = new Object();

- 5 private boolean printedFirstName = false;

- 6 private boolean spaceRequested = false;

- 7 public NamePrinter(String firstName, String surName) {

- 8 this.firstName = firstName;

- 9 this.surName = surName;

- 10 }

- 11 public void print() {

- 12 new FirstNamePrinter().start();

- 13 new SpacePrinter().start();

- 14 new SurnamePrinter().start();

- 15 }

- 16 private class FirstNamePrinter extends Thread {

- 17 public void run() {

- 18 try {

- 19 synchronized (lock) {

- 20 while (firstName == null) {

- 21 lock.wait();

- 22 }

- 23 System.out.print(firstName);

- 24 printedFirstName = true;

- 25 spaceRequested = true;

- 26 lock.notifyAll();

- 27 }

- 28 } catch (InterruptedException e) {

- 29 assert (false);

- 30 }

- 31 }

- 32 }

- 33 private class SpacePrinter extends Thread {

- 34 public void run() {

- 35 try {

- 36 synchronized (lock) {

- 37 while ( ! spaceRequested) {

- 38 lock.wait();


- 39 }

- 40 System.out.print(' ');

- 41 spaceRequested = false;

- 42 lock.notifyAll();

- 43 }

- 44 } catch (InterruptedException e) {

- 45 assert (false);

- 46 }

- 47 }

- 48 }

- 49 private class SurnamePrinter extends Thread {

- 50 public void run() {

- 51 try {

- 52 synchronized(lock) {

while ( ! printedFirstName || spaceRequested || surName == null) {

- 53

- 54 lock.wait();

- 55 }

- 56 System.out.println(surName);

- 57 }

- 58 } catch (InterruptedException e) {

- 59 assert (false);

- 60 }

- 61 }

- 62 }

- 63 public static void main(String[] args) {

- 64 System.out.println();

- 65 new NamePrinter("Washington", "Irving").print();

- 66 }

- 67 }

- 68


如果您愿意，您可以编译和运⾏此类并且检验它是否像预期的那样把名字打印出来。 然后，把所有的 同步协议删除，如清单 2 所示：

- 清单 2. ⽆修饰的 NamePrinter


<table>
  <tr>
    <th>1 public class NakedNamePrinter {<br><br>2 private final String firstName;<br><br>3 private final String surName;<br><br>4 public NakedNamePrinter(String firstName, String surName) {<br><br>5 this.firstName = firstName;<br><br>6 this.surName = surName;<br><br>7 new FirstNamePrinter().start();<br><br>8 new SpacePrinter().start();<br><br>9 new SurnamePrinter().start();<br><br>10 }<br><br>11 private class FirstNamePrinter extends Thread {<br><br>12 public void run() {<br><br>13 System.out.print(firstName);<br><br>14 }<br><br>15 }<br><br>16 private class SpacePrinter extends Thread {<br><br>17 public void run() {<br><br>18 System.out.print(' ');<br><br>19 }<br><br>20 }<br><br>21 private class SurnamePrinter extends Thread {<br><br>22 public void run() {<br><br>23 System.out.println(surName);<br><br>24 }<br><br>25 }<br><br>26 public static void main(String[] args) {<br><br>27 System.out.println();<br><br>28 new NakedNamePrinter("Washington", "Irving");<br><br>29 }<br><br>30 }<br><br>31<br></th>
  </tr>
</table>


##### 这个步骤使类变得完全错误：它不再包含能保证事情以正确顺序发⽣的指令。但我们编译和运⾏此类 时会发⽣什么情况呢？所有的事情都完全相同！"Washington Irving" 以正确的顺序打印出来。

此试验的寓义是什么？设想 NamePrinter 以及它的同步协议是并⾏类。 您运⾏单元测试 - 也许很多 次 - 并且它每次都运⾏得很好。⾃然地，您认为可以放⼼它是正确的。但是正如您刚才所看到的，在 根本没有同步协议的情况下输出同样也是正确的，并且您可以安全地推断在有很多错误的协议实现的 情况下输出也是正确的。因此，当您认 为 已经测试了您的协议时， 您并没有真 正 地 测试它。 现在我们看⼀下另外的⼀个例⼦。

### 多 bug 的任务队列

下⾯的类是⼀种常⻅的并⾏实⽤程序模型：任务队列。它有⼀个能使任务⼊队的⽅法和另外⼀个使任 务出队的⽅法。在从队列中删除⼀个任务之前， work() ⽅法进⾏检查以查看队列是否为空，如果为 空则等待。 enqueue() ⽅法通知所有等待的线程（如果有的话）。 为了使此示例简单，⽬标仅仅是 字符串，任务是把它们打印出来。再⼀次， main() 充当单元测试。顺便说⼀下，此类有⼀个 bug。

- 清单 3. PrintQueue


- 1 import java.util.*;

- 2 public class PrintQueue {

- 3 private LinkedList<String> queue = new LinkedList<String>();

- 4 private final Object lock = new Object();

- 5 public void enqueue(String str) {

- 6 synchronized (lock) {

- 7 queue.addLast(str);

- 8 lock.notifyAll();

- 9 }

- 10 }

- 11 public void work() {

- 12 String current;

- 13 synchronized(lock) {

- 14 if (queue.isEmpty()) {

- 15 try {

- 16 lock.wait();

- 17 } catch (InterruptedException e) {

- 18 assert (false);

- 19 }

- 20 }

- 21 current = queue.removeFirst();

- 22 }

- 23 System.out.println(current);

- 24 }

- 25 public static void main(String[] args) {

- 26 final PrintQueue pq = new PrintQueue();

- 27 Thread producer1 = new Thread() {

- 28 public void run() {

- 29 pq.enqueue("anemone");

- 30 pq.enqueue("tulip");

- 31 pq.enqueue("cyclamen");

- 32 }

- 33 };

- 34 Thread producer2 = new Thread() {

- 35 public void run() {

- 36 pq.enqueue("iris");

- 37 pq.enqueue("narcissus");

- 38 pq.enqueue("daffodil");


- 39 }

- 40 };

- 41 Thread consumer1 = new Thread() {

- 42 public void run() {

- 43 pq.work();

- 44 pq.work();

- 45 pq.work();

- 46 pq.work();

- 47 }

- 48 };

- 49 Thread consumer2 = new Thread() {

- 50 public void run() {

- 51 pq.work();

- 52 pq.work();

- 53 }

- 54 };

- 55 producer1.start();

- 56 consumer1.start();

- 57 consumer2.start();

- 58 producer2.start();

- 59 }

- 60 }

- 61


运⾏测试以后，所有看起来都正常。作为类的开发者，您很可能感到⾮常满意：此测试看起来很有⽤ （两个 producer、两个 consumer 和它们之间的能试验 wait 的有趣顺序），并且它能正确地运 ⾏。 但是这⾥有⼀个我们提到的 bug。您看到了吗？如果没有看到，先等⼀下；我们将很快捕获它。

回⻚⾸

## 并⾏程序设计中的确定性

为什么这两个示例单元测试不能测试出并⾏ bug？虽然原则上线程调度程序可 以 在运⾏的中间切换线 程并以不同的顺序运⾏它们，但是它往往 不进⾏切换。因为在单元测试中的并⾏任务通常很⼩同时也 很少，在调度程序切换线程之前它们通常⼀直运⾏到结束，除⾮强迫它（也就是通过 wait() ）。并 且当它确 实 执⾏了线程切换时，每次运⾏程序时它往往都在同⼀个位置进⾏切换。

像我们前⾯所说的⼀样，问题在于程序是太 确 定 的 ：您只是在很多交错情况的⼀种交 错 （不同线程中 命令的相对顺序）中结束了测试。更多的交错在什么时候试验？当有更多的并⾏任务以及在并⾏类和 协议之间有更复杂的相互影响时，也就是当您运⾏系统测试和功能测试时 - 或当整个产品在⽤户的站 点运⾏时，这些地⽅将是暴露出 bug 的地⽅。

回⻚⾸

## 使⽤ ConTest进⾏单元测试

当进⾏单元测试时需要 JVM 具有低的确定性，同时是更“模糊的”。这就是要⽤到 ConTest 的地⽅。如 果使⽤ ConTest 运⾏⼏次 的 NakedNamePrinter ， 将得到各种结果，如清单 4 所示：

清单 2

- 清单 4. 使⽤ ConTest 的⽆修饰的 NamePrinter


<table>
  <tr>
    <th>>Washington Irving (the expected result)> WashingtonIrving (the space was printed first)>Irving</th>
  </tr>
</table>


Washington (surname + new-line printed first)> IrvingWashington (space, surname, first name)

注意不需要得到像上⾯那样顺序的结果或相继顺序的结果；您可能在看到后⾯的两个结果之前先看到 ⼏次前⾯的两个结果。但是很快，您将看到所有的结果。ConTest 使各种交错情况出现；由于随机地 选择交错，每次运⾏同⼀个测试时都可能产⽣不同的结果。相⽐较的是，如果使⽤ ConTest 运⾏如

清 单 1

所示的 NamePrinter ，您将总是得到预期的结果。在此情况下，同步协议强制以正确的顺序执 ⾏，所以 ConTest 只是⽣成合 法 的 交错。 如果您使⽤ ConTest 运⾏ PrintQueue ，您将得到不同顺序的结果，这些对于单元测试来说可能是可 接受的结果。但是运⾏⼏次以后，第 24 ⾏的 LinkedList.removeFirst() 会突然抛 出 NoSuchElementException 。bug 潜藏在如下的情形中：

- 1.
- 2.
- 3.
- 4.


启动了两个 consumer 线程，发现队列是空的，执⾏ wait() 。 ⼀个 producer 把任务放⼊队列中并通知两个 consumer。 ⼀个 consumer 获得锁，运⾏任务，并把队列清空。然后它释放锁。 第⼆个 consumer 获得锁（因为通知了它所以它可以继续向下进⾏）并试图运⾏任务，但是现在 队列是空的。

这虽然不是此单元测试的常⻅交错，但上⾯的场景是合法的并且在更复杂地使⽤类的时候可能发⽣这 种情况。使⽤ ConTest 可以使它在单元测试中发⽣。（顺便问⼀下，您知道如何修复 bug 吗？注意： ⽤ notify() 取代 notifyAll() 能解决此情形中的问题，但是在其他情形中将会失败！）

回⻚⾸

ConTest的⼯作⽅式

ConTest 背后的基本原理是⾮常简单的。instrumentation 阶段转换类⽂件，注⼊挑选的⽤来调⽤ ConTest 运⾏时函数的位置。在运⾏时，ConTest 有时试图在这些位置引起上下⽂转换。 挑选的是线 程的相对顺序很可能影响结果的那些位置：进⼊和退出 synchronized 块的位置、访问共享变量的位置 等等。通过调⽤诸如 yield() 或 sleep() ⽅法来尝试上下⽂转换。决定是随机的以便在每次运⾏ 时尝试不同的交错。使⽤试探法试图显示典型的 bug。

注意 ConTest 不知道实际是否已经显示出 bug- 它没有预期程序将如何运⾏的概念。是您，也就是⽤ 户应该进⾏测试并且应该知道哪个测试结果将被认为是正确的以及哪个测试结果表示 bug。ConTest 只是帮助显示出 bug。另⼀⽅⾯，没有错误警报：就 JVM 规则⽽⾔所有使⽤ ConTest 产⽣的交错都是 合法的。 正如您看到的⼀样，通过多次运⾏同⼀个测试得到了多个值。实际上，我们推荐整个晚上都反复运⾏ 它。然后您就可以很⾃信地认为所有可能的交错都已经执⾏过了。

回⻚⾸

## ConTest的特性

除了它的基本的⽅法之外，ConTest 在显示并⾏ bug ⽅⾯引⼊了⼏个主要特性：

同步覆盖：在单元测试中极⼒推荐测量代码覆盖，但是在测试并⾏程序时使⽤它，代码覆盖容易产 ⽣误导。在前两个例⼦中，⽆修饰的 NamePrinter 和多 bug 的 Print Queue，给出的单元测试显示 完整的语句覆盖（ 除了 InterruptedException 处理）没有显示出 bug。 同步覆盖弥补了此缺 陷：它测量在 synchronized 块之间存在多少竞争；也就是说，是否它们做了“有意义的”事情，您是 否覆盖了有趣的交错。有关附加信息请参⻅ 参考资料 。

死锁预防： ConTest 可以分析是否以冲突的顺序嵌套地拥有锁，这表明有死锁的危险。此分析是在 运⾏测试后离线地进⾏。

调试帮助：ConTest 可以⽣成⼀些对并⾏调试有⽤的运⾏时报告：关于锁的状态的报告（哪个线程 拥有哪个锁，哪个线程处于等待状态等等），当前的线程的位置的报告和关于最后分配给变量和从 变量读取的值的报告。您也可以远程进⾏这些查询；例如，您可以从不同的机器上查询服务器（运 ⾏ ConTest）的状态。另⼀个对调试有⽤的特性可能是重 放 ，它试图重复⼀个给定运⾏的交错（不 能保证，但是有很⾼的可能性）。

UDP ⽹络混乱：ConTest ⽀持通过 UDP（数据报）套接字进⾏⽹络通信的域中的并⾏混乱的概 念。 UDP 程序不能依靠⽹络的可靠性；分组可能丢失或重新排序，它依靠应⽤程序处理这些情 况。与多线程相似，这带来对测试的挑战：在正常环境中，分组往往是按正确的顺序到达，实际上 并没有测试混乱处理功能。ConTest 能够模拟不利的⽹络状况，因此能够运⽤此功能并显示它的 bug。

回⻚⾸

## 挑战与未来⽅向

ConTest 是为 Java 平台创建的。⽤于 pthread 库的 C/C+ 版本的 ConTest 在 IBM 内部使⽤，但是不 包含 Java 版的所有特性。出于两种原因，⽤ ConTest 操作 Java 代码⽐操作 C/C+ 代码简单：同步是 Java 语⾔的⼀部分，并且字节码⾮常容易使⽤。我们正在开发⽤于其他库的 ConTest，例如 MPI 库。 如果您想要使⽤ C/C+ 版的ConTest，请与作者联系。硬实时软件对于 ConTest 也是⼀个问题，因为 ⼯具是通过增加延迟⽽⼯作。为使⽤ ConTest，我们正在研究与监视硬实时软件相似的⽅法，但是在 ⽬前我们还不能确定如何克服此问题。

#### ⾄于将来的⽅向，我们正在研究发布⼀种 监 听 器 体系结构，它将允许我们在 ConTest 上应⽤基于监听 器的⼯具。使⽤监听器体系结构将使创建原⼦数检查器、死锁侦听器和其他分析器以及尝试不必写⼊ 有关的基础设施的新的延迟机制成为可能。

