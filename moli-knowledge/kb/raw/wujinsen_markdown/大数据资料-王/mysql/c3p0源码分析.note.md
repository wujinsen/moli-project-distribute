数据源 ⼊⼝类:ComboPoledDataSource

![image 1](<c3p0源码分析.note_images/imageFile1.png>)

getConection⽅法在AbstractPolBackedDataSource类中实现。 获取连接 触发机制： 应⽤主动调⽤。 调⽤过程：

- 1、 AbstractPolBackedDataSource类getConection⽅ 法 public Conection getConection() throwsSQLException { PoledConectionpc = getPolManager().getPol().checkoutPoledConection(); returnpc.getConection(); }


接是newProxyConection,不是物理连接得到连。 说明： PoledConection

包名是javax.sql,是sun针对连接池的接⼝，它本身包含conection,和这个conection相关的所有 statement,result,⼀个checkout的conection所做的所有数据库操作，都被poledconection所管 理。 c3p0默认的实现是NewPoledConection

Conection,Statement,Result

操作数据库相关接⼝，在c3p0中对应于 NewProxyConection,NewProxyStatement,NewProxyResultSet,这些东⻄统⼀被PoledConection 管理。

- 2、 C3P0PoledConectionPol类 publicPoledConectioncheckoutPoledConection() return(PoledConection)this.rp.checkoutResource(this.checkoutTimeout);
- 3、 BasicResourcePol类 public Object checkoutResource(long timeout)


- 1) 关键步骤代码：Object resc = prelimCheckoutResource(timeout); 查看池中是否有未使⽤的conection,有就返回（还要判断是否空闲、是否过期）；没有，如果没有达 到最⼤数，就⽣成⼀个，或者就等待。
- 2) 关键步骤代码： boleanrefurb = atemptRefurbishResourceOnCheckout(resc); 得到连接后，检测连接的可⽤性。
- 3) 连接可⽤，接着判断连接是否处于管理中，不在就再调⽤本⽅法获取⼀个，在就返回本 连接。 添加连接 触发机制： 1、 获取连接时，BasicResourcePol类prelimCheckoutResource⽅法调⽤。 调⽤过程：


- 1、 BasicResourcePol类prelimCheckoutResource⽅法当可⽤连接数为0时调⽤ recheckResizePol⽅法。
- 2、 _recheckResizePol⽅法判断需要扩容则调⽤expandPol⽅法
- 3、 expandPol⽅法使⽤线程池调⽤AcquireTask(实现Runable接⼝)任务进⾏扩 容。 4、 AcquireTask调⽤mgr.acquireResource⽅法获取连接，如果连接池⾥的连接未到最


⼤数量将获取的连接加⼊管理和未使⽤的list中，否则销毁连接。 关闭（回收）连接 触发机制： 应⽤程序调⽤连接的close⽅法。 调⽤过程：

- 1、 C3P0PoledConectionPol类⾥的ConectionEventListenerImpl监听器类的 conectionClosed⽅法被触发。
- 2、 判断同步还是异步（默认是同步）。异步就采⽤线程池来处理回收，同步就直接调⽤ BasicResourcePol类的checkinResource⽅法对连接进⾏回收，注意是回收不是关闭物 理连接。 死锁 C3p0很多操作都是多线程来处理的（如：添加连接，回收连接等），所以进⾏必要的死锁检测。 由ThreadPolAsynchronousRuner类的⼦类DeadlockDetector来执⾏具体的操作。


![image 2](<c3p0源码分析.note_images/imageFile2.png>)

DeadlockDetector类继承了TimerTask，由Timer来调度DeadlockDetecto运⾏。 DeadlockDetector类 有2个属性： LinkedListlast = nul; LinkedListcurent = nul; 这2个list放的是待执⾏的任务，根据属性名就知道last是上次检测时待执⾏的任务，curent是这次检测 时要执⾏的任务，当pendingTasks（当前要执⾏的任务，克隆给curent）不为空且 curent.equals( last )时认为死锁发⽣。 死锁发⽣后，当前任务交给ThreadPerTaskAsynchronousRuner类来处理,这个类会启⽤新的线程 DispatchThread来处理当前的任务队列。 ⼩结 c3p0是现在⽤的⽐较⼴泛的连接池之⼀，获取连接的效率有待提⾼，使⽤jdk1.6之前的可以考虑

