#!/bin/bash while true; do

- A=`ps -ef|grep tomcat |wc -l`
- B=`ps -ef|grep kepalived |wc -l` echo $A


- if [ $A -eq 1 ];then echo 'restart tomcat !' /usr/local/server/apache-tomcat-6.0.37/bin/startup.sh d

- if [ $A -eq 1 ];then
- if [ $B -gt 1 ];then echo 'tomcat dead ! kil kepalived' kilal kepalived


fi fi

fi

- if [ $A -eq 2 ];then if [ $B -eq 1 ];then


echo 'tomcat live ! start kepalived' service kepalived start

fi

fi sl ep 3 done

