⽬标：在Windows环境下，⽤netstat命令查看某个端⼝号是否占⽤，为哪个进程所占⽤. 操作：操作分为两步：（1）查看该端⼝被那个PID所占⽤;⽅法⼀：有针对性的查看端⼝，使⽤命令 Netstat –ano|findstr “<端⼝号>”,如图，最后⼀列为PID。图中的端⼝号为1068，所对应的PID为 3840。

(a)图 ⽅法⼆：查看所有的，然后找到对应的端⼝和PID。

- (b)图 第⼀幅图中的5列就是上⾯(a)图对应的5列 （2）查看该PID对应的进程名称。 ⽅法⼀：⼀直⽤命令查找，tasklist|findstr “<PID号>”
- (c)图 从 (c)图 可以看出，PID为3840所对应的进程名字为msnmsgr.exe。 ⽅法⼆：⽤任务管理器查看。 调出任务管理器，选择列，如d图。
- (d)图

查看PID对应的进程名称。如(e)图中的msnmsgr.exe

- (e)图


附录：在命令⾏中输⼊netstat /? 可以查看netstat的相关信息。 C:\Documents and Settings\Administrator>netstat /? 显示协议统计信息和当前 TCP/IP ⽹络连接。 NETSTAT [-a] [-b] [-e] [-n] [-o] [-p proto] [-r] [-s] [-t] [-v] [interval]

- -a 显示所有连接和监听端⼝。
- -b 显示包含于创建每个连接或监听端⼝的 可执⾏组件。在某些情况下已知可执⾏组件 拥有多个独⽴组件，并且在这些情况下 包含于创建连接或监听端⼝的组件序列 被显示。这种情况下，可执⾏组件名 在底部的 [] 中，顶部是其调⽤的组件， 等等，直到 TCP/IP 部分。注意此选项 可能需要很⻓时间，如果没有⾜够权限 可能失败。
- -e 显示以太⽹统计信息。此选项可以与 -s 选项组合使⽤。
- -n 以数字形式显示地址和端⼝号。
- -o 显示与每个连接相关的所属进程 ID。
- -p proto 显示 proto 指定的协议的连接；proto 可以是


- 下列协议之⼀: TCP、UDP、TCPv6 或 UDPv6。 如果与 -s 选项⼀起使⽤以显示按协议统计信息，proto 可以是下列协议之⼀: IP、IPv6、ICMP、ICMPv6、TCP、TCPv6、UDP 或 UDPv6。
- -r 显示路由表。
- -s 显示按协议统计信息。默认地，显示 IP、 IPv6、ICMP、ICMPv6、TCP、TCPv6、UDP 和 UDPv6 的统计信息；

-p 选项⽤于指定默认情况的⼦集。

- -t 显示当前连接卸载状态。
- -v 与 -b 选项⼀起使⽤时将显示包含于 为所有可执⾏组件创建连接或监听端⼝的 组件。


interval 重新显示选定统计信息，每次显示之间 暂停时间间隔(以秒计)。按 CTRL+C 停⽌重新 显示统计信息。如果省略，netstat 显示当前 配置信息(只显示⼀次)

