Alt + / 这个快捷键还是很有⽤的，⽐如HttpServletResponse不知如何拼写，只要打H，然后Alt + / 就可以列出H开头的类了。

如果Alt + / 或对象后输⼊“.”没有反应，那就要查看⾸选项设置了。原因,MyEclipse把alt+/分配给 了单词提示,还有代码提示的快捷代码也不是alt+/,因此要恢复代码提示⽤alt+/.需要做两件事。

⾸先看代码提示的Keys 设置正确没有：

- 1,windows-->preference-->General-->keys 下设置Content Assist 的快捷键：Alt + /

- 2,windows-->preference-->General-->keys 下设置wordcomletion 的快捷键改成其它的……


如果还是不提示有可能是提示功能被关了. 如下做：

- 1、菜单window->Preferences->Java->Editor->Content Assist，Enable auto activation 选项要打 上勾。

- 2、 window->Preferences->Java->Editor->Content Assist->Advanced 上⾯的选项卡Select the proposal kinds contained in the 'default' content assist list: 中把 Other Java Proposals 选项打上 勾就可以了。


另⼀篇⽂章：

MyEclipse安装完成之后，有⼀个很常⽤的快捷键Content Assist（即代码提示）被默认为Ctrl + Space。

这会使得我们使⽤中⽂操作系统的计算机⽤起来很不⽅便，因为我们平时打字必⽤的切换打字法 的快捷键，恰恰就是这个！

当 然，你可以直接在Windows => Preferences... => General => Keys去替换掉这个快捷键，不 过可能还会有其他快捷键与我们的中⽂操作系统有冲突。这⾥介绍⼀个⼀劳永逸，也是更“Eclipse 化”的⽅法来改变这种 尴尬局⾯：

在MyEclipse的启动选项中，其实有Duser.language这样⼀项设置，在MyEclipse安装完之后，它的 默认值是等于en的，只要把它改成zh，重启MyEclipse之后，即可将Content Assist的快捷键改成Alt

+ /。

具体修改⽅式如下：

【⽅法⼀】右击MyEclipse快捷⽅式，选择属性，打开属性对话框，如下：

将 -Duser.language=en 改为 -Duser.language=zh；

【⽅法⼆】在Eclipse安装⽬录下，有⼀个eclipse.ini的配置⽂件，⽤⽂本编辑器打开它，找到Duser.language=en这样⼀⾏，将它改为-Duser.language=zh即可：

改完之后，可以到Edit => Content Assist菜单下或快捷键设置⾯板中看到新的快捷键：

这样，就通过更改MyEclipse的默认系统语⾔，来达到更改使⽤习惯（如快捷键）的⽬的a

