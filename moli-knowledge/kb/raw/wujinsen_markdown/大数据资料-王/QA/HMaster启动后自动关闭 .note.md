好久没有来写博客了，总算是忙完了，今天回到阔别已久的CU。⼀早来到单位，就开始着⼿调试新测 试镜像。但是⼀启动就出了问题，原先调试好的分布式平台却提示了错误： Zokeper available but no active master location found 直观的感觉是HMaster的问题，果然，JPS查看发现没有了 HMaster进程，进⼊到hbase-master⽇志中查看，发现了以下错误提示： Could not obtain block: blk_number. .file=/hbase/hbase.version ⽆法访问数据块的原因⽆⾮有两个：⼀是该数据块不存 在；⼆是该数据块没有权限。⾃⼰去HDFS下查看发现了/hbase⽬录，也有hbase.version⽂件，虽然该 ⽂件⼤⼩为0kb。于是⾃⼰⾸先想到是权限问题，接下来开始为/hbase修改权限： %hadop fs chmod 7 /hbase %hadop fs -chmod -R 7 /hbase （修改⽬录权限） 但是试过之后结果 依旧。这时⾃⼰确定HMaster⾃动关闭的问题不是因为⽬录权限拒绝访问，那么是什么呢？之前也发⽣ 过HMaster启动后⾃动关闭的问题，⾃⼰当时的解决办法是格式化namenode即可： %hadop namenode -format 但是这次试过之后仍旧不成功，于是⾃⼰考虑会不会是由于分布式环境下不同节 点的hdfs的重复⼯作导致的不⼀致使得HMaster⽆法正常启动呢？抱着这样的想法删掉了各个节点和 master上的hdfs数据，在master上重新启动hbase结果成功，HMaster不再⾃动关闭。 这时我们需 要重新复制⽣成HDFS⼲净的HDFS： %rm -Rf reports/data %hadop fs -copyFromLocal reports /texaspete/templates/reports %hadop fs -put match/src/main/resources/regexes /texaspete/regexes⼩结： HMaster启动后⾃动关闭可能有多种原因，按照⾃⼰的经验，可以试着尝 试以下⽅法：1. 重新格式化namenode，重启HMaster看问题是否依旧存在；2. 检查/hbase⽬录的hdfs 权限设置是否有问题；3. 清楚各个节点上的hbase数据，然后重新启动hbase，⾃⼰猜测是各节点状态 不⼀致导致的HMaster⽆法启动问题。 当然，应该还会有其他的情况，随着学习的深⼊慢慢来积累 总结吧！

