在使⽤secureCRT的linux服务器时候，很多时候需要安装软件，⽽服务器本身是没有连接 外⽹的 ，这时候就需要⽤到rz命令了。 在使⽤rz命令时候，有时候上传⽂件会失败，是因为上传的⽂件流中包含某新特殊控制字 符，造成rz提前结束退出。 这时候需要使⽤参数 -be 其中-b是 -binary⽤⼆进制的⽅式上传，

-e是 -escape强制escape所有控制字符 这样的话⼀般可以上传成功。 ⽽在上传libxml2的tar.gz⽂件时候虽然能上传成功，但是压缩包却莫名的缩⼩，然后解压缩 会失败， 后来发现在使⽤了rz -be命令后，在弹出的窗⼝中要去掉upload files as ASCI选项，然后就 上传⽂件成功了

总结:

# 使⽤rz命令时，添加 -be选项并且不勾选Upload files as ASCI选 项，这样就可以确保上传⽂件的正确上传

