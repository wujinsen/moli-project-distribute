标记-清除算法（Mark-Swep）

- 1、标记出所有需要回收的对象，在标记完成后统⼀回收所有被标记的对象
- 2、在标记完成后统⼀回收所有被标记的对象


![image 1](<垃圾收集算法.note_images/imageFile1.png>)

缺点：⼀个是效率问题，标记和清除两个过程的效率都不⾼； 另⼀个是空间问题，标记清除之后会产⽣⼤量不连续的内存碎⽚，空间碎⽚太多可能会导致以后在程 序运⾏过程中 需要分配较⼤对象时，⽆法找到⾜够的连续内存⽽不得不提前触发另⼀次垃圾收集动作。

复制算法（Copying）

- 1、将可⽤内存按容量划分为⼤⼩相等的两块，每次只使⽤其中的⼀块。
- 2、当这⼀块的内存⽤完了，就将还存活着的对象复制到另外⼀块上⾯，然后再把已使⽤过的内存空间 ⼀次清理掉。


![image 2](<垃圾收集算法.note_images/imageFile2.png>)

优点：这样使得每次都是对整个半区进⾏内存回收，内存分配时也就不⽤考虑内存碎⽚等 复杂情况，只要移动堆顶指针，按顺序分配内存即可，实现简单，运⾏⾼效。只是这种算法的代价是 将内存缩⼩为 了原来的⼀半，未免太⾼了⼀点。 缺点：复制收集算法在对象存活率较⾼时就要进⾏较多的复制操作，效率将会变低

标记-整理算法（Mark-Compact）

- 1、标记
- 2、让所有存活的对象都向⼀端移动，然后直接清理掉端边界以外的内存


![image 3](<垃圾收集算法.note_images/imageFile3.png>)

分代收集算法（Generational Colection）

- 1、根据对象存活周期的不同将内存划分为⼏块。
- 2、⼀般是把Java堆分为新⽣代和⽼年代，这样就可以根据各个年代的特点采⽤最适当的收集算法。
- 3、在新⽣代中，每次垃圾收集时都发现有⼤批对象死去，只有少量存活，那就选⽤复制算法，只需要 付出少量存活对象的复制成本就可以完成收集。
- 4、⽼年代中因为对象存活率⾼、没有额外空间对它进⾏分配担保，就必须使⽤“标记—清理”或者“标记


—整理”算法来进⾏回收。

