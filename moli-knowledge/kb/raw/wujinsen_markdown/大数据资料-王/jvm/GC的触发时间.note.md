GC的回收是⾃动的，那么什么时候GC才会做回收呢？

在JDK1.6中，CMS收集器的启动阈值时92%，也就是当⽼年代的内存达到92%时触发GC 其他的收集器，等到⽼年代⼏乎被填满在进⾏GC 在GC阶段，有个很重要的概念，safepoint点，在safepoint会执⾏GC 还有个概念是safe region，在saferegion中，随时GC

其中涉及到的3个重要的特点：

枚举根节点

1、可作为GC Rots的节点主要在全局性的引⽤（例如常量或类静态属性）与执⾏上下⽂（例如栈帧 中的本地变量表）中，

现在很多应⽤仅仅⽅法区就有数百兆，如果要逐个检查这⾥⾯的引⽤，那么必然会消耗很多时 间。

2、另外，可达性分析对执⾏时间的敏感还体现在GC停顿上，因为这项分析⼯作必须在⼀个能确保 ⼀致性的快照中 进⾏⸺这⾥“⼀致性”的意思是指在整个分析期间整个执⾏系统看起来就像被冻结在某个时间点上，不 可以出现 分析过程中对象引⽤关系还在不断变化的情况，该点不满⾜的话分析结果准确性就⽆法得到保证。这 点是导致GC进 ⾏时必须停顿所有Java执⾏线程（Sun将这件事情称为“Stop The World”）的其中⼀个重要原因，即使 是在号称 （⼏乎）不会发⽣停顿的CMS收集器中，枚举根节点时也是必须要停顿的。 由于⽬前的主流Java虚拟机使⽤的都是准确式GC（这个概念在第1章介绍Exact VM对Clasic VM的改 进时讲 过），所以当执⾏系统停顿下来后，并不需要⼀个不漏地检查完所有执⾏上下⽂和全局的引⽤位置， 虚拟机应当是 有办法直接得知哪些地⽅存放着对象引⽤。在HotSpot的实现中，是使⽤⼀组称为OopMap的数据结构 来达到这个⽬ 的的，在类加载完成的时候，HotSpot就把对象内什么偏移量上是什么类型的数据计算出来，在JIT编 译过程中，也 会在特定的位置记录下栈和寄存器中哪些位置是引⽤。这样，GC在扫描时就可以直接得知这些信息 了。下⾯的代码 清单3-3是HotSpot Client VM⽣成的⼀段String.hashCode（）⽅法的本地代码，可以看到在 0x026eb7a9处的cal 指令有OopMap记录，它指明了EBX寄存器和栈中偏移量为16的内存区域中各有⼀个普通对象指针 （Ordinary Object

Pointer）的引⽤，有效范围为从cal指令开始直到0x026eb730（指令流的起始位置）+142（OopMap 记录的偏移 量）=0x026eb7be，即hlt指令为⽌。

- 代码清单3-3 String.hashCode（）⽅法编译后的本地代码 [Verified Entry Point] 0x026eb730：mov%eax，-0x8 0（%esp）


… ；ImplicitNulCheckStub slow case 0x026eb7a9：cal 0x026e83e0 ；OopMap{ebx=Oop[16]=Oop of=142} ；*caload ；-java.lang.String： （line 1489） ；{runtime_cal} 0x026eb7ae：push$0x83c5c18 ；{external_word} 0x026eb7b3：cal 0x026eb7b8

hashCode@48

- 0x026eb7b8：pusha
- 0x026eb7b9：cal 0x082bec0；{runtime_cal} 0x026eb7be：hlt


- 3.4.2 安全点 在OopMap的协助下，HotSpot可以快速且准确地完成GC Rots枚举，但⼀个很现实的问题随之


⽽来：可能导致 引⽤关系变化，或者说OopMap内容变化的指令⾮常多，如果为每⼀条指令都⽣成对应的OopMap，那 将会需要⼤量的 额外空间，这样GC的空间成本将会变得很⾼。

实际上，HotSpot也的确没有为每条指令都⽣成OopMap，前⾯已经提到，只是在“特定的位置”记 录了这些信 息，这些位置称为安全点（Safepoint），即程序执⾏时并⾮在所有地⽅都能停顿下来开始GC，只有在 到达安全点 时才能暂停。Safepoint的选定既不能太少以致于让GC等待时间太⻓，也不能过于频繁以致于过分增⼤ 运⾏时的负 荷。所以，安全点的选定基本上是以程序“是否具有让程序⻓时间执⾏的特征”为标准进⾏选定的⸺因 为每条指 令执⾏的时间都⾮常短暂，程序不太可能因为指令流⻓度太⻓这个原因⽽过⻓时间运⾏，“⻓时间执 ⾏”的最明显

特征就是指令序列复⽤，例如⽅法调⽤、循环跳转、异常跳转等，所以具有这些功能的指令才会产⽣ Safepoint。 对于Sefepoint，另⼀个需要考虑的问题是如何在GC发⽣时让所有线程（这⾥不包括执⾏JNI调⽤的线 程）都“跑”到最近的安全点上再停顿下来。这⾥有两种⽅案可供选择：抢先式中断 （Premptive Suspension）和主 动式中断（Voluntary Suspension），其中抢先式中断不需要线程的执⾏代码主动去配合，在GC发⽣ 时，⾸先把所 有线程全部中断，如果发现有线程中断的地⽅不在安全点上，就恢复线程，让它“跑”到安全点上。现 在⼏乎没有 虚拟机实现采⽤抢先式中断来暂停线程从⽽响应GC事件。 ⽽主动式中断的思想是当GC需要中断线程的时候，不直接对线程操作，仅仅简单地设置⼀个标志，各 个线程执 ⾏时主动去轮询这个标志，发现中断标志为真时就⾃⼰中断挂起。轮询标志的地⽅和安全点是重合 的，另外再加上 创建对象需要分配内存的地⽅。下⾯代码清单3-4中的test指令是HotSpot⽣成的轮询指令，当需要暂 停线程时，虚 拟机把0x16010的内存⻚设置为不可读，线程执⾏到test指令时就会产⽣⼀个⾃陷异常信号，在预先 注册的异常处 理器中暂停线程实现等待，这样⼀条汇编指令便完成安全点轮询和触发线程中断。

- 代码清单3-4 轮询指令 0x01b6d627：cal 0x01b2b210；OopMap{[60]=Oop of=460} ；*invokeinterface size ；-Client1： （line 23） ；{virtual_cal} 0x01b6d62c：nop ；OopMap{[60]=Oop of=461} ；*if_icmplt ；-Client1： （line 23）


main@13

main@18

- 0x01b6d62d：test%eax，0x16010；{pol}
- 0x01b6d63：mov 0x50（%esp），%esi 0x01b6d637：cmp%eax，%esi


- 3.4.3 安全区域 使⽤Safepoint似乎已经完美地解决了如何进⼊GC的问题，但实际情况却并不⼀定。Safepoint机制保 证了程序


执⾏时，在不太⻓的时间内就会遇到可进⼊GC的Safepoint。但是，程序“不执⾏”的时候呢？所谓的程 序不执⾏ 就是没有分配CPU时间，典型的例⼦就是线程处于Sl ep状态或者Blocked状态，这时候线程⽆法响应 JVM的中断请 求，“⾛”到安全的地⽅去中断挂起，JVM也显然不太可能等待线程重新被分配CPU时间。对于这种情 况，就需要安 全区域（Safe Region）来解决。 安全区域是指在⼀段代码⽚段之中，引⽤关系不会发⽣变化。在这个区域中的任意地⽅开始GC都是安 全的。我 们也可以把Safe Region看做是被扩展了的Safepoint。 在线程执⾏到Safe Region中的代码时，⾸先标识⾃⼰已经进⼊了Safe Region，那样，当在这段时间⾥ JVM要 发起GC时，就不⽤管标识⾃⼰为Safe Region状态的线程了。在线程要离开Safe Region时，它要检查 系统是否已经 完成了根节点枚举（或者是整个GC过程），如果完成了，那线程就继续执⾏，否则它就必须等待直到 收到可以安全 离开Safe Region的信号为⽌。 到此，笔者简要地介绍了HotSpot虚拟机如何去发起内存回收的问题，但是虚拟机如何具体地进⾏内存 回收动 作仍然未涉及，因为内存回收如何进⾏是由虚拟机所采⽤的GC收集器决定的，⽽通常虚拟机中往往不 ⽌有⼀种GC收 集器。下⾯继续来看HotSpot中有哪些GC收集器。

