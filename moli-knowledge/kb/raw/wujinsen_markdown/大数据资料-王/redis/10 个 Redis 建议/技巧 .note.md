Redis 在当前的技术社区⾥是⾮常热⻔的。从来⾃ Antirez ⼀个⼩⼩的个⼈项⽬到成为内存数据存储⾏ 业的标准，Redis已经⾛过了很⻓的⼀段路。随之⽽来的⼀系列最佳实践，使得⼤多数⼈可以正确地使 ⽤ Redis。下⾯我们将探索正确使⽤ Redis 的10个技巧。

- 1、停⽌使⽤ KEYS * Okay，以挑战这个命令开始这篇⽂章，或许并不是⼀个好的⽅式，但其确实可能是最重要的⼀点。很 多时候当我们关注⼀个redis实例的统计数据，我们会快速地输⼊”KEYS *”命令，这样key的信息会很明 显地展示出来。平⼼⽽论，从程序化的⻆度出发往往倾向于写出下⾯这样的伪代码：

但是当你有130万个key时，执⾏速度将会变慢。因为KEYS命令的时间复杂度是O(n)，其中n是要返 回的keys的个数，这样这个命令的复杂度就取决于数据库的⼤⼩了。并且在这个操作执⾏期间，其它 任何命令在你的实例中都⽆法执⾏。 作为⼀个替代命令，看⼀下 吧，其允许你以⼀种更友好的⽅式来执⾏… SCAN 通过增量迭代的 ⽅式来扫描数据库。这⼀操作基于游标的迭代器来完成的，因此只要你觉得合适，你可以随时停⽌或 继续。

- 2、找出拖慢 Redis 的罪魁祸⾸ 由于 Redis 没有⾮常详细的⽇志，要想知道在 Redis 实例内部都做了些什么是⾮常困难的。幸运的是 Redis 提供了⼀个下⾯这样的命令统计⼯具：

通过这个⼯具可以查看所有命令统计的快照，⽐如命令执⾏了多少次，执⾏命令所耗费的毫秒数(每个 命令的总时间和平均时间) 只需要简单地执⾏ CONFIG RESETSTAT 命令就可以重置，这样你就可以得到⼀个全新的统计结果。

- 3、 将 Redis-Benchmark 结果作为参考，⽽不要⼀概⽽论 Redis 之⽗ Salvatore 就说过：“通过执⾏GET/SET命令来测试Redis就像在⾬天检测法拉利的⾬刷清洁 镜⼦的效果”。很多时候⼈们跑到我这⾥，他们想知道为什么⾃⼰的Redis-Benchmark统计的结果低于 最优结果 。但我们必须要把各种不同的真实情况考虑进来，例如：


<table>
  <tr>
    <th>1<br>2<br></th>
    <th>for key in 'keys *' : doAllTheThings()<br><br></th>
  </tr>
</table>


- 3


SCAN

<table>
  <tr>
    <th>1<br>2<br>3<br>4<br>5<br>6<br><br><br></th>
    <th>127.0.0.1:6379> INFO commandstats # Commandstats cmdstat_get:calls=78,usec=608,usec_<br><br>per_call=7.79<br><br>cmdstat_setex:calls=5,usec=71,usec_ per_call=14.20<br><br>cmdstat_keys:calls=2,usec=42,usec_p er_call=21.00<br><br>cmdstat_info:calls=10,usec=1931,use c_per_call=193.10</th>
  </tr>
</table>


可能受到哪些客户端运⾏环境的限制？

是同⼀个版本号吗？

测试环境中的表现与应⽤将要运⾏的环境是否⼀致？

Redis-Benchmark的测试结果提供了⼀个保证你的 Redis-Server 不会运⾏在⾮正常状态下的基准点， 但是你永远不要把它作为⼀个真实的“压⼒测试”。压⼒测试需要反应出应⽤的运⾏⽅式，并且需要⼀ 个尽可能的和⽣产相似的环境。

- 4、Hashes 是你的最佳选择 以⼀种优雅的⽅式引⼊ hashes 吧。hashes 将会带给你⼀种前所未有的体验。之前我曾看到过许多类 似于下⾯这样的key结构：

上⾯的例⼦中，fo 可能是⼀个⽤户的⽤户名，其中的每⼀项都是⼀个单独的 key。这就增加了 犯错的 空间，和⼀些不必要的 key。使⽤ hash 代替吧，你会惊奇地发现竟然只需要⼀个 key ：

- 5、设置 key 值的存活时间 ⽆论什么时候，只要有可能就利⽤key超时的优势。⼀个很好的例⼦就是储存⼀些诸如临时认证key之 类的东⻄。当你去查找⼀个授权key时⸺以OAUTH为例⸺通常会得到⼀个超时时间。这样在设置 key的时候，设成同样的超时时间，Redis就会⾃动为你清除！⽽不再需要使⽤KEYS *来遍历所有的key 了，怎么样很⽅便吧？
- 6、 选择合适的回收策略 既然谈到了清除key这个话题，那我们就来聊聊回收策略。当 Redis 的实例空间被填满了之后，将会尝 试回收⼀部分key。根据你的使⽤⽅式，我强烈建议使⽤ volatile-lru 策略⸺前提是你对key已经设置 了超时。但如果你运⾏的是⼀些类似于 cache 的东⻄，并且没有对 key 设置超时机制，可以考虑使⽤ alkeys-lru 回收机制。我的建议是先在 查看⼀下可⾏的⽅案。
- 7、如果你的数据很重要，请使⽤ Try/Except


<table>
  <tr>
    <th>1<br>2<br></th>
    <th>foo:first_name foo:last_name foo:address<br><br></th>
  </tr>
</table>


3

<table>
  <tr>
    <th>1<br>2<br>3<br>4<br>5<br>6<br>7<br>8<br>9<br><br><br>0<br>1<br><br><br>12</th>
    <th>127.0.0.1:6379> HSET foo first_name "Joe" (integer) 1 127.0.0.1:6379> HSET foo last_name<br><br>"Engel" (integer) 1 127.0.0.1:6379> HSET foo address "1 Fanatical Pl" (integer) 1 127.0.0.1:6379> HGETALL foo<br><br>1) "first_name"<br><br>2) "Joe"<br><br>3) "last_name"<br><br>4) "Engel"<br><br>5) "address"<br><br>6) "1 Fanatical Pl" 127.0.0.1:6379> HGET foo<br><br><br>first_name "Joe"<br><br></th>
  </tr>
</table>


这⾥

如果必须确保关键性的数据可以被放⼊到 Redis 的实例中，我强烈建议将其放⼊ try/except 块中。⼏ 乎所有的Redis客户端采⽤的都是“发送即忘”策略，因此经常需要考虑⼀个 key 是否真正被放到 Redis 数据库中了。⾄于将 try/expect 放到 Redis 命令中的复杂性并不是本⽂要讲的，你只需要知道这样做 可以确保重要的数据放到该放的地⽅就可以了。

- 8、不要耗尽⼀个实例 ⽆论什么时候，只要有可能就分散多redis实例的⼯作量。从3.0.0版本开始，Redis就⽀持集群了。 Redis集群允许你基于key范围分离出部分包含主/从模式的key。完整的集群背后的“魔法”可以在 找 到。但如果你是在找教程，那 是⼀个再适合不过的地⽅了。如果不能选择集群，考虑⼀下命名空 间吧，然后将你的key分散到多个实例之中。关于怎样分配数据，在redis.io⽹站上有 精彩的评论。
- 9、内核越多越好吗？！ 当然是错的。Redis 是⼀个单线程进程，即使启⽤了持久化最多也只会消耗两个内核。除⾮你计划在⼀ 台主机上运⾏多个实例⸺希望只会是在开发测试的环境下！⸺否则的话对于⼀个 Redis 实例是不需 要2个以上内核的。
- 10、⾼可⽤ 到⽬前为⽌ Redis Sentinel 已经经过了很全⾯的测试，很多⽤户已经将其应⽤到了⽣产环境中（包括 ObjectRocket ）。如果你的应⽤重度依赖于 Redis ，那就需要想出⼀个⾼可⽤⽅案来保证其不会掉 线。当然，如果不想⾃⼰管理这些东⻄，ObjectRocket 提供了⼀个⾼可⽤平台，并提供7×24⼩时的技 术⽀持，有意向的话可以考虑⼀下。


这⾥ 这⾥

这篇

