要掌握redis的⽤法，配置⽂件的重要参数必须掌握，下⾯是笔者对各个参数的学习笔记，同时会针对 特殊参数的使⽤场景做简短说明。 include /path/to/other.conf 解释：include和c⾥⾯的include概念类似，可以把redis的的参数分散到其他⽂件，当运⾏多个redis实 例时，通过include⼀个comon的配置⽂件让所有redis-server共享，简化redis.conf的内容。 daemonize no 解释： 毋容置疑，这个参数指定redis是否以后台进程⽅式启动，默认不是，⽣产环境可以改成yes，另外， Redis是单线程的。 pidfile /var/run/redis.pid 解释： 当daemonize参数为yes时，后台进程⽅式启动后，redis会创建进程⽂件，这个参数是指定此⽂件的路 径。 port 6379 解释：redistcp 监听端⼝，客户端通过此端⼝与redis服务器交互。⾮集群模式下，redis只有这个监听 端⼝ tcp-backlog 51 解释：客户端连接队列值，这个值是socket⾥⾯listen函数的参数。如果服务器caps很⾼，需要把这个 参数改⼤些。 bind 127.0.0.1 解释：监听地址，默认是监听所有⽹卡，也可以指定监听⼀个或多个。多个的格式： bind 192.168.1.1 192.168.1.2 loglevel notice 解释： 指定log级别，默认是notice，包括debug,verbose ,notice, , warning. ⽣产环境建议⽤notice,测试阶段可以⽤debug或者verbose,debug的log最多。 Logfile 解释： log ⽂件指定，默认是空，打印到控制台，可指定路径,如/var/log/redis.log save 90 1 解释： 这个参数是redis持久化的⽀持，基于snapshot机制，定期执⾏持久化存储，⽣成rdb⽂件，save 命令 打开此配置，第⼀个参数指定多久（单位是秒）执⾏⼀次，第⼆个是在第⼀个参数指定的时间内执⾏ 多少次写操作才执⾏持久化操作。 dbfilename dump.rdb save⽣成的数据库⽂件名字 dir ./

数据库的⽬录路径,默认和server⼀个⽬录 slaveof <masterip> <masterport> 解释： Redis 主从复制功能，通过这个参数可以让⼀个redis启动时作为某个master的slave， Slaveof 第⼀个参数是master的ip，第⼆个参数是master的port. 也可以通过redis-cli控制台执⾏slaveof命令动态把某个redis作为另⼀个master的slave。 requirepas paswd 解释： 此redis的密码，如果开启，客户端连接时需要设置密码，另外⼀个作⽤是redis作为slave时，如果 master开启了此参数，slave需要设置masterauth参数的值为此参数(requirepas)的值, masterauth <master-pasword> 解释： 这个参数只有在当前redis作为slave时管⽤，⽤于slave和master之间的鉴权，如果master开启了鉴权 功能，则masterauth的意思是此slave和master通信时⽤这个密码。 另外，这个参数只有在master的redis.conf⽂件中开启了reqireuth参数时才被使⽤。 maxclients 1 0 解释：最⼤并发客户端数。同时存在1 0个tcp连接 maxmemory 解释：redis申请的最⼤内存，字节为单位，默认不限制。 apendonly yes 解释： Redis持久化第⼆种⽅式，AOF，默认开启，默认每秒执⾏⼀次持久化保存，也可以设置成实时保持， 这个⽐save更安全。 apendfilename "apendonly.aof" 解释：AOF⽅式下，保持的数据⽂件名字 apendfsync no 解释：执⾏aof的时机，可以为每秒（everysec），实时（always），不⼿动，交给操作系统执⾏flush （no） cluster-enabled yes 解释：开启当前redis的集群模式，3.0版本才出现，默认关闭。 cluster-config-file nodes-6379.conf 解释： 集群模式下，每个redis节点⽣成⼀个⾃⼰的集群配置⽂件，这个⽂件不需要⼈⼯修改，由redis⾃⼰维 护。 cluster-node-timeout 15 0 解释：

# 集群模式时，当前节点在与其他节点保活探测时，多久没有响应时认为其他节点处于fail状态，上⾯是 15秒。

