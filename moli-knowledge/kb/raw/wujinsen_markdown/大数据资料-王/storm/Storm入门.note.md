Storm框架⼊门1 Topology构成 和同样是计算框架的Mapreduce相⽐，Mapreduce集群上运⾏的是Job，⽽Storm集群上 运⾏的是Topology。但是Job在运⾏结束之后会⾃⾏结束，Topology却只能被⼿动的kill掉，否则会⼀直运⾏下去。 Storm集群中有两种节点，⼀种是控制节点(Nimbus节点)，另⼀种是⼯作节点(Supervisor节点)。所有Topology任务的提 交必须在Storm客户端节点上进⾏(需要配置~/.storm/storm.yaml⽂件)，由Nimbus节点分配给其他Supervisor节点进⾏ 处理。Nimbus节点⾸先将提交的Topology进⾏分⽚，分成⼀个个的Task，并将Task和Supervisor相关的信息提交到 zookeeper集群上，Supervisor会去zookeeper集群上认领⾃⼰的Task，通知⾃⼰的Worker进程进⾏Task的处理。总体的 Topology处理流程图为： 每个Topology都由Spout和Bolt组成，在Spout和Bolt传递信息的基本单位叫做Tuple，由 Spout发出的连续不断的Tuple及其在相应Bolt上处理的⼦Tuple连起来称为⼀个Steam，每个Stream的命名是在其⾸个 Tuple被Spout发出的时候，此时Storm会利⽤内部的Ackor机制保证每个Tuple可靠的被处理。 ⽽Tuple可以理解成键 值对，其中，键就是在定义在declareStream⽅法中的Fields字段，⽽值就是在emit⽅法中发送的Values字段。2 Conﬁguration 在运⾏Topology之前，可以通过⼀些参数的配置来调节运⾏时的状态，参数的配置是通过Storm框架部 署⽬录下的conf/storm.yaml⽂件来完成的。在次⽂件中可以配置运⾏时的Storm本地⽬录路径、运⾏时Worker的数⽬ 等。 在代码中，也可以设置Conﬁg的⼀些参数，但是优先级是不同的，不同位置配置Conﬁg参数的优先级顺序为： default.yaml<storm.yaml<topology内部的conﬁguration<内部组件的special conﬁguration<外部组件的special conﬁguration 在storm.yaml中常⽤的⼏个选项为：配置选项名称配置选项作⽤topology.max.task.parallelism每个 Topology运⾏时最⼤的executor数⽬topology.workers每个Topology运⾏时的worker的默认数⽬，若在代码中设置，则此 选项值被覆盖storm.zookeeper.serverszookeeper集群的节点列表storm.local.dirStorm⽤于存储jar包和临时⽂件的本地存储 ⽬录storm.zookeeper.rootStorm在zookeeper集群中的根⽬录，默认是“/”ui.portStorm集群的UI地址端⼜号，默认是 8080nimbus.host:Nimbus节点的hostsupervisor.slots.portsSupervisor节点的worker占位槽，集群中的所有Topology公⽤这 些槽位数，即使提交时设置了较⼤数值的槽位数，系统也会按照当前集群中实际剩余的槽位数来进⾏分配，当所有的 槽位数都分配完时，新提交的Topology只能等待，系统会⼀直监测是否有空余的槽位空出来，如果有，就再次给新提 交的Topology分配supervisor.worker.timeout.secsWorker的超时时间，单位为秒，超时后，Storm认为当前worker进程死 掉，会重新分配其运⾏着的task任务drpc.servers在使⽤drpc服务时，drpc server的服务器列表drpc.port在使⽤drpc服务 时，drpc server的服务端⼜ 3 Spouts Spout是Stream的消息产⽣源， Spout组件的实现可以通过继承BaseRichSpout类或 者其他*Spout类来完成，也可以通过实现IRichSpout接⼜来实现。 需要根据情况实现Spout类中重要的⼏个⽅法 有： 3.1 open⽅法 当⼀个Task被初始化的时候会调⽤此open⽅法。⼀般都会在此⽅法中对发送Tuple的对象 SpoutOutputCollector和配置对象TopologyContext初始化。⽰例如下：1 public void open(Map conf, TopologyContext context, SpoutOutputCollector collector) {2 3 _collector = collector;4 5 } 3.2 declareOutputFields⽅法 此⽅法⽤于声 明当前Spout的Tuple发送流。Stream流的定义是通过OutputFieldsDeclare.declareStream⽅法完成的，其中的参数包括了 发送的域Fields。⽰例如下：1 public void declareOutputFields(OutputFieldsDeclarer declarer) {2 3 declarer.declare(new Fields("word"));4 5 } 3.3 getComponentConﬁguration⽅法 此⽅法⽤于声明针对当前组件的特殊的 Conﬁguration配置。⽰例如下： 1 public Map<String, Object> getComponentConﬁguration() { 2 3 if(!_isDistributed) { 4 5 Map<String, Object> ret = new HashMap<String, Object>(); 6 7 ret.put(Conﬁg.TOPOLOGY_MAX_TASK_PARALLELISM, 3); 8 9 return ret;10 11 } else {12 13 return null;14 15 }16 17 } 这⾥便是设置了Topology中当前Component的线程数量上限。3.4 nextTuple⽅法 这是Spout 类中最重要的⼀个⽅法。发射⼀个Tuple到Topology都是通过这个⽅法来实现的。⽰例如下： 1 public void nextTuple() { 2 3 Utils.sleep(100); 4 5 ﬁnal String[] words = new String[] {"twitter","facebook","google"}; 6 7 ﬁnal Random rand = new Random(); 8 9 ﬁnal String word = words[rand.nextInt(words.length)];10 11 _collector.emit(new Values(word));12 13 } 这⾥便是从⼀个数组中随机选取⼀个单词作为Tuple，然后通过_collector 发送到Topology。 另外，除了上述⼏个⽅法之外，还有ack、fail和close⽅法等。Storm在监测到⼀个Tuple被成功处 理之后会调⽤ack⽅法，处理失败会调⽤fail⽅法，这两个⽅法在BaseRichSpout类中已经被隐式的实现了。 4 Bolts Bolt类接收由Spout或者其他上游Bolt类发来的Tuple，对其进⾏处理。Bolt组件的实现可以通过继承BasicRichBolt类或

者IRichBolt接⼜来完成。 Bolt类需要实现的主要⽅法有： 4.1 prepare⽅法 此⽅法和Spout中的open⽅法类似，为 Bolt提供了OutputCollector，⽤来从Bolt中发送Tuple。Bolt中Tuple的发送可以在prepare⽅法中、execute⽅法中、 cleanup等⽅法中进⾏，⼀般都是些在execute中。 ⽰例如下：1 public void prepare(Map conf, TopologyContext context, OutputCollector collector) {2 3 _collector = collector;4 5 } 4.2 declareOutputFields⽅法 ⽤于声明当前Bolt发送的 Tuple中包含的字段，和Spout中类似。 ⽰例如下：1 public void declareOutputFields(OutputFieldsDeclarer declarer) {2 3 declarer.declare(new Fields("obj", "count", "actualWindowLengthInSeconds"));4 5 } 此例说明当前Bolt类发送的 Tuple包含了三个字段："obj", "count", "actualWindowLengthInSeconds"。4.3 getComponentConﬁguration⽅法 和Spout 类⼀样，在Bolt中也可以有getComponentConﬁguration⽅法。 ⽰例如下：1 public Map<String, Object> getComponentConﬁguration() {2 3 Map<String, Object> conf = new HashMap<String, Object>();4 5 conf.put(Conﬁg.TOPOLOGY_TICK_TUPLE_FREQ_SECS, emitFrequencyInSeconds);6 7 return conf;8 9 } 此例定 义了从系统组件“_system”的“_tick”流中发送Tuple到当前Bolt的频率，当系统需要每隔⼀段时间执⾏特定的处理时，就 可以利⽤这个系统的组件的特性来完成。4.4 execute⽅法 这是Bolt中最关键的⼀个⽅法，对于Tuple的处理都可以放 到此⽅法中进⾏。具体的发送也是通过emit⽅法来完成的。此时，有两种情况，⼀种是emit⽅法中有两个参数，另⼀ 个种是有⼀个参数。(1)emit有⼀个参数：此唯⼀的参数是发送到下游Bolt的Tuple，此时，由上游发来的旧的Tuple在 此隔断，新的Tuple和旧的Tuple不再属于同⼀棵Tuple树。新的Tuple另起⼀个新的Tuple树。(2)emit有两个参数：第⼀ 个参数是旧的Tuple的输⼊流，第⼆个参数是发往下游Bolt的新的Tuple流。此时，新的Tuple和旧的Tuple是仍然属于同 ⼀棵Tuple树，即，如果下游的Bolt处理Tuple失败，则会向上传递到当前Bolt，当前Bolt根据旧的Tuple流继续往上游传 递，申请重发失败的Tuple。保证Tuple处理的可靠性。这两种情况要根据⾃⼰的场景来确定。 ⽰例如下： 1 public void execute(Tuple tuple) { 2 3 _collector.emit(tuple, new Values(tuple.getString(0) + "!!!")); 4 5 _collector.ack(tuple); 6 7 } 8 9 public void execute(Tuple tuple) {10 11 _collector.emit(new Values(tuple.getString(0) + "!!!"));12 13 } 此外还有ack⽅法、fail⽅法、cleanup⽅法等。其中cleanup⽅法和Spout中的close⽅法类似，都是在当 前Component关闭时调⽤，但是针对实时计算来说，除⾮⼀些特殊的场景要求以外，这两个⽅法⼀般都很少⽤到。5 Stream grouping 上⽂中介绍了Topology的基本组件Spout和Bolt，在Topology中，数据流Tuple的处理就是不断的通过 调⽤不同的Spout和Bolt来完成的。不同的Bolt和Spout的上下游关系是通过在⼊⼜类中定义的。⽰例如下： 1 builder = new TopologyBuilder(); 2 3 builder.setSpout(spoutId, new TestWordSpout(), 5); 4 5 builder.setBolt(counterId, new RollingCountBolt(9, 3), 4).ﬁeldsGrouping(spoutId, new Fields("word")); 6 7 builder.setBolt(intermediateRankerId, new IntermediateRankingsBolt(TOP_N), 4).ﬁeldsGrouping(counterId, new Fields("obj")); 8 builder.setBolt(totalRankerId, new TotalRankingsBolt(TOP_N)).globalGrouping(intermediateRankerId); 9 此例中的builder是TopologyBuilder对象，通过它 的createTopology⽅法可以创建⼀个Topology对象，同时此builder还要定义当前Topology中⽤到的Spout和Bolt对象，分 别通过setSpout⽅法和setBolt⽅法来完成。 setSpout⽅法和setBolt⽅法中的第⼀个参数是当前的Component组件的 Stream流ID号；第⼆个参数是具体的Component实现类的构造；第三个参数是当前Component的并⾏执⾏的线程数 ⽬，Storm会根据这个数字的累加和来确定Topology的Task数⽬。最后的⼩尾巴*Grouping是指的⼀个Stream应如何分 配数据给Bolt上⾯的Task。⽬前Storm的Stream Grouping有如下⼏种： (1)ShufﬂeGrouping:随机分组，随机分发Stream 中的tuple，保证每个Bolt的Task接收Tuple数量⼤致⼀致； (2)FieldsGrouping：按照字段分组，保证相同字段的Tuple 分配到同⼀个Task中； (3)AllGrouping：⼴播发送，每⼀个Task都会受到所有的Tuple； (4)GlobalGrouping：全局 分组，所有的Tuple都发送到同⼀个Task中，此时⼀般将当前Component的并发数⽬设置为1； (5)NonGrouping：不 分组，和ShufﬂeGrouping类似，当前Task的执⾏会和它的被订阅者在同⼀个线程中执⾏； (6)DirectGrouping：直接 分组，直接指定由某个Task来执⾏Tuple的处理，⽽且，此时必须有emitDirect⽅法来发送； (7) localOrShufﬂeGrouping：和ShufﬂeGrouping类似，若Bolt有多个Task在同⼀个进程中，Tuple会随机发给这些Task。 不同的的Grouping，需要根据不同的场景来具体设定，不⼀⽽论。6 Topology运⾏6.1 Topology运⾏⽅式Topology的运 ⾏可以分为本地模式和分布式模式，模式的设置可以在配置⽂件中设定，也可以在代码中设置。(1)本地运⾏的提交⽅ 式：1 LocalCluster cluster = new LocalCluster();2 3 cluster.submitTopology(topologyName, conf, topology);4 5

cluster.killTopology(topologyName);6 7 cluster.shutdown(); (2)分布式提交⽅式： StormSubmitter.submitTopology(topologyName, topologyConﬁg, builder.createTopology()); 需要注意的是，在Storm代码编 写完成之后，需要打包成jar包放到Nimbus中运⾏，打包的时候，不需要把依赖的jar都打进去，否则如果把依赖的 storm.jar包打进去的话，运⾏时会出现重复的配置⽂件错误导致Topology⽆法运⾏。因为Topology运⾏之前，会加载 本地的storm.yaml配置⽂件。在Nimbus运⾏的命令如下：storm jar StormTopology.jar maincalss args6.2 Topology运⾏流 程 有⼏点需要说明的地⽅：(1)Storm提交后，会把代码⾸先存放到Nimbus节点的inbox⽬录下，之后，会把当前 Storm运⾏的配置⽣成⼀个stormconf.ser⽂件放到Nimbus节点的stormdist⽬录中，在此⽬录中同时还有序列化之后的 Topology代码⽂件；(2)在设定Topology所关联的Spouts和Bolts时，可以同时设置当前Spout和Bolt的executor数⽬和task 数⽬，默认情况下，⼀个Topology的task的总和是和executor的总和⼀致的。之后，系统根据worker的数⽬，尽量平均 的分配这些task的执⾏。worker在哪个supervisor节点上运⾏是由storm本⾝决定的；(3)任务分配好之后，Nimbes节点会 将任务的信息提交到zookeeper集群，同时在zookeeper集群中会有workerbeats节点，这⾥存储了当前Topology的所有 worker进程的⼼跳信息；(4)Supervisor节点会不断的轮询zookeeper集群，在zookeeper的assignments节点中保存了所有 Topology的任务分配信息、代码存储⽬录、任务之间的关联关系等，Supervisor通过轮询此节点的内容，来领取⾃⼰的 任务，启动worker进程运⾏；(5)⼀个Topology运⾏之后，就会不断的通过Spouts来发送Stream流，通过Bolts来不断的 处理接收到的Stream流，Stream流是⽆界的。最后⼀步会不间断的执⾏，除⾮⼿动结束Topology。6.3 Topology⽅法调 ⽤流程 Topology中的Stream处理时的⽅法调⽤过程如下： 有⼏点需要说明的地⽅： (1)每个组件(Spout或者Bolt)的 构造⽅法和declareOutputFields⽅法都只被调⽤⼀次。 (2)open⽅法、prepare⽅法的调⽤是多次的。⼊⼜函数中设定的 setSpout或者setBolt⾥的并⾏度参数指的是executor的数⽬，是负责运⾏组件中的task的线程 的数⽬，此数⽬是多 少，上述的两个⽅法就会被调⽤多少次，在每个executor运⾏的时候调⽤⼀次。相当于⼀个线程的构造⽅法。

(3)nextTuple⽅法、execute⽅法是⼀直被运⾏的，nextTuple⽅法不断的发射Tuple，Bolt的execute不断的接收Tuple进⾏ 处理。只有这样不断地运⾏，才会产 ⽣⽆界的Tuple流，体现实时性。相当于线程的run⽅法。 (4)在提交了⼀个 topology之后，Storm就会创建spout/bolt实例并进⾏序列化。之后，将序列化的component发送给所有的任务所在的机 器(即Supervisor节 点)，在每⼀个任务上反序列化component。 (5)Spout和Bolt之间、Bolt和Bolt之间的通信，是 通过zeroMQ的消息队列实现的。 (6)上图没有列出ack⽅法和fail⽅法，在⼀个Tuple被成功处理之后，需要调⽤ack⽅ 法来标记成功，否则调⽤fail⽅法标记失败，重新处理这个Tuple。6.4 Topology并⾏度 在Topology的执⾏单元⾥，有 ⼏个和并⾏度相关的概念。(1)worker:每个worker都属于⼀个特定的Topology，每个Supervisor节点的worker可以有多 个，每个worker使⽤⼀个单独的端⼜，它对Topology中的每个component运⾏⼀个或者多个executor线程来提供task的 运⾏服务。(2)executor：executor是产⽣于worker进程内部的线程，会执⾏同⼀个component的⼀个或者多个task。

(3)task:实际的数据处理由task完成，在Topology的⽣命周期中，每个组件的task数⽬是不会发⽣变化的，⽽executor的 数⽬却不⼀定。executor数⽬⼩于等于task的数⽬，默认情况下，⼆者是相等的。 在运⾏⼀个Topology时，可以根据 具体的情况来设置不同数量的worker、task、executor，⽽设置的位置也可以在多个地⽅。(1)worker设置：(1.1)可以通 过设置yaml中的topology.workers属性(1.2)在代码中通过Conﬁg的setNumWorkers⽅法设定(2)executor设置： 通过在 Topology的⼊⼜类中setBolt、setSpout⽅法的最后⼀个参数指定，不指定的话，默认为1；(3)task设置： (3.1) 默认情 况下，和executor数⽬⼀致； (3.2)在代码中通过TopologyBuilder的setNumTasks⽅法设定具体某个组件的task数⽬； 6.5 终⽌Topology 通过在Nimbus节点利⽤如下命令来终⽌⼀个Topology的运⾏：storm kill topologyName kill之后， 可以通过UI界⾯查看topology状态，会⾸先变成KILLED状态，在清理完本地⽬录和zookeeper集群中的和当前Topology 相关的信息之后，此Topology就会彻底消失了。7 Topology跟踪 Topology提交后，可以在Nimbus节点的web界⾯查

看，默认的地址是 。 8 Storm应⽤ 上⾯给出了如何编写Storm框架任务Topology的⽅法，那 么在哪些场景下能够使⽤Storm框架呢？下⾯介绍Storm框架的⼏个典型的应⽤场景。(1)利⽤Storm框架的DRPC进⾏⼤ 量的函数并⾏调⽤，即实现分布式的RPC；(2)利⽤Storm框架的Transaction Topology，可以进⾏实时性的批量更新或 者查询数据库操作或者应⽤需要同⼀批内的消息以及批与批之间的消息并⾏处理这样的场景，此时Topology中只能有 ⼀个TrasactionalSpout；(3)利⽤滑动窗⼜的逻辑结合Storm框架来计算得出某段时间内的售出量最多的产品、购买者最

# htp:/NimbusIp:8080

多的TopN地区等；(4)精确的⼴告推送，在⽤户浏览产品的时候，将浏览记录实时性的搜集，发送到Bolt，由Bolt来根 据⽤户的账户信息(如果有的话)完成产品的分类统计，产品的相关性查询等逻辑计算之后，将计算结果推送给⽤户； (5)实时⽇志的处理，Storm可以和⼀个分布式存储结合起来，实时性的从多个数据源发送数据到处理逻辑Bolts，Bolts 完成⼀些逻辑处理之后，交给分布式存储框架进⾏存储，此时，Spout可以是多个；(6)实时性的监控舆论热点，⽐如 针对某个关键词，在⽤户查询的时候，产⽣数据源Spout，结合语义分析等，由Bolt来完成查询关键词的统计分析，汇 总当前的舆论热点；(7)数据流的实时聚合操作。9 参考⽹址

# htp:/xumingming.sinap.com/138/twiter-stor m%E5%85%A5%E9%97%A8/htps:/github.com/nathanmarz/storm/wikihtp:/nathanmarz.github.i o/storm/doc/index-al.htmlhtps:/github.com/nathanmarz

有理解不到位的地⽅，欢迎批评指正，⼀起交流 ~

