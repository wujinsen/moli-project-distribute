⼀、简介 storm可以确保spout发送出来的每个消息都会被完整的处理。本章将会描述storm体系是如何达到这个 ⽬标的，并将会详述开发者应该如何使⽤storm的这些机制来实现数据的可靠处理。 ⼆、理解消息被完整处理 ⼀个消息(tuple)从spout发送出来，可能会导致成百上千的消息基于此消息被创建。 我们来思考⼀下流式的“单词统计”的例⼦： storm任务从数据源（Kestrel queue）每次读取⼀个完整的英⽂句⼦；将这个句⼦分解为独⽴的单词， 最后，实时的输出每个单词以及它出现过的次数。 本例中，每个从spout发送出来的消息（每个英⽂句⼦）都会触发很多的消息被创建，那些从句⼦中分 隔出来的单词就是被创建出来的新消息。 这些消息构成⼀个树状结构，我们称之为“tuple tre”，看起来如图1所示：

- 图1 示例tuple tre 在什么条件下，Storm才会认为⼀个从spout发送出来的消息被完整处理呢？答案就是下⾯的条件同时 被满⾜：


tuple tre不再⽣⻓

树中的任何消息被标识为“已处理”

如果在指定的时间内，⼀个消息衍⽣出来的tuple tre未被完全处理成功，则认为此消息未被完整处 理。这个超时值可以通过任务级参数Config.TOPOLOGY_MESAGE_TIMEOUT_SECS 进⾏配置，默认 超时值为30秒。 三、消息的⽣命周期 如果消息被完整处理或者未被完整处理，Storm会如何进⾏接下来的操作呢？为了弄清这个问题，我们 来研究⼀下从spout发出来的消息的⽣命周期。这⾥列出了spout应该实现的接⼝：

⾸先， Storm使⽤spout实例的nextTuple()⽅法从spout请求⼀个消息（tuple）。 收到请求以后， spout使⽤open⽅法中提供的SpoutOutputColector向它的输出流发送⼀个或多个消息。每发送⼀个消 息，Spout会给这个消息提供⼀个mesage ID，它将会被⽤来标识这个消息。 假设我们从kestrel队列中读取消息，Spout会将kestrel 队列为这个消息设置的ID作为此消息的 mesage ID。 向SpoutOutputColector中发送消息格式如下：

接来下，这些消息会被发送到后续业务处理的bolts， 并且Storm会跟踪由此消息产⽣出来的新消息。 当检测到⼀个消息衍⽣出来的tuple tre被完整处理后，Storm会调⽤Spout中的ack⽅法，并将此消息 的mesageID作为参数传⼊。同理，如果某消息处理超时，则此消息对应的Spout的fail⽅法会被调 ⽤，调⽤时此消息的mesageID会被作为参数传⼊。 注意：⼀个消息只会由发送它的那个spout任务来调⽤ack或fail。如果系统中某个spout由多个任务运 ⾏，消息也只会由创建它的spout任务来应答（ack或fail），决不会由其他的spout任务来应答。

我们继续使⽤从kestrel队列中读取消息的例⼦来阐述⾼可靠性下spout需要做些什么(假设这个spout的 名字是KestrelSpout)。 我们先简述⼀下kestrel消息队列： 当KestrelSpout从kestrel队列中读取⼀个消息，表示它“打开”了队列中某个消息。这意味着，此消息并 未从队列中真正的删除，⽽是将此消息设置为“pending”状态，它等待来⾃客户端的应答，被应答以 后，此消息才会被真正的从队列中删除。处于“pending”状态的消息不会被其他的客户端看到。另外， 如果⼀个客户端意外的断开连接，则由此客户端“打开”的所有消息都会被重新加⼊到队列中。当消息 被“打开”的时候，kestrel队列同时会为这个消息提供⼀个唯⼀的标识。 KestrelSpout就是使⽤这个唯⼀的标识作为这个tuple的mesageID的。稍后当ack或fail被调⽤的时 候，KestrelSpout会把ack或者fail连同mesageID⼀起发送给kestrel队列，kestrel会将消息从队列中真 正删除或者将它重新放回队列中。 四、可靠相关的API 为了使⽤Storm提供的可靠处理特性，我们需要做两件事情：

⽆论何时在tuple tre中创建了⼀个新的节点，我们需要明确的通知Storm；

当处理完⼀个单独的消息时，我们需要告诉Storm 这棵tuple tre的变化状态。

通过上⾯的两步，storm就可以检测到⼀个tuple tre何时被完全处理了，并且会调⽤相关的ack或fail ⽅法。Storm提供了简单明了的⽅法来完成上述两步。 为tuple tre中指定的节点增加⼀个新的节点，我们称之为锚定（anchoring）。锚定是在我们发送消息 的同时进⾏的。为了更容易说明问题，我们使⽤下⾯代码作为例⼦。本示例的bolt将包含整句话的消息 分解为⼀系列的⼦消息，每个⼦消息包含⼀个单词。

每个消息都通过这种⽅式被锚定：把输⼊消息作为emit⽅法的第⼀个参数。因为word消息被锚定在了 输⼊消息上，这个输⼊消息是spout发送过来的tuple tre的根节点，如果任意⼀个word消息处理失 败，派⽣这个tuple tre那个spout 消息将会被重新发送。 与此相反，我们来看看使⽤下⾯的⽅式emit消息时，Storm会如何处理：

如果以这种⽅式发送消息，将会导致这个消息不会被锚定。如果此tuple tre中的消息处理失败，派⽣ 此tuple tre的根消息不会被重新发送。根据任务的容错级别，有时候很适合发送⼀个⾮锚定的消息。 ⼀个输出消息可以被锚定在⼀个或者多个输⼊消息上，这在做join或聚合的时候是很有⽤的。⼀个被多 重锚定的消息处理失败，会导致与之关联的多个spout消息被重新发送。多重锚定通过在emit⽅法中指 定多个输⼊消息来实现：

多重锚定会将被锚定的消息加到多棵tuple tre上。 注意：多重绑定可能会破坏传统的树形结构，从⽽构成⼀个DAGs（有向⽆环图），如图2所示：

- 图2 多重锚定构成的钻⽯型结构 Storm的实现可以像处理树那样来处理DAGs。


锚定表明了如何将⼀个消息加⼊到指定的tuple tre中，⾼可靠处理API的接下来部分将向您描述当处理 完tuple tre中⼀个单独的消息时我们该做些什么。这些是通过OutputColector 的ack和fail⽅法来实现 的。回头看⼀下例⼦SplitSentence，可以发现当所有的word消息被发送完成后，输⼊的表示句⼦的消 息会被应答（acked）。 每个被处理的消息必须表明成功或失败（acked 或者failed）。Storm是使⽤内存来跟踪每个消息的处 理情况的，如果被处理的消息没有应答的话，迟早内存会被耗尽！ 很多bolt遵循特定的处理流程： 读取⼀个消息、发送它派⽣出来的⼦消息、在execute结尾处应答此消 息。⼀般的过滤器（filter）或者是简单的处理功能都是这类的应⽤。Storm有⼀个BasicBolt接⼝封装 了上述的流程。示例SplitSentence可以使⽤BasicBolt来重写：

使⽤这种⽅式，代码⽐之前稍微简单了⼀些，但是实现的功能是⼀样的。发送到BasicOutputColector 的消息会被⾃动的锚定到输⼊消息，并且，当execute执⾏完毕的时候，会⾃动的应答输⼊消息。 很多情况下，⼀个消息需要延迟应答，例如聚合或者是join。只有根据⼀组输⼊消息得到⼀个结果之 后，才会应答之前所有的输⼊消息。并且聚合和join⼤部分时候对输出消息都是多重锚定。然⽽，这些 特性不是IBasicBolt所能处理的。 五、⾼效的实现tuple tre Storm 系统中有⼀组叫做“acker”的特殊的任务，它们负责跟踪DAG（有向⽆环图）中的每个消息。每 当发现⼀个DAG被完全处理，它就向创建这个根消息的spout任务发送⼀个信号。拓扑中acker任务的 并⾏度可以通过配置参数Config.TOPOLOGY_ACKERS来设置。默认的acker任务并⾏度为1，当系统中 有⼤量的消息时，应该适当提⾼acker任务的并发度。 为了理解Storm可靠性处理机制，我们从研究⼀个消息的⽣命周期和tuple tre的管理⼊⼿。当⼀个消 息被创建的时候（⽆论是在spout还是bolt中），系统都为该消息分配⼀个64bit的随机值作为id。这些 随机的id是acker⽤来跟踪由spout消息派⽣出来的tuple tre的。 每个消息都知道它所在的tuple tre对应的根消息的id。每当bolt新⽣成⼀个消息，对应tuple tre中的 根消息的mesageId就拷⻉到这个消息中。当这个消息被应答的时候，它就把关于tuple tre变化的信 息发送给跟踪这棵树的acker。例如，他会告诉acker：本消息已经处理完毕，但是我派⽣出了⼀些新 的消息，帮忙跟踪⼀下吧。 举个例⼦，假设消息D和E是由消息C派⽣出来的，这⾥演示了消息C被应答时，tuple tre是如何变化 的。

因为在C被从树中移除的同时D和E会被加⼊到tuple tre中，因此tuple tre不会被过早的认为已完全处 理。 关于Storm如何跟踪tuple tre，我们再深⼊的探讨⼀下。前⾯说过系统中可以有任意个数的acker，那 么，每当⼀个消息被创建或应答的时候，它怎么知道应该通知哪个acker呢？ 系统使⽤⼀种哈希算法来根据spout消息的mesageId确定由哪个acker跟踪此消息派⽣出来的tuple tre。因为每个消息都知道与之对应的根消息的mesageId，因此它知道应该与哪个acker通信。

当spout发送⼀个消息的时候，它就通知对应的acker⼀个新的根消息产⽣了，这时acker就会创建⼀个 新的tuple tre。当acker发现这棵树被完全处理之后，他就会通知对应的spout任务。 tuple是如何被跟踪的呢？系统中有成千上万的消息，如果为每个spout发送的消息都构建⼀棵树的话， 很快内存就会耗尽。所以，必须采⽤不同的策略来跟踪每个消息。由于使⽤了新的跟踪算法，Storm只 需要固定的内存（⼤约20字节）就可以跟踪⼀棵树。这个算法是storm正确运⾏的核⼼，也是storm最 ⼤的突破。 acker任务保存了spout消息id到⼀对值的映射。第⼀个值就是spout的任务id，通过这个id，acker就知 道消息处理完成时该通知哪个spout任务。第⼆个值是⼀个64bit的数字，我们称之为“ack val”， 它是 树中所有消息的随机id的异或结果。ack val表示了整棵树的的状态，⽆论这棵树多⼤，只需要这个固 定⼤⼩的数字就可以跟踪整棵树。当消息被创建和被应答的时候都会有相同的消息id发送过来做异或。 每当acker发现⼀棵树的ack val值为0的时候，它就知道这棵树已经被完全处理了。因为消息的随机ID 是⼀个64bit的值，因此ack val在树处理完之前被置为0的概率⾮常⼩。假设你每秒钟发送⼀万个消 息，从概率上说，⾄少需要50, 0, 0年才会有机会发⽣⼀次错误。即使如此，也只有在这个消息确 实处理失败的情况下才会有数据的丢失！ 六、选择合适的可靠性级别 Acker任务是轻量级的，所以在拓扑中并不需要太多的acker存在。可以通过Storm UI来观察acker任务 的吞吐量，如果看上去吞吐量不够的话，说明需要添加额外的acker。 如果你并不要求每个消息必须被处理（你允许在处理过程中丢失⼀些信息），那么可以关闭消息的可 靠处理机制，从⽽可以获取较好的性能。关闭消息的可靠处理机制意味着系统中的消息数会减半（每 个消息不需要应答了）。另外，关闭消息的可靠处理可以减少消息的⼤⼩（不需要每个tuple记录它的 根id了），从⽽节省带宽。 有三种⽅法可以关系消息的可靠处理机制：

将参数Config.TOPOLOGY_ACKERS设置为0，通过此⽅法，当Spout发送⼀个消息的时候，它的 ack⽅法将⽴刻被调⽤；

第⼆个⽅法是Spout发送⼀个消息时，不指定此消息的mesageID。当需要关闭特定消息可靠性的 时候，可以使⽤此⽅法；

最后，如果你不在意某个消息派⽣出来的⼦孙消息的可靠性，则此消息派⽣出来的⼦消息在发送时 不要做锚定，即在emit⽅法中不指定输⼊消息。因为这些⼦孙消息没有被锚定在任何tuple tre中， 因此他们的失败不会引起任何spout重新发送消息。

七、集群的各级容错 到现在为⽌，⼤家已经理解了Storm的可靠性机制，并且知道了如何选择不同的可靠性级别来满⾜需 求。接下来我们研究⼀下Storm如何保证在各种情况下确保数据不丢失。

- 1、任务级失败 因为bolt任务crash引起的消息未被应答。此时，acker中所有与此bolt任务关联的消息都会因为超时⽽ 失败，对应spout的fail⽅法将被调⽤。


acker任务失败。如果acker任务本身失败了，它在失败之前持有的所有消息都将会因为超时⽽失 败。Spout的fail⽅法将被调⽤。

Spout任务失败。这种情况下，Spout任务对接的外部设备（如MQ）负责消息的完整性。例如当客 户端异常的情况下，kestrel队列会将处于pending状态的所有的消息重新放回到队列中。

- 2、任务槽(slot) 故障
- 3.、集群节点（机器）故障


worker失败。每个worker中包含数个bolt（或spout）任务。supervisor负责监控这些任务，当 worker失败后，supervisor会尝试在本机重启它。

supervisor失败。supervisor是⽆状态的，因此supervisor的失败不会影响当前正在运⾏的任务，只 要及时的将它重新启动即可。supervisor不是⾃举的，需要外部监控来及时重启。

nimbus失败。nimbus是⽆状态的，因此nimbus的失败不会影响当前正在运⾏的任务（nimbus失败 时，⽆法提交新的任务），只要及时的将它重新启动即可。nimbus不是⾃举的，需要外部监控来及 时重启。

storm集群中的节点故障。此时nimbus会将此机器上所有正在运⾏的任务转移到其他可⽤的机器上 运⾏。

zokeper集群中的节点故障。zokeper保证少于半数的机器宕机仍可正常运⾏，及时修复故障机 器即可。

⼋、⼩结 本章介绍了storm集群如何实现数据的可靠处理。借助于创新性的tuple tre跟踪技术，storm⾼效的通 过数据的应答机制来保证数据不丢失。 storm集群中除nimbus外，没有单点存在，任何节点都可以出故障⽽保证数据不会丢失。nimbus被设 计为⽆状态的，只要可以及时重启，就不会影响正在运⾏的任务。 【编辑推荐】

- 1.
- 2.
- 3.


Storm⼊⻔教程：前⾔ Storm⼊⻔教程：构建Topology Storm⼊⻔教程：安装部署步骤详解

