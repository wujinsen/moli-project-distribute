hive的配置： hive.dl.output.format：hive的 dl语句的输出格式，默认是text，纯⽂本，还有json格式，这个是 0.90以后才出的新配置； hive.exec.script.wraper：hive调⽤脚本时的包装器，默认是nul，如果设置为python的话，那么在做 脚本调⽤操作时语句会变为python <script comand>，nul的话就是直接执⾏<script comand>； hive.exec.plan：hive执⾏计划的⽂件路径，默认是nul，会在运⾏时⾃动设置，形如 hdfs:/ x/ x/x； hive.exec.scratchdir：hive⽤来存储不同阶段的map/reduce的执⾏计划的⽬录，同时也存储中间输出 结果，默认是/tmp/<user.name>/hive，我们实际⼀般会按组区分，然后组内⾃建⼀个tmp⽬录存储； hive.exec.submitviachild：在⾮local模式下，决定hive是否要在独⽴的jvm中执⾏map/reduce；默认 是false，也就是说默认map/reduce的作业是在hive的jvm上去提交的； hive.exec.script.maxersize：当⽤户调⽤transform或者map或者reduce执⾏脚本时，最⼤的序列化错 误数，默认1 0，⼀般也不⽤修改； hive.exec.compres.output：⼀个查询的最后⼀个map/reduce任务输出是否被压缩的标志，默认为 false，但是⼀般会开启为true，好处的话，节省空间不说，在不考虑cpu压⼒的时候会提⾼io； hive.exec.compres.intermediate：类似上个，在⼀个查询的中间的map/reduce任务输出是否要被压 缩，默认false， hive.jar.path：当使⽤独⽴的jvm提交作业时，hive_cli.jar所在的位置，⽆默认值； hive.aux.jars.path：当⽤户⾃定义了UDF或者SerDe，这些插件的jar都要放到这个⽬录下，⽆默认值； hive.partition.pruning：在编译器发现⼀个query语句中使⽤分区表然⽽未提供任何分区谓词做查询 时，抛出⼀个错误从⽽保护分区表，默认是nonstrict；（待读源码后细化，⽹上资料极少） hive.map.agr：map端聚合是否开启，默认开启； hive.join.emit.interval：在发出join结果之前对join最右操作缓存多少⾏的设定，默认1 0；hive jira⾥ 有个对该值设置太⼩的bugfix； hive.map.agr.hash.percentmemory：map端聚合时hash表所占⽤的内存⽐例，默认0.5，这个在map 端聚合开启后使⽤， hive.default.fileformat：CREATE TABLE语句的默认⽂件格式，默认TextFile，其他可选的有 SequenceFile、RCFile还有Orc； hive.merge.mapfiles：在只有map的作业结束时合并⼩⽂件，默认开启true； hive.merge.mapredfiles：在⼀个map/reduce作业结束后合并⼩⽂件，默认不开启false； hive.merge.size.per.task：作业结束时合并⽂件的⼤⼩，默认256MB； hive.merge.smalfiles.avgsize：在作业输出⽂件⼩于该值时，起⼀个额外的map/reduce作业将⼩⽂件 合并为⼤⽂件，⼩⽂件的基本阈值，设置⼤点可以减少⼩⽂件个数，需要mapfiles和mapredfiles为 true，默认值是16MB；

mapred.reduce.tasks：每个作业的reduce任务数，默认是hadop client的配置1个； hive.exec.reducers.bytes.per.reducer：每个reducer的⼤⼩，默认是1G，输⼊⽂件如果是10G，那么 就会起10个reducer；hive.exec.reducers.max：reducer的最⼤个数，如果在mapred.reduce.tasks设 置为负值，那么hive将取该值作为reducers的最⼤可能值。当然还要依赖（输⼊⽂件⼤ ⼩/hive.exec.reducers.bytes.per.reducer）所得出的⼤⼩，取其⼩值作为reducer的个数，hive默认是

9；hive.fileformat.check：加载数据⽂件时是否校验⽂件格式，默认是true； hive.groupby.skewindata：group by操作是否允许数据倾斜，默认是false，当设置为true时，执⾏计 划会⽣成两个map/reduce作业，第⼀个MR中会将map的结果随机分布到reduce中，达到负载均衡的 ⽬的来解决数据倾斜，hive.groupby.mapagr.checkinterval：map端做聚合时，group by 的key所允 许的数据⾏数，超过该值则进⾏分拆，默认是1 0；hive.mapred.local.mem：本地模式时， map/reduce的内存使⽤量，默认是0，就是⽆限制； hive.mapjoin.folowby.map.agr.hash.percentmemory：map端聚合时hash表的内存占⽐，该设置约 束group by在map join后进⾏，否则使⽤hive.map.agr.hash.percentmemory来确认内存占⽐，默认 值0.3；hive.map.agr.hash.force.flush.memeory.threshold：map端聚合时hash表的最⼤可⽤内存， 如果超过该值则进⾏flush数据，默认是0.9；hive.map.agr.hash.min.reduction：如果hash表的容量 与输⼊⾏数之⽐超过这个数，那么map端的hash聚合将被关闭，默认是0.5，设置为1可以保证hash聚 合永不被关闭；hive.optimize.groupby：在做分区和表查询时是否做分桶group by，默认开启true； hive.multigroupby.singlemr：将多个group by产出为⼀个单⼀map/reduce任务计划，当然约束前提是 group by有相同的key，默认是false；hive.optimize.cp：列裁剪，默认开启true，在做查询时只读取 ⽤到的列，这个是个有⽤的优化；hive.optimize.index.filter：⾃动使⽤索引，默认不开启false； hive.optimize.index.groupby：是否使⽤聚集索引优化group-by查询，默认关闭false； hive.optimize. pd：是否⽀持谓词下推，默认开启；所谓谓词下推，将外层查询块的 WHERE ⼦句中 的谓词移⼊所包含的较低层查询块（例如视图），从⽽能够提早进⾏数据过滤以及有可能更好地利⽤ 索引。hive.optimize. pd.storage：谓词下推开启时，谓词是否下推到存储handler，默认开启，在谓 词下推关闭时不起作⽤；hive. pd.recognizetransivity：在等值join条件下是否产地重复的谓词过滤 器，默认开启；hive.join.cache.size：在做表join时缓存在内存中的⾏数，默认25 0； hive.mapjoin.bucket.cache.size：mapjoin时内存cache的每个key要存储多少个value，默认10； hive.optimize.skewjoin：是否开启数据倾斜的join优化，默认不开启false；hive.skewjoin.key：判断数 据倾斜的阈值，如果在join中发现同样的key超过该值则认为是该key是倾斜的join key，默认是 1 0；hive.skewjoin.mapjoin.map.tasks：在数据倾斜join时map join的map数控制，默认是 1 0；hive.skewjoin.mapjoin.min.split：数据倾斜join时map join的map任务的最⼩split⼤⼩，默认 是 35432，该参数要结合上⾯的参数共同使⽤来进⾏细粒度的控制；hive.mapred.mode：hive操 作执⾏时的模式，默认是nonstrict⾮严格模式，如果是strict模式，很多有⻛险的查询会被禁⽌运⾏， ⽐如笛卡尔积的join和动态分区；

hive.exec.script.maxersize：⼀个map/reduce任务允许打印到标准错误⾥的最⼤字节数，为了防⽌脚 本把分区⽇志填满，默认是1 0；hive.exec.script.alow.partial.consumption：hive是否允许脚本 不从标准输⼊中读取任何内容就成功退出，默认关闭false；hive.script.operator.id.env.var：在⽤户使 ⽤transform函数做⾃定义map/reduce时，存储唯⼀的脚本标识的环境变量的名字，默认 HIVE_SCRIPT_OPERATOR_ID；hive.exec.compres.output：控制hive的查询结果输出是否进⾏压 缩，压缩⽅式在hadop的mapred.output.compres中配置，默认不压缩false； hive.exec.compres.intermediate：控制hive的查询中间结果是否进⾏压缩，同上条配置，默认不压缩 false；hive.exec.paralel：hive的执⾏job是否并⾏执⾏，默认不开启false，在很多操作如join时，⼦ 查询之间并⽆关联可独⽴运⾏，这种情况下开启并⾏运算可以⼤⼤加速； hvie.exec.paralel.thread.number：并⾏运算开启时，允许多少作业同时计算，默认是8； hive.exec.rowofset：是否提供⾏偏移量的虚拟列，默认是false不提供，Hive有两个虚拟列:⼀个是 INPUT_FILE_NAME,表示输⼊⽂件的路径，另外⼀个是BLOCK_OFSET_INSIDE_FILE，表示记 录在⽂件中的块偏移量，这对排查出现不符合预期或者nul结果的查询是很有帮助的； hive.task.progres：控制hive是否在执⾏过程中周期性的更新任务进度计数器，开启这个配置可以帮 助job tracker更好的监控任务的执⾏情况，但是会带来⼀定的性能损耗，当动态分区标志 hive.exec.dynamic.partition开启时，本配置⾃动开启；hive.exec.pre.hoks：执⾏前置条件，⼀个⽤ 逗号分隔开的实现了org.apache.hadop.hive.ql.hoks.ExecuteWithHokContext接⼝的java clas列 表，配置了该配置后，每个hive任务执⾏前都要执⾏这个执⾏前钩⼦，默认是空； hive.exec.post.hoks：同上，执⾏后钩⼦，默认是空；hive.exec.failure.hoks：同上，异常时钩⼦， 在程序发⽣异常时执⾏，默认是空；hive.mergejob.maponly：试图⽣成⼀个只有map的任务去做 merge，前提是⽀持CombineHiveInputFormat，默认开启true；hive.mapjoin.smaltable.filesize：输 ⼊表⽂件的mapjoin阈值，如果输⼊⽂件的⼤⼩⼩于该值，则试图将普通join转化为mapjoin，默认 25MB；hive.mapjoin.localtask.max.memory.usage：mapjoin本地任务执⾏时hash表容纳key/value的 最⼤量，超过这个值的话本地任务会⾃动退出，默认是0.9； hive.mapjoin.folowby.gby.localtask.max.memory.usage：类似上⾯，只不过是如果mapjoin后有⼀个 group by的话，该配置控制类似这样的query的本地内存容量上限，默认是0.5； hive.mapjoin.check.memory.rows：在运算了多少⾏后执⾏内存使⽤量检查，默认1 0； hive.heartbeat.interval：发送⼼跳的时间间隔，在mapjoin和filter操作中使⽤，默认1 0； hive.auto.convert.join：根据输⼊⽂件的⼤⼩决定是否将普通join转换为mapjoin的⼀种优化，默认不开 启false；hive.script.auto.progres：hive的transform/map/reduce脚本执⾏时是否⾃动的将进度信息 发送给TaskTracker来避免任务没有响应被误杀，本来是当脚本输出到标准错误时，发送进度信息，但 是开启该项后，输出到标准错误也不会导致信息发送，因此有可能会造成脚本有死循环产⽣，但是 TaskTracker却没有检查到从⽽⼀直循环下去；hive.script.serde：⽤户脚本转换输⼊到输出时的SerDe 约束，默认是org.apache.hadop.hive.serde2.lazy.LazySimpleSerDe；hive.script.recordreader：从 脚本读数据的时候的默认reader，默认是org.apache.hadop.hive.ql.exec.TextRecordReader； hive.script.recordwriter：写数据到脚本时的默认writer，默认 org.apache.hadop.hive.ql.exec.TextRecordWriter；hive.input.format：输⼊格式，默认是

org.apache.hadop.hive.ql.io.CombineHiveInputFormat，如果出现问题，可以改⽤ org.apache.hadop.hive.ql.io.HiveInputFormat；hive.udtf.auto.progres：UDTF执⾏时hive是否发送 进度信息到TaskTracker，默认是false；hive.mapred.reduce.tasks.speculative.execution：reduce任 务推测执⾏是否开启，默认是true；hive.exec.counters.pul.interval：运⾏中job轮询JobTracker的时 间间隔，设置⼩会影响JobTracker的load，设置⼤可能看不出运⾏任务的信息，要去平衡，默认是 1 0；hive.enforce.bucketing：数据分桶是否被强制执⾏，默认false，如果开启，则写⼊table数据 时会启动分桶，hive.enforce.sorting：开启强制排序时，插数据到表中会进⾏强制排序，默认false； hive.optimize.reducededuplication：如果数据已经根据相同的key做好聚合，那么去除掉多余的 map/reduce作业，此配置是⽂档的推荐配置，建议打开，默认是true；hive.exec.dynamic.partition： 在DML/ DL中是否⽀持动态分区，默认false；hive.exec.dynamic.partition.mode：默认strict，在 strict模式下，动态分区的使⽤必须在⼀个静态分区确认的情况下，其他分区可以是动态； hive.exec.max.dynamic.partitions：动态分区的上限，默认1 0； hive.exec.max.dynamic.partitions.pernode：每个maper/reducer节点可以创建的最⼤动态分区数， 默认10；hive.exec.max.created.files：⼀个mapreduce作业能创建的HDFS⽂件最⼤数，默认是 1 0；hive.exec.default.partition.name：当动态分区启⽤时，如果数据列⾥包含nul或者空字符串 的话，数据会被插⼊到这个分区，默认名字是 _HIVE_DEFAULT_PARTITION_； hive.fetch.output.serde：FetchTask序列化fetch输出时需要的SerDe，默认是 org.apache.hadop.hive.serde2.DelimitedJSONSerDe;hive.exec.mode.local.auto：是否由hive决定 ⾃动在local模式下运⾏，默认是false，

hive.exec.drop.ignorenonexistent：在drop表或者视图时如果发现表或视图不存在，是否报错，默认 是true；hive.exec.show.job.failure.debug.info：在作业失败时是否提供⼀个任务debug信息，默认 true；hive.auto.progres.timeout：运⾏⾃动progresor的时间间隔，默认是0等价于forever； hive.table.parameters.default：新建表的属性字段默认值，默认是empty空； hive.variable.substitute：是否⽀持变量替换，如果开启的话，⽀持语法如${var} ${system:var}和 ${env.var}，默认是true；hive.eror.on.empty.partition：在遇到结果为空的动态分区时是否报错，默 认是false；hive.exim.uri.scheme.whitelist：在导⼊导出数据时提供的⼀个⽩名单列表，列表项之间由 逗号分隔，默认hdfs,pfile；hive.limit.row.max.size：字⾯意思理解就是在使⽤limit做数据的⼦集查询 时保证的最⼩⾏数据量，默认是1 0；hive.limit.optimize.limit.file：使⽤简单limit查询数据⼦集 时，可抽样的最⼤⽂件数，默认是10；hive.limit.optimize.enable：使⽤简单limit抽样数据时是否开启 优化选项，默认是false，关于limit的优化问题，在hive progra ming书中解释的是这个feature有 drawback，对于抽样的不确定性给出了⻛险提示；hive.limit.optimize.fetch.max：使⽤简单limit抽样 数据允许的最⼤⾏数，默认5 0，查询query受限，insert不受影响；hive.rework.mapredwork：是 否重做mapreduce，默认是false；hive.sample.sednumber：⽤来区分抽样的数字，默认是0； hive.io.exception.handlers：io异常处理handler类列表，默认是空，当record reader发⽣io异常时， 由这些handler来处理异常；hive.autogen.columnalias.prefix.label：当在执⾏中⾃动产⽣列别名的前 缀，当类似count这样的聚合函数起作⽤时，如果不明确指出count(a) as x的话，那么默认会从列的 位置的数字开始算起添加，⽐如第⼀个count的结果会冠以列名_c0，接下来依次类推，默认值是_c， 数据开发过程中应该很多⼈都看到过这个别名；hive.autogen.columnalias.prefix.includefuncname： 在⾃动⽣成列别名时是否带函数的名字，默认是false；hive.exec.perf.loger：负责记录客户端性能指 标的⽇志类名，必须是org.apache.hadop.hive.ql.log.PerfLoger的⼦类，默认是 org.apache.hadop.hive.ql.log.PerfLoger；hive.start.cleanup.scratchdir：当启动hive服务时是否清 空hive的scratch⽬录，默认是false；hive.output.file.extension：输出⽂件扩展名，默认是空； hive.insert.into.multilevel.dirs：是否插⼊到多级⽬录，默认是false；hive.files.umask.value：hive创 建⽂件夹时的dfs.umask值，默认是 02；

hive.metastore.local：控制hive是否连接⼀个远程metastore服务器还是开启⼀个本地客户端jvm，默 认是true，Hive0.10已经取消了该配置项；javax.jdo.option.ConectionURL：JDBC连接字符串，默认 jdbc:derby:;databaseName=metastore_db;create=true；javax.jdo.option.ConectionDriverName： JDBC的driver，默认org.apache.derby.jdbc.EmbededDriver； javax.jdo.PersisteneManagerFactoryClas：实现JDO PersistenceManagerFactory的类名，默认 org.datanucleus.jdo.JDOPersistenceManagerFactory；javax.jdo.option.DetachAlOnComit：事务 提交后detach所有提交的对象，默认是true；javax.jdo.option.NonTransactionalRead：是否允许⾮事 务的读，默认是true；javax.jdo.option.ConectionUserName：username，默认AP； javax.jdo.option.ConectionPasword：pasword，默认mine；javax.jdo.option.Multithreaded：是 否⽀持并发访问metastore，默认是true；datanucleus.conectionPolingType：使⽤连接池来访问 JDBC metastore，默认是DBCP；datanucleus.validateTables：检查是否存在表的schema，默认是 false；datanucleus.validateColumns：检查是否存在列的schema，默认false； datanucleus.validateConstraints：检查是否存在constraint的schema，默认false； datanucleus.stroeManagerType：元数据存储类型，默认rdbms；datanucleus.autoCreateSchema： 在不存在时是否⾃动创建必要的schema，默认是true；datanucleus.aotuStartMechanismMode：如 果元数据表不正确，抛出异常，默认是checked；datanucleus.transactionIsolation：默认的事务隔离 级别，默认是read-comited；datanucleus.cache.level2：使⽤⼆级缓存，默认是false； datanucleus.cache.level2.type：⼆级缓存的类型，有两种，SOFT:软引⽤，WEAK:弱引⽤，默认是 SOFT；datanucleus.identifierFactory：id⼯⼚⽣产表和列名的名字，默认是datanucleus； datanucleus.plugin.pluginRegistryBundleCheck：当plugin被发现并且重复时的⾏为，默认是LOG； hive.metastroe.warehouse.dir：数据仓库的位置，默认是/user/hive/warehouse； hive.metastore.execute.setugi：⾮安全模式，设置为true会令metastore以客户端的⽤户和组权限执 ⾏DFS操作，默认是false，这个属性需要服务端和客户端同时设置；hive.metastore.event.listeners： metastore的事件监听器列表，逗号隔开，默认是空； hive.metastore.partition.inherit.table.properties：当新建分区时⾃动继承的key列表，默认是空； hive.metastore.end.function.listeners：metastore函数执⾏结束时的监听器列表，默认是空； hive.metastore.event.expiry.duration：事件表中事件的过期时间，默认是0； hive.metastore.event.clean.freq：metastore中清理过期事件的定时器的运⾏周期，默认是0； hive.metastore.conect.retries：创建metastore连接时的重试次数，默认是5； hive.metastore.client.conect.retry.delay：客户端在连续的重试连接等待的时间，默认1； hive.metastore.client.socket.timeout：客户端socket超时时间，默认20秒； hive.metastore.rawstore.impl：原始metastore的存储实现类，默认是 org.apache.hadop.hive.metastore.ObjectStore；hive.metastore.batch.retrieve.max：在⼀个batch 获取中，能从metastore⾥取出的最⼤记录数，默认是30；hive.metastore.ds.conection.url.hok： 查找JDO连接url时hok的名字，默认是javax.jdo.option.ConectionURL； hive.metastore.ds.retry.atempts：当出现连接错误时重试连接的次数，默认是1次； hive.metastore.ds.retry.interval：metastore重试连接的间隔时间，默认1 0毫秒；

hive.metastore.server.min.threads：在thrift服务池中最⼩的⼯作线程数，默认是20； hive.metastore.server.max.threads：最⼤线程数，默认是1 0； hive.metastore.server.tcp.kepalive：metastore的server是否开启⻓连接，⻓连可以预防半连接的积 累，默认是true；hive.metastore.sasl.enabled：metastore thrift接⼝的安全策略，开启则⽤SASL加 密接⼝，客户端必须要⽤Kerberos机制鉴权，默认是不开启false； hive.metastore.kerberos.keytab.file：在开启sasl后kerberos的keytab⽂件存放路径，默认是空； hive.metastore.kerberos.principal：kerberos的principal，_HOST部分会动态替换，默认是hivemetastore/_HOST@EXAMPLE.COM；hive.metastore.cache.pinobjtypes：在cache中⽀持的 metastore的对象类型，由逗号分隔，默认是 Table,StorageDescriptor,SerDeInfo,Partition,Database,Type,FieldSchema,Order； hive.metastore.authorization.storage.checks：在做类似drop partition操作时，metastore是否要认证 权限，默认是false；hive.metastore.schema.verification：强制metastore的schema⼀致性，开启的 话会校验在metastore中存储的信息的版本和hive的jar包中的版本⼀致性，并且关闭⾃动schema迁 移，⽤户必须⼿动的升级hive并且迁移schema，关闭的话只会在版本不⼀致时给出警告，默认是false 不开启；

hive.index.compact.file.ignore.hdfs：在索引⽂件中存储的hdfs地址将在运⾏时被忽略，如果开启的 话；如果数据被迁移，那么索引⽂件依然可⽤，默认是false； hive.optimize.index.filter.compact.minsize：压缩索引⾃动应⽤的最⼩输⼊⼤⼩，默认是 5368709120；hive.optimize.index.filter.compact.maxsize：同上，相反含义，如果是负值代表正⽆ 穷，默认是-1；hive.index.compact.query.max.size：⼀个使⽤压缩索引做的查询能取到的最⼤数据 量，默认是10737418240 个byte；负值代表⽆穷⼤；hive.index.compact.query.max.entries：使⽤压 缩索引查询时能读到的最⼤索引项数，默认是1 0；负值代表⽆穷⼤； hive.index.compact.binary.search：在索引表中是否开启⼆分搜索进⾏索引项查询，默认是true； hive.exec.concatenate.check.index：如果设置为true，那么在做ALTER TABLE tbl_name CONCATENATE on a table/partition（有索引） 操作时，抛出错误；可以帮助⽤户避免index的删除和 重建；hive.stats.dbclas：存储hive临时统计信息的数据库，默认是jdbc:derby； hive.stats.autogather：在insert overwrite命令时⾃动收集统计信息，默认开启true； hive.stats.jdbcdriver：数据库临时存储hive统计信息的jdbc驱动；hive.stats.dbconectionstring：临 时统计信息数据库连接串，默认jdbc:derby:databaseName=TempStatsStore;create=true； hive.stats.defaults.publisher：如果dbclas不是jdbc或者hbase，那么使⽤这个作为默认发布，必须 实现StatsPublisher接⼝，默认是空；hive.stats.defaults.agregator：如果dbclas不是jdbc或者 hbase，那么使⽤该类做聚集，要求实现StatsAgregator接⼝，默认是空；hive.stats.jdbc.timeout： jdbc连接超时配置，默认30秒；hive.stats.retries.max：当统计发布合聚集在更新数据库时出现异常时 最⼤的重试次数，默认是0，不重试；hive.stats.retries.wait：重试次数之间的等待窗⼝，默认是3 0 毫秒；hive.client.stats.publishers：做count的job的统计发布类列表，由逗号隔开，默认是空；必须 实现org.apache.hadop.hive.ql.stats.ClientStatsPublisher接⼝；hive.client.stats.counters：没什么 ⽤ ~hive.security.authorization.enabled：hive客户端是否认证，默认是false； hive.security.authorization.manager：hive客户端认证的管理类，默认是 org.apache.hadop.hive.ql.security.authorization.DefaultHiveAuthorizationProvider；⽤户定义的要 实现org.apache.hadop.hive.ql.security.authorization.HiveAuthorizationProvider； hive.security.authenticator.manager：hive客户端授权的管理类，默认是 org.apache.hadop.hive.ql.security.HadopDefaultAuthenticator；⽤户定义的需要实现 org.apache.hadop.hive.ql.security.HiveAuthenticatorProvider； hive.security.authorization.createtable.user.grants：当表创建时⾃动授权给⽤户，默认是空； hive.security.authorization.createtable.group.grants：同上，⾃动授权给组，默认是空； hive.security.authorization.createtable.role.grants：同上，⾃动授权给⻆⾊，默认是空； hive.security.authorization.createtable.owner.grants：同上，⾃动授权给owner，默认是空； hive.security.metastore.authorization.manager：metastore的认证管理类，默认是 org.apache.hadop.hive.ql.security.authorization.DefaultHiveMetastoreAuthorizationProvider；⽤ 户定义的必须实现 org.apache.hadop.hive.ql.security.authorization.HiveMetastoreAuthorizationProvider接⼝；接⼝参 数要包含org.apache.hadop.hive.ql.security.authorization.StorageBasedAuthorizationProvider接

⼝；使⽤HDFS的权限控制认证⽽不是hive的基于grant的⽅式； hive.security.metastore.authenticator.manager：metastore端的授权管理类，默认是 org.apache.hadop.hive.ql.security.HadopDefaultMetastoreAuthenticator，⾃定义的必须实现 org.apache.hadop.hive.ql.security.HiveAuthenticatorProvider接⼝； hive.metastore.pre.event.listeners：在metastore做数据库任何操作前执⾏的事件监听类列表；

fs.har.impl：访问Hadop Archives的实现类，低于hadop 0.20版本的都不兼容，默认是 org.apache.hadop.hive.shims.HiveHarFileSystem；hive.archive.enabled：是否允许归档操作，默 认是false；hive.archive.har.parentdir.setable：在创建HAR⽂件时必须要有⽗⽬录，需要⼿动设置， 在新的hadop版本会⽀持，默认是false；hive.suport.concurency：hive是否⽀持并发，默认是 false，⽀持读写锁的话，必须要起zokeper；hive.lock.mapred.only.operation：控制是否在查询时 加锁，默认是false；hive.lock.numretries：获取锁时尝试的重试次数，默认是10； hive.lock.sl ep.betwen.retries：在重试间隔的睡眠时间，默认60秒；hive.zokeper.quorum：zk 地址列表，默认是空；hive.zokeper.client.port：zk服务器的连接端⼝，默认是2181； hive.zokeper.sesion.timeout：zk客户端的sesion超时时间，默认是6 0； hive.zokeper.namespace：在所有zk节点创建后的⽗节点，默认是hive_zokeper_namespace； hive.zokeper.clean.extra.nodes：在sesion结束时清除所有额外node； hive.cluster.delegation.token.store.clas：代理token的存储实现类，默认是 org.apache.hadop.hive.thrift.MemoryTokenStore，可以设置为 org.apache.hadop.hive.thrift.ZoKeperTokenStore来做负载均衡集群； hive.cluster.delegation.token.store.zokeper.conectString：zk的token存储连接串，默认是 localhost:2181；hive.cluster.delegation.token.store.zokeper.znode：token存储的节点跟路径，默 认是/hive/cluster/delegation；hive.cluster.delegation.token.store.zokeper.acl：token存储的 ACL，默认是sasl:hive/host1@example.com:cdrwa,sasl:hive/host2@example.com:cdrwa； hive.use.input.primary.region：从⼀张input表创建表时，创建这个表到input表的主region，默认是 true；hive.default.region.name：默认region的名字，默认是default；hive.region.properties：region 的默认的⽂件系统和jobtracker，默认是空；hive.cli.print.header：查询输出时是否打印名字和列，默 认是false；hive.cli.print.curent.db：hive的提示⾥是否包含当前的db，默认是false； hive.hbase.wal.enabled：写⼊hbase时是否强制写wal⽇志，默认是true；hive.hwi.war.file：hive在 web接⼝是的war⽂件的路径，默认是lib/hive-hwi- x(version).war；hive.hwi.listen.host：hwi监听 的host地址，默认是0.0.0.0；hive.hwi.listen.port：hwi监听的端⼝，默认是 9；hive.test.mode： hive是否运⾏在测试模式，默认是false；hive.test.mode.prefix：在测试模式运⾏时，表的前缀字符 串，默认是test_；hive.test.mode.samplefreq：如果hive在测试模式运⾏，并且表未分桶，抽样频率 是多少，默认是32；hive.test.mode.nosamplelist：在测试模式运⾏时不进⾏抽样的表列表，默认是 空；

来源:htp:/ w.blogjava.net/changedi/archive/2013/1/13/406295.html

# 参考资料:htp:/blog.fens.me/hadop-hive-intro/

