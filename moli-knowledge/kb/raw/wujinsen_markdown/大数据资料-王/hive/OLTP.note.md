On-Line Transaction Procesing 系统(OLTP) 也称为⾯向交易的处理系统，其基本特征是顾客的 可以⽴即传送到 进⾏处理，并在 很短的时间内给出处理结果。

联机事务处理

原始数据 计算中⼼

⽬录

1定义 2特征 3应用领域 4什么是OLAP 5两者区别

# 1定义编辑

这样做的最⼤优点是可以即时地处理输⼊的数据，及时地回答。也称为 (Real time System)。 衡量联机 的⼀个重要性能指标是系统性能，具体体现为实时响应时间(Response Time)， 即⽤户在 上送⼊数据之后，到 对这个请求给出答复所需要的时间。OLTP是由 负 责完成的。 OLTP 旨在使 仅写⼊所需的数据，以便尽快处理单个事务。

实时系统

事务处理系统 终端 计算机 数据库引擎

数据库 事务应⽤程序

# 2特征编辑

⽀持⼤量并发⽤户定期添加和修改数据。 反映随时变化的单位状态，但不保存其历史记录。 包含⼤量数据，其中包括⽤于验证事务的⼤量数据。 具有复杂的结构。 可以进⾏优化以对 活动做出响应。 提供⽤于⽀持单位⽇常运营的技术基础结构。 个别 能够很快地完成，并且只需访问相对较少的数据。OLTP 系统旨在处理同时输⼊的成百上千的

事务

事务 事务

。 实时性要求⾼。 数据量不是很⼤。 交易⼀般是确定的，所以OLTP是对确定性的数据进⾏存取。(⽐如存取款都有⼀个特定的⾦额) 并发性要求⾼并且严格的要求事务的完整、安全性。(⽐如这种情况：有可能你和你的家⼈同时在不同 的银⾏取同⼀个帐号的款)。

# 3应⽤领域编辑

OLTP 系统中的数据主要被组织为⽀持如下 ： 记录来⾃销售点终端或通过⽹站输⼊的订单。

事务

当库存量降到指定级别时，订购更多的货物。 在 中将零部件组装为成品时对零部件进⾏跟踪。 记录雇员数据。 当今的 ⼤致可以分成两⼤类： OLTP（on-line transaction procesing）、

制造⼚

数据处理 联机事务处理 联机 分析处理 关系型数据库

OLAP（On-Line Analytical Procesing）。OLTP是传统的 的主要应⽤，主要是 基本的、⽇常的事务处理，例如银⾏交易。OLAP是 系统的主要应⽤，⽀持复杂的分析操作， 侧重决策⽀持，并且提供直观易懂的查询结果。

数据仓库

