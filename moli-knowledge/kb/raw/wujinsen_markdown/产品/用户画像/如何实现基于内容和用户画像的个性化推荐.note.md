基于内容和⽤户画像的个性化推荐，有两个实体：内容和⽤户。需要有⼀个联系这两者的东⻄，即为 标签。内容转换为标签即为内容特征化，⽤户则称为⽤户特征化。

因此，对于此种推荐，主要分为以下⼏个关键部分：

标签库

内容特征化 ⽤户特征化 隐语义推荐

综合上⾯讲述的各个部分即可实现⼀个基于内容和⽤户画像的个性化推荐系统。如下图所示：

标签库 标签是联系⽤户与物品、内容以及物品、内容之间的纽带，也是反应⽤户兴趣的重要数据源。标签库 的最终⽤途在于对⽤户进⾏⾏为、属性标记。是将其他实体转换为计算机可以理解的语⾔关键的⼀ 步。 标签库则是对标签进⾏聚合的系统，包括对标签的管理、更新等。 ⼀般来说，标签是以层级的形式组织的。可以有⼀级维度、⼆级维度等。 标签的来源主要有：

已有内容的标签

⽹络抓取流⾏标签

对运营的内容进⾏关键词提取

对于内容的关键词提取，使⽤结巴分词 + TFIDF即可。此外，也可以使⽤TextRank来提取内容关键 词。 内容特征化 内容特征化即给内容打标签。⽬前有两种⽅式：

⼈⼯打标签

机器⾃动打标签

针对机器⾃动打标签，需要采取机器学习的相关算法来实现，即针对⼀系列给定的标签，给内容选取 其中匹配度最⾼的⼏个标签。这不同于通常的分类和聚类算法。可以采取使⽤分词 + Word2Vec来实 现，过程如下：

将⽂本语料进⾏分词，以空格,tab隔开都可以，使⽤结巴分词。

使⽤word2vec训练词的相似度模型。

使⽤tfidf提取内容的关键词A,B,C。

遍历每⼀个标签，计算关键词与此标签的相似度之和。

取出TopN相似度最⾼的标签即为此内容的标签。(N这⾥取3)

⽤户特征化 ⽤户特征化即为⽤户打标签。通过⽤户的⾏为⽇志和⼀定的模型算法得到⽤户的每个标签的权重。

⽤户对内容的⾏为：点击、不敢兴趣、浏览

对内容发⽣的⾏为可以认为对此内容所带的标签的⾏为

⽤户的兴趣是时间衰减的，即离当前时间越远的兴趣⽐重越低。时间衰减函数使⽤1/[log(t)+1], t为 事件发⽣的时间距离当前时间的⼤⼩

要考虑到热⻔内容会⼲预⽤户的标签，需要对热⻔内容进⾏降权。使⽤click/pv来降低热⻔内容的权 重

隐语义推荐 有了内容特征和⽤户特征，可以使⽤隐语义模型进⾏推荐。这⾥可以使⽤其简化形式，以达到实时计 算的⽬的。 ⽤户对于某⼀个内容的兴趣度(可以认为是CTR)：

其中i=1…N是内容具有的标签，m(ci)指的内容c和标签i的关联度(⽬前都为1),n(ui)指的是⽤户u的标签i 的权重值,q©指的是内容c的质量，暂时使⽤点击率表示。

