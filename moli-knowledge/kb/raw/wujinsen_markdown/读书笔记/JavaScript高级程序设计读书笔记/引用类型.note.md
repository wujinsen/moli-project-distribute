Aray类型。 ECMAScript数组的每⼀项可以保存任何类型的数据。 数组⼤⼩是可以动态调整的。 创建数组第⼀种基本⽅式⽅式: 使⽤Aray构造函数 var colors = new Aray(); var colors = new Aray(20);/length为20的数组f var colors = new Aray("Red","Blue","Yelow",1);/创建包含3个字符串和⼀个数字的数组 创建数组第⼆种基本⽅式：使⽤数组字⾯量表示法 var c o l o r s = ["Red","Blue","Green"]; var colors = [];//创建空的数组 var colors = [1,2,];//不推荐这样做，会创建2或3项的数组 var colors = [,,,,,];//不推荐这样做 数组的定义和创建要符合语法规范，不然可能出现意想不到的错误。 数组的内容可以修改，在javascript中，数组的⻓度也可以修改。 var c o l o r s = ["Red","Blue","Green"];//数组下标从0开始，此处数组元素为 colors[0],colors[1],colors[2] alert(colors.length);//4 colors.length为数组⻓度 alert(colors.lenth-1);//3 数组最后⼀项元素是colors.length-1 colors.length = 2;//修改数组⻓度为2 alert(colors[2]);//undeﬁned 因为把修改数组⻓度为2,数组元素只有colors[0] colors[1] 利⽤数组的length可以⽅便的在数组末尾添加新的元素。 var c o l o r s = ["Red","Blue","Green"]; colors[colors.length] = "Black";//在3位置添加⼀个新的元素 colors[colors.length] = "Brown";//在4位置添加⼀个新的元素 检测数组 单个⻚⾯或者⼀个全局作⽤域，可以使⽤instanceof来检测是否是Array类型 if(value instanceof Array){

//do something...

} 如果⽹⻚中包含多个框架，就存在多个不同的全局执⾏环境，存在多个不同版本的Array构造函数，此 种情况instanceof⽆效。 ECMAScript5新增了Array.isArray()⽅法⽤于解决以上问题。

⽀持Array.isArray()⽅法的浏览器有IE9+，FireFox4+,Safari5+,Opera10.5+和Chrome 转换⽅法 所有对象都具有toLocaleString() toString() valueOf()⽅法。 var c o l o r s = ["Red","Blue","Green"]; console.log(colors.toString());//Red,Blue,Green 字符串格式 console.log(colors.valueOf());//Red,Blue,Green 数组 console.log(colors);

栈⽅法 ECMAScript也提供了类似栈的数据结构。 栈的数据结构是LIFO(Last Input Fist Out)后进先出。 push():将元素添加到数组末尾 prop():取得最后⼀个元素 var colors = new Array(); var count = colors.push("Red","Blue"); alert(count);//2 队列⽅法 队列的数据结构是FIFO 数组操作⽅法 concat():基于当前数组所有项创建⼀个新的数组 var c o l o r s = ["Red","Blue","Green"]; var colors2 = colors.concat("Yellow",["Black","Brown"]); alert(colors);//Red,Blue,Green alert(colors2);//Red,Blue,Green,Yellow,Black,Brown slice():基于当前数组中的⼀项或多项创建⼀个新的数组 var c o l o r s = ["Red","Blue","Green","Black","Orange"];

- var colors2 = colors.slice(1);

- var colors3 = colors.slice(1,4);


- console.log(colors2);//Blue,Geen,Black,Orange

- console.log(colors3);//Blue,Green,Black


Function类型 function sum(num1, num2){//函数声明

return num1+num2; }

var sum = funciton(num1, num2){//函数表达式定义函数 return num1+num2;

} 这两种函数定义⽅式⼏乎相同。第⼆个代码定义了变量sum并将其初始化为⼀个函数。

函数声明与函数表达式 解析器会率先读取函数声明,并使其在执⾏任何代码之前可⽤。 alert(sum(10,10)); function sum(num1,num2){//函数声明

return num1+num2;

} alert(sum(10,10);//unexpected identiﬁer错误 var sum = function(num1,num2){//函数表达式

return num1+num2; }

以上代码运⾏没问题。

函数内部属性 函数内部有两个特殊对象arguments和this。 arguments是⼀个类数组对象，⽤于保存函数参数，这个对象有⼀个callee属性，该属性是⼀个指针， 指向拥有这个arguments对象的函数。callee可以消除紧耦合

//经典的阶乘函数

# // 函 数 内 部 属 性 a r g u m e n t s . c a l l e e 属 性 //c a l l e e 属 性 是 ⼀ 个 指 针 ， 指 向 拥 有 这 个 a r g u m e n t s 对 象 的 函 数 function f a c t o r i a l (num) {// 函 数 声 明

if (num <= 1) { return 1; } else {

# return num * f a c t o r i a l (num - 1);// 为了 消 除 紧 耦 合 ， 使 ⽤ a r g u m e n t s . c a l l e e }

} var f = f a c t o r i a l ; f a c t o r i a l = function(){

return 0;

console.log("f: "+f (5));// 返 回 0 console.log("factorial(5): "+f a c t o r i a l (5));

function f a c t o r i a l (num){

if (num <= 1) { return 1; } else {

return num * arguments.callee(num - 1);// 为了 消 除 紧 耦 合 ， 使 ⽤ a r g u m e n t s . c a l l e e }

} console.log("f: "+f (5));// 1 2 0 console.log("factorial(5): "+f a c t o r i a l (5));// 0

this对象。 window.color = "red"; var o = {color: "blue"}; function sayColor(){ return this.color;

} sayColor();//"red" 函数是全局定义的，this引⽤的是全局对象window

- o.sayColor = sayColor;//要想不执⾏函数⽽是访问函数指针的话，去掉括号就⾏了

- o.sayColor();


基本包装类型

- var s1 = "Hello World";

- var s2 = s1.substring(2); s1是基本类型，包含了字符串。基本类型不是对象，从逻辑上讲不能有substring()⽅法。 实际上后台 已经完成了⼀下操作：


- 1.创建String类型的⼀个实例。

- 2.在实例上调⽤指定的⽅法

- 3.销毁这个实例 var s1 = "Hello World"; s1.color = "red"; alert(s1.color);//undeﬁned


第⼆⾏创建的String对象在执⾏第三⾏代码时已经被销毁了。第三⾏代码⼜创建了⾃⼰的String对象， 然⽽该对象没有color属性。 var obj = new Object("hello"); alert(obj instanceof String);//true

尽管不建议显示的创建包装类型对象，但他们操作基本类型能⼒⾮常重要，有必要了解。 Boolean类型

函数属性和⽅法

每个函数都包含两个⾮继承⽽来的⽅法:apply()和call()。这两个⽅法的⽤途都是在特定的作⽤域中调⽤ 函数。

apply()⽅法接收两个参数:⼀个是在其中运⾏函数的作⽤域，另⼀个是参数数组。 function sum(num1, num2){

return num1+num2;

} function callSum1(num1, num2){

return sum.apply(this, arguments);

} function callSum2(num1, num2){

return sum.apply(this, [num1, num2]);

} alert(callSum1);//20 alert(callSum2);//20 call()⽅法和apply()⽅法的作⽤相同，它们的区别仅在于接收参数的⽅式不同。如果使⽤call()⽅法，必 须明确传⼊每个参数 function sum(num1, num2){

return num1+num2;

} function callSum1(num1, num2){

return sum.call(this, arguments);

function callSum2(num1, num2){ return sum.call(this, [num1, num2]);

} apply()和call()除了传递⽅法之外，真正强⼤的地⽅是能够扩充函数依赖运⾏的作⽤域。 window.color = "red"; var o = {color: "blue"}; function sayColor(){ return this.color;

} sayColor.call(this);//red sayColor()函数是全局定义的，所以this引⽤的是全局对象 sayColor.call(window);//red sayColor.call(o);//blue 使⽤call()或apply()来扩充作⽤域的最⼤好处就是对象不需要与⽅法有任何的耦合关系。 之前使⽤的步骤可以省略了，之前的代码中以下两个步骤可以省略。

- o.sayColor = sayColor;

- o.sayColor(); ECMAScript5还定义了⼀个⽅法bind()。这个⽅法会创建⼀个函数的实例，其this值会被绑定到传给 bind()函数的值 window.color = "red"; var o = {color: "blue"}; function sayColor(){


alert(this.color); }

