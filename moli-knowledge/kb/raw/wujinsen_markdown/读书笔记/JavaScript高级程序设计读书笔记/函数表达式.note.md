定义函数两种⽅式：

- 1.函数声明 function sayHi(){

alert("Hi");

} sayHi();//调⽤函数

- 2.函数表达式 var sayHi = function(){


alert("Hi");

} sayHi();//调⽤函数 函数声明提升(function declaration hoisting)：在执⾏代码之前会先读取函数声明。 sayHi(); function sayHi(){//函数声明可以放到调⽤它的语句后⾯

alert("Hi");

} sayHi();//函数表达式则不⾏，会报错，找不到这个函数 var sayHi = function(){

alert("Hi"); }

递归

function f a c t o r i a l 2 (num){

if (num <= 1) { return 1; } else {

# return num * f a c t o r i a l 2 (num - 1);// 为了 消 除 紧 耦 合 ， 使 ⽤ a r g u m e n t s . c a l l e e }

} var f 2 = f a c t o r i a l 2 ; f a c t o r i a l 2 = null;

# // c o n s o l e . l o g ( " f a c t o r i a l 2 ( 5 ) : " + f a c t o r i a l 2 ( 5 ) ) ; console.log("f2: "+f 2 (5));// 使 ⽤ a r g u m e n t s . c a l l e e 就 不 会 报 错

factorial2()函数保存在变量f2中，然后将factorial2变量设置为null，结果指向原始函数的引⽤只剩下 ⼀个。接下来调⽤f()时，由于必须执⾏factorial2()，⽽factorial2已经不再是函数，所以会报错。可以 使⽤arguments.callee解决这个问题。 function f a c t o r i a l 2 (num){

if (num <= 1) { return 1; } else {

return num * arguments.callee(num - 1);// 为了 消 除 紧 耦 合 ， 使 ⽤ a r g u m e n t s . c a l l e e }

} var f 2 = f a c t o r i a l 2 ; f a c t o r i a l 2 = null;

# // c o n s o l e . l o g ( " f a c t o r i a l 2 ( 5 ) : " + f a c t o r i a l 2 ( 5 ) ) ; console.log("f2: "+f 2 (5));// 使 ⽤ a r g u m e n t s . c a l l e e 就 不 会 报 错

闭包 指有权访问另⼀个函数作⽤域中的变量的函数。创建闭包的常⽤⽅式，就是在⼀个函数内部创建另⼀ 个函数。

闭包与变量 由于作⽤域链的机制,闭包只能取得包含函数中任何变量的最后⼀个值。 闭包保存的是整个变量对象，⽽不是某个特殊的对象。

# // 闭 包 与 变 量 function c r e a t e F u n c t i o n s () {

var result = new Array(); for (var i = 0; i < 10; i++) {

result[i] = function () {

return i; }

} return result;

}

function c r e a t e F u n c t i o n s () { var result = new Array(); for (var i = 0; i < 10; i++) {

result[i] = function (num) { return function(){

return num; };

}(i);

} return result;

}

this对象 this对象的绑定取决于运⾏时函数的执⾏环境:全局函数中，this等于window，⽽函数被作为某个对象 的⽅法调⽤时，this等于那个对象。

- var n a m e = "Hello World";

- var o b j e c t = { name: "HaHa", getNameFunc: function(){


return function(){// 返 回 匿名 函 数

return this.name; }

}

# } console.log(o b j e c t .getNameFunc()());//" H e l l o W o r l d " ⾮ 严 格 模 式 下 输 出 的 是 全 局对 象

- var n a m e 2 = "Hello World";

- var o b j e c t 2 = { name2: "HaHa", getNameFunc: function(){


var that = this; return function(){// 返 回 匿名 函 数

return that.name2; }

}

} console.log(o b j e c t 2 .getNameFunc()());//" H a H a " 输 出 的 是 t h i s 对 象

- var n a m e 3 = "Hello World";

- var o b j e c t 3 = {


name3: "Haha", getName: function(){

return this.name3; }

} console.log("object3.getName(): "+o b j e c t 3 .getName());//H a H a console.log((o b j e c t 3 .getName)());//H a H a console.log((o b j e c t 3 .getName = o b j e c t 3 .getName)());//H e l l o W o r l d

