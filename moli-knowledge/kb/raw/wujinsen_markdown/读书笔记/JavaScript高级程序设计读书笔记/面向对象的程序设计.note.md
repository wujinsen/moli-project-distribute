创建对象

⼯⼚模式 function createPerson(name, age, job){

htp:/wiki.zirom.com/pages/viewpage.action?pageId=4059601

var o = new Object();

- o.name = name;
- o.age = age;
- o.job = job;
- o.sayName = function(){ alert(this.name);


} return o;

}

- var person1 = createPerson("zhangsan", 18, "JavaScript Progra mer");
- var person2 = createPerson("lisi", 18, "Java Progra mer");


构造函数模式

function Person(name, age, job){ this.name = name; this.age = age; this.job = job; this.sayName = function(){

alert(this.name); }

}

- var person1 = createPerson("zhangsan", 18, "JavaScript Progra mer");
- var person2 = createPerson("lisi", 18, "Java Progra mer"); 这部分代码跟⼯⼚模式代码不同之处是： 没有显示的创建对象 直接将属性和⽅法赋值给了this对象 没有return语句


2.构造函数的问题 function Person(name, age, job){

this.name = name; this.age = age; this.job = job; this.sayName = sayName;

} function sayName(){

alert(this.name); }

- var person1 = Person("zhangsan", 18, "JavaScript Progra mer");
- var person2 = Person("lisi", 18, "Java Progra mer"); 这⾥把sayName()函数的定义放到了构造函数外⾯，全局定义。因此person1和person2对象共享了在 全局作⽤域中定义的同⼀个sayName()函数。 问题也来了，如果对象要定义很多⽅法，那么就需要定义很多全局函数，我们⾃定义的引⽤类型就没 有封装性可⾔了。好在原型模式可以解决这个问题。 原型模式 function P e r s o n 2 () {


P e r s o n 2 .prototype.name = "zhangsan"; P e r s o n 2 .prototype.age = 18; P e r s o n 2 .prototype.sayName = function () {

console.log(("Person2 sayName: " + this.name)); }

}

- var p 1 = new P e r s o n 2 ();// 调 ⽤ 了 ⼀ 个 词 s a y N a m e p 1 .sayName();

- var p 2 = new P e r s o n 2 ();


// p 2 . s a y N a m e ( ) ;

console.log((p 1 .sayName() == p 2 .sayName())); console.log("isPrototypeOf: "+P e r s o n 2 .prototype.isPrototypeOf(p1)); isPrototypeOf()⽅法 原型对象 只要创建了⼀个新的函数，就会根据⼀组特定的规则为该函数创建⼀个prototype属性，这个属性指向 函数的原型对象。

