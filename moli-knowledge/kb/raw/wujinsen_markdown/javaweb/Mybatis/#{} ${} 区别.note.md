最近在⽤mybatis，之前⽤过ibatis，总体来说差不多，不过还是遇到了不少问题，再次记录下， ⽐如说⽤#{}，和 ${}传参的区别， 使⽤#传⼊参数是，sql语句解析是会加上 ",⽐如 select * from table where name = #{name} ,传⼊的 name为⼩李，那么最后打印出来的就是 select * from table where name = ‘⼩李'，就是会当成字符串来解析，这样相⽐于$的好处是⽐较明显 对的吧，#{}传参能防⽌sql注⼊，如果你传⼊的参数为 单引号'，那么如果使⽤${},这种⽅式 那么是会 报错的， 另外⼀种场景是，如果你要做动态的排序，⽐如 order by column，这个时候务必要⽤${},因为如果你 使⽤了#{},那么打印出来的将会是 select * from table order by 'name' ,这样是没⽤， ⽬前来看，能⽤#就不要⽤$, mybatis中的#和$的区别

- 1. #将传⼊的数据都当成⼀个字符串，会对⾃动传⼊的数据加⼀个双引号。如：order by #user_id#， 如果传⼊的值是 1,那么解析成sql时的值为order by " 1", 如果传⼊的值是id，则解析成的sql为order by "id".
- 2. $将传⼊的数据直接显示⽣成在sql中。如：order by $user_id$，如果传⼊的值是 1,那么解析成sql 时的值为order by user_id, 如果传⼊的值是id，则解析成的sql为order by id.
- 3. #⽅式能够很⼤程度防⽌sql注⼊。
- 4.$⽅式⽆法防⽌Sql注⼊。
- 5.$⽅式⼀般⽤于传⼊数据库对象，例如传⼊表名.
- 6.⼀般能⽤#的就别⽤$. MyBatis排序时使⽤order by 动态参数时需要注意，⽤$⽽不是# 字符串替换 默认情况下，使⽤#{}格式的语法会导致MyBatis创建预处理语句属性并以它为背景设置安全的值（⽐ 如?）。这样做很安全，很迅速也是⾸选做法，有时你只是想直接在SQL语句中插⼊⼀个不改变的字符 串。⽐如，像ORDER BY，你可以这样来使⽤： ORDER BY ${columnName} 这⾥MyBatis不会修改或转义字符串。 重要：接受从⽤户输出的内容并提供给语句中不变的字符串，这样做是不安全的。这会导致潜在的 SQL注⼊攻击，因此你不应该允许⽤户输⼊这些字段，或者通常⾃⾏转义并检查。 Mybatis中$和#的区别简单⼩结 前不久，有⼈来我们公司⾯试，我们的经理问道了这个问题，我也是⼀知半解，所以就去百度了⼀ 番。 其实区别很简单的，举个例⼦⼤家就会明⽩的。写⼀句SQL-例如：select * from user_role where user_code = "10"; 这句话⽽⾔，需要写成 select * from ${tableName} where user_code = #{userCode} 所以，$符是直接拼成sql的 ，#符则会以字符串的形式 与sql进⾏拼接。


