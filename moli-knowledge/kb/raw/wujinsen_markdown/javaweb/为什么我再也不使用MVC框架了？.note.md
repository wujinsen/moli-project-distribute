该⽂通过作者亲身设计思考经历，从遭遇MVC使⽤问题开始，然后引⼊函数式Reactive响应式编程，重新设 计了前端架构，最后颠覆了基于 范式的传统MVC模式。

O

以下是原⽂⼤意翻译：

这些天我的最糟糕⼯作是为前端开发⼈员设计API，下⾯对话是不可避免的： 前端开发⼈员: 这个屏幕有数据元素x,y,z. 你能创建⼀个API输出格式是 {x: , y:, z: }吗？ 我：Ok

其实毫⽆争议的是，⽆数个API项⽬都是和经常变化的屏幕绑定的。你会有很多针对不同平台或终端设备的不 同API，甚⾄Sam Newman将其总结为 ，该模式建议为每个类型的设备或平台开发对应特殊的API， 甚⾄为AP的不同版本。Netflix的 将其解释为Experience APIs，⽆语。

BF模式 Daniel Jacobson

⼏个⽉以前，我开始⼀段思考旅程，思考为什么我们会最终变成这样，我们还能够做些什么？这段思考旅程 将我引向了应⽤架构中最教条的模式：MVC。当我再游历了reactive响应式编程和函数编程等技术思想以后， 我开始聚焦简单和业界擅⻓的偷偷膨胀的过程，我相信你会感兴趣我的发现。

每个屏幕都在使⽤的模式是MVC，也就是Mode-View-Controler，MVC在没有Web时已经被发明出来，其软 件架构是很棒的，起初，厚客户端是直接和⼀个数据库交互访问通讯，数⼗年以后，使⽤了MVC，⽤于构建

OmniChanel 应⽤

。

Angular2即将发布，也许现在是重新评估MVC⽤途的最好时候，重新评估MVC框架为应⽤架构带来的价值。

90年代后期，MVC模式被Struts采⽤到基于Htp的Web应⽤中，今天它是所有应⽤的基⽯。

“React is jus t the View in MVC”.

MVC如此光彩照⼈，以⾄于react.js只能使⽤委婉的⽅式介绍它们的框架似乎偏离了MVC主流：

当我去年开始使⽤React时，我感觉有些⾮常不同之处，如果你改变某个地⽅的⼀部分数据，那么⽴即，⽽且 没有视图View和模型Model之间明显的交互，整个UI就改变了，这就是说，我很失望于React的编程模型，变 得孤独起来，因为从MVC⻆度来看，React模型将MVC分离的⼏个关注混淆在同⼀个组件中。

作为⼀个服务器端后端的API设计者，我得出结论，肯定没有好的办法将API调⽤和React前端协调在⼀起，精 确地说，因为React只注重视图View，竟然没有MVC中的控制器Controler。

Flux模式 Redux

Facebok⼀直拒绝在框架层次弥补这个“缺憾”，React团队⾸先引⼊了 。因为对Facebok的失望， Dan Abramov甚⾄推荐了另外⼀个模式： ，某种程度上⾛上了正确⽅向，但是也没有提供合适⽅式来 连接API和前端。

Gogle在这⽅⾯⼀直似乎很擅⻓，不断推出从GWT， Android SDK到Angular。但是Angular 1并不是基于组 件的概念，⽽是使⽤定制的逻辑将控制器和⻚⾯各种元素连接在⼀起，由此带来了复杂性。那么基于组件的 Angular2会更加简单⼀些吗？也不是⾮常简单，Angular2核⼼包有180个语法，整个框架将近有50个语法， 都是基于HTML5和CS3，谁有时间学习和掌握这样⼀个框架⽤来构建⼀个Web应⽤呢？那么Angular3会如 何呢？

在使⽤了React和预⻅了即将发布的Angular2，我感觉沮丧：这些框架有组织地强迫我使⽤BF模式，每个后 端API都必须匹配⼀种屏幕的数据集。

我开始诅咒了，再也不使⽤React Angular 和任何MVC框架来开发Web应⽤，并且试图在视图和底层API之间 发现⼀个更好的衔接⽅式。

我真正喜欢React是它的模型和视图之间关系，React并不是基于模板的事实，视图⾃⼰没有办法请求后端的 数据，你只能将后端数据传递给视图。

React在 系列纯函数中解耦了视图

当你研究React久了以后，你会意识到 ，这就是JSX语法： <V params={M}/> 和如下纯函数没有任何区别： V = f( M )

我使⽤⾃⼰的Gliph作为案例说明这种JSX的函数意义，它是使⽤如下函数构建的：

![image 1](<为什么我再也不使用MVC框架了？.note_images/imageFile1.png>)

这个函数是如下模型作为参数输⼊：

![image 2](<为什么我再也不使用MVC框架了？.note_images/imageFile2.png>)

当你意识到普通Javascript函数也能很好实现时，你会疑问为什么还⽤使⽤React的这种⽅式呢？

# htp s:/github.com/Mat-Esch/virtual-dom

是因为virtual-dom虚拟Dom？如果你喜欢你可以需要它，但是不肯定更多⼈会喜欢，还有其他可选如：

是因为GraphQL? 不是真的，GraphQL只不过是⼀种声明式的⽅式来创建⼀个视图模型。强迫变形模型去匹 配视图本身就是⼀个问题, ⽽不是解决⽅案。它不过是被前端⼯程师因为来⾃视图需求 强⾏编写的，希望从客户端获得它们想要的，根本不会考虑其他任何设计。

GraphQL团队好像在JSX语法后⾯迷失了，微妙的改变是函数隔离了模型与视图，不像模板性语⾔或被前端 ⼯程编写的查询语⾔(GraphQL)，函数并不需要模型强迫去适应视图：

当视图从⼀个函数（⽽不是⼀个模板或查询）创建后，你能转换需要的模型到视图进⾏渲染，不需要任何额 外的代价⽐如变形扭曲你的模型。

举例来说，如果⼀个视图显示模型中⼀个值v，并且还需要显示⼀个图像显示器⽤来表达这个值v是很好 好或 坏⼏个程度，这⾥没有任何理由将显示器显示的这⼏个程度数据也放⼊你的模型：因为函数应该可以简单地 使⽤模型中的值v计算出显示器的这个⼏个程度值。

直接将这种计算嵌⼊视图也不是好主意，但是将视图-模型view-model 作为⼀个纯函数也许并不困难，当你 需要⼀个明确的view-model时，也没有什么特别理由需要使⽤GraphQL：

V = f( vm(M) )

这种函数式的⽅式有⼏个好处：⾸先，就像React，它允许你解耦你的视图到组件，他们创建的⾃然接⼝允许 你以不同技术渲染你的视图，函数实现能够潜在地增强我们实现响应式设计。

我并不奇怪，以后⼏个与⼈们开始将HTML5主题作为基于组件的Javascript函数传递，⽽⾃⼰在做我的⽹站 项⽬时，我弄⼀个模板，⽴即使⽤javascript函数将他包装起来，我再也不使⽤WordPres，在同样的努⼒付 出情况下，我能够得到HTML5和CS3的优点。

这种⽅式重新定义了设计者和开发者之间的关系，任何⼈能够编写这些javascript函数，尤其是模板设计者， 不需要有任何绑定语法需要学习，没有JSX，没有Angular 模板，只是普通的Javascript函数。

有趣的是，从⼀个reactive流程⻆度看，这些函数能够部署到任何地⽅：后端或客户端前端。

但是更重要的是，这些⽅式允许视图定义与模型之间最⼩的联系，将决定权留给模型，以便其以最好的⽅式 将数据带给视图，不像 懒加载或混合管理orchestration，⼀致性是在模型的可控范围内，不像模板或 GraphQL，再也不需要⼀种从视图直接发出请求的⽅式。

缓存

现在我们使⽤函数包装Html5+CS3的⽅式解耦了视图与模型，那么下⼀步是如何创建⼀个模型，控制器应 该是什么样，为了回答这个问题，让我们回到MVC。

![image 3](<为什么我再也不使用MVC框架了？.note_images/imageFile3.png>)

（此处略去原作者分析MVC原理，直接得出结论）

如何整合MVC中的Action控制器到⼀个响应式reactive流程中，这⾥引⼊了Dr. Lamport发明的“Temporal Logic of Actions”，简称 ，在TLA+中，Action是⼀种纯函数：

# TLA+

dataʼ = A (data)

上⾯的A代表Action，那么，⼀个reactive MVC应该如下：

V = f( M.present( A(data) )

上⾯V代表视图View，⽽M代表Model，这个表达式意思是：当⼀个Action被触发时，它会计算⼀个来⾃集合 或输⼊⽐如⽤户的输⼊的数据集，这些数据集将会被呈现给模型，由模型决定是否更新⾃⼰状态，⼀旦更新 完成，视图将渲染新的模型状态，这样Reactive循环结束。模型持久或从数据库获得数据等流程不应该由前 端⼯程师完成，这是后端⼯程师的职责所在。

Action应该是⼀个纯函数，没有任何状态或副作⽤。

⼀个Reactive MVC模型是有趣的，因为除了模型以外，其他都是纯函数。

（此处略去作者举例，直接查看他使⽤纯函数实现MVC各个部件的案例贴图）

![image 4](<为什么我再也不使用MVC框架了？.note_images/imageFile4.png>)

# Java Javascript

这⾥是作者编写的开源库包： 和 ，这是使⽤ WebSocket, Poling 和 Queuing实现浏览器和服 务器交互的案例。

现在我们已经引⼊了⼀个新的模式，作为MVC的替换，也就是SAM模型（State-Action-Model），这是⼀个 reactive 函数式的模型，其根源来⾃React.js和TLA+

SAM模式能够使⽤下⾯表达式表达：

V = S( vm( M.present( A(data) ) ), nap(M)

这个公式意思是：在⼀个action A动作实施以后，系统的V将被计算出来。这个关于模型的⼀个纯函数。

在SAM中，A和VM(View-Model)以及nap(next-action predicate预测)和S(代表状态)是⽽且必须是纯函数， 使⽤SAM，我们普遍称谓状态(系统的属性值)是限制在模型这个范围内，改变这些值的逻辑是封装在模型 中，⽆法从模型以外能够看到的。

⼀旦状态表现被创建，nap()代表下⼀个action的预测将是⼀个回调调⽤。以它⾃⼰⽅式渲染给⽤户。

![image 5](<为什么我再也不使用MVC框架了？.note_images/imageFile5.png>)

该⽂还介绍了SAM的函数组合⽅式 .

SAM模式完成改变了前端架构范式，基于TLA+，业务逻辑将被清晰地分解为： Actions作为纯函数 CRUD操作在模型内部 (banq注：类型 领域模型) 控制⾃动Action的状态State。

D

# Hivepod.io

使⽤SAM模式，微服务正好⾃然适合在模型之后，诸如 之类框架正好插⼊此位置，⾮常完美。

⾃此，⼏⼗年的⾯向对象消失了，如果没有reactive或函数式⽅式我⼏乎不能够思考。⼀旦使⽤SAM，我就能 够聚精会神地设计API和服务，再也不必⼀个屏幕⼀个API服务了。

本贴是⼤意翻译，原⽂请参考：

# Why I No Longer Use MVC Frameworks

