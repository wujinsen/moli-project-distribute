在使⽤zokeper的过程中,我们经常会看到这样⼀些说法:

- 1.zokeper cluster的节点数⽬必须是奇数。
- 2.zokeper 集群中必须超过半数节点(Majority)可⽤，整个集群才能对外可⽤。 这个说法在⼤多数情况下是正确的。 实际上ZoKeper提供了⼏种⽅式来认定整个集群是否可⽤，Majority只是其中的⼀种。 (


htp:/zokeper.apache.org/doc/r3.3.5/zokeperInternals.html)

- 1. Majority Quorums
- 2. Weight
- 3. Hierarchy of groups 所谓整个集群是否可⽤，隐含的⼀个意思就是整个集群还能够选举出⼀个”Leader”。 ZoKeper默认设置的是采⽤Majority Qunroms的⽅式来⽀持Leader选举。在ZoKeper中Quorums 有2个作⽤：


- 1. 集群中最少的节点数⽤来选举Leader保证集群可⽤
- 2. 通知客户端数据已经安全保存前集群中最少数量的节点数已经保存了该数据。⼀旦这些节点保存了 该数据，客户端将被通知已经安全保存了，可以继续其他任务。⽽集群中剩余的节点将会最终也保存 了该数据 采⽤Quoroms投票的⽅式来选举Leader主要是为了解决“Split-Brain”问题(


htp:/linux-ha.org/wiki/Spli t_Brain

)。 Split-Brain问题说的是1个集群如果发⽣了⽹络故障，很可能出现1个集群分成了两部分，⽽这两个部 分都不知道对⽅是否存活，不知道到底是⽹络问题还是直接机器down了，所以这两部分都要选举1个 Leader，⽽⼀旦两部分都选出了Leader, 并且⽹络⼜恢复了，那么就会出现两个Brain的情况，整个集 群的⾏为不⼀致了。 所以集群要防⽌出现Split-Brain的问题出现，Quoroms是⼀种⽅式，即只有集群中超过半数节点投票 才能选举出Leader。 这样的⽅式可以确保leader的唯⼀性,要么选出唯⼀的⼀个leader,要么选举失败. ZoKeper默认采⽤了这种⽅式。更⼴义地解决Split-Brain的问题，⼀般有3种⽅式:

- 1. Quorums
- 2. 采⽤Redundant comunications，冗余通信的⽅式，集群中采⽤多种通信⽅式，防⽌⼀种通信⽅式 失效导致集群中的节点⽆法通信。
- 3. Fencing, 共享资源的⽅式，⽐如能看到共享资源就表示在集群中，能够获得共享资源的锁的就是 Leader，看不到共享资源的，就不在集群中 理解了Quorums就不难理解为什么集群中的节点数⼀般配置为奇数。节点数配置成奇数的集群的容忍 度更⾼。


- ⽐如3个节点的集群，Quorums = 2, 也就是说集群可以容忍1个节点失效，这时候还能选举出1个lead， 集群还可⽤
- ⽐如4个节点的集群，它的Quorums = 3，Quorums要超过3，相当于集群的容忍度还是1，如果2个节 点失效，那么整个集群还是⽆效的


所以4个节点的集群的容忍度 = 3个节点的集群的容忍度，但是4个节点的集群多了1个节点，相当于浪 费了资源。 更极端的例⼦是10个节点的集群，如果⽹络问题导致分为两个部分，50个节点和50个节点，这样整 个集群还是不可⽤的，因为按照Quorums的⽅式必须51个节点才能保证选出1个Leader。这时候可以采 ⽤Weight加权的⽅式，有些节点的权值⾼，有些节点的权值低，最后计算权值，只要权值过半，也能 选出1个Leader

