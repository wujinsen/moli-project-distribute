Spark

Streaming提供了⾼效便捷的流式处理模式，但是在有些场景下，使⽤默认的配置达不到最优， 甚⾄⽆法实时处理来⾃外部的数据，这时候我们就需要对默认的配置进⾏相关的修改。由于现实中场 景和数据量不⼀样，所以我们⽆法设置⼀些通⽤的配置（要不然 Streaming开发者就不会弄那么 多参数，直接写死不得了），我们需要根据数据量，场景的不同设置不⼀样的配置，这⾥只是给出建 议，这些调优不⼀定试⽤于你的程序，⼀个好的配置是需要慢慢地尝试。

Spark

- 1、设置合理的批处理时间(batchDuration)。 在构建StreamingContext的时候，需要我们传进⼀个参数，⽤于设置 Streaming批处理的时间间 隔。Spark会每隔batchDuration时间去提交⼀次Job，如果你的Job处理的时间超过了batchDuration 的设置，那么会导致后⾯的作业⽆法按时提交，随着时间的推移，越来越多的作业被拖延，最后导致 整个Streaming作业被阻塞，这就间接地导致⽆法实时处理数据，这肯定不是我们想要的。 另外，虽然batchDuration的单位可以达到毫秒级别的，但是经验告诉我们，如果这个值过⼩将会导致 因频繁提交作业从⽽给整个Streaming带来负担，所以请尽量不要将这个值设置为⼩于50ms。在很多 情况下，设置为50ms性能就很不错了。 那么，如何设置⼀个好的值呢？我们可以先将这个值位置为⽐较⼤的值（⽐如10S），如果我们发现作 业很快被提交完成，我们可以进⼀步减⼩这个值，知道Streaming作业刚好能够及时处理完上⼀个批处 理的数据，那么这个值就是我们要的最优值。
- 2、增加Job并⾏度 我们需要充分地利⽤集群的资源，尽可能的将Task分配到不同的节点，⼀⽅⾯可以充分利⽤集群资 源；另⼀⽅⾯还可以及时的处理数据。⽐如我们使⽤Streaming接收来⾃Kafka的数据，我们可以对每 个Kafka分区设置⼀个接收器，这样可以达到负载均衡，及时处理数据（关于如何使⽤Streaming读取 Kafka中的数据，可以参⻅ 和

）。 再如类似reduceByKey()和Join函数都可以设置并⾏度参数。

- 3、使⽤Kryo系列化。 Spark默认的是使⽤Java内置的系列化类，虽然可以处理所有⾃继承java.io.Serializable的类系列化的 类，但是其性能不佳，如果这个成为性能瓶颈，可以使⽤Kryo系列化类，关于如何在Spark中使⽤ Kroy，请参⻅ 。使⽤系列化数据可以很好地改善GC⾏ 为。
- 4、缓存需要经常使⽤的数据 对⼀些经常使⽤到的数据，我们可以显式地调⽤ rdd.cache() 来缓存数据，这样也可以加快数据的 处理，但是我们需要更多的内存资源。

- 5、清除不需要的数据


Spark

《Spark Streaming和Kafka整合开发指南(⼀)》 《Spark Streaming和Kafk a整合开发指南(⼆)》

《在Spark中⾃定义Kryo序列化输⼊输出API》

随着时间的推移，有⼀些数据是不需要的，但是这些数据是缓存在内存中，会消耗我们宝贵的内存资 源，我们可以通过配置 spark.cleaner.ttl 为⼀个合理的值；但是这个值不能过⼩，因为如果后 ⾯计算需要⽤的数据被清除会带来不必要的麻烦。⽽且，我们还可以配置选项

spark.streaming.unpersist 为true(默认就是true)来更智能地去持久化（unpersist）RD。这 个配置使系统找出那些不需要经常保有的RD，然后去持久化它们。这可以减少Spark RD的内存使 ⽤，也可能改善垃圾回收的⾏为。

- 6、设置合理的GC GC是程序中最难调的⼀块，不合理的GC⾏为会给程序带来很⼤的影响。在集群环境下，我们可以使⽤ 并⾏Mark-Swep垃圾回收机制，虽然这个消耗更多的资源，但是我们还是建议开启。可以如下配置：

更多的关于GC⾏为的配置，请参考Java垃圾回收相关⽂章。这⾥就不详细介绍了。

- 7、设置合理的CPU资源数 很多情况下Streaming程序需要的内存不是很多，但是需要的CPU要很多。在Streaming程序中，CPU 资源的使⽤可以分为两⼤类：（1）、⽤于接收数据；（2）、⽤于处理数据。我们需要设置⾜够的 CPU资源，使得有⾜够的CPU资源⽤于接收和处理数据，这样才能及时⾼效地处理数据。


spark.executor.extraJavaOptions

=

-XX :

+UseConcMarkSweepGC

