Storm⻛暴和SparkStreaming⽕花流都是分布式流处理的开 源框架。这⾥将它们进⾏⽐较并指出它们的重要的区别。处理 模型,延迟虽然这两个框架都提供可扩展性和容错性,它们根本的 区别在于他们的处理模型。⽽Storm处理的是每次传⼊的⼀个 事件，⽽SparkStreaming是处理某个时间段窗⼝内的事件 流。因此,Storm处理⼀个事件可以达到秒内的延迟，⽽Spark Streaming则有⼏秒钟的延迟。容错、数据保证在容错数据保 证⽅⾯的权衡是，SparkStreaming提供了更好的⽀持容错状 态计算。在Storm中,每个单独的记录当它通过系统时必须被跟 踪，所以Storm能够⾄少保证每个记录将被处理⼀次，但是在 从错误中恢复过来时候允许出现重复记录。这意味着可变状态 可能不正确地被更新两次。另⼀⽅⾯，SparkStreaming只需 要在批级别进⾏跟踪处理，因此可以有效地保证每个minibatch将完全被处理⼀次，即便⼀个节点发⽣故障。(实际 上,Storm的 Tridentlibrary库也提供了完全⼀次处理。但是,它 依赖于事务更新状态,这⽐较慢,通常必须由⽤户实现。)简⽽⾔ 之,如果你需要秒内的延迟，Storm是⼀个不错的选择，⽽且没 有数据丢失。如果你需要有状态的计算，⽽且要完全保证每个 事件只被处理⼀次，SparkStreaming则更好。Spark Streaming编程逻辑也可能更容易，因为它类似于批处理程序 (Hadop)，特别是在你使⽤批次(尽管是很⼩的)时。实现,编程 apiStorm初次是由Clojure实现，⽽ SparkStreaming是使⽤

Scala.如果你想看看代码还是让⾃⼰的定制时需要注意的地 ⽅，这样以便发现每个系统是如何⼯作的。Storm是由 BackType和Twitter开发;SparkStreaming是在加州⼤学伯克 利分校开发的。Storm有⼀个JavaAPI,也⽀持其他语⾔，⽽ SparkStreaming是以Scala编程，当然也⽀持JavaSpark Streaming⼀个好的特性是其运⾏在Spark上.这样你能够你编 写批处理的同样代码，这就不需要编写单独的代码来处理实时 流数据和历史数据。产品⽀持Storm已经发布⼏年了，在 Twitter从201年运⾏⾄今，同时也有其他公司使⽤，⽽Spark Streaming是⼀个新的项⽬，它从2013年在Sharethrough有 ⼀个项⽬运⾏。Hadop⽀持Storm是⼀个 Hortonworks Hadop数据平台上的流解决⽅案，⽽SparkStreaming有 MapR的版本还有Cloudera的企业数据平台，Databricks也提 供Spark⽀持。集群管理集成尽管两个系统都运⾏在它们⾃⼰ 的集群上，Storm也能运⾏在Mesos,⽽SparkStreaming能运 ⾏在YARN和 Mesos上。

