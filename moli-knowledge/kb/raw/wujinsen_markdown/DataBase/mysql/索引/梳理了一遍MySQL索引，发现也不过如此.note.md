htps:/mp.weixin.q.com/s/FqVM8mMru7dADK1xtlslAQ

索引的出现其实就是为了提⾼数据查询的效率，就像书的⽬录⼀样。

# 索引的常⻅模型

索引有不同的实现，对应着不同的模型。最常⻅的三种模型是：哈希表、有序数组和搜索树。

哈希表

哈希表是⼀种以键 - 值（key-value）存储数据的结构，我们只要输⼊待查找的值即 key，就可以找到其 对应的值即 Value。索引中的哈希表使⽤链地址法来处理哈希冲突，所以哈希表索引的数据结构是：数 组+链表。

现在维护着⼀个身份证信息和姓名的表，需要根据身份证号查找对应的名字，这时对应的哈希索引的 示意图如下所示：

![image 1](<梳理了一遍MySQL索引，发现也不过如此.note_images/imageFile1.png>)

如果想查找某个⼈的名字，⾸先需要计算这个⼈身份证号的哈希值，根据哈希值找到数组对应的下 标，然后再该下标对应的链表中寻找身份证号。 哈希表索引不是有序的，所以哈希索引做区间查询的速度是很慢的。试想要找身份证号在 [ID_card_X, ID_card_Y] 这个区间的所有⽤户，就必须全部扫描⼀遍。 所以，哈希表这种结构适⽤于只有等值查询的场景，即查找⼀个特定值，⽐如 Memcached 及其他⼀ 些 NoSQL 引擎。⽽有序数组在等值查询和范围查询场景中的性能就都⾮常优秀。

有序数组

上⾯的例⼦如果使⽤有序数组来实现的话，示意图如下所示：

![image 2](<梳理了一遍MySQL索引，发现也不过如此.note_images/imageFile2.png>)

这个数组就是按照身份证号递增的顺序保存的。可以⽤⼆分法查找，时间复杂度是 O(log(N))。 同时，这个索引结构⽀持范围查询。⽐如要查身份证号在 [ID_card_X, ID_card_Y]区间的 User，可以 先⽤⼆分法找到 ID_card_X（如果不存在 ID_card_X，就找到⼤于 ID_card_X 的第⼀个 User），然后向 右遍历，直到查到第⼀个⼤于 ID_card_Y 的身份证号，退出循环。 可以看到，有序数组的查询效率很⾼，但是插⼊效率却不⾼，往中间插⼊元素时，后⾯元素都要挪 动。所以，有序数组索引只适⽤于静态存储引擎。

## 搜索树

如果上⾯的例⼦⽤⼆叉搜索树来实现，示意图如下：

![image 3](<梳理了一遍MySQL索引，发现也不过如此.note_images/imageFile3.png>)

上述索引的查找复杂度为O(log(N))，但前提是要保证⼆叉搜索树的平衡性，为了保证这个特性，更新 的时间复杂度也为O(log(N))。 除了⼆叉搜索树，还可以使⽤多叉搜索树，这可以使得树⾼更低，每⼀次查询需要访问的数据就会更 少，即访问磁盘的次数就会更少，效率会更⾼。 下⾯，分析⼀下InnoDB中的的索引模型。

# InnoDB 的索引模型

在 InnoDB 中，表都是根据主键顺序以索引的形式存放的，这种存储⽅式的表称为索引组织表。 InnoDB 使⽤了 B+ 树索引模型。每⼀个索引在 InnoDB ⾥⾯对应⼀棵 B+ 树。 假设有⼀个主键列为 ID 的表，表中有字段 k，并且在 k 上有索引。这个表的建表语句是：

mysql> create table T(id int primary key, k int not null, name varchar(16),index (k))engine=InnoDB;

表中 R1~R5 的 (ID,k) 值分别为 (100,1)、(200,2)、(300,3)、(500,5) 和 (600,6)，两棵树的示例示意图如 下。

![image 4](<梳理了一遍MySQL索引，发现也不过如此.note_images/imageFile4.png>)

从图中不难看出，根据叶⼦节点的内容，索引类型分为主键索引和⾮主键索引。

主键索引的叶⼦节点存的是整⾏数据。在 InnoDB ⾥，主键索引也被称为聚簇索引（clustered index）。

⾮主键索引的叶⼦节点内容是主键的值。在 InnoDB ⾥，⾮主键索引也被称为⼆级索引（secondary index）或⾮聚簇索引。

那么，基于主键索引和普通索引的查询有什么区别？

如果语句是 select * from T where ID=500，即主键查询⽅式，则只需要搜索 ID这棵 B+ 树；

如果语句是 select * from T where k=5，即普通索引查询⽅式，则需要先搜索 k索引树，得到 ID 的值为 500，再到ID` 索引树搜索⼀次。这个过程称为回表。

也就是说，基于⾮主键索引的查询需要多扫描⼀棵索引树。因此，在应⽤中应该尽量使⽤主键查询。

## 索引维护

B+ 树为了维护索引有序性，在插⼊新值的时候需要做必要的维护。以上⾯这个图为例，如果插⼊新的 ⾏ ID 值为 700，则只需要在 R5 的记录后⾯插⼊⼀个新记录。如果新插⼊的 ID 值为 400，就相对麻烦 了，需要逻辑上挪动后⾯的数据，空出位置。 ⽽更糟的情况是，如果 R5 所在的数据⻚已经满了，根据 B+ 树的算法，这时候需要申请⼀个新的数据 ⻚，然后挪动部分数据过去。这个过程称为⻚分裂。在这种情况下，性能⾃然会受影响。 除了性能外，⻚分裂操作还影响数据⻚的利⽤率。原本放在⼀个⻚的数据，现在分到两个⻚中，整体 空间利⽤率降低⼤约 50%。 当然有分裂就有合并。当相邻两个⻚由于删除了数据，利⽤率很低之后，会将数据⻚做合并。合并的 过程，可以认为是分裂过程的逆过程。 基于上⾯的知识，讨论⼀下⾃增主键的问题。

⾃增主键是指⾃增列上定义的主键，在建表语句中⼀般是这么定义的：NOT NULL PRIMARY KEY AUTO_INCREMENT。插⼊新记录的时候可以不指定 ID 的值，系统会获取当前 ID 最⼤值加 1 作为下⼀条 记录的 ID 值。那么，什么时候使⽤⾃增主键，什么时候不使⽤呢？ 从上⾯可以看出，⾃增主键的插⼊数据模式，正符合了前⾯提到的递增插⼊的场景。每次插⼊⼀条新 记录，都是追加操作，都不涉及到挪动其他记录，也不会触发叶⼦节点的分裂。⽽有业务逻辑的字段 做主键，则往往不容易保证有序插⼊，这样写数据成本相对较⾼。 除了考虑性能外，我们还可以从存储空间的⻆度来看。假设表中确实有⼀个唯⼀字段，⽐如字符串类 型的身份证号，那应该⽤身份证号做主键，还是⽤⾃增字段做主键呢？由于每个⾮主键索引的叶⼦节 点上都是主键的值。如果⽤身份证号做主键，那么每个⼆级索引的叶⼦节点占⽤约 20 个字节，⽽如果 ⽤整型做主键，则只要 4 个字节，如果是⻓整型（bigint）则是 8 个字节。显然，主键⻓度越⼩，普 通索引的叶⼦节点就越⼩，普通索引占⽤的空间也就越⼩。 所以，从性能和存储空间⽅⾯考量，⾃增主键往往是更合理的选择。 有没有什么场景适合⽤业务字段直接做主键的呢？还是有的。⽐如，有些业务的场景需求是这样的：

只有⼀个索引；

该索引必须是唯⼀索引。

这就是典型的 KV 场景。 由于没有其他索引，所以也就不⽤考虑其他索引的叶⼦节点⼤⼩的问题 这时候我们就要优先考虑上⼀段提到的“尽量使⽤主键查询”原则，直接将这个索引设置为主键，可以 避免每次查询需要搜索两棵树。

# 补丁

## 聚簇索引和⾮聚簇索引

聚簇索引是对磁盘上实际数据重新组织以按指定的⼀个或多个列的值排序的算法。特点是存储数据的 顺序和索引顺序⼀致。⼀般情况下主键会默认创建聚簇索引，且⼀张表只允许存在⼀个聚簇索引。

聚簇索引默认是主键，如果表中没有定义主键，InnoDB 会选择⼀个唯⼀的⾮空索引代替。如果没有这 样的索引，InnoDB 会隐式定义⼀个主键来作为聚簇索引。如果已经设置了主键为聚簇索引⽽想要修改 聚簇索引，必须先删除主键，然后添加想要的聚簇索引，最后恢复设置主键即可。

优点是查询速度快，因为⼀旦具有第⼀个索引值的记录被找到，具有连续索引值的记录也⼀定物理的紧跟 其后。

缺点是对表进⾏修改速度较慢，这是为了保持表中的记录的物理顺序与索引的顺序⼀致，⽽把记录插⼊到 数据⻚的相应位置，必须在数据⻚中进⾏数据重排，降低了执⾏速度。在插⼊新记录时数据⽂件为了维持 B+Tree 的特性⽽频繁的分裂调整，⼗分低效。

什么时候使⽤聚簇索引呢？

如果经常需要返回列中某⼀范围的数

某列包含了⼩数⽬的不同值

某列经常被分组排序

⾮聚簇索引引的记录的物理顺序和索引的顺序不⼀致。聚簇索引的叶节点就是数据节点，⽽⾮聚簇索 引的叶节点仍然是索引节点。

## 索引组织表

在InnoDB 存储引擎中，表都是根据主键顺序组织存放的，这种存储⽅式的额表称为索引组织表。在 InnoDB存储引擎表中，每张表都有个主键(Primary key)，如果在创建表时 没有显示地定义主键，则 InnoDB存储引擎会按如下⽅式选择或创建主键：

⾸先判断表是否有⾮空的唯⼀索引(Unique not null),如果有，则该列即为主键。

如果不符合上述条件，InnoDB存储引擎⾃动创建⼀个6字节⼤⼩的指针。

当表中有多个⾮空唯⼀索引时，InnoDB 存储引擎将选择建表时第⼀个定义的⾮空唯⼀索引为主键。这 ⾥需要注意的是，主键的选择根据的是定义索引的顺序，⽽不是建表时列的顺序。 如下所示：

mysql> create table T( a int not null, b int null, c int not null, d int not null, unique key(b), unique key(d), unique key(c));

上述表中，b被⾸先声明为唯⼀索引，但是b允许为空，所以b不能作为主键索引；c和d中，虽然建表时 先建⽴的c，但是声明唯⼀索引时先声明的d，所以d为唯⼀索引。

## 各种索引的区别

InnoDB默认使⽤B+索引，B+索引中的所以可以分为两⼤类：主键索引和⼆级索引，⼆级索引⼜可以分 为⼏类，具体如下：

主键索引：InnoDB默认为主键创建⼀个聚簇索引，所以默认情况下主键索引就是聚簇索引，主键索引⼀定 是唯⼀索引，且主键索引不能为空。

⼆级索引：

唯⼀索引：并不是说索引时唯⼀的，⽽是索引中的字段是唯⼀的。⼀个数据库可以有很多唯⼀索引。 唯⼀索引允许为空。

普通索引：普通索引的唯⼀任务是加快对数据的访问速度，因此，应该只为那些最经常出现在查询条 件（WHERE column=）或者排序条件（ORDERBY column）中的数据列创建索引。

参考 https://www.jianshu.com/p/dd4fe101cffe参考 https://www.cnblogs.com/-619569179/p/6528896.html唯⼀索引与主键索引的⽐较 https://www.cnblogs.com/joshua317/p/5041111.html

参考： Mysql InnoDB引擎 -索引组织表 mysql：InnoDB的主键采⽤聚簇索引，⼆级索引不采⽤聚簇索引 【Mysql优化】聚簇索引与⾮聚簇索引概念 深⼊理解四种数据库索引类型（- 唯⼀索引/⾮唯⼀索引 - 主键索引（主索引） - 聚集索引/⾮聚集索引 组合索引）

聚簇索引(Clustered Index)和⾮聚簇索引 (Non- Clustered Index)举例说明 聚簇索引与⾮聚簇索引（也叫⼆级索引）

![image 5](<梳理了一遍MySQL索引，发现也不过如此.note_images/imageFile5.png>)

- ● 全局锁和表锁 ：给表加个字段怎么有这么多阻碍？
- ● ⼀⽂带你学习Kafka核⼼概念
- ● ⼲货：分布式基础-RPC你值得了解⼀下
- ● ⼲货|今⽇头条都在⽤的ClickHouse技术你应该了解⼀下
- ● ⾯试：Spring--AOP如果⾯试官再问，直接怼回去
- ● ⾯试：你说你精通Java并发，给我讲讲Java并发之J.U.C


