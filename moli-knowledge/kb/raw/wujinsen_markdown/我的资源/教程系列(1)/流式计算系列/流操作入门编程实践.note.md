Storm是⼀个分布式是实时计算系统，它设计了⼀种对流和计算的抽象，概念⽐较简单，实际编程开发 起来相对容易。下⾯，简单介绍编程实践过程中需要理解的Storm中的⼏个概念： Topology Storm中Topology的概念类似于Hadop中的MapReduce Job，是⼀个⽤来编排、容纳⼀组计算逻辑组 件（Spout、Bolt）的对象（Hadop MapReduce中⼀个Job包含⼀组Map Task、Reduce Task），这 ⼀组计算组件可以按照DAG图的⽅式编排起来（通过选择Stream Groupings来控制数据流分发流 向），从⽽组合成⼀个计算逻辑更加负责的对象，那就是Topology。⼀个Topology运⾏以后就不能停 ⽌，它会⽆限地运⾏下去，除⾮⼿动⼲预（显式执⾏bin/storm kil ）或意外故障（如停机、整个Storm 集群挂掉）让它终⽌。 Spout Storm中Spout是⼀个Topology的消息⽣产的源头，Spout应该是⼀个持续不断⽣产消息的组件，例 如，它可以是⼀个Socket Server在监听外部Client连接并发送消息，可以是⼀个消息队列（MQ）的消 费者、可以是⽤来接收Flume Agent的Sink所发送消息的服务，等等。Spout⽣产的消息在Storm中被 抽象为Tuple，在整个Topology的多个计算组件之间都是根据需要抽象构建的Tuple消息来进⾏连接， 从⽽形成流。 Bolt Storm中消息的处理逻辑被封装到Bolt组件中，任何处理逻辑都可以在Bolt⾥⾯执⾏，处理过程和普通 计算应⽤程序没什么区别，只是需要根据Storm的计算语义来合理设置⼀下组件之间消息流的声明、分 发、连接即可。Bolt可以接收来⾃⼀个或多个Spout的Tuple消息，也可以来⾃多个其它Bolt的Tuple消 息，也可能是Spout和其它Bolt组合发送的Tuple消息。 Stream Grouping Storm中⽤来定义各个计算组件（Spout、Bolt）之间流的连接、分组、分发关系。Storm定义了如下7 种分发策略：Shufle Grouping（随机分组）、Fields Grouping（按字段分组）、Al Grouping（⼴播 分组）、Global Grouping（全局分组）、Non Grouping（不分组）、Direct Grouping（直接分组）、 Local or Shufle Grouping（本地/随机分组），各种策略的具体含义可以参考Storm官⽅⽂档、⽐较容 易理解。 下⾯，作为⼊⻔实践，我们简单介绍⼏种开发中常⽤的流操作处理⽅式的实现： Storm组件简单串⾏ 这种⽅式是最简单最直观的，只要我们将Storm的组件（Spout、Bolt）串⾏起来即可实现，只需要了 解编写这些组件的基本⽅法即可。在实际应⽤中，如果我们需要从某⼀个数据源连续地接收消息，然 后顺序地处理每⼀个请求，就可以使⽤这种串⾏⽅式来处理。如果说处理单元的逻辑⾮常复杂，那么 就需要处理逻辑进⾏分离，属于同⼀类操作的逻辑封装到⼀个处理组件中，做到各个组件之间弱耦合 （除了定义Field的schema外，只通过发送消息来连接各个组件）。 下⾯，我实现⼀个简单的WordCount的例⼦，各个组件之间的连接⽅式，如下图所示：

![image 1](<流操作入门编程实践.note_images/imageFile1.png>)

ProduceRecordSpout类是⼀个Spout组件，⽤来产⽣消息，我们这⾥模拟发送⼀些英⽂句⼦，实际应 ⽤中可以指定任何数据源，如数据库、消息中间件、Socket连接、RPC调⽤等等

htp:/ w.tuicol.com/articles/RV7zuE

