协同过滤算法⼤致可分为两类，基于物品的与基于⽤户的；区分很简单，根据上⾯的逻辑，若你挖掘的关系是物品与 物品间的，就是基于物品的协同过滤算法，若你挖掘的关系是⽤户与⽤户间的，就是基于⽤户的协同过滤算法；由于 它们实现是有所不同，所以我分开整理，先来看看基于物品的协同过滤实现，我⾃⼰画了⼀幅图：

我通过数字的顺序，来标示数据变化的⽅向（由⼩到⼤）；下⾯分析下每⼀个步骤的功能以及实现。 ⾸先，说明下两个⼤的数据源，⽤户偏好数据：UserID、ItemID、Preference：表示⼀个对⼀个物品的喜好程 度；关系数据：ItemIDA(UserIDA)、ItemIDB(UserIDB)、Similarity：表示两个⼈或物品间的相似程度；接着⼀个⽤ 户来了，我们需要为其推荐，得拿到他的身份标示，⼀般是UserID，于是：

- ①. 查找这个⽤户喜欢过的物品（即偏好的产品，并查出偏好值后⾯会⽤），以及还没有喜欢过的商品，前者是

推荐运算的根据，后者作为⼀个产⽣推荐的⼀个集合；如② 画的那样。

- ②. 这⾥是⼀个可扩展的地⽅（我⾃⼰理解）；因为这两部分的数据的作⽤⾮常明显，修改这两个集合对后⾯产

⽣的推荐结果可产⽣⾮常直观的影响，⽐如清洗过滤，或根据⽤户属性缩⼩集合；不仅使后⾯推荐效果更优，运算性 能也可以⼤幅度提⾼。

- ③. 查找这两个集合之间的关系，这是⼀对多的关系：⼀个没有偏好过的物品与该⽤户所有偏好过的物品间的关

系，有⼀个值来衡量这个关系叫相似度Similarity；这个关系怎么来的，看蓝⾊箭头的指向。步骤⑥

- ④. 得到这个⼀对多的关系后，就可以计算这个物品对于这个⽤户的推荐值了，图中similarity_i-x表示Item_i 与


Item_x 之间的相似度，Item_x是该⽤户偏好过得，该⽤户对其偏好值记为 value_x ，相乘；Item_i 与 该⽤户偏好过 的所有物品以此做以上运算后，得到的值取平均值 便是 Item_i的推荐值了。注：有可能Item_i 不是与所有 该⽤户偏 好过的物品都都存在相似性，不存在的，不计算即可；另外这⾥⽅便理解介绍的都是最简单的实现；你也可以考⼀些 复杂的数学元素，⽐如⽅差来判断离散性等。

- ⑤. 这步就简单多了，刚才对该⽤户没有偏好过的集合中的所有Item都计算了推荐值，这⾥就会得到⼀个list，按 推荐值由⼤到⼩排序，返回前⾯的⼀个⼦集即可。
- ⑥。 前⾯已经提到，关系数据时怎么来的，也是根据⽤户的偏好数据；你把其看成⼀个矩阵，横着看过来，参考 两个Item间的共同⽤户，以及共同⽤户的偏好的值的接近度；这⾥的可选择的相似度算法很多，不⼀⼀介绍了，前⾯ 提到的IBM博客也详细讲解了。


基于物品的协同过滤算法分析完了，下⾯是基于⽤户的协同过滤算法，还是⾃⼰画了⼀幅图：

- ①. 同样也是查询，只是查询的对象不⼀样了，查询的是与该⽤户相似的⽤户，所以⼀来直接查了关系数据源。

以及相似⽤户与该⽤户的相似度。

- ②. 与刚才类似，也是对数据集的⼀个优化，不过作⽤可能没那么⼤。（个⼈感觉）
- ③. 查询关系数据源，得到相似⽤户即邻居偏好过的物品；如步骤④；图中由于空间⼩，没有把所有邻居的偏好

关系都列出来，⽤ …表示。其次还要得到该⽤户偏好过的物品集合。

- ④. 被推荐的Item集合是由该⽤户的所有邻居的偏好过的物品的并集，同时再去掉该⽤户⾃⼰偏好过的物品。作

⽤就是得到你的相似⽤户喜欢的物品，⽽你还没喜欢过的。

- ⑤. 集合优化同基于物品的协同过滤算法的步骤②。
- ⑥. 也是对应类似的，依次计算被推荐集合中Item_i 的推荐值，计算的⽅式略有不同，Value_1_i表示邻居1对，


Item_i的偏好值，乘以该⽤户与邻居1的相似度 Similarity1；若某个邻居对Item_i偏好过，就重复上述运算，然后取平 均值；得到Item_i的推荐值。

⑦、⑧. 与上⼀个算法的最后两部完全类似，只是步骤 ⑧你竖着看，判断两个⽤户相似的法⼦和判断两个物品相似 的法⼦⼀样。

