# 堆内存

Java 中的堆是 JVM 所管理的最⼤的⼀块内存空间，主要⽤于存放各种类的实例对象。 在 Java 中，堆被划分成两个不同的区域：新⽣代 ( Young )、⽼年代 ( Old )。新⽣代 ( Young ) ⼜被划 分为三个区域：Eden、From Survivor、To Survivor。 这样划分的⽬的是为了使 JVM 能够更好的管理堆内存中的对象，包括内存的分配以及回收。 堆的内存模型⼤致为：

从图中可以看出： 堆⼤⼩ = 新⽣代 + ⽼年代。其中，堆的⼤⼩可以通过参数 –Xms、-Xmx 来指定。 本⼈使⽤的是 JDK1.6，以下涉及的 JVM 默认值均以该版本为准。 默认的，新⽣代 ( Young ) 与⽼年代 ( Old ) 的⽐例的值为 1 2 ( 该值可以通过参数 –X NewRatio 来指 定 )，即：新⽣代 ( Young ) = 1/3 的堆空间⼤⼩。 ⽼年代 ( Old ) = 2/3 的堆空间⼤⼩。其中，新⽣代 ( Young ) 被细分为 Eden 和 两个 Survivor 区域， 这两个 Survivor 区域分别被命名为 from 和 to，以示区分。 默认的，Edem : from : to = 8 : 1 : 1 ( 可以通过参数 –X SurvivorRatio 来设定 )，即： Eden = 8/10 的 新⽣代空间⼤⼩，from = to = 1/10 的新⽣代空间⼤⼩。 JVM 每次只会使⽤ Eden 和其中的⼀块 Survivor 区域来为对象服务，所以⽆论什么时候，总是有⼀块 Survivor 区域是空闲着的。 因此，新⽣代实际可⽤的内存空间为 9/10 ( 即90% )的新⽣代空间。

