- 1.分区
- 2.G1停顿预测模型
- 3.卡表和位图
- 4.对象头
- 5.内存分配和管理
- 6.线程
- 7.⽇志解读
- 8.参数介绍和调优


本章详细介绍了G1中对象的快速分配和慢速分配，其中快速分配和TLAB相关。本节给出实际应⽤中对 象分配⽤到的相关参数和⼀些个⼈经验，如下所示：

在优化调试TLAB的时候，在调试环境中可以通过打开PrintTLAB来观察TLAB分配和使⽤的情况。·参数 UseTLAB，指是否使⽤TLAB。⼤量的实验可以证明使⽤TLAB能够加速对象分配；该参数默认是打开 的，不要关闭它。·参数ResizeTLAB，指是否允许TLAB⼤⼩动态调整。前⾯提到TLAB会进⾏动态化调 整，主要是基于历史信息（分配⼤⼩、线程数等），有基准测试表明使⽤动态调整TLAB⼤⼩效率更 ⾼。[插图]·参数MinTLABSize，指设置TLAB的最⼩值。实际应⽤需要设置该值，⽐如64K，⼀般可以 根据情况设置和调整该值。·参数TLABSize，指设置TLAB的⼤⼩。实际中不要设置TLABSize，设置之 后TLAB就不能动态调整了，即会使⽤⼀个固定⼤⼩的TLAB，前⾯我们提到GC可以根据情况动态调整 TLAB，在分配效率和内存碎⽚之间找到⼀个平衡点，如果设置该值则这种平衡就失效了。·参数 TLABWasteTargetPercent，指的是TLAB可占⽤的Eden空间的百分⽐，默认值是1。可以根据情况调整 TLABWasteTargetPercent，增⼤则可以分配更多的TLAB，3.1节中给出了具体的计算⽅式；另外如果 实际中线程数⽬很多，建议增⼤该值，这样每个线程的TLAB不⾄于太⼩。

参数TLABRefilWasteFraction，指的是TLAB中浪费空间和TLAB块的⽐例，默认值是64。可以根据情 况调整TLABRefilWasteFraction，主要考量点是内存碎⽚和分配效率的平衡，如果发现⽇志waste中的 slow和fast很⼤，说明浪费严重，可以适当减少该参数值。·参数TLABWasteIncrement，指的是动态的 增加浪费空间的字节数，默认值是4。增加该值会增加TLAB浪费的空间；⼀般不⽤设置。·参数 GCLockerRetryAlocationCount默认值为2，表示当分配中的垃圾回收次数超过这个阈值之后则直接失 败。最后再强调⼀点，TLAB不是G1才引⼊的，对象分配是JVM提供的基础分配功能，只不过G1结合⾃ ⼰内存分区的特征，以及垃圾回收的具体实现，重新实现了分配的策略，重⽤了这些参数的功能和使 ⽤⽅法，且没有引⼊额外的参数，所以这⼀部分内容不仅适⽤于G1的调优，其他的垃圾回收器同样适 ⽤。

