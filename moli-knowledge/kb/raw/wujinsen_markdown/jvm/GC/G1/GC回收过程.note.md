⼀、新⽣代回收

⼆、混合回收 1.第⼀阶段: 并发标记

1.1初始标记⼦阶段 负责标记所有直接可达的根对象, 需要stw 1.2并发标记⼦阶段

并发标记对所有分区进⾏标记，完成存活内存数据计算 不需要stw 1.3再标记⼦阶段 找出所有未被访问的存活对象,完成存活内存数据计算, 需要 stw

- 1.4清理⼦阶段 该阶段不会清理垃圾对象。 主要执⾏以下操作：统计存活对象;交换标记位图，为下次并发标记准备;重置Rset; 需要stw
- 2.第⼆阶段:垃圾回收


