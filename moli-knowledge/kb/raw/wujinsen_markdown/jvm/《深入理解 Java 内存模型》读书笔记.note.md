# 前提

《深⼊理解 Java 内存模型》程晓明著，该书在以前看过⼀遍，现在学的东⻄越多，感觉那块越重要， 于是⼜再细看⼀遍，于是便有了下⾯的读书笔记总结。全书⻚数虽不多，内容讲得挺深的。细看的 话，也是挺花时间的，看完收获绝对挺⼤的。也建议 Java 开发者都去看看。⾥⾯主要有 Java 内存模 型的基础、重排序、顺序⼀致性、Volatile 关键字、锁、final。本⽂参考书中内容。

# 基础

并发编程的模型分类 在并发编程需要处理的两个关键问题是：线程之间如何通信 和 线程之间如何同步。 通信 通信 是指线程之间以何种机制来交换信息。在命令式编程中，线程之间的通信机制有两种：共享内 存 和 消息传递。 在共享内存的并发模型⾥，线程之间共享程序的公共状态，线程之间通过写-读内存中的公共状态来隐 式进⾏通信。 在消息传递的并发模型⾥，线程之间没有公共状态，线程之间必须通过明确的发送消息来显式进⾏通 信。 同步 同步 是指程序⽤于控制不同线程之间操作发⽣相对顺序的机制。 在共享内存的并发模型⾥，同步是显式进⾏的。程序员必须显式指定某个⽅法或某段代码需要在线程 之间互斥执⾏。 在消息传递的并发模型⾥，由于消息的发送必须在消息的接收之前，因此同步是隐式进⾏的。 Java 的并发采⽤的是共享内存模型，Java 线程之间的通信总是隐式进⾏，整个通信过程对程序员完全 透明。 Java 内存模型的抽象 在 Java 中，所有实例域、静态域 和 数组元素存储在堆内存中，堆内存在线程之间共享。局部变量、 ⽅法定义参数 和 异常处理器参数 不会在线程之间共享，它们不会有内存可⻅性问题，也不受内存模型 的影响。 Java 线程之间的通信由 Java 内存模型（J M）控制。J M 决定了⼀个线程对共享变量的写⼊何时对 另⼀个线程可⻅。从抽象的⻆度来看，J M 定义了线程与主内存之间的抽象关系：线程之间的共享变 量存储在主内存中，每⼀个线程都有⼀个⾃⼰私有的本地内存，本地内存中存储了该变量以读／写共 享变量的副本。本地内存是 J M 的⼀个抽象概念，并不真实存在。 J M 抽象示意图：

![image 1](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile1.png>)

从上图来看，如果线程 A 和线程 B 要通信的话，要如下两个步骤：

- 1、线程 A 需要将本地内存 A 中的共享变量副本刷新到主内存去

- 2、线程 B 去主内存读取线程 A 之前已更新过的共享变量 步骤示意图：


![image 2](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile2.png>)

举个例⼦： 本地内存 A 和 B 有主内存共享变量 X 的副本。假设⼀开始时，这三个内存中 X 的值都是 0。线程 A 正 执⾏时，把更新后的 X 值（假设为 1）临时存放在⾃⼰的本地内存 A 中。当线程 A 和 B 需要通信时，

- 线程 A ⾸先会把⾃⼰本地内存 A 中修改后的 X 值刷新到主内存去，此时主内存中的 X 值变为了 1。随 后，线程 B 到主内存中读取线程 A 更新后的共享变量 X 的值，此时线程 B 的本地内存的 X 值也变成了 1。 整体来看，这两个步骤实质上是线程 A 再向线程 B 发送消息，⽽这个通信过程必须经过主内存。J M 通过控制主内存与每个线程的本地内存之间的交互，来为 Java 程序员提供内存可⻅性保证。 重排序 在执⾏程序时为了提⾼性能，编译器和处理器常常会对指令做重排序。重排序分三类：


- 1、编译器优化的重排序。编译器在不改变单线程程序语义的前提下，可以重新安排语句的执⾏顺序。

- 2、指令级并⾏的重排序。现代处理器采⽤了指令级并⾏技术来将多条指令重叠执⾏。如果不存在数据 依赖性，处理器可以改变语句对应机器指令的执⾏顺序。

- 3、内存系统的重排序。由于处理器使⽤缓存和读／写缓冲区，这使得加载和存储操作看上去可能是在 乱序执⾏。 从 Java 源代码到最终实际执⾏的指令序列，会分别经历下⾯三种重排序：


![image 3](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile3.png>)

上⾯的这些重排序都可能导致多线程程序出现内存可⻅性问题。对于编译器，J M 的编译器重排序规 则会禁⽌特定类型的编译器重排序（不是所有的编译器重排序都要禁⽌）。对于处理器重排序，J M 的处理器重排序规则会要求 Java 编译器在⽣成指令序列时，插⼊特定类型的内存屏障指令，通过内存 屏障指令来禁⽌特定类型的处理器重排序（不是所有的处理器重排序都要禁⽌）。 J M 属于语⾔级的内存模型，它确保在不同的编译器和不同的处理器平台之上，通过禁⽌特定类型的 编译器重排序和处理器重排序，为程序员提供⼀致的内存可⻅性保证。 处理器重排序 现代的处理器使⽤写缓冲区来临时保存向内存写⼊的数据。写缓冲区可以保证指令流⽔线持续运⾏， 它可以避免由于处理器停顿下来等待向内存写⼊数据⽽产⽣的延迟。同时，通过以批处理的⽅式刷新 写缓冲区，以及合并写缓冲区中对同⼀内存地址的多次写，可以减少对内存总线的占⽤。虽然写缓冲 区有这么多好处，但每个处理器上的写缓冲区，仅仅对它所在的处理器可⻅。这个特性会对内存操作 的执⾏顺序产⽣重要的影响：处理器对内存的读/写操作的执⾏顺序，不⼀定与内存实际发⽣的读/写操 作顺序⼀致！ 举个例⼦：

![image 4](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile4.png>)

假设处理器A和处理器B按程序的顺序并⾏执⾏内存访问，最终却可能得到 x = y = 0。具体的原因如下 图所示：

![image 5](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile5.png>)

exam1-ans

处理器 A 和 B 同时把共享变量写⼊在写缓冲区中（A1、B1），然后再从内存中读取另⼀个共享变量 （A2、B2），最后才把⾃⼰写缓冲区中保存的脏数据刷新到内存中（A3、B3）。当以这种时序执⾏ 时，程序就可以得到 x = y = 0 的结果。 从内存操作实际发⽣的顺序来看，直到处理器 A 执⾏ A3 来刷新⾃⼰的写缓存区，写操作 A1 才算真正 执⾏了。虽然处理器 A 执⾏内存操作的顺序为：A1 -> A2，但内存操作实际发⽣的顺序却是：A2 -> A1。此时，处理器 A 的内存操作顺序被重排序了。 这⾥的关键是，由于写缓冲区仅对⾃⼰的处理器可⻅，它会导致处理器执⾏内存操作的顺序可能会与 内存实际的操作执⾏顺序不⼀致。由于现代的处理器都会使⽤写缓冲区，因此现代的处理器都会允许 对写-读操作重排序。 内存屏障指令 为了保证内存可⻅性，Java 编译器在⽣成指令序列的适当位置会插⼊内存屏障指令来禁⽌特定类型的 处理器重排序。J M 把内存屏障指令分为下列四类：

![image 6](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile6.png>)

hapens-before JSR-13 内存模型使⽤ hapens-before 的概念来阐述操作之间的内存可⻅性。在 J M 中，如果⼀个 操作执⾏的结果需要对另⼀个操作可⻅，那么这两个操作之间必须要存在 hapens-before 关系。这⾥ 提到的两个操作既可以是在⼀个线程之内，也可以是在不同线程之间。 与程序员密切相关的 hapens-before 规则如下：

程序顺序规则：⼀个线程中的每个操作，hapens-before 于该线程中的任意后续操作。 监视器锁规则：对⼀个监视器的解锁，hapens-before 于随后对这个监视器的加锁。 volatile 变量规则：对⼀个 volatile 域的写，hapens-before 于任意后续对这个 volatile 域的读。 传递性：如果 A hapens-before B，且 B hapens-before C，那么 A hapens-before C。

注意，两个操作之间具有 hapens-before 关系，并不意味着前⼀个操作必须要在后⼀个操作之前执 ⾏！hapens-before 仅仅要求前⼀个操作（执⾏的结果）对后⼀个操作可⻅，且前⼀个操作按顺序排 在第⼆个操作之前（the first is visible to and ordered before the second）。 hapens-before 与 J M 的关系如下图所示：

![image 7](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile7.png>)

如上图所示，⼀个 hapens-before 规则对应于⼀个或多个编译器和处理器重排序规则。 数据依赖性 如果两个操作访问同⼀个变量，且这两个操作中有⼀个为写操作，此时这两个操作之间就存在数据依 赖性。数据依赖分下列三种类型：

<table>
  <tr>
    <th>名称</th>
    <th>代码示例</th>
    <th>说明</th>
  </tr>
  <tr>
    <td>写后读</td>
    <td>a = 1; b = a;</td>
    <td>写⼀个变量之后，再读这个位 置。</td>
  </tr>
  <tr>
    <td>写后写</td>
    <td>a = 1; a = 2;</td>
    <td>写⼀个变量之后，再写这个变 量。</td>
  </tr>
  <tr>
    <td>读后写</td>
    <td>a = b; b = 1;</td>
    <td>读⼀个变量之后，再写这个变 量。</td>
  </tr>
</table>


上⾯三种情况，只要重排序两个操作的执⾏顺序，程序的执⾏结果将会被改变。 前⾯提到过，编译器和处理器可能会对操作做重排序。编译器和处理器在重排序时，会遵守数据依赖 性，编译器和处理器不会改变存在数据依赖关系的两个操作的执⾏顺序。 注意，这⾥所说的数据依赖性仅针对单个处理器中执⾏的指令序列和单个线程中执⾏的操作，不同处 理器之间和不同线程之间的数据依赖性不被编译器和处理器考虑。 as-if-serial 语义 as-if-serial 语义的意思指：不管怎么重排序（编译器和处理器为了提⾼并⾏度），（单线程）程序的 执⾏结果不能被改变。编译器，runtime 和处理器都必须遵守 as-if-serial 语义。

为了遵守 as-if-serial 编译器和处理器不会对存在数据依赖关系的操作做重排序，因为这种重排序会改 变执⾏结果。但是如果操作之间没有数据依赖关系，这些操作就可能被编译器和处理器重排序。 举个例⼦：

- 1double pi = 3.14; //A

- 2double r = 1.0; //B

- 3double area = pi * r * r; //C 上⾯三个操作的数据依赖关系如下图所示：


![image 8](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile8.png>)

如上图所示，A 和 C 之间存在数据依赖关系，同时 B 和 C 之间也存在数据依赖关系。因此在最终执⾏ 的指令序列中，C 不能被重排序到 A 和 B 的前⾯（C 排到 A 和 B 的前⾯，程序的结果将会被改变）。 但 A 和 B 之间没有数据依赖关系，编译器和处理器可以重排序 A 和 B 之间的执⾏顺序。下图是该程序 的两种执⾏顺序：

![image 9](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile9.png>)

在计算机中，软件技术和硬件技术有⼀个共同的⽬标：在不改变程序执⾏结果的前提下，尽可能的开 发并⾏度。编译器和处理器遵从这⼀⽬标，从 hapens-before 的定义我们可以看出，J M 同样遵从 这⼀⽬标。 重排序对多线程的影响 举例：

1class Demo {

- 2 int a = 0;

- 3 boolean flag = false;

- 4

- 5 public void write() {

- 6 a = 1; //1

- 7 flag = true; //2

- 8 }

- 9

- 10 public void read() {

- 11 if(flag) { //3

- 12 int i = a * a; //4

- 13 }

- 14 }

- 15} 由于操作 1 和 2 没有数据依赖关系，编译器和处理器可以对这两个操作重排序；操作 3 和操作 4 没有 数据依赖关系，编译器和处理器也可以对这两个操作重排序。


- 1、当操作 1 和操作 2 重排序时，可能会产⽣什么效果？

如上图所示，操作 1 和操作 2 做了重排序。程序执⾏时，线程 A ⾸先写标记变量 flag，随后线程 B 读 这个变量。由于条件判断为真，线程 B 将读取变量 a。此时，变量 a 还根本没有被线程 A 写⼊，在这 ⾥多线程程序的语义被重排序破坏了！

- 2、当操作 3 和操作 4 重排序时会产⽣什么效果（借助这个重排序，可以顺便说明控制依赖性）。


![image 10](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile10.png>)

![image 11](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile11.png>)

在程序中，操作 3 和操作 4 存在控制依赖关系。当代码中存在控制依赖性时，会影响指令序列执⾏的 并⾏度。为此，编译器和处理器会采⽤猜测（Speculation）执⾏来克服控制相关性对并⾏度的影响。 以处理器的猜测执⾏为例，执⾏线程 B 的处理器可以提前读取并计算 a * a，然后把计算结果临时保存 到⼀个名为重排序缓冲（reorder bufer ROB）的硬件缓存中。当接下来操作 3 的条件判断为真时，就 把该计算结果写⼊变量 i 中。 从图中我们可以看出，猜测执⾏实质上对操作3和4做了重排序。重排序在这⾥破坏了多线程程序的语 义！ 在单线程程序中，对存在控制依赖的操作重排序，不会改变执⾏结果（这也是 as-if-serial 语义允许对 存在控制依赖的操作做重排序的原因）；但在多线程程序中，对存在控制依赖的操作重排序，可能会 改变程序的执⾏结果。

# 顺序⼀致性

顺序⼀致性内存模型 顺序⼀致性内存模型有两⼤特性：

⼀个线程中的所有操作必须按照程序的顺序来执⾏。 （不管程序是否同步）所有线程都只能看到⼀个单⼀的操作执⾏顺序。在顺序⼀致性内存模型中， 每个操作都必须原⼦执⾏且⽴刻对所有线程可⻅。

顺序⼀致性内存模型为程序员提供的视图如下：

![image 12](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile12.png>)

在概念上，顺序⼀致性模型有⼀个单⼀的全局内存，这个内存通过⼀个左右摆动的开关可以连接到任 意⼀个线程，同时每⼀个线程必须按照程序的顺序来执⾏内存读/写操作。从上⾯的示意图我们可以看 出，在任意时间点最多只能有⼀个线程可以连接到内存。当多个线程并发执⾏时，图中的开关装置能 把所有线程的所有内存读/写操作串⾏化。 举个例⼦： 假设有两个线程 A 和 B 并发执⾏。其中 A 线程有三个操作，它们在程序中的顺序是：A1 -> A2 -> A3。B 线程也有三个操作，它们在程序中的顺序是：B1 -> B2 -> B3。 假设这两个线程使⽤监视器锁来正确同步：A 线程的三个操作执⾏后释放监视器锁，随后 B 线程获取 同⼀个监视器锁。那么程序在顺序⼀致性模型中的执⾏效果将如下图所示：

![image 13](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile13.png>)

## 现在我们再假设这两个线程没有做同步，下⾯是这个未同步程序在顺序⼀致性模型中的执⾏示意图：

![image 14](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile14.png>)

未同步程序在顺序⼀致性模型中虽然整体执⾏顺序是⽆序的，但所有线程都只能看到⼀个⼀致的整体 执⾏顺序。以上图为例，线程 A 和 B 看到的执⾏顺序都是：B1 -> A1 -> A2 -> B2 -> A3 -> B3。之所 以能得到这个保证是因为顺序⼀致性内存模型中的每个操作必须⽴即对任意线程可⻅。 但是，在 J M 中就没有这个保证。未同步程序在 J M 中不但整体的执⾏顺序是⽆序的，⽽且所有线 程看到的操作执⾏顺序也可能不⼀致。⽐如，在当前线程把写过的数据缓存在本地内存中，在还没有 刷新到主内存之前，这个写操作仅对当前线程可⻅；从其他线程的⻆度来观察，会认为这个写操作根 本还没有被当前线程执⾏。只有当前线程把本地内存中写过的数据刷新到主内存之后，这个写操作才 能对其他线程可⻅。在这种情况下，当前线程和其它线程看到的操作执⾏顺序将不⼀致。 同步程序的顺序⼀致性效果 下⾯我们对前⾯的示例程序⽤锁来同步，看看正确同步的程序如何具有顺序⼀致性。 请看下⾯的示例代码：

1class demo {

- 2 int a = 0;

- 3 boolean flag = false;

- 4

- 5 public synchronized void write() { //获取锁

- 6 a = 1;


- 7 flag = true;

- 8 } //释放锁

- 9

- 10 public synchronized void read() { //获取锁

- 11 if(flag) {

- 12 int i = a;

- 13 }

- 14 } //释放锁

- 15} 上⾯示例代码中，假设 A 线程执⾏ write() ⽅法后，B 线程执⾏ reade() ⽅法。这是⼀个正确同步的多 线程程序。根据J M规范，该程序的执⾏结果将与该程序在顺序⼀致性模型中的执⾏结果相同。下⾯ 是该程序在两个内存模型中的执⾏时序对⽐图：


![image 15](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile15.png>)

在顺序⼀致性模型中，所有操作完全按程序的顺序执⾏。⽽在 J M 中，临界区内的代码可以重排序 （但 J M 不允许临界区内的代码“逸出”到临界区之外，那样会破坏监视器的语义）。J M 会在退出 临界区和进⼊临界区这两个关键时间点做⼀些特别处理，使得线程在这两个时间点具有与顺序⼀致性 模型相同的内存视图。虽然线程 A 在临界区内做了重排序，但由于监视器的互斥执⾏的特性，这⾥的

- 线程 B 根本⽆法“观察”到线程 A 在临界区内的重排序。这种重排序既提⾼了执⾏效率，⼜没有改变程 序的执⾏结果。 从这⾥我们可以看到 J M 在具体实现上的基本⽅针：在不改变（正确同步的）程序执⾏结果的前提 下，尽可能的为编译器和处理器的优化打开⽅便之⻔。 未同步程序的执⾏特性 未同步程序在 J M 中的执⾏时，整体上是⽆序的，其执⾏结果⽆法预知。未同步程序在两个模型中的 执⾏特性有下⾯⼏个差异：


- 1.
- 2.
- 3.


顺序⼀致性模型保证单线程内的操作会按程序的顺序执⾏，⽽ J M 不保证单线程内的操作会按程 序的顺序执⾏（⽐如上⾯正确同步的多线程程序在临界区内的重排序）。 顺序⼀致性模型保证所有线程只能看到⼀致的操作执⾏顺序，⽽ J M 不保证所有线程能看到⼀致 的操作执⾏顺序。 JM 不保证对 64 位的 long 型和 double 型变量的读/写操作具有原⼦性，⽽顺序⼀致性模型保 证对所有的内存读/写操作都具有原⼦ 。

第三个差异与处理器总线的⼯作机制密切相关。在计算机中，数据通过总线在处理器和内存之间传 递。每次处理器和内存之间的数据传递都是通过总线事务来完成的。总线事务包括读事务和写事务。 读事务从内存传送数据到处理器，写事务从处理器传递数据到内存，每个事务会读／写内存中⼀个或 多个物理上连续的字。总线会同步试图并发使⽤总线的事务。在⼀个处理器执⾏总线事务期间，总线 会禁⽌其它所有的处理器和 I／O 设备执⾏内存的读／写。 总线的⼯作机制：

![image 16](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile16.png>)

如上图所示，假设处理器 A、B、和 C 同时向总线发起总线事务，这时总线仲裁会对竞争作出裁决，假 设总线在仲裁后判定处理器 A 在竞争中获胜（总线仲裁会确保所有处理器都能公平的访问内存）。此 时处理器 A 继续它的总线事务，⽽其它两个处理器则要等待处理器 A 的总线事务完成后才能开始再次 执⾏内存访问。假设在处理器 A 执⾏总线事务期间（不管这个总线事务是读事务还是写事务），处理 器 D 向总线发起了总线事务，此时处理器 D 的这个请求会被总线禁⽌。 总线的这些⼯作机制可以把所有处理器对内存的访问以串⾏化的⽅式来执⾏；在任意时间点，最多只 能有⼀个处理器能访问内存。这个特性确保了单个总线事务之中的内存读/写操作具有原⼦性。 在⼀些 32 位的处理器上，如果要求对 64 位数据的写操作具有原⼦性，会有⽐较⼤的开销。为了照顾 这种处理器，Java 语⾔规范⿎励但不强求 JVM 对 64 位的 long 型变量和 double 型变量的写具有原⼦ 性。当 JVM 在这种处理器上运⾏时，会把⼀个 64 位 long/ double 型变量的写操作拆分为两个 32 位 的写操作来执⾏。这两个 32 位的写操作可能会被分配到不同的总线事务中执⾏，此时对这个 64 位变 量的写将不具有原⼦性。 当单个内存操作不具有原⼦性，将可能会产⽣意想不到后果。请看下⾯示意图：

![image 17](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile17.png>)

如上图所示，假设处理器 A 写⼀个 long 型变量，同时处理器 B 要读这个 long 型变量。处理器 A 中 64 位的写操作被拆分为两个 32 位的写操作，且这两个 32 位的写操作被分配到不同的写事务中执 ⾏。同时处理器 B 中 64 位的读操作被分配到单个的读事务中执⾏。当处理器 A 和 B 按上图的时序来 执⾏时，处理器 B 将看到仅仅被处理器 A “写了⼀半“的⽆效值。 注意，在 JSR -13 之前的旧内存模型中，⼀个 64 位 long/ double 型变量的读/写操作可以被拆分为 两个 32 位的读/写操作来执⾏。从 JSR -13 内存模型开始（即从JDK5开始），仅仅只允许把⼀个 64 位 long/ double 型变量的写操作拆分为两个 32 位的写操作来执⾏，任意的读操作在JSR -13中都必 须具有原⼦性（即任意读操作必须要在单个读事务中执⾏）。

# Volatile

Volatile 特性 举个例⼦：

1public class VolatileTest {

- 2 volatile long a = 1L; // 使⽤ volatile 声明 64 位的 long 型

- 3

- 4 public void set(long l) {

- 5 a = l; //单个 volatile 变量的写

- 6 }


- 7

- 8 public long get() {

- 9 return a; //单个 volatile 变量的读

- 10 }

- 11

- 12 public void getAndIncreament() {

- 13 a++; // 复合（多个） volatile 变量的读 /写

- 14 }

- 15} 假设有多个线程分别调⽤上⾯程序的三个⽅法，这个程序在语义上和下⾯程序等价：


1public class VolatileTest {

- 2 long a = 1L; // 64 位的 long 型普通变量

- 3

- 4 public synchronized void set(long l) { //对单个普通变量的写⽤同⼀个锁同步

- 5 a = l;

- 6 }

- 7

- 8 public synchronized long get() { //对单个普通变量的读⽤同⼀个锁同步

- 9 return a;

- 10 }

- 11

- 12 public void getAndIncreament() { //普通⽅法调⽤

- 13 long temp = get(); //调⽤已同步的读⽅法

- 14 temp += 1L; //普通写操作

- 15 set(temp); //调⽤已同步的写⽅法

- 16 }

- 17} 如上⾯示例程序所示，对⼀个 volatile 变量的单个读/写操作，与对⼀个普通变量的读/写操作使⽤同⼀ 个锁来同步，它们之间的执⾏效果相同。 锁的 hapens-before 规则保证释放锁和获取锁的两个线程之间的内存可⻅性，这意味着对⼀个 volatile 变量的读，总是能看到（任意线程）对这个 volatile 变量最后的写⼊。 锁的语义决定了临界区代码的执⾏具有原⼦性。这意味着即使是 64 位的 long 型和 double 型变量， 只要它是 volatile变量，对该变量的读写就将具有原⼦性。如果是多个 volatile 操作或类似于 volatile+ 这种复合操作，这些操作整体上不具有原⼦性。 简⽽⾔之，volatile 变量⾃身具有下列特性：


可⻅性。对⼀个 volatile 变量的读，总是能看到（任意线程）对这个 volatile 变量最后的写⼊。 原⼦性：对任意单个 volatile 变量的读/写具有原⼦性，但类似于 volatile+ 这种复合操作不具有原 ⼦性。

volatile 写-读的内存定义

当写⼀个 volatile 变量时，J M 会把该线程对应的本地内存中的共享变量值刷新到主内存。 当读⼀个 volatile 变量时，J M 会把该线程对应的本地内存置为⽆效。线程接下来将从主内存中读 取共享变量。

假设上⾯的程序 flag 变量⽤ volatile 修饰

![image 18](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile18.png>)

volatile 内存语义的实现 下⾯是 J M 针对编译器制定的 volatile 重排序规则表：

![image 19](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile19.png>)

为了实现 volatile 的内存语义，编译器在⽣成字节码时，会在指令序列中插⼊内存屏障来禁⽌特定类型 的处理器重排序。 下⾯是基于保守策略的 J M 内存屏障插⼊策略：

在每个 volatile 写操作的前⾯插⼊⼀个 StoreStore 屏障。 在每个 volatile 写操作的后⾯插⼊⼀个 StoreLoad 屏障。 在每个 volatile 读操作的后⾯插⼊⼀个 LoadLoad 屏障。 在每个 volatile 读操作的后⾯插⼊⼀个 LoadStore 屏障。

下⾯是保守策略下，volatile 写操作 插⼊内存屏障后⽣成的指令序列示意图：

![image 20](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile20.png>)

下⾯是在保守策略下，volatile 读操作 插⼊内存屏障后⽣成的指令序列示意图：

![image 21](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile21.png>)

上述 volatile 写操作和 volatile 读操作的内存屏障插⼊策略⾮常保守。在实际执⾏时，只要不改变 volatile 写-读的内存语义，编译器可以根据具体情况省略不必要的屏障。

# 锁

锁释放和获取的内存语义 当线程释放锁时，J M 会把该线程对应的本地内存中的共享变量刷新到主内存中。 当线程获取锁时，J M 会把该线程对应的本地内存置为⽆效。从⽽使得被监视器保护的临界区代码必 须要从主内存中去读取共享变量。 锁内存语义的实现

借助 RentrantLock 来讲解，PS： 后⾯专⻔讲下这块（RentrantLock、Synchronized、公平锁、⾮ 公平锁、AQS等），可以看看⼤明哥的博客：[htp:/cmsblogs.com/?p=210]() concurent 包的实现 如果我们仔细分析 concurent 包的源代码实现，会发现⼀个通⽤化的实现模式：

- 1.
- 2.
- 3.


⾸先，声明共享变量为 volatile； 然后，使⽤ CAS 的原⼦条件更新来实现线程之间的同步； 同时，配合以 volatile 的读/写和 CAS 所具有的 volatile 读和写的内存语义来实现线程之间的通 信。

AQS，⾮阻塞数据结构和原⼦变量类（java.util.concurent.atomic 包中的类），这些 concurent 包中 的基础类都是使⽤这种模式来实现的，⽽ concurent 包中的⾼层类⼜是依赖于这些基础类来实现的。 从整体来看，concurent 包的实现示意图如下：

![image 22](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile22.png>)

# final

对于 final 域，编译器和处理器要遵守两个重排序规则：

- 1.
- 2.


在构造函数内对⼀个 final 域的写⼊，与随后把这个被构造对象的引⽤赋值给⼀个引⽤变量，这两 个操作之间不能重排序。 初次读⼀个包含 final 域的对象的引⽤，与随后初次读这个 final 域，这两个操作之间不能重排序。

写 final 域的重排序规则 写 final 域的重排序规则禁⽌把 final 域的写重排序到构造函数之外。这个规则的实现包含下⾯2个⽅ ⾯：

J M 禁⽌编译器把 final 域的写重排序到构造函数之外。

编译器会在 final 域的写之后，构造函数 return 之前，插⼊⼀个 StoreStore 屏障。这个屏障禁⽌处 理器把 final 域的写重排序到构造函数之外。

读 final 域的重排序规则 在⼀个线程中，初次读对象引⽤与初次读该对象包含的 final 域，J M 禁⽌处理器重排序这两个操作 （注意，这个规则仅仅针对处理器）。编译器会在读 final 域操作的前⾯插⼊⼀个 LoadLoad 屏障。 final 域是引⽤类型 对于引⽤类型，写 final 域的重排序规则对编译器和处理器增加了如下约束： 在构造函数内对⼀个 final 引⽤的对象的成员域的写⼊，与随后在构造函数外把这个被构造对象的引⽤ 赋值给⼀个引⽤变量，这两个操作之间不能重排序。

# 总结

JM，处理器内存模型与顺序⼀致性内存模型之间的关系 J M 是⼀个语⾔级的内存模型，处理器内存模型是硬件级的内存模型，顺序⼀致性内存模型是⼀个理 论参考模型。下⾯是语⾔内存模型，处理器内存模型和顺序⼀致性内存模型的强弱对⽐示意图：

![image 23](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile23.png>)

JM 的设计示意图

![image 24](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile24.png>)

JM 的内存可⻅性保证 Java 程序的内存可⻅性保证按程序类型可以分为下列三类：

- 1.单线程程序。单线程程序不会出现内存可⻅性问题。编译器，runtime 和处理器会共同确保单线程程 序的执⾏结果与该程序在顺序⼀致性模型中的执⾏结果相同。

- 2.正确同步的多线程程序。正确同步的多线程程序的执⾏将具有顺序⼀致性（程序的执⾏结果与该程序 在顺序⼀致性内存模型中的执⾏结果相同）。这是 J M 关注的重点，J M通过限制编译器和处理器 的重排序来为程序员提供内存可⻅性保证。

- 3.未同步/未正确同步的多线程程序。J M 为它们提供了最⼩安全性保障：线程执⾏时读取到的值，要 么是之前某个线程写⼊的值，要么是默认值（0，nul，false）。 下图展示了这三类程序在 J M 中与在顺序⼀致性内存模型中的执⾏结果的异同：


![image 25](<《深入理解 Java 内存模型》读书笔记.note_images/imageFile25.png>)

