在LINUX中的源代码编译程序都遵守⼀个规则，就是以指定的⽬录为相对⽬录，安装程序，例如，安 装前./configure-prefix=/ABC 那么，安装后，在/ABC下就有对于这个程序的/ABC/include该程序的头⽂件⽬录 /ABC/etc该程序的配 置⽬录，/ABC/bin该程序应⽤⽬录等，这时，我们直接打应⽤程序名就不可能运⾏得起来，因为PATH 变量⾥没有指向/ABC/bin应⽤⽬录,所以运⾏要⽤绝对路径/ABC/bin/aplication_name 所以通常我们./configure的时候，默认是 -prefix=/usr，这样的话，本程序的配置⽂件就会装 到/usr/etc,应⽤⽂件就会安装到/usr/bin,本程序的私有库也会安装到/usr/lib等等，，我们也不⽤设置 PATH就可以直接⽤了，，

注意：windows下，我们⼀般是把应⽤程序装到⾃⼰的⽬录，在卸载程序不能⽤的时候，可以能过强 制删除应⽤⽬录来节省空间。 在Linux下， -prefix=/ABC，以后，也实现了这个功能，，以后我们要删除，就直接删得⽐WINDOWS 更彻底， 因为windows把所有程序的配置⽂件都放到注册表管理，要是在直接删⽬录的情况下，是删不⼲净 的，，⽽LINUX的驱动程序则不同，他每个程序的配置⽂件都⼏乎是放在安装⽬录下的/etc⼦⽂件夹， 应⽤⽂件都是安装在安装⽬录下的/bin⼦⽂件夹,库⽂件也是安装在安装⽬录下的/lib⽂件夹，这样⼀删 就全删了，，可以说是绿⾊。。 正是LINUX的这种规定，使得⽬录存放得更加清楚，简便。

