⽹上资料很多，但是都不怎么实⽤，这⾥给⼤家总结⼀下。nat模式上⽹。因为nat本身就能上⽹为什 么还要设置ip。这有点⾃找麻烦。但是在集群这是必须的。要么你搭建伪分布，要么⾄少具有三台物理 机器。为了节省成本，⼀般会选择伪分布。 伪分布也有选择，三种模式那种⽅式⽐较合适。

- 1.桥接，这种⽅式对于⼤多数来说⽐较简单。只要设置⼀下ip，dns，⽹关等，主机和虚拟机之间关系 不是很⼤。但是如果是pc机拨号上⽹，恐怕这种⽅式就不适合了。
- 2.nat模式，可是⾃动获取ip，不需要设置即可实现上⽹，缺点不能和主机通信。正因为他的缺点，这 ⾥才有我们发挥的余地。这也是本篇⽂章的重点。


2.host模式，虚拟机之间可以通信，但是和主机不能通信。其实他也能上⽹，这⾥以后在补充。

nat模式上⽹ nat模式上⽹，需要注意的⼏个问题。nat如果上⽹⾸先需要查看物理机（pc机）这个服务器已经启 动。上⽹的原理：流量是⾛的物理⽹卡，但是它的ip,却是和物理机没有关系的。 也就是说，⽆论你怎么更换⽹络环境，你的虚拟机都不需要做改变。这就是它的优点

⾸先在安装之后，我们会看到多了两个⽹卡

这个如果经过⽹络配置的折磨，应该能找到着两个⽹卡：vmnet1和vmnet8，这⾥讲的是vmnet8。

重点看右侧第三图你会看到ip地址，这个ip地址，因个⼈⽽异，也就是说不同⽹络，不同环境，这个ip 变化的概率是很⼤的。可能是192.168.0.1等等。这⾥也相当于我们编程的⼀个变量，这⾥是需要你根 据你的环境来赋值的。

-

-

上⾯我们通过看到的虚拟机⽹卡的ip，为了避免混肴，上⾯的虚拟⽹卡和本地⽹络适配器处于同⼀界 ⾯。如下图

⽽下⾯则是在打开虚拟机之后，通过菜单弹出

弹出之后，我们在看⼀下虚拟机的（交换机）vmnet8.

可以看到vmnet8⼦⽹ip和虚拟机⽹卡是出于同⼀个⽹段的。 同时在右侧net设置界⾯， 我们可以看到⽹关为：192.168.106.2，这个很重要因为，我们在后⾯虚拟机的设置会⽤到。

-

上⾯我们已经有了整体的认识，下⾯我们该设置Linux的⽹络： ⼀、服务器版⽹络配置

⾸先需要设置ip，⽹关，⼦⽹掩码 ⾸先我们通过

打开interfaces,然后设置ip，⽹关，⼦⽹掩码

上⾯设置完毕之后，我们在设置dns。 dns的设置有两种⽅式，这⾥可以参考 因为设置在

Ubuntu常⽤命令总结及修改DNS的多种⽅法总结

1.

/etc/resolv.conf复制代码中设置之后，重启电脑，配置会被还原。最后 在/etc/resolvconf/resolv.cong.d的head和base中设置。

设置完毕之后，执⾏

1. 1.

resolvconf -u复制代码然后查看 cat /etc/resolv.conf复制代码的内容就是刚才在head中设置的内容（这⾥⾯需要说明的有的是在 head中设置，有的是在base中设置⽣效）。

设置完毕，我们就能看到上图内容。

-

上⾯基本⼤功告成，但是还需要重启⼀下⽹卡

1.

sudo /etc/init.d/networking restart复制代码

1.

ping w.baidu.com复制代码看到下⾯，就可以连接上⽹了。

-

-

⼆、桌⾯版配置： 上⾯有的同学在设置完毕之后，还不能上⽹，如果使⽤时桌⾯版ubuntu，我们可以在设置⼀下： 第⼀步：如下图看到单击上下箭头-》Edit Conetctions ,会弹出图2

- 图1 第⼆步：弹出之后我们选中⽹卡-》Edit
- 图2第三步：我们选中 IPv Setings ,Method中选择Manual，（


当你选择遇到问题的时候看这⾥，记得拖住⿏标不要放，光标放到之后，然后放开就选中了），选中 之后我们设置⽹卡和⽹关，DNS。记得这⾥⼀定要和上⾯⽹卡的信息⼀致。

注释：对于桌⾯版ubuntu只需要通过界⾯配置即可，不需要再次通过命令配置

总结:

# NAT模式好处是⽆论外部⽹络环境怎么改变，内部都不需要做修改，直接能上⽹。NAT模式 能直接PING通主机，但是主机不能PING通虚拟机，需要进⾏⼀些设置

虚拟机⽹络编辑器下： ⼦⽹IP：192.168.217.0 ⽹关：192.168.217.2：

# VMnet8: 192.168.217.1 (主机与虚拟机通信的虚拟⽹卡)

这3个IP都不能相同 虚拟机上设置 gateway=192.168.217.2

