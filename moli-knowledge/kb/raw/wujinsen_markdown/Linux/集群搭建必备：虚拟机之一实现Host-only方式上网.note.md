阅读本⽂可以带着下⾯⼏个⼩问：

- 1、虚拟机实现上⽹的三种⽅式，其中⽹桥模式和主机模式本帖不予讨论， 。
- 2、虚拟机host-only模式不是只能VM与主机通信吗，⼜怎么才能上⽹呢。


感兴趣的同志请前往

Host-only模式实现联⽹得考虑如下配置过程：

- 1、 安装VMware-Workstation，安装虚拟机Linux（centos、ubuntu等）完毕。
- 2、设置虚拟机上⽹⽅式是Host-only，然后确认如下全部ip设置：


- （1）配置VMware Network Adapter VMnet1（即，主机的虚拟⽹卡，⽤来跟虚拟机通信的） 查看主机的⽹络连接设置，找到如图示三个连接。

- 如图1：

其中，VMware Network Adapter VMnet8和NAT⽅式相关，本⽂暂不讨论。这⾥VMware Network Adapter VMnet1和Host-only相关，设置VMware Network Adapter VMnet1的ip如下，其中紫红⾊“2” 可以换成不冲突的任意⼦⽹，⽽蓝⾊不可更改，这⾥的1表示分配给主机的⼦⽹内ip，即全部完成后虚 拟机ping192.168.2.1就会ping通主机（后⽂检验）。

- 如图2：

（2）共享本地连接 打开本地连接设置，找到主机的⽹络配置共享⻚，设置⻚⾯如下：

- 如图3：

host-only连接设置上⽹，选择VMnet1。

（3）配置虚拟机⽹络所在⼦⽹⽹段 如图192.168.2.0指明了虚拟机和主机之间构成了⼀个局域⼦⽹⽹段是2，所以主机相对于虚拟机的ip配 置的是192.168.2.1，⽽虚拟机开机后配置的ip也必须配置成192.168.2.*的样⼦这样就算主机没有连⽹， 和虚拟机也能互联。

- 如图4：


- （4）虚拟机Linux配置ip


开机修改ip如图，⼿动，ip说明在⼦⽹2段内，ip为 9，⽹关即为主机ip，全部配置完成后主机ping 192.168.2. 9会ping通虚拟机。

- 如图5：

注意：安装虚拟机时或之后设置⽹络属性要保证⽹络配置项为hostonly。

- 如图6：


- （5）检验ip是否正确 主机ping 虚拟机：ping192.168.2. 9 虚拟机ping 主机：ping 192.168.2.1 也可ping下百度：ping 如有必要请重启虚拟机！


w.baidu.com

