
spring:
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:28548}
        namespace: dev

dubbo:
  registry:
    # 挂载到 Spring Cloud 注册中心
    address: spring-cloud://127.0.0.1
  cloud:
    # 订阅的服务提供方
    subscribed-services: user-center-server
  protocols:
    dubbo:
      port: 28883
  consumer:
    check: false

---

spring:
  profiles: test
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:28548}
        namespace: 8cf9fb08-7e7c-4084-9ba5-b87ac9da2f78
